embedded.txt
.DS_Store
/.release
/.worktrees
/packages
/public/packages

# Phase 6 local prod snapshots — contain real household data; never commit.
# (also match the worktree symlink form, not just the directory)
custom/grocy_AI/.snapshots
custom/grocy_AI/.snapshots/
custom/grocy_AI/**/*.sqlite
