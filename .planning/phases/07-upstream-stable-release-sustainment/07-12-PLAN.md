---
phase: 07-upstream-stable-release-sustainment
plan: "12"
type: execute
wave: 8
depends_on: ["07-08", "07-09", "07-10"]
files_modified:
  - custom/grocy_AI/tests/deployment-gate.sh
  - .planning/phases/07-upstream-stable-release-sustainment/07-DEPLOYMENT-EVIDENCE.md
  - .planning/phases/07-upstream-stable-release-sustainment/07-PHASE7-BASELINE.sha256
autonomous: true
requirements: [REL-04, REL-05]
must_haves:
  truths:
    - "REL-04/D-08/D-09: The stable image is built ONLY from the exact full-40 stable-runtime SHA and identified by immutable digest; module_version, stable version.json `Customization` marker, and `$grocyAiAssetVersion` are recorded as the bumped-in-lockstep values."
    - "REL-05/D-10: `deployment-gate.sh` is re-pointed at Phase-7 manifest/evidence artifacts and Phase-7 topology (grocy-only, no companion candidate), not the Phase-2 baseline, without weakening any existing gate identity, and its `predeploy` mode passes against live prod (read-only capture)."
    - "The recorded digest, source SHA, and marker values in `07-DEPLOYMENT-EVIDENCE.md` are the exact identities the 07-13 cutover must deploy and verify; no live rebuild happens in this plan."
  artifacts:
    - path: custom/grocy_AI/tests/deployment-gate.sh
      provides: "Phase-7-keyed live-prod evidence gate (grocy-only identity, Phase-7 baseline + markers + migration state, Phase-7 manifest/evidence fields)"
    - path: .planning/phases/07-upstream-stable-release-sustainment/07-DEPLOYMENT-EVIDENCE.md
      provides: "The built image digest, source runtime SHA, marker values, prior-image identity, and predeploy protected-state fingerprint for the cutover"
    - path: .planning/phases/07-upstream-stable-release-sustainment/07-PHASE7-BASELINE.sha256
      provides: "The Phase-7 acceptance baseline the deployment gate verifies (replacing the Phase-1 baseline the Phase-2 gate keyed to)"
  key_links:
    - from: .planning/phases/07-upstream-stable-release-sustainment/07-RELEASE-MANIFEST.md
      to: "built stable image digest"
      via: "image built from stable_runtime_sha; digest recorded in 07-DEPLOYMENT-EVIDENCE.md"
      pattern: "stable_image_id: sha256:"
    - from: custom/grocy_AI/tests/deployment-gate.sh
      to: .planning/phases/07-upstream-stable-release-sustainment/07-DEPLOYMENT-EVIDENCE.md
      via: "predeploy prior-image + fingerprint capture written to Phase-7 evidence"
      pattern: "PREDEPLOY_FINGERPRINT"
---

## Phase Goal

**As a** release maintainer, **I want to** build the v1.0 stable image by immutable digest from the exact runtime SHA and pass the Phase-7 predeploy/evidence gates, **so that** the cutover deploys a provenance-pinned artifact that the live gate can verify — not a mutable tag or a Phase-2-keyed check.

<objective>
Rewire the deployment-evidence gate to Phase-7 reality, build the immutable-digest stable image from the runtime SHA, and pass predeploy — recording the digest/SHA/markers the cutover will deploy. No live rebuild here.

Purpose: turn the green source/migration gates (07-08/07-09) into a built, digest-identified artifact with a passing predeploy gate, so 07-13 is a deterministic deploy of a known artifact.
Output: Phase-7-keyed `deployment-gate.sh`, `07-PHASE7-BASELINE.sha256`, and `07-DEPLOYMENT-EVIDENCE.md` with the digest, source SHA, markers, prior-image, and predeploy fingerprint.
</objective>

<execution_context>@/Users/ian/.codex/get-shit-done/workflows/execute-plan.md</execution_context>

<context>
@.planning/phases/07-upstream-stable-release-sustainment/07-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-upstream-stable-release-sustainment/07-RELEASE-MANIFEST.md
@.planning/phases/07-upstream-stable-release-sustainment/07-08-SUMMARY.md
@.planning/phases/07-upstream-stable-release-sustainment/07-09-SUMMARY.md
@.planning/phases/07-upstream-stable-release-sustainment/07-RECOVERY-RUNBOOK.md
@custom/grocy_AI/tests/deployment-gate.sh
@custom/grocy_AI/tests/release-gate.sh
@.planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-20-PLAN.md
<interfaces>
Current `deployment-gate.sh` is Phase-2-keyed: `verify_baseline` requires `02-PHASE1-BASELINE.sha256` + the Phase-1 "SKIPPED — NOT ACCEPTED" acceptance; `assert_repo_identities` reads companion (`/Users/ian/Documents/Repos/grocy-mcp`) and stable (`/Users/ian/Documents/Repos/grocy-atech-release`) macOS worktrees and requires `companion_candidate_sha`/`stable_runtime_sha`/`stable_adapter_sha`/`stable_portable_sha`/`main_candidate_sha`; `assert_migration_state` checks the Phase-2 canonical index `ix_product_barcodes_canonical_gtin` + migration `0256.php`; smoke checks companion port 3061. Modes: `candidate|predeploy` (2 args) and `postdeploy-companion|postdeploy-stable|postsmoke|final` (3 args).
Phase-7 topology: only the `grocy` stack rebuilds from `atech-release` (`image: atechpcs/grocy-ai:4.6`, `pull_policy: build`); the companion (grocy-mcp) is NOT rebuilt in this release, so the Phase-7 manifest carries NO companion candidate — the gate's companion-candidate identity assertions must be dropped (companion left as an untouched running service or not asserted). Stable worktree lives on the Linux host (path from 07-05/07-06 summaries), not the macOS path.
Markers (D-09): `module_version` (was `2.5.0`), stable `custom/grocy_AI/version.json` `"Customization"` marker, and `$grocyAiAssetVersion` — bumped in lockstep by 07-07; their values live in `07-RELEASE-MANIFEST.md`.
Prod = `10.10.0.156`, container `grocy`, port 9283, bind `/etc/komodo/grocy:/config`, SSH `root@10.10.0.156` via 1Password `ssh:Personal Docker (102)`. `predeploy` is read-only (prior-image + fingerprint capture). Run PHP as `php8.5`/`GROCY_AI_PHP`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-point deployment-gate.sh at Phase-7 baseline, topology, markers, and migration state</name>
  <files>custom/grocy_AI/tests/deployment-gate.sh, .planning/phases/07-upstream-stable-release-sustainment/07-PHASE7-BASELINE.sha256</files>
  <read_first>custom/grocy_AI/tests/deployment-gate.sh; .planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-20-PLAN.md; .planning/phases/07-upstream-stable-release-sustainment/07-RELEASE-MANIFEST.md; MISTAKES.md</read_first>
  <behavior>
    - Test 1: `verify_baseline` points at `07-PHASE7-BASELINE.sha256` (a Phase-7 acceptance-artifact baseline) and its byte check passes; the Phase-1 `02-PHASE1-BASELINE.sha256` / "SKIPPED — NOT ACCEPTED" semantics are removed for Phase-7 use without deleting the Phase-2 artifacts on disk.
    - Test 2: `assert_repo_identities` reads the Phase-7 main + stable worktree identities (portable/adapter/runtime parent chain from the Phase-7 manifest) on the Linux host and no longer requires a companion-candidate SHA or the macOS `grocy-mcp`/`grocy-atech-release` paths; companion candidate assertions are dropped.
    - Test 3: `assert_migration_state` verifies the v1.0 module migration state (the bulk/categorization/capture module tables + version rows present) instead of the Phase-2 canonical index / `0256.php`; marker checks read `module_version` and stable `version.json` `Customization` = the D-09 bumped values from the Phase-7 manifest.
    - Test 4: `sh -n custom/grocy_AI/tests/deployment-gate.sh` parses clean and no existing gate identity (mount RW check, auth-denied smoke, private-media headers, protected fingerprint, evidence-privacy scrub) is weakened or removed.
  </behavior>
  <action>Rewire `deployment-gate.sh` to Phase-7 without weakening it (mirror how 02-20 kept the gate strong while re-pointing artifacts). (a) Repoint `verify_baseline` to a new `07-PHASE7-BASELINE.sha256` under the Phase-7 phase dir, listing the Phase-7 acceptance artifacts to fingerprint (e.g. the pre-cutover acceptance record); generate that baseline file and its byte hashes. (b) Repoint `phase_dir` and `baseline` to the Phase-7 directory; drop the Phase-1-specific "SKIPPED — NOT ACCEPTED" and empty-`phone-timings.jsonl` assertions for Phase-7. (c) In `assert_repo_identities`, resolve the stable worktree from the Phase-7 stable path (from 07-05/07-06 summaries) not the hardcoded macOS path, verify the `stable_portable_sha` → `stable_adapter_sha` → `stable_runtime_sha` parent chain and the `main_candidate_sha` ancestry, and REMOVE the companion-candidate identity checks (no companion in this release). (d) In `assert_companion_deployment` / smoke, drop companion-candidate + port-3061 identity assertions (leave companion untouched, not verified as a candidate); keep the `grocy` stable identity path intact. (e) Repoint `assert_migration_state` to assert the v1.0 module tables/version rows exist (query `sqlite_master` for `grocy_ai_bulk_*`, categorization, and capture tables) rather than the Phase-2 canonical index + `0256.php`. (f) Repoint marker checks to the D-09 values in the Phase-7 manifest (`module_version`, `version.json` `Customization`; add the `$grocyAiAssetVersion`/served-asset marker check against the new value). Keep `is_sha256`, the RW `/config` mount check, unauthenticated-denied smoke, private-media header checks, the protected-fingerprint capture, and `assert_evidence_privacy` unchanged. Use `$php_runner`/`GROCY_AI_PHP` and no bare `php`/no hardcoded macOS path (MISTAKES.md). This plan edits the gate only; it does not run postdeploy/postsmoke (those belong to 07-13/07-14 against the live rebuild).</action>
  <verify><automated>sh -n custom/grocy_AI/tests/deployment-gate.sh && rg -n "07-PHASE7-BASELINE|07-upstream-stable-release-sustainment" custom/grocy_AI/tests/deployment-gate.sh && ! rg -n "grocy-mcp|02-PHASE1-BASELINE|ix_product_barcodes_canonical_gtin|0256.php|companion_candidate_sha" custom/grocy_AI/tests/deployment-gate.sh</automated></verify>
  <acceptance_criteria>The gate parses clean, keys to the Phase-7 baseline/topology/markers/migration state, drops companion-candidate + Phase-2 identity checks, and retains every mount/auth/media/fingerprint/privacy identity.</acceptance_criteria>
  <done>`deployment-gate.sh` is Phase-7-keyed and no weaker than the Phase-2 gate.</done>
</task>

<task type="auto">
  <name>Task 2: Build the immutable-digest stable image and pass predeploy/evidence gates</name>
  <files>.planning/phases/07-upstream-stable-release-sustainment/07-DEPLOYMENT-EVIDENCE.md</files>
  <read_first>.planning/phases/07-upstream-stable-release-sustainment/07-RELEASE-MANIFEST.md; .planning/phases/07-upstream-stable-release-sustainment/07-RECOVERY-RUNBOOK.md; custom/grocy_AI/tests/deployment-gate.sh; custom/grocy_AI/tests/release-gate.sh</read_first>
  <behavior>
    - Test 1: `release-gate.sh predeploy <07-RELEASE-MANIFEST>` passes from clean worktrees (main + stable), proving byte parity, immutable ancestry, and synchronized markers before any build.
    - Test 2: the stable image is built ONLY from the exact full-40 `stable_runtime_sha` (via `Dockerfile.atech`, `FROM lscr.io/linuxserver/grocy:v4.6.0-ls334`) and its immutable digest is recorded as `stable_image_id: sha256:...` in `07-DEPLOYMENT-EVIDENCE.md`, alongside the source SHA and the D-09 marker values.
    - Test 3: `deployment-gate.sh predeploy <07-RELEASE-MANIFEST>` passes against live prod (read-only): clean worktrees, RW `/config` mount, resolvable prior images, and the predeploy protected-state fingerprint — the fingerprint + prior-image digest are written to evidence for the cutover/rollback.
    - Test 4: `07-DEPLOYMENT-EVIDENCE.md` passes the evidence-privacy scrub (no URLs/keys/GTINs/product values/bodies) and records only immutable identities and closed outcomes.
  </behavior>
  <action>Require the 07-08 manifest and green gates. Run `GROCY_AI_PHP=php8.5 bash custom/grocy_AI/tests/release-gate.sh predeploy <manifest>` and confirm PASS. Build the stable image strictly from the committed `stable_runtime_sha` archive (never a mutable checkout) using `Dockerfile.atech`; capture the resulting immutable digest. Record in `07-DEPLOYMENT-EVIDENCE.md`: `stable_source_sha` (= runtime SHA), `stable_image_id` (`sha256:` digest), `stable_module_version`, `stable_cache_marker`, `asset_version`, and the mutable build tag (`atechpcs/grocy-ai:4.6`) for reference. Run `custom/grocy_AI/tests/deployment-gate.sh predeploy <manifest>` (read-only prod capture) and record its emitted `PREDEPLOY_FINGERPRINT` as `predeploy_fingerprint` and `PRIOR_STABLE_IMAGE_ID` as `prior_stable_image_id` — reconcile the latter with the 07-10 runbook's `prior_image_digest` (they must agree). Confirm `release-gate.sh evidence <manifest>` passes over the evidence file. Do NOT deploy to prod, rebuild the live stack, or write any prod state — this plan only builds the artifact and captures read-only predeploy evidence.</action>
  <verify><automated>rg -n "stable_image_id: sha256:[0-9a-f]{64}" .planning/phases/07-upstream-stable-release-sustainment/07-DEPLOYMENT-EVIDENCE.md && rg -n "stable_source_sha: [0-9a-f]{40}" .planning/phases/07-upstream-stable-release-sustainment/07-DEPLOYMENT-EVIDENCE.md && rg -n "predeploy_fingerprint: [0-9a-f]{64}|prior_stable_image_id: sha256:" .planning/phases/07-upstream-stable-release-sustainment/07-DEPLOYMENT-EVIDENCE.md && GROCY_AI_PHP=php8.5 bash custom/grocy_AI/tests/release-gate.sh evidence .planning/phases/07-upstream-stable-release-sustainment/07-RELEASE-MANIFEST.md</automated></verify>
  <acceptance_criteria>predeploy passes from clean worktrees, the image is built from the exact runtime SHA and recorded by immutable digest with the D-09 markers, the deployment-gate predeploy fingerprint + prior-image are captured (agreeing with 07-10), and the evidence file is redacted and passes the evidence gate.</acceptance_criteria>
  <done>A provenance-pinned v1.0 image exists by digest with a passing predeploy/evidence gate and the identities the cutover will deploy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| runtime SHA → built image | An image built from anything but the exact committed runtime SHA breaks provenance. |
| Phase-2 gate → Phase-7 gate | Re-pointing must not silently weaken any mount/auth/media/fingerprint/privacy identity. |
| predeploy capture → evidence | Read-only prod fingerprint/prior-image must be recorded without leaking private data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-07-12-01 | Spoofing | image provenance | mitigate | Build only from the full-40 runtime SHA; identify by immutable `sha256:` digest; predeploy asserts parent chain + parity. |
| T-07-12-02 | Tampering | gate strength | mitigate | Rewire keeps every existing identity; `sh -n` + a negative grep prove Phase-2/companion identities are dropped, not the security checks. |
| T-07-12-03 | Information Disclosure | deployment evidence | mitigate | `assert_evidence_privacy` scrub retained; evidence records only immutable identities and closed outcomes. |
| T-07-12-04 | Tampering | live prod | mitigate | No build step touches prod; predeploy is read-only capture only. |
| T-07-12-SC | Tampering | package installs | accept | No package installation is planned. |
</threat_model>

<verification>
Run `release-gate.sh predeploy`, build from the exact runtime SHA, `deployment-gate.sh predeploy` (read-only), and `release-gate.sh evidence`; confirm the recorded `sha256:` digest, runtime SHA, D-09 markers, predeploy fingerprint, and prior-image (agreeing with 07-10). Confirm `sh -n` clean and the negative grep proves Phase-2/companion identities are gone while security checks remain.
</verification>

<success_criteria>
- `deployment-gate.sh` is Phase-7-keyed (baseline, topology, markers, migration state) and no weaker than before.
- The stable image is built from the exact runtime SHA and recorded by immutable digest with the D-09 markers.
- predeploy + evidence gates pass; the predeploy fingerprint and prior-image are captured for the cutover/rollback.
- No prod state was written.
</success_criteria>

<output>Create .planning/phases/07-upstream-stable-release-sustainment/07-12-SUMMARY.md when done</output>
