---
phase: 07-upstream-stable-release-sustainment
plan: "03"
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - .planning/phases/07-upstream-stable-release-sustainment/07-SOURCE-AUDIT.md
  - CUSTOMIZATIONS.md
autonomous: true
requirements: [REL-01]
must_haves:
  truths:
    - "REL-01/D-05: Every native Grocy seam the four phases touch (controller base class + JsonMiddleware, root conditional route hook, product-form Blade Save hook, native GenericEntityApiController pre-save hook, Dockerfile.atech COPY layout, version/cache markers) is enumerated with justification, confirming feature code stays inside custom/grocy_AI/ + public/custom/grocy_AI/ wherever possible."
    - "D-05 (RESOLVED): GrocyAiCaptureController.php extends the branch-specific `BaseController`, so it is CONFIRMED a stable-adapter seam that stays OUT of the manifest and rides the 07-06 adapter commit; the audit quotes the deciding base-class line and reconciles this with 07-01's manifest (excluded) and 07-02's capture gate (asserted not-in-manifest)."
    - "REL-01: Each file in the dev-vs-stable diff (~171 files) is classified upstream-drift / portable-module / ATECHPCS-adapter, proving no feature logic leaked into native trees, and the audit output is the exact stable-adapter path allowlist 07-06 consumes."
  artifacts:
    - path: .planning/phases/07-upstream-stable-release-sustainment/07-SOURCE-AUDIT.md
      provides: the enumerated native-seam inventory, the full dev-vs-stable diff classification, and the finite stable-adapter allowlist for 07-06
    - path: CUSTOMIZATIONS.md
      provides: the recorded GrocyAiCaptureController.php disposition and the seam justifications
  key_links:
    - from: .planning/phases/07-upstream-stable-release-sustainment/07-SOURCE-AUDIT.md
      to: custom/grocy_AI/portable-files.txt
      via: CaptureController classification reconciled with 07-01's manifest
      pattern: stable-adapter-allowlist
    - from: .planning/phases/07-upstream-stable-release-sustainment/07-SOURCE-AUDIT.md
      to: 07-06 stable-adapter commit
      via: the exact framework-seam diff-tree allowlist
      pattern: stable_adapter_paths
---

## Phase Goal

**As a** maintainer promoting four phases onto a divergent stable base, **I want** a definitive audit of every ATECHPCS core-hook seam and of the entire dev-vs-stable diff, **so that** I can prove feature code lives inside the module boundary and hand 07-06 an exact, justified stable-adapter allowlist.

<objective>
Audit the ATECHPCS core-hook points and classify the full dev-vs-stable diff so the portable/adapter split is provable, not assumed.

Purpose: REL-01 requires that promotion adapts only framework seams and never drags feature logic into native Grocy trees; this audit is the evidence and the source of the 07-06 allowlist (D-03 order: audit precedes mirror/adapter).
Output: `07-SOURCE-AUDIT.md` enumerating every touched native seam with justification, classifying each of the ~171 diff files as upstream-drift / portable / adapter, resolving the `GrocyAiCaptureController.php` classification, and publishing the finite stable-adapter path allowlist for 07-06.
</objective>

<execution_context>@/Users/ian/.codex/get-shit-done/workflows/execute-plan.md</execution_context>

<context>
@.planning/phases/07-upstream-stable-release-sustainment/07-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-safety-baseline-mobile-diagnostics/01-09-PLAN.md
@.planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-19-PLAN.md
@custom/grocy_AI/portable-files.txt
@custom/grocy_AI/phase-v1-changed-paths.txt
@custom/grocy_AI/tests/check-portable-parity.sh
@custom/grocy_AI/src/GrocyAiApiController.php
@custom/grocy_AI/src/GrocyAiCaptureController.php
@custom/grocy_AI/routes.php
@CUSTOMIZATIONS.md
<interfaces>
The established adapter seam set (from 01-09 / 02-19 `check-portable-parity.sh` documented adapters): `custom/grocy_AI/src/GrocyAiApiController.php` (stable controller namespace/base class), `custom/grocy_AI/routes.php` (stable middleware/bootstrap syntax + root conditional route hook), `views/productform.blade.php` (product-form integration + Save hook + `$grocyAiAssetVersion`), `custom/grocy_AI/version.json` (independent stable cache marker), `Dockerfile.atech` (COPY overlay), `CUSTOMIZATIONS.md`. Phase 4-8 add the module Blade views `views/grocyai_bulkreview.blade.php`, `views/grocyai_capture.blade.php`, `views/grocyai_capture_review.blade.php`, `views/grocyai_conversioncoverage.blade.php` and possibly new `public/viewjs/*` assets — all candidate adapter seams because they live in the core `views/`/`viewjs/` trees, not under `custom/grocy_AI/`.
Diff reality (from 07-CONTEXT code_context): `git diff --name-only origin/atech-release..HEAD` over `custom/ views/ public/ routes.php controllers/ migrations/` returns ~171 files, of which ~125 are native upstream drift (LinuxServer v4.6.0 vs newer upstream) and must be classified as drift, NOT adapter. The audit must separate the three buckets so 07-06's allowlist contains only genuine ATECHPCS feature-integration seams.
CaptureController decision procedure (D-05, RESOLVED): open `GrocyAiCaptureController.php` and confirm the deciding evidence — it extends the branch-specific `BaseController` (a Grocy framework base class that differs across the upstream-drift branches), NOT a framework-agnostic self-contained class. That makes it a stable-adapter seam: it stays OUT of the manifest, rides the 07-06 adapter commit, and appears on the 07-06 adapter allowlist. There is NO 16th portable file — the audit CONFIRMS the adapter classification rather than promoting the controller to portable. Quote the deciding base-class line and reconcile with 07-01 (which excludes it definitively) and with 07-02's capture-mode seam assertion (asserts not-in-manifest).
07-06 adapter allowlist (must be identical in 07-01/07-03/07-06): the exact stable-adapter diff-tree is `CUSTOMIZATIONS.md`, `custom/grocy_AI/routes.php`, `custom/grocy_AI/src/GrocyAiApiController.php`, `custom/grocy_AI/src/GrocyAiCaptureController.php`, `views/grocyai_bulkreview.blade.php`, `views/grocyai_capture.blade.php`, `views/grocyai_capture_review.blade.php`, `views/grocyai_conversioncoverage.blade.php`. The runtime markers `Dockerfile.atech`, `custom/grocy_AI/version.json`, and `views/productform.blade.php` are separately-adapted seams that ride the 07-07 runtime commit, not this 07-06 allowlist.
No code changes: this plan produces documentation and a classification only. It does not edit source, gate scripts (beyond none), or commit on the stable branch (07-05/06 own that).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate the native core-hook seams with justification</name>
  <files>.planning/phases/07-upstream-stable-release-sustainment/07-SOURCE-AUDIT.md, CUSTOMIZATIONS.md</files>
  <read_first>.planning/phases/01-safety-baseline-mobile-diagnostics/01-09-PLAN.md; .planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-19-PLAN.md; custom/grocy_AI/src/GrocyAiApiController.php; custom/grocy_AI/routes.php; custom/grocy_AI/tests/check-portable-parity.sh</read_first>
  <behavior>
    - Test 1: `07-SOURCE-AUDIT.md` lists every native Grocy seam the four phases touch — controller base class + JsonMiddleware, the root conditional route hook in `routes.php`, the `views/productform.blade.php` Save hook + asset token, the native `GenericEntityApiController` pre-save lifecycle, the four `views/grocyai_*.blade.php` module views, any new `public/viewjs/*` asset, `Dockerfile.atech` COPY layout, and `version.json`/`module-version.json`/Blade cache markers — each with a one-line justification for why it must live at the seam and not inside `custom/grocy_AI/`.
    - Test 2: for each seam the audit states the module-boundary rule it satisfies (feature logic stays in `custom/grocy_AI/` + `public/custom/grocy_AI/`; only branch-coupled glue rides the seam), matching how 01-09/02-19 justified the original adapter set.
    - Test 3: the seam inventory is internally consistent with `check-portable-parity.sh`'s documented adapters (no seam listed as both portable and adapter).
  </behavior>
  <action>Write the seam inventory into `07-SOURCE-AUDIT.md` with a table (seam file | native mechanism | why it cannot be portable | which phase(s) use it). Cross-reference the 01-09/02-19 adapter rationale. Add a short pointer in `CUSTOMIZATIONS.md` to the audit. Assert nothing about code correctness here beyond the boundary classification; make no source edits.</action>
  <verify><automated>test -f .planning/phases/07-upstream-stable-release-sustainment/07-SOURCE-AUDIT.md && grep -Fq 'GenericEntityApiController' .planning/phases/07-upstream-stable-release-sustainment/07-SOURCE-AUDIT.md && grep -Fq 'productform.blade.php' .planning/phases/07-upstream-stable-release-sustainment/07-SOURCE-AUDIT.md && grep -Fq 'Dockerfile.atech' .planning/phases/07-upstream-stable-release-sustainment/07-SOURCE-AUDIT.md && for v in grocyai_bulkreview grocyai_capture grocyai_capture_review grocyai_conversioncoverage; do grep -Fq "$v" .planning/phases/07-upstream-stable-release-sustainment/07-SOURCE-AUDIT.md || { echo "missing $v"; exit 1; }; done</automated></verify>
  <acceptance_criteria>Every touched native seam is enumerated with a boundary justification, consistent with the documented adapter set, and no feature-logic seam is missing from the inventory.</acceptance_criteria>
  <done>The audit proves which native seams the promotion must adapt and why, with feature code confined to the module tree.</done>
</task>

<task type="auto">
  <name>Task 2: Classify the full dev-vs-stable diff, resolve CaptureController, and publish the 07-06 allowlist</name>
  <files>.planning/phases/07-upstream-stable-release-sustainment/07-SOURCE-AUDIT.md, CUSTOMIZATIONS.md</files>
  <read_first>custom/grocy_AI/src/GrocyAiCaptureController.php; custom/grocy_AI/src/GrocyAiApiController.php; custom/grocy_AI/portable-files.txt; custom/grocy_AI/phase-v1-changed-paths.txt</read_first>
  <behavior>
    - Test 1: every path in `git diff --name-only origin/atech-release..HEAD` (over `custom/ views/ public/ routes.php controllers/ migrations/`) is classified in the audit as exactly one of upstream-drift / portable-module / ATECHPCS-adapter; the counts are recorded and the three buckets partition the diff with no unclassified file.
    - Test 2: `GrocyAiCaptureController.php` is CONFIRMED a stable-adapter seam by the D-05 base-class check — the deciding line showing it extends the branch-specific `BaseController` is quoted — and the outcome is reconciled with 07-01's manifest (stays excluded; no 16th portable file).
    - Test 3: the audit publishes a finite, sorted stable-adapter path allowlist (the framework seams only — no upstream-drift path, no portable path) that 07-06's `stable_adapter_paths` diff-tree must equal.
  </behavior>
  <action>Run the dev-vs-stable diff, bucket every path, and record the classification table + bucket counts in `07-SOURCE-AUDIT.md`. Open `GrocyAiCaptureController.php`, apply the D-05 base-class test, quote the deciding line, and state the classification (expected: stable-adapter — it extends the branch-specific `BaseController`); confirm 07-01 keeps it excluded from the manifest (no 16th portable file) and note the reconciliation for 07-02's capture seam assertion. Emit the finite stable-adapter allowlist as a fenced, `LC_ALL=C sort`-ed list labeled for 07-06 consumption. Confirm no portable path (from `portable-files.txt`) and no upstream-drift path appears on the adapter allowlist. Make no source or manifest edits — reconciliation edits belong to 07-01/07-05/07-06.</action>
  <verify><automated>test -f .planning/phases/07-upstream-stable-release-sustainment/07-SOURCE-AUDIT.md && grep -Eiq 'BaseApiController|JsonMiddleware' .planning/phases/07-upstream-stable-release-sustainment/07-SOURCE-AUDIT.md && grep -Eiq 'upstream.?drift' .planning/phases/07-upstream-stable-release-sustainment/07-SOURCE-AUDIT.md && grep -Eiq 'stable.?adapter (allowlist|path)' .planning/phases/07-upstream-stable-release-sustainment/07-SOURCE-AUDIT.md</automated></verify>
  <acceptance_criteria>The ~171-file diff is fully partitioned into drift/portable/adapter with recorded counts, CaptureController is classified with quoted base-class evidence and reconciled to the manifest, and a finite sorted stable-adapter allowlist (seams only) is published for 07-06.</acceptance_criteria>
  <done>No feature logic is shown to have leaked into native trees, and 07-06 has an exact, justified adapter allowlist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| module tree -> native Grocy trees | Feature logic must not live in `controllers/`, core `views/`, `public/viewjs/`, or `migrations/` beyond the justified seams. |
| dev lineage -> stable base | Upstream drift must not be mistaken for an ATECHPCS adapter, or 07-06 would carry unrelated upstream bytes. |
| CaptureController classification -> manifest/gate | A wrong portable/adapter call breaks either byte-parity (if a branch-coupled file is called portable) or the adapter diff-tree (if a portable file rides the adapter commit). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-07-03-01 | Tampering | native seam inventory | mitigate | Enumerate every seam with justification; cross-check against the documented adapter set. |
| T-07-03-02 | Spoofing | diff classification | mitigate | Partition all ~171 diff paths into drift/portable/adapter with recorded counts; no unclassified file. |
| T-07-03-03 | Elevation of privilege | CaptureController call | mitigate | Decide by quoted base-class evidence (D-05); reconcile to manifest and capture gate. |
| T-07-03-04 | Repudiation | adapter allowlist | mitigate | Publish a finite sorted seams-only allowlist for 07-06 to match exactly. |
</threat_model>

<verification>
Confirm `07-SOURCE-AUDIT.md` partitions the entire dev-vs-stable diff (recorded counts sum to the diff size), enumerates every native seam with justification, quotes the CaptureController base-class evidence, and publishes a sorted adapter allowlist that shares no path with `portable-files.txt` or the upstream-drift bucket. No gate run is required in this plan.
</verification>

<success_criteria>
REL-01 is evidenced: feature code is confined to the module tree, every native seam is justified, the full diff is classified, CaptureController is resolved by the D-05 test, and 07-06 has an exact stable-adapter allowlist.
</success_criteria>

<output>Create .planning/phases/07-upstream-stable-release-sustainment/07-03-SUMMARY.md when done</output>
</content>
