---
phase: 07-upstream-stable-release-sustainment
plan: "13"
type: execute
wave: 9
depends_on: ["07-11", "07-12"]
files_modified:
  - .planning/phases/07-upstream-stable-release-sustainment/07-DEPLOYMENT-EVIDENCE.md
autonomous: false
requirements: [REL-05]
must_haves:
  truths:
    - "D-13: Cutover = push `atech-release` → rebuild the Komodo `grocy` stack on 10.10.0.156 (`pull_policy: build`) to the recorded runtime SHA/digest → CLEAR `GROCY_DATAPATH/viewcache/route_cache.php` → verify; the route-cache clear MUST precede route verification (the documented 404 trap)."
    - "REL-05: `/etc/komodo/grocy:/config` data, product images, and the DB survive restart — 444 products intact — and feature flags (`GROCY_FEATURE_FLAG_GROCY_AI=true` + companion URL/key), module migration state, routes, and cache markers reflect the new release."
    - "The live rebuild is a blocking `checkpoint:human-action` (the destructive prod step); it deploys the exact 07-12 digest/runtime SHA, with the 07-10 runbook staged for rollback on any failure."
  artifacts:
    - path: .planning/phases/07-upstream-stable-release-sustainment/07-DEPLOYMENT-EVIDENCE.md
      provides: "The live post-cutover record — running image digest/revision, persistent-data continuity, route-cache clear, route/flag/migration/marker verification, and postdeploy/postsmoke gate outcomes"
  key_links:
    - from: .planning/phases/07-upstream-stable-release-sustainment/07-DEPLOYMENT-EVIDENCE.md
      to: "running grocy container image digest"
      via: "postdeploy-stable asserts running image = recorded stable_image_id and revision = stable_source_sha"
      pattern: "stable_running_image|stable_running_revision"
    - from: "route-cache clear"
      to: "route verification"
      via: "rm GROCY_DATAPATH/viewcache/route_cache.php ordered before any /api/grocy-ai or module-page check"
      pattern: "route_cache.php"
---

## Phase Goal

**As a** household maintainer, **I want to** deploy the v1.0 stable image to prod by rebuilding the Komodo stack, clear the route cache, and confirm the household data and module survive, **so that** the promoted release runs on the single household instance without data loss or 404ing module routes.

<objective>
Execute the prod cutover: push `atech-release`, rebuild the `grocy` stack to the recorded digest, clear the route cache, and verify persistent continuity, routes, flags, migration state, and markers — with rollback staged.

Purpose: the destructive prod step, gated behind human action, deploying the exact 07-12 artifact and honoring the route-cache-clear-before-verify invariant (D-13).
Output: `07-DEPLOYMENT-EVIDENCE.md` completed with live running identity, continuity, route-cache clear, and passing postdeploy/postsmoke gates.
</objective>

<execution_context>@/Users/ian/.codex/get-shit-done/workflows/execute-plan.md</execution_context>

<context>
@.planning/phases/07-upstream-stable-release-sustainment/07-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-upstream-stable-release-sustainment/07-RELEASE-MANIFEST.md
@.planning/phases/07-upstream-stable-release-sustainment/07-DEPLOYMENT-EVIDENCE.md
@.planning/phases/07-upstream-stable-release-sustainment/07-RECOVERY-RUNBOOK.md
@.planning/phases/07-upstream-stable-release-sustainment/07-HUMAN-VERIFY.md
@custom/grocy_AI/tests/deployment-gate.sh
<interfaces>
Prod = `10.10.0.156`; Komodo stack `grocy` on periphery "Personal Docker (102)"; container `grocy`, port 9283, bind `/etc/komodo/grocy:/config`; compose at `/etc/komodo/stacks/grocy/personal-102/grocy/compose.yaml` builds `github.com/ATECHPCS/grocy.git#atech-release` via `Dockerfile.atech`, `image: atechpcs/grocy-ai:4.6`, `pull_policy: build`. SSH `root@10.10.0.156` via 1Password `ssh:Personal Docker (102)`.
Route cache (the load-bearing clear): inside the container `GROCY_DATAPATH=/config/data`, so clear `/config/data/viewcache/route_cache.php` (host `/etc/komodo/grocy/data/viewcache/route_cache.php`). Without it, every module route 404s after rebuild (documented trap, MISTAKES/MEMORY).
From 07-12: `07-DEPLOYMENT-EVIDENCE.md` already carries `stable_source_sha`, `stable_image_id`, D-09 markers, `predeploy_fingerprint`, and `prior_stable_image_id`. The Phase-7 `deployment-gate.sh` modes `postdeploy-stable`/`postsmoke`/`final` verify running identity, mount, markers, migration state, and the protected fingerprint against the manifest/evidence.
Env already set on prod: `GROCY_FEATURE_FLAG_GROCY_AI=true`, `GROCY_AI_SERVICE_URL`, `GROCY_AI_SERVICE_API_KEY`.
Preconditions: 07-11 sign-off recorded, 07-12 predeploy/evidence green, 07-10 runbook staged. Root-on-prod SSH + destructive stack ops are gated by the auto-mode classifier and need explicit authorization (global CLAUDE.md).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Rebuild the prod grocy stack to the recorded digest and clear the route cache</name>
  <read_first>.planning/phases/07-upstream-stable-release-sustainment/07-DEPLOYMENT-EVIDENCE.md; .planning/phases/07-upstream-stable-release-sustainment/07-RECOVERY-RUNBOOK.md; .planning/phases/07-upstream-stable-release-sustainment/07-HUMAN-VERIFY.md; custom/grocy_AI/tests/deployment-gate.sh</read_first>
  <action>Block until the maintainer confirms all preconditions: 07-11 sign-off recorded, 07-12 `release-gate.sh predeploy` + `deployment-gate.sh predeploy` green, `07-RECOVERY-RUNBOOK.md` staged with the `prior_stable_image_id` pinned, and the household LAN/Komodo path reachable. Explicit authorization is required for the root-on-prod SSH and the destructive rebuild.
  Execute the cutover in order: (1) push `atech-release` so `origin/atech-release` = the recorded `stable_source_sha` (runtime SHA); (2) rebuild the Komodo `grocy` stack on periphery "Personal Docker (102)" (`pull_policy: build`) so it builds that exact SHA; (3) confirm the container restarts and the running image digest = `stable_image_id` and running revision = `stable_source_sha`; (4) **CLEAR `GROCY_DATAPATH/viewcache/route_cache.php`** (`rm` inside the container / on the host `/etc/komodo/grocy/data/viewcache/route_cache.php`) — this MUST happen before any route verification. Do NOT verify routes before the clear. On ANY failure at any step (build failure, digest mismatch, container not healthy, mount lost), STOP and execute the 07-10 rollback runbook (Path A prior-image redeploy, Path B DB restore if data is affected) rather than improvising.</action>
  <what-built>The v1.0 stable image (07-12 digest) deployed to the single household prod instance by Komodo rebuild, with the route cache cleared.</what-built>
  <how-to-verify>1. `origin/atech-release` HEAD equals `stable_source_sha`. 2. The Komodo `grocy` stack rebuilt without error and the container is running/healthy. 3. Running image digest = `stable_image_id`; running `org.opencontainers.image.revision` = `stable_source_sha`. 4. `/etc/komodo/grocy:/config` is still mounted RW and the DB/product images are present. 5. `route_cache.php` has been removed (confirm the file is absent) BEFORE any route check.</how-to-verify>
  <resume-signal>Type "rebuilt" once the stack is rebuilt to the digest and the route cache is cleared, or "rolled back" plus the failure if recovery was executed.</resume-signal>
  <verify><automated>GROCY_AI_PHP=php8.5 bash custom/grocy_AI/tests/deployment-gate.sh postdeploy-stable .planning/phases/07-upstream-stable-release-sustainment/07-RELEASE-MANIFEST.md .planning/phases/07-upstream-stable-release-sustainment/07-DEPLOYMENT-EVIDENCE.md</automated></verify>
  <done>The prod stack runs the recorded v1.0 digest with `/config` data intact and the route cache cleared.</done>
</task>

<task type="auto">
  <name>Task 2: Verify persistent continuity, routes, flags, migration state, markers; record evidence</name>
  <files>.planning/phases/07-upstream-stable-release-sustainment/07-DEPLOYMENT-EVIDENCE.md</files>
  <read_first>custom/grocy_AI/tests/deployment-gate.sh; .planning/phases/07-upstream-stable-release-sustainment/07-DEPLOYMENT-EVIDENCE.md; .planning/phases/07-upstream-stable-release-sustainment/07-RELEASE-MANIFEST.md</read_first>
  <behavior>
    - Test 1 (continuity): `/etc/komodo/grocy:/config` mount is RW and unchanged; the DB reports 444 products; product images are present; the post-rebuild protected fingerprint matches `predeploy_fingerprint` (no household-data mutation from the rebuild).
    - Test 2 (route-cache order): `route_cache.php` was cleared before route checks; module routes resolve — `/api/grocy-ai/*` return auth-required (401/403) unauthenticated (not 404), and the product form / module pages render.
    - Test 3 (flags + markers + migration): `GROCY_FEATURE_FLAG_GROCY_AI=true` and companion URL/key are active; running `module_version`, `version.json` `Customization`, and `$grocyAiAssetVersion` equal the D-09 values; the v1.0 module tables/version rows exist.
    - Test 4 (gate + evidence): `deployment-gate.sh postdeploy-stable` and `postsmoke` pass; `07-DEPLOYMENT-EVIDENCE.md` records live running identity, continuity, route-cache clear, and closed PASS/DENIED outcomes, redacted.
  </behavior>
  <action>After the rebuild + route-cache clear, run the ordered REL-05 verification (route-cache clear already precedes this): (1) persistent-data continuity — RW `/config` mount, `COUNT(*) FROM products = 444`, product-picture tree present, and the protected fingerprint = `predeploy_fingerprint`; (2) route resolution — module pages render and `/api/grocy-ai/*` are auth-gated (401/403), NOT 404; (3) feature flag + companion env active; (4) module migration state — bulk/categorization/capture tables + version rows present, idempotent (no re-migration on restart); (5) cache markers — running `module_version` / `Customization` / asset version = D-09 values. Run `deployment-gate.sh postdeploy-stable` then `postsmoke` against the Phase-7 manifest + evidence, using `GROCY_AI_BROWSER_SMOKE_ATTESTATION` or `GROCY_AI_AUTH_HEADER` for the authenticated reads (private inputs stay in-session). Record every outcome in `07-DEPLOYMENT-EVIDENCE.md` (running digest/revision, continuity MATCH, route-cache-cleared, routes-resolve, flags, migration, markers, gate PASS), redacted. If any check fails, STOP and invoke the 07-10 runbook. Leave the `final` gate + physical acceptance to 07-14.</action>
  <verify><automated>rg -n "stable_running_image: sha256:|stable_running_revision: [0-9a-f]{40}" .planning/phases/07-upstream-stable-release-sustainment/07-DEPLOYMENT-EVIDENCE.md && rg -n "product_count: 444|protected_state_after_rebuild: MATCH|route_cache_cleared: yes|module_routes_resolve: PASS|feature_flag_active: true" .planning/phases/07-upstream-stable-release-sustainment/07-DEPLOYMENT-EVIDENCE.md && GROCY_AI_PHP=php8.5 bash custom/grocy_AI/tests/deployment-gate.sh postsmoke .planning/phases/07-upstream-stable-release-sustainment/07-RELEASE-MANIFEST.md .planning/phases/07-upstream-stable-release-sustainment/07-DEPLOYMENT-EVIDENCE.md</automated></verify>
  <acceptance_criteria>Continuity holds (444 products, fingerprint match, images present), the route cache was cleared before routes were verified as resolving (no 404), flags/markers/migration reflect v1.0, postdeploy-stable + postsmoke pass, and evidence is recorded redacted.</acceptance_criteria>
  <done>The cutover is verified: the new release runs with household data intact and module routes live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| recorded digest → running container | The rebuild must run the exact 07-12 digest/runtime SHA, not a drifted rebuild or stale tag. |
| stable image → persistent volume | The rebuild mounts irreplaceable household state; data must survive with a matching fingerprint. |
| route cache → module routes | A stale `route_cache.php` 404s every module route; the clear must precede verification. |
| live responses → evidence | Authenticated live reads must not leak private household/provider data into the record. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-07-13-01 | Spoofing/Tampering | running image identity | mitigate | postdeploy-stable asserts running digest = `stable_image_id` and revision = `stable_source_sha`. |
| T-07-13-02 | Tampering/DoS | persistent volume | mitigate | RW `/config` mount check, 444-product count, and protected-fingerprint match vs predeploy; rollback runbook staged. |
| T-07-13-03 | Denial of Service | module routes | mitigate | Mandatory `route_cache.php` clear ordered before route verification; routes proven auth-gated (not 404). |
| T-07-13-04 | Information Disclosure | deployment evidence | mitigate | Evidence-privacy scrub retained; only immutable identities + closed outcomes recorded. |
| T-07-13-05 | Elevation of Privilege | live routes | mitigate | postsmoke proves unauthenticated denial and authenticated reads before acceptance. |
| T-07-13-SC | Tampering | package installs | accept | No package installation is planned. |
</threat_model>

<verification>
Confirm `origin/atech-release` = `stable_source_sha`, the running image digest/revision match the recorded values, `route_cache.php` was cleared before routes were verified as resolving (no 404), 444 products + fingerprint MATCH + images present, flags/markers/migration reflect v1.0, and `deployment-gate.sh postdeploy-stable` + `postsmoke` pass. On any failure, the 07-10 runbook is executed.
</verification>

<success_criteria>
- The prod `grocy` stack runs the recorded v1.0 digest from `atech-release`.
- `/etc/komodo/grocy` data, 444 products, and product images survive restart with a matching protected fingerprint.
- The route cache is cleared before route verification and module routes resolve (no 404).
- Feature flags, module migration state, and cache markers reflect the new release; postdeploy/postsmoke gates pass.
</success_criteria>

<output>Create .planning/phases/07-upstream-stable-release-sustainment/07-13-SUMMARY.md when done</output>
