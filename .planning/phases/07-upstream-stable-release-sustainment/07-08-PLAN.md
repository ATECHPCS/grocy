---
phase: 07-upstream-stable-release-sustainment
plan: "08"
type: execute
wave: 6
depends_on: ["07-02", "07-05", "07-06", "07-07"]
files_modified:
  - .planning/phases/07-upstream-stable-release-sustainment/07-RELEASE-MANIFEST.md
  - custom/grocy_AI/tests/release-gate.sh
autonomous: true
requirements: [REL-02, REL-03]
must_haves:
  truths:
    - "D-10: The Phase-7 release manifest records lower-case full-40 main / stable-portable / stable-adapter / stable-runtime SHAs, direct-parent equalities, marker values, and a finite post-candidate allowlist — no wildcard, directory prefix, or implementation path."
    - "The dual-branch `release-gate.sh candidate` and every per-phase mode (`taxonomy`, `conversions`, `bulk`, `categorization`, `capture`) pass from clean worktrees with `php8.5`, using the 07-02 plumbing fixes (manifest-derived portable count + `phase-v1-changed-paths.txt`)."
    - "The categorization/conversion-audit gate passes against the real 444-product prod-shaped snapshot with 0 suspicious (D-16); byte parity and immutable ancestry are verified. Hard blocking."
  artifacts:
    - path: .planning/phases/07-upstream-stable-release-sustainment/07-RELEASE-MANIFEST.md
      provides: Phase-7 candidate identities, marker values, path-scope blocks, pinned portable blob hashes, and candidate gate results
    - path: custom/grocy_AI/tests/release-gate.sh
      provides: Closed candidate/predeploy replay bound to the Phase-7 manifest and per-phase modes
  key_links:
    - from: "manifest candidate fields (main/portable/adapter/runtime SHAs)"
      to: "release-gate candidate/predeploy"
      via: "full-SHA + direct-parent + exact-scope replay"
      pattern: "immutable_ancestry|stable_portable_scope|stable_adapter_scope|stable_runtime_scope"
    - from: "categorization + capture gate modes"
      to: "444-product prod-shaped snapshot"
      via: "portable-manifest membership, storage-boundary, fail-closed unit suite on prod-shaped data"
      pattern: "categorization|capture"
---

## Phase Goal

**As a** household maintainer, **I want to** bind the promoted release to a single trustworthy manifest and prove the dual-branch gate green against production-shaped data, **so that** the v1.0 cutover is certified by tooling that matches the 46+-path module reality — not the retired Phase-2 scope.

<objective>
Author the Phase-7 release manifest and replay the closed dual-branch release gate (candidate + every per-phase mode) against prod-shaped data as a hard blocking exit gate.

Purpose: The release is trustworthy only if the manifest names only the fixed immutable candidates and the gate proves every promoted phase's manifest membership, storage boundary, and fail-closed suite on real-shaped data.
Output: `07-RELEASE-MANIFEST.md` + a green candidate/predeploy replay with all five per-phase modes passing on the 444-product snapshot.
</objective>

<execution_context>
@/home/dev/personal-projects/Grocy AI/grocy/.planning/phases/07-upstream-stable-release-sustainment/07-CONTEXT.md
</execution_context>

<context>
@.planning/phases/07-upstream-stable-release-sustainment/07-CONTEXT.md
@.planning/phases/07-upstream-stable-release-sustainment/07-05-SUMMARY.md
@.planning/phases/07-upstream-stable-release-sustainment/07-06-SUMMARY.md
@.planning/phases/07-upstream-stable-release-sustainment/07-07-SUMMARY.md
@.planning/phases/07-upstream-stable-release-sustainment/07-SNAPSHOT.md
@.planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-RELEASE-MANIFEST.md
@.planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-19-PLAN.md
@custom/grocy_AI/tests/release-gate.sh
@custom/grocy_AI/tests/check-portable-parity.sh
@custom/grocy_AI/portable-files.txt
@custom/grocy_AI/phase-v1-changed-paths.txt

<interfaces>
- `MAIN` = dev-lineage checkout; `STABLE` = `atech-release` worktree; `COMPANION` = the companion checkout the existing gate expects (branch `main`). Single-checkout fallback is supported per the breakdown; if the companion checkout is absent, run the companion assertions in single-checkout mode and record that.
- Manifest shape (mirror `02-RELEASE-MANIFEST.md` field/block names): `main_branch`, `main_candidate_sha` (immutable dev-head from 07-05), `stable_branch: atech-release`, `stable_base_sha`, `stable_portable_sha` + `stable_portable_parent_sha` (07-05), `stable_adapter_sha` + `stable_adapter_parent_sha` (07-06), `stable_runtime_sha` + `stable_runtime_parent_sha` (07-07), `stable_module_version: 2.6.0`, `stable_cache_marker: ATECHPCS-grocy_AI-11`, the `*_sha256` block hashes, then the sorted-unique blocks `portable_paths`, `phase_v1_changed_paths`, `stable_adapter_paths`, `stable_runtime_paths`, `main_post_candidate_paths`, `portable_blobs`, and `candidate_gate_results`.
- Direct-parent invariants the gate enforces: `stable_adapter_parent_sha == stable_portable_sha`, `stable_runtime_parent_sha == stable_adapter_sha`, and both Git-parent equalities. All SHAs lower-case full-40.
- `main_post_candidate_paths` is a FINITE sorted list: the manifest, the 07-05..07-14 plan/summary artifacts, `07-DEPLOYMENT-EVIDENCE.md`, `07-PHASE-ACCEPTANCE.md`, `custom/grocy_AI/tests/release-gate.sh`, `custom/grocy_AI/tests/deployment-gate.sh`. NEVER a wildcard, directory prefix, or implementation/provider/migration/adapter/Dockerfile/household-data path.
- Per-phase gate modes: `taxonomy`, `conversions`, `bulk` already exist; `categorization` (Phase 6) and `capture` (Phase 8) are added by 07-02 — this plan RUNS them and asserts they pass at the depth of `bulk`/`conversions` (portable-manifest membership incl. the D-04 additions, storage boundary, fail-closed unit suite, PHP lint). The `views/grocyai_*.blade.php` views stay OUT of the portable manifest (adapter-carried), matching the existing `bulk_blade_view_not_manifest_tracked` assertion.
- D-16: run the categorization/conversion-audit gate against the real 444-product prod-shaped snapshot (from 07-04/07-SNAPSHOT.md) and confirm `0 suspicious` (prod = 62 global + 18 expected / 9 products). A failure here is a hard blocker.
- Run PHP as `php8.5` (`GROCY_AI_PHP=php8.5`). This is a HARD BLOCKING exit gate: any red mode blocks the cutover. Do NOT build the image or deploy (07-12/07-13 own that).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the Phase-7 release manifest binding only fixed immutable candidate identities</name>
  <read_first>
    .planning/phases/07-upstream-stable-release-sustainment/07-05-SUMMARY.md
    .planning/phases/07-upstream-stable-release-sustainment/07-06-SUMMARY.md
    .planning/phases/07-upstream-stable-release-sustainment/07-07-SUMMARY.md
    .planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-RELEASE-MANIFEST.md
    custom/grocy_AI/portable-files.txt
    custom/grocy_AI/phase-v1-changed-paths.txt
  </read_first>
  <files>.planning/phases/07-upstream-stable-release-sustainment/07-RELEASE-MANIFEST.md</files>
  <behavior>
    - Test 1: every SHA field (`main_candidate_sha`, `stable_portable_sha`, `stable_portable_parent_sha`, `stable_adapter_sha`, `stable_adapter_parent_sha`, `stable_runtime_sha`, `stable_runtime_parent_sha`) is a lower-case full-40 hex.
    - Test 2: `stable_adapter_parent_sha == stable_portable_sha`, `stable_runtime_parent_sha == stable_adapter_sha`, and both equal their Git parents in the `STABLE` repo.
    - Test 3: the `portable_paths` and `phase_v1_changed_paths` blocks equal the reconciled files; every `portable_blobs` line's stable blob hash equals the `main_candidate_sha` blob hash; `main_post_candidate_paths` is finite and contains no wildcard/directory/implementation path.
  </behavior>
  <action>Write `07-RELEASE-MANIFEST.md` mirroring the `02-RELEASE-MANIFEST.md` structure with the Phase-7 SHAs (from 07-05/06/07 summaries), markers (`2.6.0` / `ATECHPCS-grocy_AI-11`), sorted-unique path blocks, recomputed portable blob hashes (`git show <sha>:<path> | sha256`), and the finite `main_post_candidate_paths` list. Populate `candidate_gate_results` with the per-mode PASS counts once Task 2 runs. Bind ONLY fixed immutable identities — no mutable ref, no wildcard scope.</action>
  <verify>
    <automated>set -eu; MAIN="/home/dev/personal-projects/Grocy AI/grocy"; STABLE="/home/dev/personal-projects/Grocy AI/grocy-atech-release"; m="$MAIN/.planning/phases/07-upstream-stable-release-sustainment/07-RELEASE-MANIFEST.md"; for f in main_candidate_sha stable_portable_sha stable_portable_parent_sha stable_adapter_sha stable_adapter_parent_sha stable_runtime_sha stable_runtime_parent_sha; do v=$(sed -n "s/^$f: //p" "$m" | head -1); test "${#v}" -eq 40; case "$v" in *[!0-9a-f]*) exit 1 ;; esac; done; ap=$(sed -n 's/^stable_adapter_parent_sha: //p' "$m"|head -1); pp=$(sed -n 's/^stable_portable_sha: //p' "$m"|head -1); test "$ap" = "$pp"; rp=$(sed -n 's/^stable_runtime_parent_sha: //p' "$m"|head -1); as=$(sed -n 's/^stable_adapter_sha: //p' "$m"|head -1); test "$rp" = "$as"; test "$(git -C "$STABLE" rev-parse "${as}^")" = "$pp"; rs=$(sed -n 's/^stable_runtime_sha: //p' "$m"|head -1); test "$(git -C "$STABLE" rev-parse "${rs}^")" = "$as"; ! grep -nE 'main_post_candidate_paths_begin' -A100 "$m" | grep -qE '(\*|/\.\.|^\-[[:space:]]*/)'</automated>
  </verify>
  <acceptance_criteria>All SHA fields are lower-case full-40, both direct-parent chains hold in Git, path blocks equal the reconciled manifests, portable blob hashes match the pinned source, and the post-candidate allowlist is finite with no wildcard/implementation path.</acceptance_criteria>
  <done>The Phase-7 manifest binds only fixed immutable candidate identities with closed post-candidate scope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replay the dual-branch candidate/predeploy gate and every per-phase mode on prod-shaped data</name>
  <read_first>
    custom/grocy_AI/tests/release-gate.sh
    custom/grocy_AI/tests/check-portable-parity.sh
    .planning/phases/07-upstream-stable-release-sustainment/07-SNAPSHOT.md
    .planning/phases/07-upstream-stable-release-sustainment/07-RELEASE-MANIFEST.md
  </read_first>
  <files>custom/grocy_AI/tests/release-gate.sh, .planning/phases/07-upstream-stable-release-sustainment/07-RELEASE-MANIFEST.md</files>
  <behavior>
    - Test 1: `release-gate.sh candidate <manifest>` passes from clean worktrees — `immutable_ancestry`, `stable_portable_scope` (== `phase-v1-changed-paths.txt`), `stable_adapter_scope`, `stable_runtime_scope`, `portable_blob_parity` (manifest-derived count, NOT the retired `-eq 12`), `synchronized_markers`, and `main_post_candidate_scope` all green.
    - Test 2: every per-phase mode passes with `php8.5` — `taxonomy`, `conversions`, `bulk`, `categorization`, `capture` — each asserting portable-manifest membership (incl. the D-04 additions), storage boundary, and fail-closed unit suite; `views/grocyai_*.blade.php` confirmed OUT of the manifest.
    - Test 3: the categorization/conversion-audit gate run against the 444-product prod-shaped snapshot reports `0 suspicious` (62 global + 18 expected / 9 products) per D-16; `release-gate.sh predeploy` passes with all three worktrees clean.
  </behavior>
  <action>Point the gate at `07-RELEASE-MANIFEST.md` (and the 07-02-fixed portable count + `phase-v1-changed-paths.txt` compare). Run `candidate` and each per-phase mode with `GROCY_AI_PHP=php8.5`. Run the `categorization`/conversion-audit assertions against the prod-shaped snapshot from 07-04. Backfill `candidate_gate_results` in the manifest with the observed PASS counts. Run `predeploy` last from clean worktrees. Any red mode is a hard block — do not proceed, do not weaken an assertion to pass. Do not build/deploy the image.</action>
  <verify>
    <automated>set -eu; MAIN="/home/dev/personal-projects/Grocy AI/grocy"; export GROCY_AI_PHP=php8.5; m="$MAIN/.planning/phases/07-upstream-stable-release-sustainment/07-RELEASE-MANIFEST.md"; for mode in taxonomy conversions bulk categorization capture; do bash "$MAIN/custom/grocy_AI/tests/release-gate.sh" "$mode" "$m"; done; bash "$MAIN/custom/grocy_AI/tests/release-gate.sh" candidate "$m"; bash "$MAIN/custom/grocy_AI/tests/release-gate.sh" predeploy "$m"; rg -n "0 suspicious|candidate_gate_results" "$m"</automated>
  </verify>
  <acceptance_criteria>`candidate`, `predeploy`, and all five per-phase modes pass under `php8.5`; the manifest-derived portable count and `phase-v1-changed-paths.txt` compare replace the Phase-2 literals; the conversion-audit reports 0 suspicious on the 444-product snapshot; `candidate_gate_results` records the counts.</acceptance_criteria>
  <done>The dual-branch gate certifies the Phase-7 release against prod-shaped data as a hard blocking exit gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Replacement commits → release manifest | Stale identities, mutable refs, or scope exceptions can falsely certify fixed source. |
| Reconciled tooling → gate result | A stale portable count or a Phase-2 changed-paths compare would false-pass (stale scope) or false-fail (12-path literal). |
| Prod-shaped snapshot → conversion-audit gate | A wrong or scrubbed-away shape could hide a suspicious-conversion regression at cutover. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-07-08-01 | Repudiation | Candidate provenance | mitigate | Record lower-case full-40 SHAs, direct-parent equalities, and recomputed pinned portable blob hashes. |
| T-07-08-02 | Tampering | Post-candidate scope | mitigate | Restrict `main_post_candidate_paths` to finite planning/evidence/tooling paths; replay closed candidate/predeploy. |
| T-07-08-03 | Tampering | Gate plumbing | mitigate | Use the 07-02 manifest-derived count and `phase-v1-changed-paths.txt` compare; assert the D-04 additions are manifest members and the grocyai_* views are not. |
| T-07-08-04 | Spoofing | Data-shape coverage | mitigate | Run categorization/conversion-audit on the real 444-product prod-shaped snapshot and require 0 suspicious. |
| T-07-08-05 | Elevation of Privilege | Per-phase storage boundary | mitigate | `categorization`/`capture` modes assert no native/parallel write path at `bulk`/`conversions` depth. |
</threat_model>

<verification>
`release-gate.sh candidate` + `predeploy` + `taxonomy`/`conversions`/`bulk`/`categorization`/`capture` all green under `php8.5` from clean worktrees; manifest SHAs lower-case full-40 with both direct-parent chains verified in Git; portable blob parity via manifest-derived count; conversion-audit reports 0 suspicious on the 444-product snapshot. Any red mode blocks the cutover.
</verification>

<success_criteria>
- The manifest names only fixed full-40 identities, pinned parity hashes, and a finite post-candidate allowlist.
- Candidate and predeploy gates and all five per-phase modes pass from clean worktrees on prod-shaped data.
- The conversion-audit is green (0 suspicious) on the real 444-product snapshot.
- No image build or deployment occurs in this plan.
</success_criteria>

<output>
Create .planning/phases/07-upstream-stable-release-sustainment/07-08-SUMMARY.md when done.
</output>
