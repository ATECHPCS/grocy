---
phase: 07-upstream-stable-release-sustainment
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - custom/grocy_AI/portable-files.txt
  - custom/grocy_AI/phase-v1-changed-paths.txt
  - custom/grocy_AI/README.md
  - CUSTOMIZATIONS.md
autonomous: true
requirements: [REL-01, REL-02]
must_haves:
  truths:
    - "D-04: Every shippable Phase 4/5/6/8 module file is classified portable / stable-adapter / dev-only-excluded, and the currently-missing portable files are added to portable-files.txt (LC_ALL=C sorted-unique, safe relative paths only)."
    - "D-05: Framework-coupled seams (GrocyAiApiController.php, GrocyAiCaptureController.php, routes.php, views/grocyai_*.blade.php) stay OUT of the manifest; dev-only bin/snapshot-refresh.sh, bin/snapshot-scrub.sql and the release/parity/red tooling scripts are excluded from both the manifest and the image."
    - "D-06/the agent's-discretion: A v1.0 changed-paths manifest (phase-v1-changed-paths.txt) enumerates the exact portable diff-tree the mirror commit will produce against atech-release, replacing the Phase-2-scoped phase2-changed-paths.txt for this release, while every portable path still holds the SHA-pinned byte-parity contract."
  artifacts:
    - path: custom/grocy_AI/portable-files.txt
      provides: the reconciled full-release portable manifest covering Phase 1-8 portable module bytes, sorted-unique with safe paths
    - path: custom/grocy_AI/phase-v1-changed-paths.txt
      provides: the expected sorted-unique portable diff-tree of the v1.0 portable-mirror commit relative to the current atech-release tip
  key_links:
    - from: custom/grocy_AI/portable-files.txt
      to: custom/grocy_AI/tests/release-gate.sh
      via: manifest-membership + SHA-pinned byte-parity loop over every listed path
      pattern: portable_blob_parity
    - from: custom/grocy_AI/phase-v1-changed-paths.txt
      to: custom/grocy_AI/tests/release-gate.sh
      via: stable_portable_scope exact diff-tree compare (replaces phase2-changed-paths.txt)
      pattern: stable_portable_scope
---

## Phase Goal

**As a** household maintainer promoting the 4+5+6+8 milestone as one v1.0 release, **I want** the portable manifest and the changed-paths contract to describe the *current* module reality, **so that** the dual-branch release gate can neither false-pass on stale scope nor false-fail on a Phase-2-era literal.

<objective>
Reconcile `custom/grocy_AI/portable-files.txt` to the current module tree and author the v1.0 changed-paths manifest that replaces `phase2-changed-paths.txt`.

Purpose: make the release tooling's scope inputs match the code before any mirror commit (07-05) or gate replay (07-08) exists — this is the blocking, wave-1 prerequisite of the whole promotion (D-03).
Output: an LC_ALL=C sorted-unique `portable-files.txt` that lists every Phase 1-8 portable module byte, an explicit written classification of each new/ambiguous path (portable / stable-adapter / dev-only-excluded), and a new `custom/grocy_AI/phase-v1-changed-paths.txt` enumerating the exact portable diff-tree the v1.0 mirror commit will produce.
</objective>

<execution_context>@/Users/ian/.codex/get-shit-done/workflows/execute-plan.md</execution_context>

<context>
@.planning/phases/07-upstream-stable-release-sustainment/07-CONTEXT.md
@.planning/REQUIREMENTS.md
@custom/grocy_AI/portable-files.txt
@custom/grocy_AI/phase2-changed-paths.txt
@custom/grocy_AI/tests/release-gate.sh
@custom/grocy_AI/tests/check-portable-parity.sh
@.planning/phases/03-food-taxonomy-categorization-pilot/03-03-PLAN.md
@MISTAKES.md
<interfaces>
From `release-gate.sh` (dual-branch section): the portable manifest is iterated line-by-line for SHA-pinned byte parity (`portable_blob_parity`), the per-phase modes (`taxonomy`/`conversions`/`bulk`) additionally assert each phase's required paths are manifest members and that the manifest is LC_ALL=C sorted-unique with safe relative paths, and `stable_portable_scope` (line 542-544) asserts the mirror commit's `diff-tree` equals `phase2-changed-paths.txt` sorted. 07-02 re-points line 542 at `phase-v1-changed-paths.txt` and line 565 at a manifest-derived count; this plan produces both inputs so 07-02's fixes have real targets.
From `check-portable-parity.sh`: runs from the `atech-main` worktree, rejects any manifest path that is absolute or contains `..`, and compares every listed path byte-for-byte against a supplied stable SHA. Every path this plan adds MUST be a real file on dev HEAD and byte-parity-eligible.
Portable-set truth: the portable set is exactly `custom/grocy_AI/` + `public/custom/grocy_AI/` module bytes MINUS the framework-coupled adapter seams and MINUS dev-only tooling. Adapter seams (`GrocyAiApiController.php`, `GrocyAiCaptureController.php`, `routes.php`, `views/grocyai_*.blade.php`) ride the 07-06 adapter commit, and the runtime markers (stable `version.json`, `Dockerfile.atech`, `views/productform.blade.php`) ride the 07-07 runtime commit; none of these are manifest members. Dev-only excluded (never shipped, never in the manifest): `custom/grocy_AI/bin/snapshot-refresh.sh`, `custom/grocy_AI/bin/snapshot-scrub.sql`, and the tooling scripts `tests/release-gate.sh`, `tests/deployment-gate.sh`, `tests/check-portable-parity.sh`, `tests/assert-expected-red.sh`.
Confirmed missing portable files on dev HEAD (15) that MUST be added: Phase 6 — `custom/grocy_AI/bin/audit-conversions.php`, `custom/grocy_AI/bin/verify-inventory-diff.php`, `custom/grocy_AI/tests/categorization.php`, `custom/grocy_AI/tests/conversion_audit.php`, `custom/grocy_AI/tests/group_suggestion.php`, `custom/grocy_AI/tests/inventory_diff.php`, `custom/grocy_AI/tests/inventory_scope.php`; Phase 8 — `custom/grocy_AI/src/GrocyAiCaptureMigration.php`, `custom/grocy_AI/src/GrocyAiCaptureService.php`, `custom/grocy_AI/tests/capture.php`, `custom/grocy_AI/tests/fixtures/capture-cases.json`, `public/custom/grocy_AI/capture.js`, `public/custom/grocy_AI/capture.test.js`, `public/custom/grocy_AI/capture-review.js`, `public/custom/grocy_AI/capture-review.test.js`. (`GrocyAiGroupSuggestionService.php` and `GrocyAiInventoryScope.php` are already manifest members.)
GrocyAiCaptureController.php disposition (RESOLVED — definitively excluded): it is a stable-adapter seam (it extends the branch-specific `BaseController`, verified by 07-06), NOT a portable file, so it stays OUT of the manifest permanently and rides the 07-06 stable-adapter commit. The portable "missing files" set is therefore exactly the 15 Phase 6/8 files below — there is NO 16th portable file. 07-03's source audit CONFIRMS this adapter classification (it does not add the controller to the portable set). Record the adapter classification in `CUSTOMIZATIONS.md`.
Changed-paths contract: `phase-v1-changed-paths.txt` = the sorted-unique set of portable paths whose dev-HEAD bytes DIFFER from the current `atech-release` tip — i.e., the exact diff-tree the 07-05 mirror commit will create against its parent. This is a subset of `portable-files.txt` (a portable file already byte-identical on stable stays in the manifest for parity but does NOT appear in the changed-paths file). Compute it, never guess it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reconcile portable-files.txt and record the portable/adapter/dev-excluded classification</name>
  <files>custom/grocy_AI/portable-files.txt, custom/grocy_AI/README.md, CUSTOMIZATIONS.md</files>
  <read_first>custom/grocy_AI/portable-files.txt; custom/grocy_AI/tests/release-gate.sh; custom/grocy_AI/tests/check-portable-parity.sh; .planning/phases/03-food-taxonomy-categorization-pilot/03-03-PLAN.md; MISTAKES.md</read_first>
  <behavior>
    - Test 1: every one of the 15 confirmed-missing portable paths (Phase 6 bin/tests + Phase 8 src/tests/fixtures/public) is present in `portable-files.txt`, each names a real file on dev HEAD, and the manifest is byte-for-byte equal to its own `LC_ALL=C sort -u` output (sorted-unique) with no absolute or `..` path.
    - Test 2: no adapter seam is a manifest member — `GrocyAiApiController.php`, `routes.php`, every `views/grocyai_*.blade.php`, stable `version.json`, and `Dockerfile.atech` are absent from `portable-files.txt`; and no dev-only tooling path (`bin/snapshot-refresh.sh`, `bin/snapshot-scrub.sql`, `tests/release-gate.sh`, `tests/deployment-gate.sh`, `tests/check-portable-parity.sh`, `tests/assert-expected-red.sh`) is a member.
    - Test 3: `GrocyAiCaptureController.php` is absent from the manifest (stable-adapter seam — extends the branch-specific `BaseController`), and this adapter classification plus the full portable/adapter/dev-excluded classification is written into `CUSTOMIZATIONS.md`/`README.md` so every shippable file has a recorded disposition.
  </behavior>
  <action>Add the 15 confirmed-missing portable paths to `custom/grocy_AI/portable-files.txt`, then re-sort the whole file with `LC_ALL=C sort -u` in place. Grep the result to confirm no adapter-seam or dev-only tooling path leaked in. Append a "v1.0 portable-set classification" section to `CUSTOMIZATIONS.md` (and a short pointer in `README.md`) enumerating: the portable module files, the stable-adapter seams that ride 07-06 (including `GrocyAiCaptureController.php`, a stable-adapter seam because it extends the branch-specific `BaseController`), and the dev-only-excluded tooling. Change no source code, no gate script, and no adapter file in this plan.</action>
  <verify><automated>diff <(LC_ALL=C sort -u custom/grocy_AI/portable-files.txt) custom/grocy_AI/portable-files.txt && for p in custom/grocy_AI/bin/audit-conversions.php custom/grocy_AI/bin/verify-inventory-diff.php custom/grocy_AI/tests/categorization.php custom/grocy_AI/tests/conversion_audit.php custom/grocy_AI/tests/group_suggestion.php custom/grocy_AI/tests/inventory_diff.php custom/grocy_AI/tests/inventory_scope.php custom/grocy_AI/src/GrocyAiCaptureMigration.php custom/grocy_AI/src/GrocyAiCaptureService.php custom/grocy_AI/tests/capture.php custom/grocy_AI/tests/fixtures/capture-cases.json public/custom/grocy_AI/capture.js public/custom/grocy_AI/capture.test.js public/custom/grocy_AI/capture-review.js public/custom/grocy_AI/capture-review.test.js; do grep -Fqx -- "$p" custom/grocy_AI/portable-files.txt || { echo "MISSING $p"; exit 1; }; test -f "$p" || { echo "NOFILE $p"; exit 1; }; done && ! grep -Eq 'GrocyAiApiController\.php|GrocyAiCaptureController\.php|^custom/grocy_AI/routes\.php$|grocyai_.*\.blade\.php|snapshot-refresh\.sh|snapshot-scrub\.sql|release-gate\.sh|deployment-gate\.sh|check-portable-parity\.sh|assert-expected-red\.sh' custom/grocy_AI/portable-files.txt && ! grep -Eq '(^|:)/|\.\.' custom/grocy_AI/portable-files.txt</automated></verify>
  <acceptance_criteria>All 15 portable paths are present and back real files; the manifest equals its own sorted-unique form with only safe relative paths; every adapter seam and dev-only tooling path is absent; the classification (including the CaptureController adapter classification) is recorded in CUSTOMIZATIONS.md.</acceptance_criteria>
  <done>The portable manifest describes the full v1.0 portable set with a written, auditable disposition for every shippable and excluded file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author the v1.0 changed-paths manifest that replaces phase2-changed-paths.txt</name>
  <files>custom/grocy_AI/phase-v1-changed-paths.txt, CUSTOMIZATIONS.md</files>
  <read_first>custom/grocy_AI/phase2-changed-paths.txt; custom/grocy_AI/tests/release-gate.sh; custom/grocy_AI/portable-files.txt</read_first>
  <behavior>
    - Test 1: `phase-v1-changed-paths.txt` equals its own `LC_ALL=C sort -u` output and contains only safe relative paths (no absolute, no `..`).
    - Test 2: every path in `phase-v1-changed-paths.txt` is also a member of `portable-files.txt` (changed-paths is a subset of the portable set — adapter/runtime paths never appear here).
    - Test 3: the file's contents equal the actual set of portable paths whose dev-HEAD bytes differ from the current `atech-release` tip — i.e., the exact diff-tree the 07-05 mirror commit will produce against its parent (verified by recomputation).
  </behavior>
  <action>Compute the changed-paths set: for every path in `portable-files.txt`, compare its dev-HEAD blob to the same path on `origin/atech-release` (`git diff --name-only origin/atech-release HEAD -- <portable paths>`, or an equivalent per-path `git diff --quiet` loop), keep the paths that differ, and write them `LC_ALL=C sort -u` to `custom/grocy_AI/phase-v1-changed-paths.txt`. Record in `CUSTOMIZATIONS.md` that this file is the v1.0 replacement for `phase2-changed-paths.txt`, that it is a subset of `portable-files.txt` (files already byte-identical on stable are intentionally omitted from the diff-tree but remain in the parity manifest), and that 07-02 re-points `release-gate.sh` line 542 at it. Leave `phase2-changed-paths.txt` in place for provenance; do not edit `release-gate.sh` here (07-02 owns that).</action>
  <verify><automated>diff <(LC_ALL=C sort -u custom/grocy_AI/phase-v1-changed-paths.txt) custom/grocy_AI/phase-v1-changed-paths.txt && ! grep -Eq '(^|:)/|\.\.' custom/grocy_AI/phase-v1-changed-paths.txt && while IFS= read -r p; do [ -n "$p" ] || continue; grep -Fqx -- "$p" custom/grocy_AI/portable-files.txt || { echo "not-portable $p"; exit 1; }; done < custom/grocy_AI/phase-v1-changed-paths.txt && diff <(git diff --name-only origin/atech-release HEAD -- $(cat custom/grocy_AI/portable-files.txt) | LC_ALL=C sort -u) custom/grocy_AI/phase-v1-changed-paths.txt</automated></verify>
  <acceptance_criteria>phase-v1-changed-paths.txt is sorted-unique with safe paths, is a subset of portable-files.txt, and equals the recomputed dev-HEAD-vs-atech-release portable diff-tree; the replacement relationship to phase2-changed-paths.txt is documented.</acceptance_criteria>
  <done>The v1.0 changed-paths contract exists and exactly predicts the mirror commit's diff-tree, ready for 07-02 to wire into the gate and 07-05 to satisfy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| portable manifest -> release gate | A missing or mis-classified path lets an unshipped or feature-in-native-tree file evade byte-parity, or false-fails the gate. |
| changed-paths manifest -> mirror commit scope | A wrong changed-paths set lets the mirror commit carry extra/fewer paths than certified. |
| adapter seam -> portable manifest | Listing a framework-coupled file as portable would demand impossible byte-parity across divergent branches. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-07-01-01 | Tampering | portable-files.txt | mitigate | Sorted-unique + safe-path assertion; every listed path must back a real file; adapter/dev-only paths grep-rejected. |
| T-07-01-02 | Spoofing | changed-paths manifest | mitigate | Recompute the diff-tree from git rather than transcribe it; assert subset-of-manifest. |
| T-07-01-03 | Information disclosure | classification doc | accept | CUSTOMIZATIONS.md records file dispositions only; no household data or secrets. |
| T-07-01-04 | Tampering | CaptureController mis-classification | mitigate | Classified stable-adapter (extends branch-specific BaseController); excluded from the manifest definitively and recorded in writing; 07-03 confirms the adapter classification. |
</threat_model>

<verification>
Run the Task 1 and Task 2 automated verify blocks. Then dry-check the downstream contract: confirm `git diff --name-only origin/atech-release HEAD -- $(cat custom/grocy_AI/portable-files.txt) | LC_ALL=C sort -u` equals `phase-v1-changed-paths.txt`, and that no adapter seam or dev-only tooling path appears in either manifest. Do not run the full release gate here — the two defects it still carries are 07-02's to fix.
</verification>

<success_criteria>
The portable manifest covers every Phase 1-8 portable module byte (sorted-unique, safe paths, real files), each shippable/excluded file has a written disposition, `GrocyAiCaptureController.php` is classified a stable-adapter seam (extends the branch-specific `BaseController`, confirmed by 07-03) and excluded from the manifest, and `phase-v1-changed-paths.txt` exactly predicts the v1.0 mirror commit's diff-tree.
</success_criteria>

<output>Create .planning/phases/07-upstream-stable-release-sustainment/07-01-SUMMARY.md when done</output>
</content>
</invoke>
