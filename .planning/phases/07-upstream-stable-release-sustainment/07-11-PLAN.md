---
phase: 07-upstream-stable-release-sustainment
plan: "11"
type: execute
wave: 7
depends_on: ["07-08", "07-09"]
files_modified:
  - .planning/phases/07-upstream-stable-release-sustainment/07-HUMAN-VERIFY.md
  - .planning/phases/05-bulk-maintenance-recovery-engine/05-11-SUMMARY.md
  - .planning/phases/06-inventory-categorization/06-ACCEPTANCE.md
autonomous: false
requirements: [REL-06]
must_haves:
  truths:
    - "D-14: The PENDING Phase 5 (`05-11` Task 3), Phase 6, and Phase 8 human-verify checkpoints all run on the production-shaped snapshot / test instance BEFORE cutover, so promotion is never gated on live prod."
    - "D-15/D-16/D-17: Phase 8 (capture) and Phase 6 (categorization, conversion-audit 0 suspicious against 62 global + 18 expected/9 products) are verified as promoted; Phase 4 conversions are confirmed shipping INACTIVE and are NOT verified as active."
    - "Any defect found in a phase blocks the cutover until resolved or the phase is explicitly descoped per D-15/D-16; sign-off is recorded before 07-12 builds the image."
  artifacts:
    - path: .planning/phases/07-upstream-stable-release-sustainment/07-HUMAN-VERIFY.md
      provides: "The three enumerated pre-cutover human-verify checklists (Phase 5 / Phase 6 / Phase 8) and the recorded maintainer sign-off or blocking defects"
  key_links:
    - from: .planning/phases/07-upstream-stable-release-sustainment/07-HUMAN-VERIFY.md
      to: "the 07-04 production-shaped snapshot / test instance"
      via: "all workflow verification performed against 444-product prod-shaped data, never live prod"
      pattern: "snapshot"
    - from: .planning/phases/07-upstream-stable-release-sustainment/07-HUMAN-VERIFY.md
      to: .planning/phases/05-bulk-maintenance-recovery-engine/05-11-SUMMARY.md
      via: "Phase 5 05-11 Task 3 checklist executed and signed off here"
      pattern: "05-11 Task 3"
---

## Phase Goal

**As a** household maintainer, **I want to** verify the Phase 5, 6, and 8 workflows by hand on a production-shaped snapshot before the prod cutover, **so that** a real defect is caught while rollback is trivial (a snapshot), not after the release is live.

<objective>
Run the pending Phase 5 / Phase 6 / Phase 8 human-verify checkpoints on the prod-shaped snapshot pre-cutover and record sign-off, so 07-12 only builds a release the maintainer has already exercised end-to-end.

Purpose: honor D-14 — move every human gate ahead of cutover onto the snapshot, leaving REL-06 (07-14) as the single post-cutover acceptance gate.
Output: `07-HUMAN-VERIFY.md` with three enumerated checklists and a recorded PASS/blocking-defect verdict per phase; Phase 5/6 summaries updated with the sign-off reference.
</objective>

<execution_context>@/Users/ian/.codex/get-shit-done/workflows/execute-plan.md</execution_context>

<context>
@.planning/phases/07-upstream-stable-release-sustainment/07-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-bulk-maintenance-recovery-engine/05-11-PLAN.md
@.planning/phases/07-upstream-stable-release-sustainment/07-08-SUMMARY.md
@.planning/phases/07-upstream-stable-release-sustainment/07-09-SUMMARY.md
<interfaces>
Depends on gates green: 07-08 (dual-branch release gate + every per-phase mode `taxonomy`/`conversions`/`bulk`/`categorization`/`capture` passing on prod-shaped data) and 07-09 (module migrations idempotent against the 444-product snapshot on both branches). This plan is the HUMAN gate that those automated gates precede.
Verification substrate: the 07-04 production-shaped snapshot / a test Grocy instance seeded from it (444 products / 224 ungrouped / 21 groups / 80 conversions). Never live prod (D-14).
Phase 5 checklist source: `05-11` Task 3 `<how-to-verify>` (generate/select/apply/audit/export/rollback + 320px + no CLI apply).
Phase 6: grouping the 224 ungrouped + bulk classification + `bin/audit-conversions.php` (expect 62 global + 18 expected / 9 products, 0 suspicious per D-16) + `bin/verify-inventory-diff.php`.
Phase 8: purchase-capture workflow (`capture.js` / `capture-review.js` surfaces), zero-write until save, mobile 320px.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stand up the snapshot/test instance and confirm the automated pre-reqs are green</name>
  <files>.planning/phases/07-upstream-stable-release-sustainment/07-HUMAN-VERIFY.md</files>
  <read_first>.planning/phases/07-upstream-stable-release-sustainment/07-08-SUMMARY.md; .planning/phases/07-upstream-stable-release-sustainment/07-09-SUMMARY.md; custom/grocy_AI/bin/snapshot-refresh.sh</read_first>
  <behavior>
    - Test 1: a test Grocy instance is running against a read-write copy of the 07-04 snapshot with the v1.0 module tree loaded and `GROCY_FEATURE_FLAG_GROCY_AI=true`; it reports 444 products.
    - Test 2: the 07-08 release gate (`candidate` + all five per-phase modes) and the 07-09 migration evidence are confirmed green before the maintainer is asked to verify anything by hand.
    - Test 3: `07-HUMAN-VERIFY.md` is scaffolded with the three phase checklist sections, each showing the exact steps and a blank verdict line.
  </behavior>
  <action>Bring up the snapshot-backed test instance (reuse the 07-04 snapshot; restore into a working copy so it stays reusable). Confirm and record that the automated gates this human gate rides on are green: `GROCY_AI_PHP=php8.5 bash custom/grocy_AI/tests/release-gate.sh candidate <07-RELEASE-MANIFEST>` and each per-phase mode (`taxonomy`, `conversions`, `bulk`, `categorization`, `capture`), plus the 07-09 migration/idempotency evidence. Scaffold `07-HUMAN-VERIFY.md` with the three enumerated checklists (Tasks 2 content) and a per-phase verdict line. Do not ask for human verification until these pre-reqs are recorded green.</action>
  <verify><automated>rg -n "Phase 5|Phase 6|Phase 8" .planning/phases/07-upstream-stable-release-sustainment/07-HUMAN-VERIFY.md && rg -n "release_gate: PASS|migration_evidence: green|snapshot_product_count: 444" .planning/phases/07-upstream-stable-release-sustainment/07-HUMAN-VERIFY.md</automated></verify>
  <acceptance_criteria>The snapshot test instance is live at 444 products, the automated pre-reqs are recorded green, and the three checklists are scaffolded with blank verdicts.</acceptance_criteria>
  <done>The human gate has a prod-shaped substrate and confirmed-green automated foundations to verify against.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Maintainer verifies Phase 5, Phase 6, and Phase 8 on the snapshot and signs off</name>
  <action>Pause after Task 1 records the automated pre-reqs green. The maintainer works entirely on the snapshot/test instance (never live prod) and records a PASS or a blocking defect for each of the three phases in `07-HUMAN-VERIFY.md`. A defect blocks the cutover until resolved or the phase is explicitly descoped per D-15/D-16.</action>
  <what-built>The consolidated v1.0 module surface (Phase 4 inactive conversions, Phase 5 bulk engine, Phase 6 categorization, Phase 8 purchase capture) running on the production-shaped snapshot.</what-built>
  <how-to-verify>
**Phase 5 — Bulk maintenance & recovery engine (`05-11` Task 3):**
1. Sign in with MASTER_DATA_EDIT; generate a bulk plan; confirm exact included/excluded/skipped/conflicted/changed/unchanged counts and that generation writes nothing.
2. Select/reject individual items; confirm the selected diff matches the apply set.
3. Apply; confirm only selected non-conflicted items changed, a drifted item is refused as a conflict with no partial write, and re-applying repeats no mutation.
4. Open the audit trail; confirm actor, previewed/applied times, module/version, per-item outcome, and before/after values.
5. Export JSON and CSV; confirm redaction (no secrets/tokens/unrelated household detail) and the non-authoritative marker.
6. Preview rollback; confirm a manually edited field is refused and a clean item is reversible; roll back and confirm the audit records it.
7. Confirm the same behavior at 320px, and that no maintainer CLI apply exists.

**Phase 6 — Inventory categorization:**
1. Review the 224 ungrouped products; run the grouping/classification workflow and confirm suggested groups are sensible and applied only on explicit selection (no silent writes).
2. Confirm bulk classification lands via the Phase 5 reviewed-apply-audit path (no parallel write authority) and the group count moves as expected.
3. Run `php8.5 custom/grocy_AI/bin/audit-conversions.php` against the snapshot; confirm **62 global + 18 expected / 9 products, 0 suspicious** (D-16) — no tripwire fire on the legitimate product-specific conversions.
4. Run `php8.5 custom/grocy_AI/bin/verify-inventory-diff.php`; confirm only expected tables changed (whitelist the `cache__quantity_unit_conversions_resolved` rebuild) and no native table was altered.
5. Confirm Phase 4 conversions are present but INACTIVE (`selected_projection_absent`); confirm the 18 product-specific conversions are untouched (D-17) — do NOT activate.

**Phase 8 — Purchase capture:**
1. On mobile (320px), run the capture workflow (`capture.js`): capture a purchase, confirm the review surface (`capture-review.js`) renders the parsed line items read-only before save.
2. Confirm nothing is written until explicit save — capture/preview/cancel produce zero product/stock/purchase mutation.
3. Save; confirm the purchase and stock land through the authorized path with the expected audit/provenance, and re-opening shows the saved state.
4. Confirm the capture surfaces are permission-checked and degrade to a bounded state when the companion service is unavailable.
  </how-to-verify>
  <resume-signal>Type "approved" (all three PASS) or describe the blocking defect and the phase it belongs to.</resume-signal>
  <verify><automated>rg -n "phase5_verdict: PASS|phase5_verdict: BLOCKED" .planning/phases/07-upstream-stable-release-sustainment/07-HUMAN-VERIFY.md && rg -n "phase6_verdict: PASS|phase6_verdict: BLOCKED" .planning/phases/07-upstream-stable-release-sustainment/07-HUMAN-VERIFY.md && rg -n "phase8_verdict: PASS|phase8_verdict: BLOCKED" .planning/phases/07-upstream-stable-release-sustainment/07-HUMAN-VERIFY.md</automated></verify>
  <done>The maintainer has verified Phase 5/6/8 on the snapshot and each phase carries a recorded PASS (or a blocking defect that stops the cutover).</done>
</task>

<task type="auto">
  <name>Task 3: Record sign-off and cross-link the phase summaries</name>
  <files>.planning/phases/07-upstream-stable-release-sustainment/07-HUMAN-VERIFY.md, .planning/phases/05-bulk-maintenance-recovery-engine/05-11-SUMMARY.md, .planning/phases/06-inventory-categorization/06-ACCEPTANCE.md</files>
  <read_first>.planning/phases/07-upstream-stable-release-sustainment/07-HUMAN-VERIFY.md; .planning/phases/05-bulk-maintenance-recovery-engine/05-11-SUMMARY.md</read_first>
  <behavior>
    - Test 1: `07-HUMAN-VERIFY.md` carries a dated maintainer sign-off line covering all three phases, only after each verdict is PASS.
    - Test 2: the Phase 5 `05-11-SUMMARY.md` and Phase 6 `06-ACCEPTANCE.md` reference the pre-cutover sign-off (closing the previously-pending human-verify checkpoints).
    - Test 3: any blocking defect is recorded with its phase and disposition (resolved / descoped per D-15/D-16) so 07-12 does not build over an open defect.
  </behavior>
  <action>Only if all three verdicts are PASS, record the dated maintainer sign-off in `07-HUMAN-VERIFY.md` and add a back-reference from `05-11-SUMMARY.md` (marking the `05-11` Task 3 checkpoint completed pre-cutover on the snapshot) and from `06-ACCEPTANCE.md`. If any verdict is BLOCKED, record the defect, its phase, and its disposition, and do NOT record sign-off — the cutover stays gated. Note that Phase 8 has no prior summary to backlink; its verdict lives in `07-HUMAN-VERIFY.md`.</action>
  <verify><automated>rg -n "signed_off_by|sign-off|pre-cutover" .planning/phases/07-upstream-stable-release-sustainment/07-HUMAN-VERIFY.md && rg -n "07-HUMAN-VERIFY|pre-cutover" .planning/phases/05-bulk-maintenance-recovery-engine/05-11-SUMMARY.md</automated></verify>
  <acceptance_criteria>A dated sign-off exists iff all three phases PASS; the Phase 5/6 summaries link the sign-off; any defect is recorded with disposition.</acceptance_criteria>
  <done>The pre-cutover human gate is closed with recorded sign-off (or an explicit block), ready for 07-12.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| human verification → data substrate | Verification must run on the snapshot/test instance, never live prod, so a mistake during verify cannot damage household data. |
| verdict → cutover gate | A PASS must reflect actual workflow behavior; a blocking defect must stop 07-12 rather than be waived silently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-07-11-01 | Tampering | live prod data | mitigate | All human verification runs on the 07-04 snapshot copy; live prod is never touched (D-14). |
| T-07-11-02 | Repudiation | sign-off record | mitigate | Dated per-phase verdicts plus a consolidated sign-off; defects recorded with phase and disposition. |
| T-07-11-03 | Elevation of Privilege | verified surfaces | mitigate | Checklists include permission checks (MASTER_DATA_EDIT, capture auth) and the no-CLI-apply / zero-write invariants. |
| T-07-11-SC | Tampering | package installs | accept | No package installation is planned. |
</threat_model>

<verification>
Confirm `07-HUMAN-VERIFY.md` records green automated pre-reqs, three enumerated checklists, a PASS/BLOCKED verdict per phase, and a dated sign-off only when all three are PASS; confirm the Phase 5/6 summaries link the pre-cutover sign-off. All verification is snapshot-only.
</verification>

<success_criteria>
- Phase 5 (`05-11` Task 3), Phase 6, and Phase 8 are verified by the maintainer on the prod-shaped snapshot pre-cutover.
- Each phase carries a recorded verdict; a blocking defect stops the cutover.
- Sign-off is recorded before 07-12 builds the image; Phase 4 confirmed inactive, not activated.
</success_criteria>

<output>Create .planning/phases/07-upstream-stable-release-sustainment/07-11-SUMMARY.md when done</output>
