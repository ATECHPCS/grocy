---
phase: 07-upstream-stable-release-sustainment
plan: "07"
type: execute
wave: 5
depends_on: ["07-06"]
files_modified:
  - /home/dev/personal-projects/Grocy AI/grocy-atech-release/Dockerfile.atech
  - /home/dev/personal-projects/Grocy AI/grocy-atech-release/custom/grocy_AI/version.json
  - /home/dev/personal-projects/Grocy AI/grocy-atech-release/views/productform.blade.php
  - /home/dev/personal-projects/Grocy AI/grocy-atech-release/CUSTOMIZATIONS.md
autonomous: true
requirements: [REL-04]
must_haves:
  truths:
    - "D-08: `Dockerfile.atech` (still `FROM lscr.io/linuxserver/grocy:v4.6.0-ls334`) COPYs the four new module Blade views (grocyai_bulkreview/capture/capture_review/conversioncoverage) and any new viewjs the four phases require; the runtime commit is a direct child of the 07-06 adapter commit with an exact-scope diff-tree."
    - "D-09: The three synchronized markers end at the release values in lockstep — `module_version` `2.6.0` (rides the portable mirror), stable `version.json` `\"Customization\"` `ATECHPCS-grocy_AI-10`→`-11`, and blade `$grocyAiAssetVersion` `2.4.2`→`2.6.0` — so the gate `synchronized_markers` check passes and route/view caches invalidate."
    - "No portable or adapter path is re-touched; the full-40 runtime SHA and marker values are recorded, and the image is to be identified by immutable digest (built in 07-12)."
  artifacts:
    - path: /home/dev/personal-projects/Grocy AI/grocy-atech-release/Dockerfile.atech
      provides: Stable runtime overlay COPYing the four new Blade views plus required viewjs, pinned to v4.6.0-ls334
    - path: /home/dev/personal-projects/Grocy AI/grocy-atech-release/custom/grocy_AI/version.json
      provides: Bumped stable route/view cache marker
    - path: /home/dev/personal-projects/Grocy AI/grocy-atech-release/views/productform.blade.php
      provides: Bumped $grocyAiAssetVersion matching the portable module_version
  key_links:
    - from: "stable_runtime_sha"
      to: "stable_adapter_sha (07-06)"
      via: "direct Git parent and exact-scope diff-tree"
      pattern: "runtime_git_parent|stable_runtime_scope"
    - from: "portable module_version + stable version.json + blade $grocyAiAssetVersion"
      to: "release-gate synchronized_markers"
      via: "lockstep marker values (module_version==stable_blade_version, version.json Customization==stable_cache_marker)"
      pattern: "synchronized_markers"
---

## Phase Goal

**As a** household maintainer, **I want to** overlay the new Blade views into the stable Docker image and bump the version/cache markers in lockstep, **so that** the single rebuilt prod image ships every promoted view and route/view caches invalidate cleanly on deploy.

<objective>
Create the stable-runtime commit as a direct child of the adapter commit: extend the Docker overlay and bump the three markers in lockstep.

Purpose: The whole-tree `COPY custom/grocy_AI` + `COPY public/custom/grocy_AI` lines already carry portable files, but the new module Blade views live in the framework `views/` tree and must be added to `Dockerfile.atech`; the markers must move together so the gate's `synchronized_markers` check passes and viewcache/route_cache invalidates.
Output: A direct-child runtime commit with an exact-scope diff-tree and synchronized release markers, ready for the immutable-digest build in 07-12.
</objective>

<execution_context>
@/home/dev/personal-projects/Grocy AI/grocy/.planning/phases/07-upstream-stable-release-sustainment/07-CONTEXT.md
</execution_context>

<context>
@.planning/phases/07-upstream-stable-release-sustainment/07-CONTEXT.md
@.planning/phases/07-upstream-stable-release-sustainment/07-06-SUMMARY.md
@.planning/phases/03-food-taxonomy-categorization-pilot/03-03-PLAN.md
@.planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-19-PLAN.md
@custom/grocy_AI/tests/release-gate.sh

<interfaces>
- `MAIN` = dev-lineage checkout; `STABLE` = `atech-release` worktree (default `/home/dev/personal-projects/Grocy AI/grocy-atech-release`).
- The runtime commit MUST be a direct child of the 07-06 `stable_adapter_sha` (`git rev-parse "${stable_runtime_sha}^"` == `stable_adapter_sha`).
- RUNTIME ALLOWLIST (exact diff-tree): `CUSTOMIZATIONS.md`, `Dockerfile.atech`, `custom/grocy_AI/version.json`, `views/productform.blade.php`.
- DOCKERFILE OVERLAY (D-08): keep `FROM lscr.io/linuxserver/grocy:v4.6.0-ls334`. The existing overlay already COPYs `custom/grocy_AI`, `public/custom/grocy_AI`, and the Phase 1-3 core `views/*.blade.php`. ADD `COPY views/grocyai_bulkreview.blade.php`, `views/grocyai_capture.blade.php`, `views/grocyai_capture_review.blade.php`, `views/grocyai_conversioncoverage.blade.php` into `/app/www/views/`, and add a `COPY` for any NEW `public/viewjs/*` seam that 07-03/07-06 introduced (if none, add none). Do not remove or reorder existing COPY lines.
- MARKER LOCKSTEP (D-09) — the release-gate `synchronized_markers` check (release-gate.sh ~line 605-617) enforces: `main_module_version` (dev `module-version.json`) == manifest `stable_module_version`; `stable_blade_version` (stable runtime `$grocyAiAssetVersion`) == `stable_module_version`; `stable_commit_cache` (stable runtime `version.json` "Customization") == manifest `stable_cache_marker`. Therefore: `module_version` = `2.6.0` (already on the portable mirror from 07-05 — DO NOT re-touch `module-version.json` here); set stable `version.json` "Customization" `ATECHPCS-grocy_AI-10`→`ATECHPCS-grocy_AI-11`; set blade `$grocyAiAssetVersion` `'2.4.2'`→`'2.6.0'`. Record `stable_module_version=2.6.0` and `stable_cache_marker=ATECHPCS-grocy_AI-11` for the 07-08 manifest.
- Do NOT re-touch any portable path (including `module-version.json`) or any 07-06 adapter path other than the runtime allowlist entries. Record the image by IMMUTABLE DIGEST — the actual build/deploy is 07-12/07-13. Run PHP as `php8.5`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the Docker overlay and bump the markers in lockstep as one exact-scope direct child</name>
  <read_first>
    .planning/phases/07-upstream-stable-release-sustainment/07-06-SUMMARY.md
    /home/dev/personal-projects/Grocy AI/grocy-atech-release/Dockerfile.atech
    /home/dev/personal-projects/Grocy AI/grocy-atech-release/custom/grocy_AI/version.json
    /home/dev/personal-projects/Grocy AI/grocy-atech-release/views/productform.blade.php
    custom/grocy_AI/tests/release-gate.sh
  </read_first>
  <files>/home/dev/personal-projects/Grocy AI/grocy-atech-release/Dockerfile.atech, /home/dev/personal-projects/Grocy AI/grocy-atech-release/custom/grocy_AI/version.json, /home/dev/personal-projects/Grocy AI/grocy-atech-release/views/productform.blade.php, /home/dev/personal-projects/Grocy AI/grocy-atech-release/CUSTOMIZATIONS.md</files>
  <action>In a clean `STABLE` worktree at the 07-06 `stable_adapter_sha` (require HEAD == that SHA before editing), add the four `views/grocyai_*.blade.php` COPY lines (and any new `public/viewjs/*` COPY 07-06 introduced) to `Dockerfile.atech`, preserving `FROM lscr.io/linuxserver/grocy:v4.6.0-ls334` and all existing COPY lines. Bump stable `version.json` "Customization" `-10`→`-11` and blade `$grocyAiAssetVersion` `'2.4.2'`→`'2.6.0'` so both align with the portable `module_version` `2.6.0`. Document the runtime overlay and marker bump in stable `CUSTOMIZATIONS.md`. Commit ONLY after the sorted runtime diff-tree equals the runtime allowlist, then advance `atech-release` by an exact old-SHA compare-and-swap from a clean stable worktree. Do NOT re-touch `module-version.json`, portable paths, or adapter paths outside the allowlist; do NOT build or deploy here.</action>
  <verify>
    <automated>set -eu; MAIN="/home/dev/personal-projects/Grocy AI/grocy"; STABLE="/home/dev/personal-projects/Grocy AI/grocy-atech-release"; export GROCY_AI_PHP=php8.5; stable_runtime_sha=$(git -C "$STABLE" rev-parse HEAD); test "${#stable_runtime_sha}" -eq 40; test "$(git -C "$STABLE" rev-parse "${stable_runtime_sha}^")" = "$(sed -n 's/^stable_adapter_sha: //p' "$MAIN/.planning/phases/07-upstream-stable-release-sustainment/07-06-SUMMARY.md" | head -1)"; expected=$(printf '%s\n' CUSTOMIZATIONS.md Dockerfile.atech custom/grocy_AI/version.json views/productform.blade.php | LC_ALL=C sort); actual=$(git -C "$STABLE" diff-tree --no-commit-id --name-only -r "$stable_runtime_sha" | LC_ALL=C sort); test "$actual" = "$expected"; grep -q 'FROM lscr.io/linuxserver/grocy:v4.6.0-ls334' "$STABLE/Dockerfile.atech"; for v in grocyai_bulkreview grocyai_capture grocyai_capture_review grocyai_conversioncoverage; do grep -q "views/$v.blade.php" "$STABLE/Dockerfile.atech"; done; grep -q '"Customization": "ATECHPCS-grocy_AI-11"' "$STABLE/custom/grocy_AI/version.json"; grep -q "grocyAiAssetVersion = '2.6.0'" "$STABLE/views/productform.blade.php"; test "$(git -C "$STABLE" show "${stable_runtime_sha}:custom/grocy_AI/module-version.json" | sed -n 's/.*"module_version": "\([^"]*\)".*/\1/p')" = "2.6.0"; test -z "$(git -C "$STABLE" status --porcelain)"</automated>
  </verify>
  <acceptance_criteria>The runtime commit is a direct child of the adapter SHA, its sorted diff-tree equals the runtime allowlist, `Dockerfile.atech` keeps the v4.6.0-ls334 base and COPYs all four new Blade views, and the three markers align at `module_version=2.6.0` / blade `2.6.0` / cache `ATECHPCS-grocy_AI-11` with `module-version.json` untouched in this commit.</acceptance_criteria>
  <done>The stable image overlay ships every promoted view and the release markers are synchronized for cache invalidation.</done>
</task>

<task type="auto">
  <name>Task 2: Record the runtime SHA and marker values for the manifest/build</name>
  <read_first>
    custom/grocy_AI/tests/check-portable-parity.sh
    .planning/phases/07-upstream-stable-release-sustainment/07-06-SUMMARY.md
  </read_first>
  <files>.planning/phases/07-upstream-stable-release-sustainment/07-07-SUMMARY.md</files>
  <action>Record in `07-07-SUMMARY.md` the lower-case full-40 `stable_runtime_sha`, its verified parent (`stable_adapter_sha`), the runtime allowlist matched, and the synchronized marker values (`stable_module_version=2.6.0`, `stable_cache_marker=ATECHPCS-grocy_AI-11`, blade `$grocyAiAssetVersion=2.6.0`) for the 07-08 manifest and the 07-12 build. Re-confirm SHA-pinned portable parity still holds at the portable SHA (unchanged) and that no portable/adapter byte outside the runtime allowlist moved. Leave the manifest authoring and gate replay to 07-08.</action>
  <verify>
    <automated>set -eu; MAIN="/home/dev/personal-projects/Grocy AI/grocy"; STABLE="/home/dev/personal-projects/Grocy AI/grocy-atech-release"; stable_runtime_sha=$(git -C "$STABLE" rev-parse HEAD); test "${#stable_runtime_sha}" -eq 40; case "$stable_runtime_sha" in *[!0-9a-f]*) exit 1 ;; esac; rg -n "stable_runtime_sha|stable_cache_marker|stable_module_version" "$MAIN/.planning/phases/07-upstream-stable-release-sustainment/07-07-SUMMARY.md"; stable_portable_sha=$(sed -n 's/^stable_portable_sha: //p' "$MAIN/.planning/phases/07-upstream-stable-release-sustainment/07-05-SUMMARY.md" | head -1); bash "$MAIN/custom/grocy_AI/tests/check-portable-parity.sh" --stable-sha "$stable_portable_sha"; test -z "$(git -C "$STABLE" status --porcelain)"</automated>
  </verify>
  <acceptance_criteria>The runtime SHA, parent, allowlist, and synchronized marker values are documented; portable parity is intact; the manifest and gate replay are untouched.</acceptance_criteria>
  <done>The runtime/marker identity is recorded for the Phase-7 manifest and the immutable-digest build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Adapter commit → runtime commit | Broad Docker/marker changes can re-touch portable/adapter bytes and break the three-commit split. |
| Runtime markers → cache invalidation | Unsynchronized markers cause a false `synchronized_markers` pass/fail or leave viewcache/route_cache stale after deploy. |
| Dockerfile overlay → shipped views | A missing `COPY views/grocyai_*` line ships an image whose promoted routes 404. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-07-07-01 | Tampering | Runtime scope | mitigate | Require exact runtime diff-tree == allowlist and direct parent == 07-06 adapter SHA; do not re-touch portable/adapter paths. |
| T-07-07-02 | Spoofing | Version/cache markers | mitigate | Bump the two stable-only markers to match the portable `module_version`; assert all three via the gate `synchronized_markers` invariant. |
| T-07-07-03 | Denial of Service | Shipped Blade views | mitigate | Assert `Dockerfile.atech` COPYs all four `views/grocyai_*.blade.php` and keeps the v4.6.0-ls334 base. |
| T-07-07-04 | Repudiation | Runtime provenance | mitigate | Record lower-case full-40 runtime SHA, verified parent, and marker values; identify the image by immutable digest (built in 07-12). |
</threat_model>

<verification>
Prove `git rev-parse "${stable_runtime_sha}^"` == 07-06 `stable_adapter_sha`, the sorted runtime diff-tree equals the allowlist, `Dockerfile.atech` keeps `FROM ...v4.6.0-ls334` and COPYs the four new views, the markers align (`module_version` 2.6.0 on the portable mirror; blade 2.6.0; cache `-11`), and portable parity still passes. No build/deploy occurs here.
</verification>

<success_criteria>
- The runtime commit is an exact-scope direct child of the adapter commit.
- The Docker overlay ships every promoted Blade view on the pinned v4.6.0-ls334 base.
- The three release markers are synchronized so the gate `synchronized_markers` check passes and caches invalidate.
- The runtime SHA and marker values are recorded for the manifest and immutable-digest build.
</success_criteria>

<output>
Create .planning/phases/07-upstream-stable-release-sustainment/07-07-SUMMARY.md when done.
</output>
