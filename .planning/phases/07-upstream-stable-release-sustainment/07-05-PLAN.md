---
phase: 07-upstream-stable-release-sustainment
plan: "05"
type: execute
wave: 3
depends_on: ["07-01", "07-02", "07-03"]
files_modified:
  - /home/dev/personal-projects/Grocy AI/grocy-atech-release/custom/grocy_AI/**  (full portable set per reconciled portable-files.txt)
  - /home/dev/personal-projects/Grocy AI/grocy-atech-release/public/custom/grocy_AI/**  (full portable set per reconciled portable-files.txt)
autonomous: true
requirements: [REL-02]
must_haves:
  truths:
    - "D-02(a): The portable module bytes are materialized onto atech-release from an IMMUTABLE dev-head SHA via `git show <sha>:<path>` per the reconciled `portable-files.txt`, never copied from a mutable checkout, byte-identical on both branches."
    - "The stable portable commit's sorted diff-tree equals `phase-v1-changed-paths.txt` (07-01) exactly — no Dockerfile, controller, route, Blade, or marker change rides this commit."
    - "The 40-hex immutable dev-head source SHA and the full-40 stable portable SHA (and its parent) are recorded for 07-06/07-08."
  artifacts:
    - path: /home/dev/personal-projects/Grocy AI/grocy-atech-release
      provides: New immutable stable portable-mirror commit whose diff-tree is the reconciled v1.0 portable path set
    - path: custom/grocy_AI/tests/check-portable-parity.sh
      provides: SHA-pinned portable byte-parity proof at the portable-mirror SHA
  key_links:
    - from: "immutable dev-head source SHA (main_candidate_sha)"
      to: "stable portable-mirror commit"
      via: "git show <dev-head-sha>:<path> blob materialization for every and only the reconciled portable paths"
      pattern: "portable_blob_parity"
    - from: "07-01 phase-v1-changed-paths.txt"
      to: "stable portable diff-tree"
      via: "exact LC_ALL=C sorted diff-tree equality"
      pattern: "stable_portable_scope"
---

## Phase Goal

**As a** household maintainer promoting the consolidated v1.0 release, **I want to** mirror the completed Phase 4/5/6/8 portable module bytes onto the stable `atech-release` branch byte-identically, **so that** the portable implementation is provably the same on both lineages before any framework seam is adapted.

<objective>
Materialize and prove the stable portable-mirror commit from the immutable dev-head candidate.

Purpose: D-02 requires a byte-identical portable implementation on `atech-release` before any stable-only framework adapter is reapplied; the mirror must be reproducible from pinned Git blobs, never a mutable working tree.
Output: One immutable stable portable-mirror commit whose exact diff-tree is the reconciled v1.0 portable path set, byte-parity-verified against the recorded dev-head source SHA.
</objective>

<execution_context>
@/home/dev/personal-projects/Grocy AI/grocy/.planning/phases/07-upstream-stable-release-sustainment/07-CONTEXT.md
</execution_context>

<context>
@.planning/phases/07-upstream-stable-release-sustainment/07-CONTEXT.md
@.planning/phases/07-upstream-stable-release-sustainment/07-PLAN-BREAKDOWN.md
@.planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-18-PLAN.md
@.planning/phases/01-safety-baseline-mobile-diagnostics/01-08-PLAN.md
@custom/grocy_AI/portable-files.txt
@custom/grocy_AI/phase-v1-changed-paths.txt
@custom/grocy_AI/tests/check-portable-parity.sh
@MISTAKES.md

<interfaces>
Repos/branches:
- `MAIN` = this planning checkout (`/home/dev/personal-projects/Grocy AI/grocy`), the dev-lineage promotion head (`codex/phase5-bulk-engine`). Do NOT switch this checkout.
- `STABLE` = a dedicated worktree on `atech-release`. If absent, create it once: `git -C "$MAIN" worktree add "$STABLE" atech-release`. Default `STABLE=/home/dev/personal-projects/Grocy AI/grocy-atech-release`. Prove its common Git dir matches `MAIN` and its branch is exactly `atech-release`.
- The immutable source is the dev-head promotion candidate SHA. Capture it ONCE as `main_candidate_sha=$(git -C "$MAIN" rev-parse HEAD)`, prove it is a full 40-hex, and use that literal in every `git show` — never a branch name, never a dirty file.
- Reconciled inputs from 07-01: `portable-files.txt` (the reconciled ~61-path v1.0 portable allowlist including the 15 D-04-missing Phase 6/8 files; `GrocyAiCaptureController.php` is NOT among them — it is a stable-adapter seam that rides 07-06) and `phase-v1-changed-paths.txt` (the expected portable diff-tree). From 07-02: the release-gate portable count is manifest-derived and the portable-scope compare reads `phase-v1-changed-paths.txt` (NOT the retired `phase2-changed-paths.txt`).
- MARKER PRECONDITION: `custom/grocy_AI/module-version.json` is a PORTABLE file. Its `module_version` must already equal the release value (`2.6.0`) at `main_candidate_sha` so the mirror carries it byte-identically. If the pinned dev-head SHA still reads `2.5.0`, land the one-line release bump on the dev lineage FIRST, re-pin `main_candidate_sha`, then mirror. The two stable-only markers (`version.json` "Customization", blade `$grocyAiAssetVersion`) are NOT touched here — they ride 07-07.
- Run PHP as `php8.5` (set `GROCY_AI_PHP=php8.5`). This plan touches only portable paths — no adapter, Dockerfile, routes, controller-seam, Blade view (`views/grocyai_*`, `views/productform.blade.php`), or marker file.
- 07-06 owns the stable-adapter direct child; 07-07 owns the runtime/marker commit; 07-08 owns the manifest and gate replay. This plan records SHAs only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Materialize the reconciled portable path set onto atech-release from immutable dev-head blobs</name>
  <read_first>
    .planning/phases/07-upstream-stable-release-sustainment/07-CONTEXT.md
    .planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-18-PLAN.md
    custom/grocy_AI/portable-files.txt
    custom/grocy_AI/phase-v1-changed-paths.txt
    custom/grocy_AI/tests/check-portable-parity.sh
    MISTAKES.md
  </read_first>
  <files>/home/dev/personal-projects/Grocy AI/grocy-atech-release/custom/grocy_AI/ (full reconciled portable set), /home/dev/personal-projects/Grocy AI/grocy-atech-release/public/custom/grocy_AI/ (full reconciled portable set)</files>
  <action>Capture `main_candidate_sha` = the dev-head HEAD full-40 and verify `module-version.json:module_version` at that SHA is the release value `2.6.0` (bump-and-repin on the dev lineage first if it is not). Ensure the `STABLE` worktree exists on `atech-release`, is clean, and shares `MAIN`'s common Git dir; require a clean `MAIN` source. For EVERY and ONLY the paths in the reconciled `portable-files.txt`, write the stable file from `git -C "$MAIN" show "${main_candidate_sha}:${path}"` — never from an uncommitted checkout. Include unchanged portable files so the commit is a complete parity snapshot. Reject any path that is absolute, `../`-relative, or escapes the tree (portable_path_safety). Per MISTAKES.md, before writing verify no test fixture on stable spy-creates a same-named module table. Commit exactly one portable-mirror commit ONLY when its LC_ALL=C sorted diff-tree equals `phase-v1-changed-paths.txt`. Do NOT touch `Dockerfile.atech`, `routes.php`, `GrocyAiApiController.php`, `GrocyAiCaptureController.php`, `views/grocyai_*.blade.php`, `views/productform.blade.php`, `version.json`, or any deployment/household-data path.</action>
  <verify>
    <automated>set -eu; MAIN="/home/dev/personal-projects/Grocy AI/grocy"; STABLE="/home/dev/personal-projects/Grocy AI/grocy-atech-release"; export GROCY_AI_PHP=php8.5; main_candidate_sha=$(git -C "$MAIN" rev-parse HEAD); test "${#main_candidate_sha}" -eq 40; case "$main_candidate_sha" in *[!0-9a-f]*) exit 1 ;; esac; test "$(git -C "$STABLE" branch --show-current)" = atech-release; test "$(git -C "$MAIN" show "${main_candidate_sha}:custom/grocy_AI/module-version.json" | sed -n 's/.*"module_version": "\([^"]*\)".*/\1/p')" = "2.6.0"; stable_portable_sha=$(git -C "$STABLE" rev-parse HEAD); test "${#stable_portable_sha}" -eq 40; test "$(git -C "$STABLE" diff-tree --no-commit-id --name-only -r "$stable_portable_sha" | LC_ALL=C sort | shasum -a 256 | awk '{print $1}')" = "$(LC_ALL=C sort "$MAIN/custom/grocy_AI/phase-v1-changed-paths.txt" | shasum -a 256 | awk '{print $1}')"; bash "$MAIN/custom/grocy_AI/tests/check-portable-parity.sh" --stable-sha "$stable_portable_sha"</automated>
  </verify>
  <acceptance_criteria>The stable portable-mirror commit contains exactly the reconciled portable paths (diff-tree == `phase-v1-changed-paths.txt`), every portable blob is byte-identical to `main_candidate_sha`, `module_version` is `2.6.0` on both sides, and no adapter/Dockerfile/marker path is present.</acceptance_criteria>
  <done>An immutable, parity-checked portable-mirror commit exists on `atech-release`, byte-for-byte equal to the dev-head candidate.</done>
</task>

<task type="auto">
  <name>Task 2: Record the source and portable SHAs without advancing adapter/runtime/manifest evidence</name>
  <read_first>
    custom/grocy_AI/tests/check-portable-parity.sh
    custom/grocy_AI/phase-v1-changed-paths.txt
  </read_first>
  <files>.planning/phases/07-upstream-stable-release-sustainment/07-05-SUMMARY.md</files>
  <action>Record in `07-05-SUMMARY.md` the lower-case full-40 `main_candidate_sha` (immutable dev-head source), the `stable_portable_sha`, its parent (`stable_portable_parent_sha` = the prior `atech-release` tip, currently `6605ae6c...`), the manifest-derived portable path count, and the N/N parity result. Advance `atech-release` only by an exact old-SHA compare-and-swap from a clean stable worktree so 07-06 has this portable commit as its direct parent. Do NOT write `07-RELEASE-MANIFEST.md`, touch any stable adapter/runtime path, or run `release-gate.sh candidate`/`predeploy` — 07-06/07-07/07-08 own adapter identity, markers, and final provenance.</action>
  <verify>
    <automated>set -eu; MAIN="/home/dev/personal-projects/Grocy AI/grocy"; STABLE="/home/dev/personal-projects/Grocy AI/grocy-atech-release"; stable_portable_sha=$(git -C "$STABLE" rev-parse HEAD); test "${#stable_portable_sha}" -eq 40; case "$stable_portable_sha" in *[!0-9a-f]*) exit 1 ;; esac; bash "$MAIN/custom/grocy_AI/tests/check-portable-parity.sh" --stable-sha "$stable_portable_sha"; test -z "$(git -C "$STABLE" status --porcelain)"; rg -n "main_candidate_sha|stable_portable_sha|stable_portable_parent_sha" "$MAIN/.planning/phases/07-upstream-stable-release-sustainment/07-05-SUMMARY.md"; test ! -f "$MAIN/.planning/phases/07-upstream-stable-release-sustainment/07-RELEASE-MANIFEST.md"</automated>
  </verify>
  <acceptance_criteria>The immutable source SHA, portable SHA, and parent are documented; the stable worktree is clean; no manifest, adapter, runtime, or gate-replay artifact has been advanced.</acceptance_criteria>
  <done>The portable-mirror provenance is recorded and ready for an adapter-only direct child, with manifest/adapter/runtime evidence intentionally unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Immutable dev-head candidate → stable portable commit | A mutable checkout copy or an omitted/extra path silently produces divergent portable bytes. |
| Stable parent → portable branch advance | A dirty or concurrent stable change could yield a detached/non-reproducible candidate. |
| Portable-only scope → adapter/runtime scope | Any Dockerfile/controller/route/Blade/marker change leaking into this commit collapses the portable/adapter split. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-07-05-01 | Tampering | Portable source mirror | mitigate | Materialize every path only from the pinned `main_candidate_sha` via `git show`; compare blob hashes and the exact diff-tree to `phase-v1-changed-paths.txt`. |
| T-07-05-02 | Repudiation | Portable identity | mitigate | Record lower-case full-40 source/portable/parent SHAs and advance with a clean-worktree compare-and-swap. |
| T-07-05-03 | Spoofing | Stable framework behavior | mitigate | Keep all adapter/route/controller/Blade/Dockerfile/marker changes out of this portable-only commit (07-06/07-07 own them). |
| T-07-05-04 | Tampering | Fixture-name collision | mitigate | Grep stable test fixtures for same-named module-table spies before materializing (MISTAKES.md). |
| T-07-05-SC | Tampering | Package installs | accept | No package installation is planned. |
</threat_model>

<verification>
Run `check-portable-parity.sh --stable-sha <stable_portable_sha>` (N/N green), confirm the sorted diff-tree equals `phase-v1-changed-paths.txt`, confirm `module_version` is `2.6.0` on both sides, and confirm the stable worktree is clean with `MAIN` still on the dev lineage. Adapter, runtime, manifest, and deployment evidence remain untouched.
</verification>

<success_criteria>
- The stable portable-mirror commit contains exactly the reconciled v1.0 portable paths and has a full immutable SHA.
- Every portable file is byte-identical to the pinned dev-head candidate (no mutable-ref dependence).
- The immutable source SHA and portable SHA/parent are recorded for 07-06/07-08.
- No adapter, runtime, marker, manifest, or deployment change occurs in this plan.
</success_criteria>

<output>
Create .planning/phases/07-upstream-stable-release-sustainment/07-05-SUMMARY.md when done.
</output>
