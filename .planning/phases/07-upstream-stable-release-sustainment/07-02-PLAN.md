---
phase: 07-upstream-stable-release-sustainment
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - custom/grocy_AI/tests/release-gate.sh
autonomous: true
requirements: [REL-02, REL-03]
must_haves:
  truths:
    - "D-06: The Phase-2-era literal `[ \"$portable_count\" -eq 12 ]` (line 565) is replaced by a count derived from portable-files.txt itself, and the `phase2-changed-paths.txt` stable_portable_scope compare (line 542) is replaced by phase-v1-changed-paths.txt, so both pass against the reconciled full manifest."
    - "D-07: New per-phase modes `categorization` (Phase 6) and `capture` (Phase 8) exist and match the depth of the existing `bulk`/`conversions` modes — portable-manifest membership + sorted-unique/safe, storage-boundary, fail-closed unit suite, and PHP lint — with every PHP invocation running as $php_runner (php8.5)."
    - "Scope: this plan owns ALL `release-gate.sh` plumbing/mode changes only. The `deployment-gate.sh` Phase-7 rewire (baseline, topology, markers, migration state, evidence artifacts) is owned by 07-12 and is NOT touched here."
  artifacts:
    - path: custom/grocy_AI/tests/release-gate.sh
      provides: manifest-derived portable count, phase-v1 changed-paths compare, and categorization + capture per-phase gate modes
  key_links:
    - from: custom/grocy_AI/tests/release-gate.sh
      to: custom/grocy_AI/portable-files.txt
      via: portable_count compared to the manifest's own non-blank line count
      pattern: portable_path_count
    - from: custom/grocy_AI/tests/release-gate.sh
      to: custom/grocy_AI/phase-v1-changed-paths.txt
      via: stable_portable_scope exact diff-tree compare
      pattern: stable_portable_scope
---

## Phase Goal

**As a** household maintainer, **I want** the dual-branch release gate to count and scope the portable set from the reconciled manifest and to prove every promoted phase's own contract, **so that** the v1.0 gate is trustworthy for REL-02/REL-03 and cannot false-pass on stale scope or false-fail on a 12-path literal.

<objective>
Fix the two hardcoded release-gate defects and add `categorization` + `capture` per-phase modes mirroring `bulk`/`conversions`. (The `deployment-gate.sh` Phase-7 rewire is owned by 07-12, not this plan.)

Purpose: this is the blocking wave-1 tooling fix (D-03) — without it the gate is wrong for the current 46+-path manifest and blind to Phase 6/8.
Output: a `release-gate.sh` whose dual-branch count and portable-scope come from the manifests 07-01 produced, plus two new per-phase modes at the depth of the existing ones. Every PHP call runs as `$php_runner`/`php8.5`.
</objective>

<execution_context>@/Users/ian/.codex/get-shit-done/workflows/execute-plan.md</execution_context>

<context>
@.planning/phases/07-upstream-stable-release-sustainment/07-CONTEXT.md
@.planning/REQUIREMENTS.md
@custom/grocy_AI/tests/release-gate.sh
@custom/grocy_AI/portable-files.txt
@custom/grocy_AI/phase-v1-changed-paths.txt
@.planning/phases/03-food-taxonomy-categorization-pilot/03-03-PLAN.md
@MISTAKES.md
<interfaces>
Defect 1 (line 565): `[ "$portable_count" -eq 12 ] || fail portable_path_count`. `portable_count` is incremented once per non-blank manifest line in the parity loop (lines 554-564). Replace the literal with a manifest-derived count computed from the SAME file the loop reads, so the two can never drift: `manifest_count=$(sed '/^$/d' "$main_repo/custom/grocy_AI/portable-files.txt" | wc -l | tr -d ' ')` then `[ "$portable_count" -eq "$manifest_count" ] || fail portable_path_count`. (Chosen over a `portable_path_count` manifest field precisely because a separate field could drift from the file the loop iterates.)
Defect 2 (line 542): `portable_expected=$(LC_ALL=C sort "$main_repo/custom/grocy_AI/phase2-changed-paths.txt")`. Replace the filename with `phase-v1-changed-paths.txt` (07-01's artifact). The compare at line 543-544 (`stable_portable_scope`, mirror-commit diff-tree vs this sorted list) is otherwise unchanged.
Per-phase mode shape (mirror `bulk_release_gate`, lines 346-473): each mode opens a `mktemp -d` temporary_root with the EXIT/HUP/INT/TERM trap, asserts each required Phase path is a `grep -Fqx` manifest member and a real file, re-asserts the manifest is `LC_ALL=C sort -u` and safe-path, asserts framework-coupled seams stay OUT of the manifest but present on disk (mirror `bulk_blade_view_not_manifest_tracked`), pins storage-boundary greps, then runs `run_quiet` unit modes and `run_quiet <name> "$php_runner" -l <file>` lints, and ends `echo "RELEASE_GATE: PASS (<mode>)"`. Dispatch is added to the trailing `if [ "$#" -eq 1 ] && [ "$1" = <mode> ]` block (lines 475-488).
run.php dispatch facts (verified): `capture-contract` and `capture-invariants` are the two run.php argv modes for capture (they `require_once tests/capture.php`). The Phase 6 suites (`categorization.php`, `conversion_audit.php`, `group_suggestion.php`, `inventory_diff.php`, `inventory_scope.php`) do NOT each have a bespoke run.php argv mode — confirm by grep how they are dispatched (default no-arg `run.php` suite and/or direct `php8.5 tests/<file>.php`) and invoke them the way they are actually wired. `conversion_audit.php` reads `GROCY_AI_SNAPSHOT` (or `.snapshots/grocy-prod.sqlite`) and asserts `global_count===62`, `expected_count===18`, nothing suspicious across 9 products WHEN the snapshot is present, and is CI-safe-skipped when absent — 07-04 supplies the snapshot to make that assertion fire; the `categorization` gate must invoke it so the D-16 baseline runs.
GrocyAiCaptureController.php classification is pending 07-03. Write the `capture` mode's controller-seam assertions against whatever 07-03 concludes; until then, treat it as an adapter seam (assert it is NOT a manifest member, like the Blade views) and pin the frontend/service/migration assertions that are branch-independent.
MISTAKES.md GREP-01: when asserting a permission or contract lives in a specific method, extract that method's own body (awk between its signature and its closing `^\t}$`) before grepping, as `bulk_apply_permission` does — never grep the whole file.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the manifest count literal and the changed-paths filename</name>
  <files>custom/grocy_AI/tests/release-gate.sh</files>
  <read_first>custom/grocy_AI/tests/release-gate.sh (lines 542-566); custom/grocy_AI/portable-files.txt; custom/grocy_AI/phase-v1-changed-paths.txt</read_first>
  <behavior>
    - Test 1: no literal `-eq 12` count remains in `release-gate.sh`; the portable-count assertion compares `portable_count` to a value derived from `portable-files.txt` (non-blank line count).
    - Test 2: `stable_portable_scope` reads `phase-v1-changed-paths.txt`, not `phase2-changed-paths.txt`; no live reference to `phase2-changed-paths.txt` remains in the gate.
    - Test 3: `sh -n custom/grocy_AI/tests/release-gate.sh` parses clean; the `portable_path_count` and `stable_portable_scope` check names are preserved (gate identity unchanged).
  </behavior>
  <action>Edit line 565 to derive the count from the manifest (`manifest_count=$(sed '/^$/d' "$main_repo/custom/grocy_AI/portable-files.txt" | wc -l | tr -d ' ')`; `[ "$portable_count" -eq "$manifest_count" ] || fail portable_path_count`). Edit line 542 to read `phase-v1-changed-paths.txt`. Change nothing else in the dual-branch flow. Keep both `fail` identities (`portable_path_count`, `stable_portable_scope`) so downstream evidence names are stable.</action>
  <verify><automated>sh -n custom/grocy_AI/tests/release-gate.sh && ! grep -Eq 'eq 12|phase2-changed-paths\.txt' custom/grocy_AI/tests/release-gate.sh && grep -Fq 'phase-v1-changed-paths.txt' custom/grocy_AI/tests/release-gate.sh && grep -Fq 'fail portable_path_count' custom/grocy_AI/tests/release-gate.sh</automated></verify>
  <acceptance_criteria>Both defects are gone: the count is manifest-derived, the scope compare reads phase-v1-changed-paths.txt, no `-eq 12` or `phase2-changed-paths.txt` reference remains, the script parses, and both check identities survive.</acceptance_criteria>
  <done>The dual-branch gate counts and scopes the portable set from the reconciled manifests, not Phase-2 literals.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the `categorization` (Phase 6) release-gate mode</name>
  <files>custom/grocy_AI/tests/release-gate.sh</files>
  <read_first>custom/grocy_AI/tests/release-gate.sh (bulk_release_gate lines 346-473); custom/grocy_AI/tests/categorization.php; custom/grocy_AI/tests/conversion_audit.php; custom/grocy_AI/tests/group_suggestion.php; custom/grocy_AI/tests/inventory_scope.php; custom/grocy_AI/tests/inventory_diff.php; custom/grocy_AI/src/GrocyAiGroupSuggestionService.php; custom/grocy_AI/bin/audit-conversions.php</read_first>
  <behavior>
    - Test 1 (manifest membership): the Phase 6 portable files — `bin/audit-conversions.php`, `bin/verify-inventory-diff.php`, `tests/categorization.php`, `tests/conversion_audit.php`, `tests/group_suggestion.php`, `tests/inventory_diff.php`, `tests/inventory_scope.php`, `src/GrocyAiGroupSuggestionService.php`, `src/GrocyAiInventoryScope.php` — are all `grep -Fqx` manifest members backing real files; the manifest is re-asserted sorted-unique and safe-path.
    - Test 2 (storage boundary): the group-suggestion write path touches ONLY `products.product_group_id` via its named delegate and never invents a group; `audit-conversions.php` declares no write verb (no `INSERT|UPDATE|DELETE|REPLACE` against `quantity_unit_conversions|products|cache__`); classification persists ONLY to `grocy_ai_taxonomy_classifications` (no native product/taxonomy/cache ad-hoc write).
    - Test 3 (fail-closed unit + conversion-audit): every Phase 6 suite runs green via `$php_runner` (using each suite's actual dispatch — run.php mode, default suite, or direct file), including `conversion_audit` so the D-16 baseline (62 global + 18 expected + 0 suspicious across 9 products when the snapshot is present, CI-safe-skip when absent) is exercised; and `$php_runner -l` lints every Phase 6 PHP source/bin/test file. The mode ends `RELEASE_GATE: PASS (categorization)`.
  </behavior>
  <action>Add a `categorization_release_gate()` function modeled exactly on `bulk_release_gate()` (temporary_root+trap, required-portable-paths loop, sorted-unique/safe-path re-assert, storage-boundary greps scoped per MISTAKES.md GREP-01, `run_quiet` unit + lint block). Discover and use the real dispatch for each Phase 6 suite before wiring it (do not assume a run.php mode exists for suites that run in the default suite). Invoke `conversion_audit` so the snapshot baseline runs when present. Add the `if [ "$#" -eq 1 ] && [ "$1" = categorization ]` dispatch block next to the existing ones. Run every PHP through `$php_runner`.</action>
  <verify><automated>GROCY_AI_PHP=php8.5 sh custom/grocy_AI/tests/release-gate.sh categorization</automated></verify>
  <acceptance_criteria>`release-gate.sh categorization` prints `RELEASE_GATE: PASS (categorization)`; it asserts Phase 6 manifest membership + sorted-unique/safe, the group-suggestion/classification storage boundary, the read-only conversion audit, and runs every Phase 6 suite + lint green via php8.5.</acceptance_criteria>
  <done>Phase 6's portable-manifest membership, storage boundary, and fail-closed suite are provable by a dedicated gate mode at the depth of `bulk`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add the `capture` (Phase 8) release-gate mode</name>
  <files>custom/grocy_AI/tests/release-gate.sh</files>
  <read_first>custom/grocy_AI/tests/release-gate.sh (bulk_release_gate lines 346-473); custom/grocy_AI/tests/capture.php; custom/grocy_AI/src/GrocyAiCaptureService.php; custom/grocy_AI/src/GrocyAiCaptureMigration.php; custom/grocy_AI/src/GrocyAiCaptureController.php; public/custom/grocy_AI/capture.test.js; public/custom/grocy_AI/capture-review.test.js</read_first>
  <behavior>
    - Test 1 (manifest membership): the Phase 8 portable files — `src/GrocyAiCaptureMigration.php`, `src/GrocyAiCaptureService.php`, `tests/capture.php`, `tests/fixtures/capture-cases.json`, `public/custom/grocy_AI/capture.js`, `capture.test.js`, `capture-review.js`, `capture-review.test.js` — are manifest members backing real files; the manifest is re-asserted sorted-unique and safe-path.
    - Test 2 (adapter seams excluded): `views/grocyai_capture.blade.php` and `views/grocyai_capture_review.blade.php` are NOT manifest members but exist on disk (mirror `bulk_blade_view_not_manifest_tracked`); `GrocyAiCaptureController.php` is asserted per the 07-03 classification (until then: NOT a manifest member, present on disk).
    - Test 3 (storage boundary + authority): `GrocyAiCaptureService` writes only its own namespaced `grocy_ai_capture_*` tables via its named delegate, issues no ad-hoc `INSERT|UPDATE|DELETE|REPLACE` against native `products|stock|cache__` relations, and any append-only ledger has no UPDATE/DELETE path; capture write endpoints are permission-checked in their own method bodies (GREP-01) and each route is wired exactly once.
    - Test 4 (fail-closed unit + frontend): `run.php capture-contract` and `capture-invariants` pass via `$php_runner`; `$php_runner -l` lints migration/service/controller/tests; `node --test` passes `capture.test.js` and `capture-review.test.js`. The mode ends `RELEASE_GATE: PASS (capture)`.
  </behavior>
  <action>Add a `capture_release_gate()` modeled on `bulk_release_gate()` with the four test groups above, discovering the real capture route/permission method names from the controller before pinning the authority greps (GREP-01). Keep the CaptureController seam assertion aligned with 07-03's conclusion; default to adapter-seam (not-in-manifest) if 07-03 has not yet run. Add the `if [ "$#" -eq 1 ] && [ "$1" = capture ]` dispatch block. Run every PHP through `$php_runner`.</action>
  <verify><automated>GROCY_AI_PHP=php8.5 sh custom/grocy_AI/tests/release-gate.sh capture</automated></verify>
  <acceptance_criteria>`release-gate.sh capture` prints `RELEASE_GATE: PASS (capture)`; it asserts Phase 8 manifest membership, the excluded capture Blade/controller seams, the capture storage boundary + authority surface, and runs capture-contract/capture-invariants + lint + the two node frontend suites green.</acceptance_criteria>
  <done>Phase 8's portable-manifest membership, storage boundary, authority surface, and fail-closed suite are provable by a dedicated gate mode at the depth of `bulk`.</done>
</task>

</tasks>

<note>
The `deployment-gate.sh` Phase-7 rewire (repoint baseline to `07-PHASE7-BASELINE.sha256`, Linux stable worktree path, drop companion-candidate asserts, repoint migration-state to the v1.0 module tables, repoint the manifest/evidence artifact constants and markers) is owned exclusively by **07-12** — it is NOT edited here. This plan touches `release-gate.sh` only.
</note>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| manifest -> gate count/scope | A hardcoded count or stale changed-paths file lets the gate certify the wrong portable set. |
| per-phase mode -> module source | A shallow mode could pass without proving storage boundary or fail-closed behavior for a promoted phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-07-02-01 | Tampering | portable_path_count | mitigate | Derive the count from the same file the parity loop reads; no literal. |
| T-07-02-02 | Spoofing | stable_portable_scope | mitigate | Compare the mirror diff-tree to phase-v1-changed-paths.txt (07-01's recomputed artifact). |
| T-07-02-03 | Tampering | categorization/capture mode | mitigate | Mirror bulk-mode depth: membership + sorted-unique/safe + storage-boundary + fail-closed suite + lint, all via $php_runner. |
| T-07-02-04 | Elevation of privilege | capture authority greps | mitigate | Per-method-body extraction (GREP-01) before asserting permission/route-count. |
</threat_model>

<verification>
`sh -n custom/grocy_AI/tests/release-gate.sh`. Run `GROCY_AI_PHP=php8.5 sh custom/grocy_AI/tests/release-gate.sh categorization` and `... capture` to green. Confirm no `-eq 12` or `phase2-changed-paths.txt` reference remains in `release-gate.sh`. Do not touch `deployment-gate.sh` (07-12 owns its rewire). Do not run the dual-branch `candidate`/`predeploy` flow here — those need the 07-08 release manifest and the 07-05/06/07 commits.
</verification>

<success_criteria>
The two hardcoded defects are fixed against the reconciled manifests, and `categorization` and `capture` modes pass at the depth of `bulk`/`conversions` with every PHP run as php8.5. `deployment-gate.sh` is untouched here (its Phase-7 rewire is owned by 07-12).
</success_criteria>

<output>Create .planning/phases/07-upstream-stable-release-sustainment/07-02-SUMMARY.md when done</output>
</content>
