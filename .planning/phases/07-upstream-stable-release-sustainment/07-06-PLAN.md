---
phase: 07-upstream-stable-release-sustainment
plan: "06"
type: execute
wave: 4
depends_on: ["07-05"]
files_modified:
  - /home/dev/personal-projects/Grocy AI/grocy-atech-release/custom/grocy_AI/src/GrocyAiApiController.php
  - /home/dev/personal-projects/Grocy AI/grocy-atech-release/custom/grocy_AI/src/GrocyAiCaptureController.php
  - /home/dev/personal-projects/Grocy AI/grocy-atech-release/custom/grocy_AI/routes.php
  - /home/dev/personal-projects/Grocy AI/grocy-atech-release/views/grocyai_bulkreview.blade.php
  - /home/dev/personal-projects/Grocy AI/grocy-atech-release/views/grocyai_capture.blade.php
  - /home/dev/personal-projects/Grocy AI/grocy-atech-release/views/grocyai_capture_review.blade.php
  - /home/dev/personal-projects/Grocy AI/grocy-atech-release/views/grocyai_conversioncoverage.blade.php
  - /home/dev/personal-projects/Grocy AI/grocy-atech-release/CUSTOMIZATIONS.md
autonomous: true
requirements: [REL-01, REL-02]
must_haves:
  truths:
    - "D-02(b): The stable-adapter commit is a direct child of the 07-05 portable SHA and its sorted diff-tree equals the 07-03-audited stable-adapter allowlist — no portable byte changes."
    - "D-05: Adapter scope is limited to framework-coupled seams — `GrocyAiApiController.php` (extends BaseApiController), `routes.php`, the four `views/grocyai_*.blade.php` views, and `GrocyAiCaptureController.php` (extends the branch-specific `BaseController`, therefore an adapter seam)."
    - "SHA-pinned portable parity still holds at the adapter commit (`check-portable-parity.sh --stable-sha` reports only documented adapter differences)."
  artifacts:
    - path: /home/dev/personal-projects/Grocy AI/grocy-atech-release
      provides: Direct-child stable-adapter commit limited to the audited framework-seam allowlist
    - path: /home/dev/personal-projects/Grocy AI/grocy-atech-release/CUSTOMIZATIONS.md
      provides: Documented portable-vs-adapter boundary for the four promoted phases
  key_links:
    - from: "stable_adapter_sha"
      to: "stable_portable_sha (07-05)"
      via: "direct Git parent and exact-scope diff-tree"
      pattern: "adapter_git_parent|stable_adapter_scope"
    - from: "07-03 stable-adapter allowlist"
      to: "adapter commit diff-tree"
      via: "LC_ALL=C sorted exact-list equality"
      pattern: "stable_adapter_scope"
---

## Phase Goal

**As a** household maintainer promoting v1.0, **I want to** reapply only the stable framework seams as a separate exact-scope commit on top of the byte-identical portable mirror, **so that** the maintained branch runs the promoted phases without dragging newer upstream Grocy onto the stable base and without altering any portable byte.

<objective>
Reapply the stable-only framework adapters as a direct child of the 07-05 portable commit, limited to the audited seam paths.

Purpose: Stable framework adaptation must remain separable from portable parity; a merge would drag newer upstream Grocy drift onto the LinuxServer v4.6.0 base, so the seams are reapplied by hand as one exact-scope commit.
Output: A direct-child stable-adapter commit whose diff-tree equals the 07-03 allowlist, with SHA-pinned portable parity intact and the boundary documented.
</objective>

<execution_context>
@/home/dev/personal-projects/Grocy AI/grocy/.planning/phases/07-upstream-stable-release-sustainment/07-CONTEXT.md
</execution_context>

<context>
@.planning/phases/07-upstream-stable-release-sustainment/07-CONTEXT.md
@.planning/phases/07-upstream-stable-release-sustainment/07-05-SUMMARY.md
@.planning/phases/07-upstream-stable-release-sustainment/07-SOURCE-AUDIT.md
@.planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-19-PLAN.md
@.planning/phases/01-safety-baseline-mobile-diagnostics/01-09-PLAN.md
@custom/grocy_AI/portable-files.txt
@custom/grocy_AI/tests/check-portable-parity.sh
@custom/grocy_AI/tests/release-gate.sh

<interfaces>
- `MAIN` = dev-lineage checkout (`/home/dev/personal-projects/Grocy AI/grocy`); `STABLE` = `atech-release` worktree (default `/home/dev/personal-projects/Grocy AI/grocy-atech-release`). Keep `MAIN` on the dev lineage; keep `STABLE` on `atech-release`.
- The adapter commit MUST be a direct child of the `stable_portable_sha` recorded by 07-05 (`git rev-parse "${stable_adapter_sha}^"` == `stable_portable_sha`).
- ADAPTER ALLOWLIST (the exact stable-adapter diff-tree; final list comes from the 07-03 audit): `CUSTOMIZATIONS.md`, `custom/grocy_AI/routes.php`, `custom/grocy_AI/src/GrocyAiApiController.php`, `custom/grocy_AI/src/GrocyAiCaptureController.php`, `views/grocyai_bulkreview.blade.php`, `views/grocyai_capture.blade.php`, `views/grocyai_capture_review.blade.php`, `views/grocyai_conversioncoverage.blade.php`. Add native controller seams / new `public/viewjs/*` ONLY if 07-03 justified them; nothing else.
- CAPTURE-CONTROLLER CLASSIFICATION (D-05 discretion, RESOLVED): `GrocyAiCaptureController.php` extends `BaseController` — a branch-specific Grocy framework base class that differs across the upstream-drift branches — so it is a stable-adapter seam, NOT a portable file. It therefore stays OUT of `portable-files.txt`/`phase-v1-changed-paths.txt` and rides THIS commit. (`GrocyAiBulkController.php` and `GrocyAiConversionController.php` remain portable — already in the manifest — and are untouched here.)
- Preserve stable conventions: `Grocy\Controllers\BaseApiController`, class-based `JsonMiddleware::class`, the root conditional route hook, and the native `BaseController` render lifecycle for the capture view. Keep permission-first checks before work and GET-only for read routes.
- Do NOT touch `Dockerfile.atech`, `custom/grocy_AI/version.json`, `views/productform.blade.php` `$grocyAiAssetVersion`, or `module-version.json` — the runtime/marker commit (07-07) owns those. Do NOT re-touch any portable byte. Run PHP as `php8.5`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reapply the framework-seam adapters as one exact-scope direct child</name>
  <read_first>
    .planning/phases/07-upstream-stable-release-sustainment/07-05-SUMMARY.md
    .planning/phases/07-upstream-stable-release-sustainment/07-SOURCE-AUDIT.md
    .planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-19-PLAN.md
    /home/dev/personal-projects/Grocy AI/grocy-atech-release/custom/grocy_AI/src/GrocyAiApiController.php
    /home/dev/personal-projects/Grocy AI/grocy-atech-release/custom/grocy_AI/routes.php
    /home/dev/personal-projects/Grocy AI/grocy-atech-release/CUSTOMIZATIONS.md
  </read_first>
  <files>/home/dev/personal-projects/Grocy AI/grocy-atech-release/custom/grocy_AI/src/GrocyAiApiController.php, /home/dev/personal-projects/Grocy AI/grocy-atech-release/custom/grocy_AI/src/GrocyAiCaptureController.php, /home/dev/personal-projects/Grocy AI/grocy-atech-release/custom/grocy_AI/routes.php, /home/dev/personal-projects/Grocy AI/grocy-atech-release/views/grocyai_bulkreview.blade.php, /home/dev/personal-projects/Grocy AI/grocy-atech-release/views/grocyai_capture.blade.php, /home/dev/personal-projects/Grocy AI/grocy-atech-release/views/grocyai_capture_review.blade.php, /home/dev/personal-projects/Grocy AI/grocy-atech-release/views/grocyai_conversioncoverage.blade.php, /home/dev/personal-projects/Grocy AI/grocy-atech-release/CUSTOMIZATIONS.md</files>
  <action>In a clean `STABLE` worktree at the 07-05 `stable_portable_sha`, require that HEAD equals that SHA before editing. Reapply ONLY the audited stable-adapter paths as one direct child: adapt `GrocyAiApiController.php` to `BaseApiController` + `JsonMiddleware::class`; register the promoted routes (bulk-review, capture, capture-review, conversion-coverage) in `routes.php` behind the existing root conditional route hook; add `GrocyAiCaptureController.php` extending the stable `BaseController` and rendering `grocyai_capture*`; add the four `views/grocyai_*.blade.php` views as stable Blade. Preserve permission-first checks and GET-only read routes; add no automatic retry and no parallel native write path. Document the exact portable-vs-adapter boundary (including the capture-controller classification) in stable `CUSTOMIZATIONS.md`. Commit ONLY after the sorted adapter diff-tree equals the 07-03 allowlist, then advance `atech-release` by an exact old-SHA compare-and-swap from a clean stable worktree. A mismatch, extra path, dirty path, or failed lint/parity blocks without overwriting state. Do NOT change portable paths, `Dockerfile.atech`, `version.json`, `module-version.json`, `productform.blade.php`, deployment scripts, or household data.</action>
  <verify>
    <automated>set -eu; MAIN="/home/dev/personal-projects/Grocy AI/grocy"; STABLE="/home/dev/personal-projects/Grocy AI/grocy-atech-release"; export GROCY_AI_PHP=php8.5; stable_adapter_sha=$(git -C "$STABLE" rev-parse HEAD); test "${#stable_adapter_sha}" -eq 40; stable_portable_sha=$(git -C "$STABLE" rev-parse "${stable_adapter_sha}^"); test "$stable_portable_sha" = "$(sed -n 's/^stable_portable_sha: //p' "$MAIN/.planning/phases/07-upstream-stable-release-sustainment/07-05-SUMMARY.md" | head -1)"; expected=$(printf '%s\n' CUSTOMIZATIONS.md custom/grocy_AI/routes.php custom/grocy_AI/src/GrocyAiApiController.php custom/grocy_AI/src/GrocyAiCaptureController.php views/grocyai_bulkreview.blade.php views/grocyai_capture.blade.php views/grocyai_capture_review.blade.php views/grocyai_conversioncoverage.blade.php | LC_ALL=C sort); actual=$(git -C "$STABLE" diff-tree --no-commit-id --name-only -r "$stable_adapter_sha" | LC_ALL=C sort); test "$actual" = "$expected"; php8.5 -l "$STABLE/custom/grocy_AI/src/GrocyAiApiController.php"; php8.5 -l "$STABLE/custom/grocy_AI/src/GrocyAiCaptureController.php"; php8.5 -l "$STABLE/custom/grocy_AI/routes.php"; grep -q "class GrocyAiApiController extends BaseApiController" "$STABLE/custom/grocy_AI/src/GrocyAiApiController.php"; grep -q "class GrocyAiCaptureController extends BaseController" "$STABLE/custom/grocy_AI/src/GrocyAiCaptureController.php"; bash "$MAIN/custom/grocy_AI/tests/check-portable-parity.sh" --stable-sha "$stable_portable_sha"; test -z "$(git -C "$STABLE" status --porcelain)"</automated>
  </verify>
  <acceptance_criteria>The adapter commit is a direct child of the 07-05 portable SHA, its sorted diff-tree equals the audited allowlist exactly, stable framework conventions (`BaseApiController`, `JsonMiddleware::class`, `BaseController` capture render, root route hook, permission-first GET-only) are preserved, both controllers lint clean, and SHA-pinned portable parity still passes at the parent portable SHA.</acceptance_criteria>
  <done>The stable framework integration is an exact-scope, parity-preserving direct child of the portable mirror.</done>
</task>

<task type="auto">
  <name>Task 2: Record the adapter SHA and prove the parity/scope invariants for 07-07/07-08</name>
  <read_first>
    custom/grocy_AI/tests/check-portable-parity.sh
    .planning/phases/07-upstream-stable-release-sustainment/07-05-SUMMARY.md
  </read_first>
  <files>.planning/phases/07-upstream-stable-release-sustainment/07-06-SUMMARY.md</files>
  <action>Record in `07-06-SUMMARY.md` the lower-case full-40 `stable_adapter_sha`, its verified parent (`stable_portable_sha`), the exact adapter allowlist that the diff-tree matched, the capture-controller classification decision (adapter — extends `BaseController`), and the N/N parity result at the portable parent. Leave the runtime/marker commit, the release manifest, and `release-gate.sh candidate/predeploy` to 07-07/07-08. Confirm the stable worktree is clean and no portable byte changed.</action>
  <verify>
    <automated>set -eu; MAIN="/home/dev/personal-projects/Grocy AI/grocy"; STABLE="/home/dev/personal-projects/Grocy AI/grocy-atech-release"; stable_adapter_sha=$(git -C "$STABLE" rev-parse HEAD); test "${#stable_adapter_sha}" -eq 40; case "$stable_adapter_sha" in *[!0-9a-f]*) exit 1 ;; esac; rg -n "stable_adapter_sha|stable_portable_sha|GrocyAiCaptureController" "$MAIN/.planning/phases/07-upstream-stable-release-sustainment/07-06-SUMMARY.md"; test -z "$(git -C "$STABLE" status --porcelain)"; test ! -f "$MAIN/.planning/phases/07-upstream-stable-release-sustainment/07-RELEASE-MANIFEST.md"</automated>
  </verify>
  <acceptance_criteria>The adapter SHA, parent, allowlist, and capture-controller classification are documented; the stable worktree is clean; the manifest and gate replay are untouched.</acceptance_criteria>
  <done>The stable-adapter identity is recorded and ready for the runtime/marker direct child.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Portable commit → adapter commit | Broad or detached framework changes can invalidate the portable/stable split or drag upstream drift onto the stable base. |
| Adapter classification → manifest scope | Misclassifying `GrocyAiCaptureController.php` as portable would break byte-parity or leak framework coupling into the portable set. |
| Adapter commit → API authority surface | An adapter edit could weaken permission-first / GET-only read behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-07-06-01 | Tampering | Stable adaptation | mitigate | Require exact adapter diff-tree == 07-03 allowlist and direct parent == 07-05 portable SHA. |
| T-07-06-02 | Spoofing | Framework base-class coupling | mitigate | Preserve `BaseApiController`/`JsonMiddleware::class` and classify `GrocyAiCaptureController` (extends `BaseController`) as an adapter seam, kept out of the portable manifest. |
| T-07-06-03 | Elevation of Privilege | Stable API/route surface | mitigate | Preserve permission-first checks before work and GET-only read routes; add no retry or parallel write path. |
| T-07-06-04 | Repudiation | Adapter provenance | mitigate | Record lower-case full-40 adapter SHA and verified parent; advance via clean-worktree compare-and-swap. |
</threat_model>

<verification>
Prove `git rev-parse "${stable_adapter_sha}^"` == 07-05 `stable_portable_sha`, the sorted adapter diff-tree equals the audited allowlist, both controllers lint under `php8.5`, the base-class conventions are intact, and `check-portable-parity.sh --stable-sha <portable_sha>` still reports N/N. Manifest, runtime markers, and deployment evidence remain untouched.
</verification>

<success_criteria>
- The stable-adapter commit is an exact-scope direct child of the portable mirror.
- Only framework-coupled seams changed (including the correctly-classified capture controller); no portable byte moved.
- Permission-first, GET-only read behavior and stable base-class conventions are preserved.
- The adapter SHA and classification are recorded for 07-07/07-08.
</success_criteria>

<output>
Create .planning/phases/07-upstream-stable-release-sustainment/07-06-SUMMARY.md when done.
</output>
