---
phase: 07-upstream-stable-release-sustainment
plan: "14"
type: execute
wave: 10
depends_on: ["07-13"]
files_modified:
  - .planning/phases/07-upstream-stable-release-sustainment/07-PHASE-ACCEPTANCE.md
  - .planning/phases/07-upstream-stable-release-sustainment/07-DEPLOYMENT-EVIDENCE.md
autonomous: false
requirements: [REL-06, REL-07]
must_haves:
  truths:
    - "D-14/REL-06: The single post-cutover acceptance gate is a user-completed end-to-end mobile product workflow on the LIVE promoted image (scan → enrich → review → save, plus the promoted bulk/capture surfaces); it is a blocking `checkpoint:human-verify`."
    - "REL-07: On ANY acceptance or migration failure, the rehearsed 07-10 recovery (prior-image redeploy + DB restore) is executed and verified within the window — the runbook is the response, not improvisation."
    - "Final redacted deployment evidence (digest, continuity, smoke outcomes, zero-write counters) is recorded and the `final` deployment gate passes only after acceptance."
  artifacts:
    - path: .planning/phases/07-upstream-stable-release-sustainment/07-PHASE-ACCEPTANCE.md
      provides: "The recorded end-to-end mobile acceptance verdict (or the failure + executed-recovery record) closing REL-06/REL-07"
    - path: .planning/phases/07-upstream-stable-release-sustainment/07-DEPLOYMENT-EVIDENCE.md
      provides: "Final redacted evidence — digest, continuity, acceptance smoke outcomes, zero-write counters — passing the `final` gate"
  key_links:
    - from: .planning/phases/07-upstream-stable-release-sustainment/07-PHASE-ACCEPTANCE.md
      to: "the live promoted grocy image (07-13 digest)"
      via: "user completes scan→enrich→review→save + bulk/capture on the live instance"
      pattern: "acceptance_verdict"
    - from: "acceptance/migration failure"
      to: .planning/phases/07-upstream-stable-release-sustainment/07-RECOVERY-RUNBOOK.md
      via: "07-10 rehearsed recovery executed and verified"
      pattern: "recovery_executed"
---

## Phase Goal

**As a** phone user and maintainer, **I want to** complete the real end-to-end mobile workflow on the promoted prod image before accepting the release — and fall back to the rehearsed recovery if it fails, **so that** v1.0 is accepted only when it actually works on the household device, with a proven escape hatch.

<objective>
Run the single post-cutover REL-06 acceptance on the live promoted image and, on any failure, execute the rehearsed 07-10 recovery — then close the release with final redacted evidence.

Purpose: the last gate — prove the promoted release serves the real mobile workflow, or recover to known-good; either way, record the outcome.
Output: `07-PHASE-ACCEPTANCE.md` (verdict or failure+recovery) and a completed `07-DEPLOYMENT-EVIDENCE.md` passing the `final` gate.
</objective>

<execution_context>@/Users/ian/.codex/get-shit-done/workflows/execute-plan.md</execution_context>

<context>
@.planning/phases/07-upstream-stable-release-sustainment/07-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-upstream-stable-release-sustainment/07-13-SUMMARY.md
@.planning/phases/07-upstream-stable-release-sustainment/07-DEPLOYMENT-EVIDENCE.md
@.planning/phases/07-upstream-stable-release-sustainment/07-RECOVERY-RUNBOOK.md
@custom/grocy_AI/tests/deployment-gate.sh
<interfaces>
Live target: the promoted prod instance from 07-13 (`10.10.0.156:9283`, running the recorded v1.0 digest). REL-06 acceptance is the household user completing the real mobile workflow on their device against this instance — the single post-cutover gate (D-14).
Recovery on failure: `07-RECOVERY-RUNBOOK.md` (07-10) — Path A prior-image redeploy (`prior_stable_image_id`), Path B DB restore (`backup-predeploy-20260912.db`), window-timed, with the route-cache clear before route verification.
Final gate: `deployment-gate.sh final <manifest> <evidence>` re-asserts running identity, mount, markers, migration state, and the protected fingerprint. Zero-write acceptance means enrichment/preview/capture-review before save mutate no product/stock/barcode/file.
Precedent for a relaxed/waived acceptance when a solo maintainer cannot perform the full physical matrix: `01-10-PLAN.md` (record WAIVED with residual risk, never a fabricated PASS).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: User completes the end-to-end mobile acceptance on the live promoted image</name>
  <read_first>.planning/phases/07-upstream-stable-release-sustainment/07-13-SUMMARY.md; .planning/phases/07-upstream-stable-release-sustainment/07-DEPLOYMENT-EVIDENCE.md; .planning/phases/01-safety-baseline-mobile-diagnostics/01-10-PLAN.md</read_first>
  <action>Block until 07-13 recorded a verified cutover (running digest match, continuity, routes resolve). The household user runs the real workflow on their phone against the live instance and records a verdict in `07-PHASE-ACCEPTANCE.md`: PASS, or a defect (with the failing step). This is the single REL-06 post-cutover gate. If the full physical matrix is infeasible for a solo maintainer, a specific sub-item may be recorded WAIVED with residual risk (per 01-10 precedent) — never fabricated as PASS — but the core scan→enrich→review→save spine must be genuinely exercised.</action>
  <what-built>The promoted v1.0 stable image (Phase 4 inactive conversions, Phase 5 bulk, Phase 6 categorization, Phase 8 capture) live on the household prod instance.</what-built>
  <how-to-verify>
1. On the phone, open a product and run scan → enrich → review → save: confirm the enrichment contract renders, the review surface is read-only, and only the saved fields persist (enrichment/preview writes nothing).
2. Exercise a promoted **bulk** surface (generate → select → apply → audit) and confirm the reviewed-apply-audit path works on live data with no partial write.
3. Exercise the promoted **capture** surface (capture → review → save) on mobile; confirm zero-write until save and a bounded state if the companion is unavailable.
4. Confirm module pages/routes load (no 404), the feature flag is active, and behavior holds at 320px.
5. Confirm no unexpected mutation: enrichment/preview/capture-review before save leave products/stock/barcodes/files unchanged.
  </how-to-verify>
  <resume-signal>Type "accepted" (workflow verified) or describe the failing step to trigger recovery.</resume-signal>
  <verify><automated>rg -n "acceptance_verdict: PASS|acceptance_verdict: FAILED|acceptance_verdict: WAIVED" .planning/phases/07-upstream-stable-release-sustainment/07-PHASE-ACCEPTANCE.md</automated></verify>
  <done>The end-to-end mobile workflow is accepted on the live promoted image, or a failing step is recorded to trigger recovery.</done>
</task>

<task type="auto">
  <name>Task 2: On failure execute rehearsed recovery; on success close the release with final evidence</name>
  <files>.planning/phases/07-upstream-stable-release-sustainment/07-PHASE-ACCEPTANCE.md, .planning/phases/07-upstream-stable-release-sustainment/07-DEPLOYMENT-EVIDENCE.md</files>
  <read_first>.planning/phases/07-upstream-stable-release-sustainment/07-RECOVERY-RUNBOOK.md; .planning/phases/07-upstream-stable-release-sustainment/07-DEPLOYMENT-EVIDENCE.md; custom/grocy_AI/tests/deployment-gate.sh</read_first>
  <behavior>
    - Test 1 (failure path): if the acceptance verdict is FAILED (or a migration fault surfaces), the 07-10 runbook is executed — prior-image redeploy (and DB restore if data is affected), route-cache clear, then post-rollback verification (prior digest running, 444 products, routes resolve) — and `07-PHASE-ACCEPTANCE.md` records `recovery_executed: yes` with the outcome.
    - Test 2 (success path): if the verdict is PASS/accepted, the `final` deployment gate passes and `07-DEPLOYMENT-EVIDENCE.md` is completed with the running digest, continuity, acceptance smoke outcomes, and zero-write counters, redacted.
    - Test 3: exactly one path is taken and recorded; evidence passes the privacy scrub and names only immutable identities and closed outcomes.
  </behavior>
  <action>Branch on the Task 1 verdict. **On FAILED** (or any migration failure): execute `07-RECOVERY-RUNBOOK.md` — Path A prior-image redeploy to `prior_stable_image_id`; Path B DB restore from `backup-predeploy-20260912.db` if household data is affected; clear `route_cache.php` before route verification; then verify the prior image is running, 444 products present, and routes resolve. Record `recovery_executed: yes`, the path(s) taken, and the post-rollback verification in `07-PHASE-ACCEPTANCE.md`; do NOT run the `final` gate (the v1.0 release did not land). **On PASS**: run `GROCY_AI_PHP=php8.5 bash custom/grocy_AI/tests/deployment-gate.sh final <manifest> <evidence>` and complete `07-DEPLOYMENT-EVIDENCE.md` with the running digest/revision, continuity fingerprint MATCH, acceptance smoke outcomes, and zero-write counters — redacted. Record the acceptance date and, if any sub-item was WAIVED, its residual risk (01-10 precedent). Update REQUIREMENTS/STATE only per the summary; keep all private workflow inputs in-session.</action>
  <verify><automated>rg -n "acceptance_verdict:" .planning/phases/07-upstream-stable-release-sustainment/07-PHASE-ACCEPTANCE.md && { rg -q "acceptance_verdict: PASS|acceptance_verdict: WAIVED" .planning/phases/07-upstream-stable-release-sustainment/07-PHASE-ACCEPTANCE.md && GROCY_AI_PHP=php8.5 bash custom/grocy_AI/tests/deployment-gate.sh final .planning/phases/07-upstream-stable-release-sustainment/07-RELEASE-MANIFEST.md .planning/phases/07-upstream-stable-release-sustainment/07-DEPLOYMENT-EVIDENCE.md || rg -q "recovery_executed: yes" .planning/phases/07-upstream-stable-release-sustainment/07-PHASE-ACCEPTANCE.md; }</automated></verify>
  <acceptance_criteria>Exactly one path is recorded: PASS/WAIVED closes with a passing `final` gate and complete redacted evidence; FAILED records an executed, verified 07-10 recovery and does not pass `final`.</acceptance_criteria>
  <done>The release is either accepted with final evidence or recovered to known-good, with the outcome recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| user workflow → live household data | Acceptance runs against real data; enrichment/preview/capture-review before save must be zero-write. |
| acceptance failure → recovery | A failure must trigger the rehearsed runbook, not ad-hoc fixes on live prod. |
| acceptance outcome → evidence | A PASS must reflect a genuinely exercised workflow; a waiver must state residual risk, never fabricate a device result. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-07-14-01 | Tampering/Repudiation | acceptance integrity | mitigate | Core scan→enrich→review→save spine genuinely exercised; waivers carry residual risk (01-10 precedent), no fabricated PASS. |
| T-07-14-02 | Tampering | hidden writes | mitigate | Zero-write counters recorded; enrichment/preview/capture-review confirmed non-mutating before save. |
| T-07-14-03 | Denial of Service | failed release | mitigate | On failure, the rehearsed 07-10 recovery restores the prior image/DB within the window and is verified. |
| T-07-14-04 | Information Disclosure | final evidence | mitigate | `final` gate privacy scrub; only immutable identities + closed outcomes recorded; private inputs stay in-session. |
| T-07-14-SC | Tampering | package installs | accept | No package installation is planned. |
</threat_model>

<verification>
Confirm exactly one path: PASS/WAIVED → `deployment-gate.sh final` passes and `07-DEPLOYMENT-EVIDENCE.md` records digest, continuity MATCH, acceptance smoke, and zero-write counters, redacted; FAILED → `07-PHASE-ACCEPTANCE.md` records `recovery_executed: yes` with prior-image running, 444 products, and routes resolving, and `final` is not run.
</verification>

<success_criteria>
- The user completes the end-to-end mobile workflow on the live promoted image (or records a waiver with residual risk, never a fabricated PASS).
- On any failure, the rehearsed 07-10 recovery is executed and verified within the window.
- Final redacted evidence is recorded and the `final` deployment gate passes on acceptance; REL-06/REL-07 are closed.
</success_criteria>

<output>Create .planning/phases/07-upstream-stable-release-sustainment/07-14-SUMMARY.md when done</output>
