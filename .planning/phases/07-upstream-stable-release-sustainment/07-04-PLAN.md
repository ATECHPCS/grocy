---
phase: 07-upstream-stable-release-sustainment
plan: "04"
type: execute
wave: 2
depends_on: []
files_modified:
  - custom/grocy_AI/bin/snapshot-refresh.sh
  - .planning/phases/07-upstream-stable-release-sustainment/07-SNAPSHOT.md
  - .gitignore
autonomous: true
requirements: [REL-03]
must_haves:
  truths:
    - "D-11: A current production-shaped snapshot (444 products / 224 ungrouped / 21 groups / 80 conversions) is acquired via direct SSH + PDO VACUUM INTO + scp (NOT the Komodo terminal, per the 16 MiB frame cap), light-scrubbed of secrets, and gitignored so it can never be committed."
    - "D-16: The conversion-audit / categorization gate is run against the real 444-product snapshot and asserts 62 global + 18 expected product-specific conversions across 9 products with 0 suspicious — the baseline that proves the paused-conversion premise is stale and the phase promotes as-is."
    - "REL-03: The refresh command, shape assertions, and integrity checks are documented and re-runnable, and the snapshot is usable read-write by 07-09/07-10 without ever touching prod."
  artifacts:
    - path: custom/grocy_AI/bin/snapshot-refresh.sh
      provides: re-runnable prod-shaped snapshot acquisition + light-scrub with shape/integrity assertions (444/224/21/80)
    - path: .planning/phases/07-upstream-stable-release-sustainment/07-SNAPSHOT.md
      provides: the documented refresh cadence, shape/integrity assertions, and the D-16 conversion-audit baseline result
  key_links:
    - from: custom/grocy_AI/bin/snapshot-refresh.sh
      to: custom/grocy_AI/.snapshots/grocy-prod.sqlite
      via: SSH VACUUM INTO -> scp -> snapshot-scrub.sql -> shape/integrity assertions
      pattern: GROCY_AI_SNAPSHOT
    - from: custom/grocy_AI/tests/conversion_audit.php
      to: custom/grocy_AI/.snapshots/grocy-prod.sqlite
      via: GROCY_AI_SNAPSHOT baseline assertion (62 global + 18 expected + 0 suspicious)
      pattern: conversion_audit-snapshot
---

## Phase Goal

**As a** maintainer, **I want** a current, secret-scrubbed, production-shaped local snapshot with proven shape and a green conversion-audit baseline, **so that** 07-09/07-10 can rehearse migrations and recovery against real-shaped data and the D-16 conversion premise is confirmed as-is before cutover.

<objective>
Build/refresh the production-shaped snapshot, assert its shape and integrity, and run the D-16 conversion-audit baseline against it.

Purpose: REL-03 requires migrations and recovery to be proven on 444-product-shaped data; D-16 requires the conversion-audit/categorization gate to pass on the real snapshot (0 suspicious) before promotion. This wave-2 plan produces the shared read-write snapshot the later dual-branch migration/recovery plans consume.
Output: a refreshed, gitignored `grocy-prod.sqlite` acquired via direct SSH (not Komodo terminal), with shape assertions (444/224/21/80) and integrity check wired into `snapshot-refresh.sh`, documented in `07-SNAPSHOT.md`, plus a recorded green conversion-audit baseline (62 global + 18 expected across 9 products, 0 suspicious).
</objective>

<execution_context>@/Users/ian/.codex/get-shit-done/workflows/execute-plan.md</execution_context>

<context>
@.planning/phases/07-upstream-stable-release-sustainment/07-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@custom/grocy_AI/bin/snapshot-refresh.sh
@custom/grocy_AI/bin/snapshot-scrub.sql
@custom/grocy_AI/tests/conversion_audit.php
@.gitignore
@MISTAKES.md
<interfaces>
`snapshot-refresh.sh` (Phase 6) already: SSHes `root@10.10.0.156` with the 1Password key `ssh:Personal Docker (102)` (vault `API/SSH/Tokens`), runs PDO `VACUUM INTO` inside the `grocy` container (read-only against prod, aborts if the source md5 changes), `scp`s the copy down to `custom/grocy_AI/.snapshots/grocy-prod-raw.sqlite`, applies `snapshot-scrub.sql` into `grocy-prod.sqlite`, and asserts `api_keys`/`sessions` emptied + passwords blanked + product count preserved. It currently asserts only `products > 0`, NOT the full prod shape. It exports `GROCY_AI_SNAPSHOT` / `GROCY_AI_SNAPSHOT_RAW`. It refuses to clobber without `--force`; supports `--dry-run`.
`.gitignore` ALREADY ignores `custom/grocy_AI/.snapshots` and `custom/grocy_AI/.snapshots/` (lines 8-11) — verify this still holds; do not weaken it. If a worktree-symlink form is needed, only strengthen coverage.
`conversion_audit.php` reads `GROCY_AI_SNAPSHOT` (fallback `custom/grocy_AI/.snapshots/grocy-prod.sqlite`), and WHEN the snapshot is present asserts `global_count===62`, `expected_count===18`, 9 distinct products, and nothing suspicious; it CI-safe-skips when the snapshot is absent. This is the D-16 gate — it only fires with the snapshot in place.
Prod shape targets (D-11): 444 products, 224 ungrouped products, 21 product groups, 80 quantity_unit_conversions. The 80 conversions decompose (D-16) as 62 global + 18 legitimate product-specific across 9 products.
Komodo-terminal caveat (MEMORY / STATE.md): NEVER stream the snapshot through the Komodo Server terminal (16 MiB frame cap wedges the core<->periphery link). Acquisition is direct SSH + scp only — `snapshot-refresh.sh` already does this; keep it that way.
Snapshot acquisition against live prod is read-only but touches the prod host over SSH; it is a real network operation. Treat the SSH/VACUUM step as the human-gated/environment-dependent action and keep the shape/integrity/audit assertions runnable against an already-acquired snapshot.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh the snapshot and wire prod-shape + integrity assertions into snapshot-refresh.sh</name>
  <files>custom/grocy_AI/bin/snapshot-refresh.sh, .gitignore, .planning/phases/07-upstream-stable-release-sustainment/07-SNAPSHOT.md</files>
  <read_first>custom/grocy_AI/bin/snapshot-refresh.sh; custom/grocy_AI/bin/snapshot-scrub.sql; .gitignore; .planning/STATE.md; MISTAKES.md</read_first>
  <behavior>
    - Test 1: after a successful refresh (or against an already-present snapshot), `snapshot-refresh.sh` asserts the full prod shape — 444 products, 224 ungrouped products, 21 product groups, 80 quantity_unit_conversions — and fails closed with a named error if any count is off, in addition to the existing secret-scrub assertions.
    - Test 2: `sqlite3 <snapshot> 'PRAGMA integrity_check'` returns `ok`, and the acquisition path remains direct SSH + `VACUUM INTO` + scp (no Komodo terminal, no prod write — the existing md5-unchanged guard is preserved).
    - Test 3: `.gitignore` still excludes `custom/grocy_AI/.snapshots` in both directory and symlink forms; the snapshot is not tracked (`git check-ignore` confirms) and cannot be committed.
  </behavior>
  <action>Extend `snapshot-refresh.sh`'s verification block with the four shape assertions (products=444, ungrouped=224, groups=21, conversions=80) using `sqlite3` count queries and named `die` failures, and add a `PRAGMA integrity_check = ok` assertion. Keep the acquisition pipeline and the `--force`/`--dry-run` behavior unchanged. Confirm `.gitignore` coverage (add the symlink form only if missing; never weaken). Record the exact refresh command, env overrides, and shape/integrity checks in `07-SNAPSHOT.md`. Choose the cadence (D-discretion): reuse the existing Phase 6 snapshot if current, else `--force` refresh; document which was done and the acquisition timestamp. Perform the live SSH acquisition only if authorized for this environment; otherwise document the command and validate against the existing snapshot.</action>
  <verify><automated>sh -n custom/grocy_AI/bin/snapshot-refresh.sh && bash custom/grocy_AI/bin/snapshot-refresh.sh --dry-run && git check-ignore -q custom/grocy_AI/.snapshots/grocy-prod.sqlite && grep -Eq '444|products.*444' custom/grocy_AI/bin/snapshot-refresh.sh && grep -Eiq 'integrity_check' custom/grocy_AI/bin/snapshot-refresh.sh && test -f .planning/phases/07-upstream-stable-release-sustainment/07-SNAPSHOT.md</automated></verify>
  <acceptance_criteria>snapshot-refresh.sh parses, its dry-run works, it asserts the 444/224/21/80 shape + integrity ok, the snapshot stays gitignored/untracked, acquisition stays direct-SSH-only, and 07-SNAPSHOT.md documents the re-runnable command and checks.</acceptance_criteria>
  <done>A current, secret-scrubbed, shape-verified prod snapshot exists (or is reproducibly re-acquirable) and can never be committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run and record the D-16 conversion-audit baseline against the snapshot</name>
  <files>.planning/phases/07-upstream-stable-release-sustainment/07-SNAPSHOT.md</files>
  <read_first>custom/grocy_AI/tests/conversion_audit.php; custom/grocy_AI/bin/audit-conversions.php; .planning/phases/07-upstream-stable-release-sustainment/07-CONTEXT.md</read_first>
  <behavior>
    - Test 1: with `GROCY_AI_SNAPSHOT` pointed at the refreshed snapshot, `conversion_audit` runs green and its snapshot baseline fires (not the CI-safe skip): 62 global conversions, 18 expected product-specific package definitions, 9 distinct products, and 0 suspicious.
    - Test 2: the audit is read-only against the snapshot — the `quantity_unit_conversions` table is row-identical before and after the run (the audit source declares no write verb).
    - Test 3: `07-SNAPSHOT.md` records the exact baseline result (62 + 18 / 9 products / 0 suspicious) and the command used, establishing that the D-16 conversion premise is confirmed as-is and is NOT a cutover blocker.
  </behavior>
  <action>Run the conversion-audit suite with `GROCY_AI_PHP=php8.5` and `GROCY_AI_SNAPSHOT` set to the snapshot, confirm the baseline assertion fires (guard against the silent CI-skip path by verifying the snapshot exists and has a `quantity_unit_conversions` table first). Capture the printed summary and write it into `07-SNAPSHOT.md` with the command. If any suspicious row appears, STOP and record it as a cutover blocker per D-16 (do not paper over it). Make no source edits — this task consumes the existing audit; the 07-02 `categorization` gate is the reusable wrapper.</action>
  <verify><automated>GROCY_AI_PHP=php8.5 GROCY_AI_SNAPSHOT="$(pwd)/custom/grocy_AI/.snapshots/grocy-prod.sqlite" php8.5 custom/grocy_AI/tests/conversion_audit.php && grep -Eq '62' .planning/phases/07-upstream-stable-release-sustainment/07-SNAPSHOT.md && grep -Eiq 'suspicious' .planning/phases/07-upstream-stable-release-sustainment/07-SNAPSHOT.md</automated></verify>
  <acceptance_criteria>The conversion-audit baseline fires green on the real snapshot (62 global + 18 expected / 9 products / 0 suspicious), the audit is proven read-only, and the result is recorded in 07-SNAPSHOT.md as the D-16 confirmation.</acceptance_criteria>
  <done>The paused-conversion premise is proven stale on production-shaped data: the categorization/conversion-audit gate passes with 0 suspicious.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| prod DB -> local snapshot | Acquisition must be read-only against prod (VACUUM INTO, md5-unchanged guard) and never stream through the Komodo terminal. |
| snapshot -> repository | Real household data and secrets must never be committed; the snapshot stays gitignored and secret-scrubbed. |
| snapshot -> conversion audit | A silent CI-skip would let the D-16 baseline pass without actually running against real data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-07-04-01 | Tampering | prod DB during acquisition | mitigate | PDO VACUUM INTO read-only + md5-unchanged abort; direct SSH only, no Komodo terminal (16 MiB cap). |
| T-07-04-02 | Information disclosure | snapshot in repo | mitigate | snapshot-scrub.sql empties api_keys/sessions + blanks passwords; .gitignore excludes .snapshots; git check-ignore asserted. |
| T-07-04-03 | Spoofing | conversion-audit skip | mitigate | Verify snapshot + conversions table present so the baseline fires, not the CI-safe skip. |
| T-07-04-04 | Tampering | audit writes to snapshot | mitigate | Assert quantity_unit_conversions row-identical before/after; audit source declares no write verb. |
</threat_model>

<verification>
`sh -n` and `--dry-run` snapshot-refresh.sh; confirm the 444/224/21/80 shape + `integrity_check ok` assertions are present and, where authorized, actually pass against the acquired snapshot; confirm `git check-ignore` hides the snapshot. Run `conversion_audit` with `GROCY_AI_SNAPSHOT` set and confirm the baseline fires green (62 + 18 / 9 products / 0 suspicious) and is recorded in 07-SNAPSHOT.md. Never route acquisition through the Komodo terminal.
</verification>

<success_criteria>
A current, secret-scrubbed, shape-verified (444/224/21/80), gitignored prod snapshot exists and is re-runnably documented; the D-16 conversion-audit baseline passes on it with 0 suspicious; and 07-09/07-10 can consume it read-write without touching prod.
</success_criteria>

<output>Create .planning/phases/07-upstream-stable-release-sustainment/07-04-SUMMARY.md when done</output>
</content>
