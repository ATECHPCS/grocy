---
phase: 04-reusable-conversion-model
plan: "02"
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - custom/grocy_AI/src/GrocyAiConversionMigration.php
  - custom/grocy_AI/src/GrocyAiConversionService.php
  - custom/grocy_AI/src/GrocyAiApiController.php
  - custom/grocy_AI/routes.php
  - custom/grocy_AI/tests/conversions.php
  - custom/grocy_AI/tests/run.php
  - views/quantityunitconversionform.blade.php
  - public/viewjs/quantityunitconversionform.js
autonomous: true
requirements: [CONV-01, CONV-02, CONV-04, CONV-06]
must_haves:
  truths:
    - "A maintainer can validate a same-dimension universal conversion in the native conversion form before normal Save."
    - "Mass/volume mixing and package/count universal candidates are blocked without a write."
  artifacts:
    - path: custom/grocy_AI/src/GrocyAiConversionService.php
      provides: closed dimension/catalog and candidate validation service
    - path: views/quantityunitconversionform.blade.php
      provides: native-form validation region without a second save path
  key_links:
    - from: public/viewjs/quantityunitconversionform.js
      to: /api/grocy-ai/conversions/validate
      via: fresh candidate validation before native POST/PUT
      pattern: conversions/validate
    - from: public/viewjs/quantityunitconversionform.js
      to: objects/quantity_unit_conversions
      via: unchanged native save call
      pattern: objects/quantity_unit_conversions
---

## Phase Goal

**As a** household maintainer, **I want to** validate and save authoritative reusable mass or volume defaults through Grocy's normal editor, **so that** invalid universal rules cannot enter the effective conversion model.

<objective>
Deliver the first usable conversion slice: an exact, dimension-safe universal candidate is validated and then persisted only through Grocy's existing Save action.

Purpose: establish explicit dimensions and canonical factors without creating a parallel mutation authority.
Output: module-owned catalog/validation and native-form validation gate.
</objective>

<execution_context>@/Users/ian/.codex/get-shit-done/workflows/execute-plan.md</execution_context>

<context>
@.planning/phases/04-reusable-conversion-model/04-CHARACTERIZATION.md
@.planning/phases/04-reusable-conversion-model/04-CONTEXT.md
@.planning/phases/04-reusable-conversion-model/04-UI-SPEC.md
@.planning/phases/04-reusable-conversion-model/04-PATTERNS.md
<interfaces>
From `quantityunitconversionform.js`: the unchanged authority is `Grocy.Api.Post/Put('objects/quantity_unit_conversions', jsonData, ...)`.
From `GrocyAiTaxonomyMigration::Bootstrap(PDO)`: module schema bootstrap is idempotent and transaction-owned.
From Plan 01: use only the projection and cache contract selected in `04-CHARACTERIZATION.md`; do not implement another cache strategy.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement the closed exact universal catalog and candidate validator</name>
  <files>custom/grocy_AI/src/GrocyAiConversionMigration.php, custom/grocy_AI/src/GrocyAiConversionService.php, custom/grocy_AI/tests/conversions.php, custom/grocy_AI/tests/run.php</files>
  <read_first>custom/grocy_AI/src/GrocyAiTaxonomyMigration.php; custom/grocy_AI/src/GrocyAiTaxonomyService.php; custom/grocy_AI/tests/taxonomy.php; .planning/phases/04-reusable-conversion-model/04-CHARACTERIZATION.md</read_first>
  <behavior>
    - Test 1: only D-01 mass and volume units receive closed dimension identities and NIST-backed metric-anchor factors with full stored precision.
    - Test 2: a mass↔volume universal candidate, zero/non-finite factor, count/package unit, reciprocal drift, cycle, competing path, or tolerance failure returns a bounded blocker and never projects.
    - Test 3: valid same-dimension candidates retain source/version and pass only the Plan 01 selected projection contract.
  </behavior>
  <action>Create idempotent `grocy_ai_conversion_*` schema/ledger objects and a fixed-key service DTO. Per D-01/D-02, seed only the listed mass/volume units using cited NIST factors anchored to metric units; display rounding must defer to Grocy's quantity-decimal setting. Per D-04, reject pack/can/bottle/piece/count from reusable tables. Per D-10/D-15, validate dimensions, positivity/finiteness, all effective paths, reciprocal consistency, cycles, and configured relative tolerance before any eligible projection transaction. Implement precisely the projection selected by Plan 01, and keep all existing product conversion rows untouched per D-14.</action>
  <verify><automated>php custom/grocy_AI/tests/run.php conversion-rules && php -l custom/grocy_AI/src/GrocyAiConversionMigration.php && php -l custom/grocy_AI/src/GrocyAiConversionService.php</automated></verify>
  <acceptance_criteria>Focused tests prove exact same-dimension factors, full precision, closed dimensions, and no projection/write for every invalid or package/count candidate.</acceptance_criteria>
  <done>The module has a transaction-safe, characterized projection implementation for eligible universal rules only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate the native conversion editor on current validation while retaining normal Save</name>
  <files>custom/grocy_AI/src/GrocyAiApiController.php, custom/grocy_AI/routes.php, views/quantityunitconversionform.blade.php, public/viewjs/quantityunitconversionform.js</files>
  <read_first>custom/grocy_AI/src/GrocyAiApiController.php; custom/grocy_AI/routes.php; views/quantityunitconversionform.blade.php; public/viewjs/quantityunitconversionform.js; .planning/phases/04-reusable-conversion-model/04-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: changing either unit/factor invalidates an earlier validation and keeps Save disabled until the current candidate is eligible.
    - Test 2: the validate endpoint is MASTER_DATA_EDIT-protected, returns only bounded candidate/provenance/blocker DTO fields, and makes no write.
    - Test 3: an eligible form uses unchanged native POST/PUT; a server-side revalidation failure preserves form values and blocks persistence/projection.
  </behavior>
  <action>Add the UI-SPEC validation region after `#qu-conversion-inverse-info`, with exact required copy, live status/alert semantics, 44px controls, focus recovery, and per-form revision protection. Add only authenticated, permission-checked read validation routes; never add a custom conversion save endpoint. Keep `#save-quconversion-button`, `Grocy.Components.UserfieldsForm.Save`, native modal/navigation handling, and `objects/quantity_unit_conversions` POST/PUT intact. The authoritative native save path must revalidate through the selected module integration before projection; D-03 and D-15 require that stale, blocked, or changed candidates cannot activate.</action>
  <verify><automated>php custom/grocy_AI/tests/run.php conversion-rules && php -l custom/grocy_AI/src/GrocyAiApiController.php && node --check public/viewjs/quantityunitconversionform.js</automated></verify>
  <acceptance_criteria>An authenticated maintainer can validate a valid universal candidate and invoke the existing Save control; blocked/stale candidates expose a specific safe message and produce no write.</acceptance_criteria>
  <done>The first user-facing vertical slice supports exact reusable defaults through native Grocy persistence only.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser candidate → module validation | Unit IDs and factors are untrusted. |
| native Grocy save → projection | A durable conversion edit can affect cache consumers. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-04 | Tampering | candidate validation | mitigate | Closed unit/dimension enums, finite positive factor checks, full graph conflict validation, and rollback-on-throw. |
| T-04-05 | Elevation of Privilege | validation endpoint | mitigate | Require authenticated `MASTER_DATA_EDIT`; do not trust browser source/version fields. |
| T-04-06 | Tampering | save/projection boundary | mitigate | Revalidate current candidate in the native authority path and project only the Plan 01 contract in one transaction. |
| T-04-SC | Tampering | package installs | accept | No package installation is planned. |
</threat_model>
<verification>Run `php custom/grocy_AI/tests/run.php conversion-rules` and PHP/JS syntax checks.</verification>
<success_criteria>Only eligible exact mass/volume defaults can pass a fresh validation to Grocy's existing Save action; count/package and cross-dimension rules cannot.</success_criteria>
<output>Create `.planning/phases/04-reusable-conversion-model/04-02-SUMMARY.md` when done</output>
