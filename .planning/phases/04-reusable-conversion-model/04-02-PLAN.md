---
phase: 04-reusable-conversion-model
plan: "02"
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - custom/grocy_AI/src/GrocyAiConversionMigration.php
  - custom/grocy_AI/src/GrocyAiConversionService.php
  - custom/grocy_AI/src/GrocyAiApiController.php
  - custom/grocy_AI/routes.php
  - controllers/Api/GenericEntityApiController.php
  - custom/grocy_AI/tests/conversions.php
  - custom/grocy_AI/tests/run.php
autonomous: true
requirements: [CONV-01, CONV-02, CONV-04, CONV-06]
must_haves:
  truths:
    - "D-04: A valid native product-scoped package/count or measured-density POST and PUT persist normally, while reusable-scope input is blocked until its immutable activation gate passes."
    - "D-01: The editable universal catalog contains only the specified mass and volume units, and no temperature or length rules."
    - "D-02: Universal kitchen factors remain metric-anchored at full precision and use Grocy quantity-decimal settings only for display rounding."
    - "D-10: Mass/volume mixing and conflicting effective paths block reusable resolution rather than selecting a nearest factor."
    - "D-14: Valid product-scoped native conversions retain normal Save authority without changing existing product-specific rows."
    - "D-15: A reusable universal candidate cannot activate until validation reports its unit pair, precise factor, source/version, and a conflict/cycle-free impact result."
  artifacts:
    - path: custom/grocy_AI/src/GrocyAiConversionService.php
      provides: closed dimension/catalog and candidate validation service
    - path: controllers/Api/GenericEntityApiController.php
      provides: scope-aware native pre-save enforcement at AddObject and EditObject
  key_links:
    - from: controllers/Api/GenericEntityApiController.php
      to: GrocyAiConversionService::ValidateNativeConversionBeforeWrite
      via: entity-specific pre-save hook before createRow()->save()/row->update()
      pattern: quantity_unit_conversions
---

## Phase Goal

**As a** household maintainer, **I want to** validate and save authoritative reusable mass or volume defaults through Grocy's normal editor, **so that** invalid universal rules cannot enter the effective conversion model.

<objective>
Deliver the first usable conversion slice: an exact, dimension-safe universal candidate is validated and then persisted only through Grocy's existing Save action.

Purpose: establish explicit dimensions and canonical factors without creating a parallel mutation authority.
Output: module-owned catalog/validation and a tested server-side native-save boundary.
</objective>

<execution_context>@/Users/ian/.codex/get-shit-done/workflows/execute-plan.md</execution_context>

<context>
@.planning/phases/04-reusable-conversion-model/04-CHARACTERIZATION.md
@.planning/phases/04-reusable-conversion-model/04-CONTEXT.md
@.planning/phases/04-reusable-conversion-model/04-UI-SPEC.md
@.planning/phases/04-reusable-conversion-model/04-PATTERNS.md
<interfaces>
From `quantityunitconversionform.js`: the unchanged authority is `Grocy.Api.Post/Put('objects/quantity_unit_conversions', jsonData, ...)`.
From `GrocyAiTaxonomyMigration::Bootstrap(PDO)`: module schema bootstrap is idempotent and transaction-owned.
From Plan 01: use only the projection and cache contract selected in `04-CHARACTERIZATION.md`; do not implement another cache strategy.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement the closed exact universal catalog and candidate validator</name>
  <files>custom/grocy_AI/src/GrocyAiConversionMigration.php, custom/grocy_AI/src/GrocyAiConversionService.php, custom/grocy_AI/tests/conversions.php, custom/grocy_AI/tests/run.php</files>
  <read_first>custom/grocy_AI/src/GrocyAiTaxonomyMigration.php; custom/grocy_AI/src/GrocyAiTaxonomyService.php; custom/grocy_AI/tests/taxonomy.php; .planning/phases/04-reusable-conversion-model/04-CHARACTERIZATION.md</read_first>
  <behavior>
    - Test 1: only D-01 mass and volume units receive closed dimension identities and NIST-backed metric-anchor factors with full stored precision.
    - Test 2: a mass↔volume universal candidate, zero/non-finite factor, reciprocal drift, cycle, competing path, or tolerance failure returns a bounded blocker; package/count is rejected only when the candidate scope is reusable universal/profile.
    - Test 3: valid product-scoped native pack/can/bottle/piece or measured-density candidates retain normal-save eligibility, while reusable catalog/profile records stay inactive until Plan 06 evidence activation.
  </behavior>
  <action>Create idempotent `grocy_ai_conversion_*` catalog/ledger objects and a fixed-key service DTO, initially inactive. Per D-01: seed only the listed mass/volume units. Per D-02: use cited NIST factors anchored to metric units and defer display rounding to Grocy's quantity-decimal setting. Per D-04: reject pack/can/bottle/piece/count only from reusable tables; explicitly allow those and measured-density conversions when `product_id` scopes the native candidate. Per D-10: validate dimensions, positivity/finiteness, all effective paths, reciprocal consistency, cycles, and configured relative tolerance; per D-15: require that validation before any activation. Do not project catalog/profile rows in this plan; per D-14: retain every existing product conversion row unchanged.</action>
  <verify><automated>php custom/grocy_AI/tests/run.php conversion-rules && php -l custom/grocy_AI/src/GrocyAiConversionMigration.php && php -l custom/grocy_AI/src/GrocyAiConversionService.php</automated></verify>
  <acceptance_criteria>Focused tests prove exact same-dimension factors, full precision, closed dimensions, and no projection/write for every invalid or package/count candidate.</acceptance_criteria>
  <done>The module has an inactive, transaction-safe reusable catalog plus scope-aware candidate validation that preserves product-specific native conversions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce the native AddObject/EditObject scope boundary before any native/cache mutation</name>
  <files>custom/grocy_AI/src/GrocyAiApiController.php, custom/grocy_AI/routes.php, controllers/Api/GenericEntityApiController.php, custom/grocy_AI/tests/conversions.php, custom/grocy_AI/tests/run.php</files>
  <read_first>custom/grocy_AI/src/GrocyAiApiController.php; custom/grocy_AI/routes.php; controllers/Api/GenericEntityApiController.php; .planning/phases/04-reusable-conversion-model/04-CHARACTERIZATION.md</read_first>
  <behavior>
    - Test 1: the permission-checked validate route returns a bounded no-write DTO for a candidate.
    - Test 2: before and after Plan 08's immutable-evidence activation succeeds, every generic native universal POST and PUT is rejected before `createRow()->save()`/`row->update()` and leaves native/cache/projection snapshots unchanged; a rejected request may create only a named inactive module revision.
    - Test 3: controller-bound positive integration invokes `GenericEntityApiController::AddObject` with a valid product-scoped package/count POST and `::EditObject` with a valid product-scoped measured-density PUT; each persists only its expected native row/cache effect and proves the hook receives the submitted scope and actual object ID.
    - Test 4: reusable-scope package/count, cross-dimension, stale, or unapproved candidates are blocked on both POST and PUT with no native/cache mutation.
  </behavior>
  <action>Add only authenticated, permission-checked read validation routes; never add a custom conversion save endpoint. In the documented minimal upstream hook in `GenericEntityApiController::AddObject` and `::EditObject`, branch only on `quantity_unit_conversions` and call `ValidateNativeConversionBeforeWrite($requestBody, $objectId)` after filtered parsing but before `createRow()->save()` or `row->update()`. Preserve normal product-scoped Save authority: assert the hook receives `product_id` on AddObject and the persisted conversion object ID plus effective product scope on EditObject, then allow valid package/count and measured-density rows. Per D-12: every generic reusable-universal native save is fail-closed before and after Plan 08; per D-15: its validation must complete before activation. The hook may persist/deduplicate a named inactive module revision, but it returns Grocy's bounded API error before native rows/triggers/cache/projection change. `ActivateVerifiedRuleset` is the exclusive later writer of gate-created universal native rows and the selected projection after current immutable dual-branch evidence. The hook must never directly activate or project reusable rows.</action>
  <verify><automated>php custom/grocy_AI/tests/run.php conversion-rules && php custom/grocy_AI/tests/run.php conversion-native-save-hook && php -l custom/grocy_AI/src/GrocyAiApiController.php && php -l custom/grocy_AI/routes.php && php -l controllers/Api/GenericEntityApiController.php && php -l custom/grocy_AI/tests/conversions.php && php -l custom/grocy_AI/tests/run.php</automated></verify>
  <acceptance_criteria>Focused controller integration proves valid product-scoped POST/PUT saves retain normal native/cache behavior, whereas every unactivated or invalid reusable POST/PUT produces no write.</acceptance_criteria>
  <done>The real native controller boundary distinguishes scope/object identity correctly and cannot activate reusable rules before Plan 08.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser candidate → module validation | Unit IDs and factors are untrusted. |
| native Grocy save → projection | A durable conversion edit can affect cache consumers. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-04 | Tampering | candidate validation | mitigate | Closed unit/dimension enums, finite positive factor checks, full graph conflict validation, and rollback-on-throw. |
| T-04-05 | Elevation of Privilege | validation endpoint | mitigate | Require authenticated `MASTER_DATA_EDIT`; do not trust browser source/version fields. |
| T-04-06 | Tampering | save/projection boundary | mitigate | Revalidate current candidate in the native authority path and project only the Plan 01 contract in one transaction. |
| T-04-SC | Tampering | package installs | accept | No package installation is planned. |
</threat_model>
<verification>Run `php custom/grocy_AI/tests/run.php conversion-rules` and PHP/JS syntax checks.</verification>
<success_criteria>Reusable defaults pass only after fresh scope-aware validation and the native pre-save hook; cross-dimension reusable rules cannot, while valid product-scoped package/count and measured-density conversions retain normal Save.</success_criteria>
<output>Create `.planning/phases/04-reusable-conversion-model/04-02-SUMMARY.md` when done</output>
