---
phase: 04-reusable-conversion-model
plan: "06"
type: execute
wave: 6
depends_on: [04-05]
files_modified:
  - custom/grocy_AI/src/GrocyAiConversionController.php
  - custom/grocy_AI/src/GrocyAiConversionService.php
  - custom/grocy_AI/routes.php
  - views/grocyai_conversioncoverage.blade.php
  - public/custom/grocy_AI/conversion-coverage.js
  - public/custom/grocy_AI/conversion-coverage.test.js
  - custom/grocy_AI/bin/validate-conversion-rules.php
  - custom/grocy_AI/tests/conversions.php
  - custom/grocy_AI/tests/run.php
  - custom/grocy_AI/tests/release-gate.sh
  - custom/grocy_AI/portable-files.txt
  - custom/grocy_AI/README.md
  - CUSTOMIZATIONS.md
autonomous: false
requirements: [CONV-07, CONV-09]
must_haves:
  truths:
    - "A maintainer can inspect a read-only aggregate coverage report with gate, source, missing-path, redundancy, cycle, and conflict information."
    - "Reusable catalog/profile projection remains inactive unless current immutable evidence proves characterization and protected-consumer equivalence on both branches."
  artifacts:
    - path: views/grocyai_conversioncoverage.blade.php
      provides: MASTER_DATA_EDIT read-only conversion coverage page
    - path: custom/grocy_AI/bin/validate-conversion-rules.php
      provides: redacted configured-database report with bootstrap disabled
  key_links:
    - from: GrocyAiConversionController
      to: GrocyAiConversionService
      via: read-only coverage DTO
      pattern: ConversionCoverage
    - from: custom/grocy_AI/tests/release-gate.sh
      to: 04-CHARACTERIZATION.md
      via: both branch gate and protected-output evidence
      pattern: characterization
---

## Phase Goal

**As a** maintainer, **I want to** inspect conversion coverage and release compatibility before activation, **so that** reusable rules cannot degrade household inventory behavior.

<objective>
Deliver the maintainer-facing read-only coverage report and release gate that prove the completed model remains inactive unless dual-branch compatibility evidence is current.

Purpose: close the loop from rule validation to safe release verification.
Output: authenticated coverage page/API, redacted CLI report, release/parity checks, and a human visual verification checkpoint.
</objective>
<execution_context>@/Users/ian/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/04-reusable-conversion-model/04-CHARACTERIZATION.md
@.planning/phases/04-reusable-conversion-model/04-UI-SPEC.md
@.planning/phases/04-reusable-conversion-model/04-VALIDATION.md
@.planning/phases/03-food-taxonomy-categorization-pilot/03-03-SUMMARY.md
<interfaces>
From Phase 3 `validate-inventory-taxonomy.php`: configured-database maintenance CLIs require absolute `GROCY_DATAPATH`, instantiate service bootstrap-disabled, emit one redacted JSON object, and use stable failures.
From `GrocyAiConversionController`: render a normal `views/` Blade page only after `MASTER_DATA_EDIT` permission; coverage and refresh paths are read-only.
</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build read-only conversion coverage and configured-database diagnostics</name>
  <files>custom/grocy_AI/src/GrocyAiConversionController.php, custom/grocy_AI/src/GrocyAiConversionService.php, custom/grocy_AI/routes.php, views/grocyai_conversioncoverage.blade.php, public/custom/grocy_AI/conversion-coverage.js, public/custom/grocy_AI/conversion-coverage.test.js, custom/grocy_AI/bin/validate-conversion-rules.php, custom/grocy_AI/tests/conversions.php, custom/grocy_AI/tests/run.php</files>
  <read_first>custom/grocy_AI/bin/validate-inventory-taxonomy.php; custom/grocy_AI/src/GrocyAiApiController.php; controllers/StockController.php; .planning/phases/04-reusable-conversion-model/04-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: `MASTER_DATA_EDIT` coverage/report reads expose only aggregate ruleset/gate/coverage/unavailable/redundancy/blocker counts and bounded categories.
    - Test 2: the report is `Ruleset ready` only with zero blockers and both characterization/protected-consumer gates passed; otherwise it says inactive/blocked and does not activate anything.
    - Test 3: CLI rejects missing/relative data paths, opens configured DB read-only with bootstrap disabled, and snapshots prove no writes or household-value output.
    - Test 4: a fast DOM/fixture suite verifies exact inactive/ready/blocker summary copy, request sequencing preserves the last report, and a late response cannot replace newer diagnostics; Playwright remains browser E2E coverage.
  </behavior>
  <action>Create a module controller/page and refresh API that call a read-only coverage DTO; no request may bootstrap schema, project, activate, mutate, or remove rules. Render the UI-SPEC’s Conversion coverage title, source/gate summaries, blockers, missing paths, redundant product overrides, provenance, and compatibility evidence with request sequencing that preserves the last report on error. Implement the CLI using Phase 3’s read-only pattern. Per D-12/D-13 and D-14, the gate must remain inactive when characterization is absent, stale, failed, missing either immutable branch revision, or lacks any protected-consumer proof; it must not change existing product rows. Update only module/release documentation paths required by the new public surface.</action>
  <verify><automated>php custom/grocy_AI/tests/run.php conversion-coverage && php custom/grocy_AI/tests/run.php conversion-readonly-cli && node --test public/custom/grocy_AI/conversion-coverage.test.js && php -l custom/grocy_AI/src/GrocyAiConversionController.php && php -l custom/grocy_AI/src/GrocyAiConversionService.php && php -l custom/grocy_AI/bin/validate-conversion-rules.php && node --check public/custom/grocy_AI/conversion-coverage.js</automated></verify>
  <acceptance_criteria>The authenticated page and CLI show redacted diagnostics; their before/after fixture snapshots match, and no gate state can activate without current dual-branch proof.</acceptance_criteria>
  <done>Maintainers can inspect every required coverage/gate outcome without a browser or CLI write path.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Enforce portable/stable release parity and protected-consumer equivalence</name>
  <files>custom/grocy_AI/tests/release-gate.sh, custom/grocy_AI/portable-files.txt, custom/grocy_AI/tests/conversions.php, custom/grocy_AI/tests/run.php, custom/grocy_AI/README.md, CUSTOMIZATIONS.md</files>
  <read_first>custom/grocy_AI/tests/release-gate.sh; custom/grocy_AI/tests/check-portable-parity.sh; custom/grocy_AI/portable-files.txt; .planning/phases/04-reusable-conversion-model/04-CHARACTERIZATION.md; .planning/phases/03-food-taxonomy-categorization-pilot/03-03-SUMMARY.md</read_first>
  <behavior>
    - Test 1: release/activation gate fails when any current immutable main/stable characterization revision, cache/trigger contract, or protected stock/recipe/purchase/consumption/price/display/transfer/meal-plan comparison is absent or unequal; it leaves all reusable projection inactive.
    - Test 2: portable parity enumerates every Phase 4 portable module artifact and reports documented stable adapters separately.
    - Test 3: full module suite and focused browser coverage remain green with existing product rows unchanged.
  </behavior>
  <action>Extend the existing SHA-pinned parity/release gate with Phase 4 artifacts and the exact Plan 01 evidence contract. In `GrocyAiConversionService`, add the sole activation/projection operation: it must validate current immutable evidence for both branch commit IDs, selected schema/trigger/cache-key contract, and each protected-consumer proof in one transaction before it changes any active cache/projection state; missing, stale, failed, or unequal evidence returns an inactive blocker without native/cache writes. Require those checks before a release can call the ruleset ready or the native pre-save hook can project eligible reusable rules. Document portable versus stable-only adapters and module-version/cache-invalidation updates without broad upstream churn. This task implements D-12/D-13: no equivalence proof, no release/activation claim; do not introduce a data migration or Phase 6 cleanup action.</action>
  <verify><automated>php custom/grocy_AI/tests/run.php conversion-release-gate && bash custom/grocy_AI/tests/release-gate.sh conversions && php custom/grocy_AI/tests/run.php && npm --prefix custom/grocy_AI/tests/browser test -- --grep @conv04 && php -l custom/grocy_AI/src/GrocyAiConversionService.php</automated></verify>
  <acceptance_criteria>The release gate blocks incomplete/mismatched evidence, passes only an immutable documented main/stable pair, and proves all protected fixture outputs and existing product conversion snapshots remain equivalent.</acceptance_criteria>
  <done>The conversion model has a deterministic, portable/stable release proof and Phase 4 does not claim activation without it.</done>
</task>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm release-facing native conversion behavior</name>
  <action>Pause only after all automated parity, consumer-equivalence, and browser checks pass; this checkpoint verifies native UI clarity and normal Save behavior that fixture DOMs cannot fully establish.</action>
  <what-built>Read-only coverage report and native provenance states, all backed by automated no-write, parity, and protected-consumer checks.</what-built>
  <how-to-verify>1. On each maintained branch, sign in with `MASTER_DATA_EDIT` and open the Conversion coverage page. 2. Confirm Ruleset ready appears only when both characterization branches are green; otherwise verify the exact inactive/blocker copy. 3. At desktop and 320px widths, inspect a universal exact row, product override, approximate profile, unavailable product, and blocked pair in the product and resolved screens. 4. Confirm `Show conversion details` is keyboard usable and normal Grocy Save remains the only persistence action. 5. Confirm no existing product conversion row was replaced or removed.</how-to-verify>
  <resume-signal>Type "approved" or describe the issue.</resume-signal>
  <verify><automated>php custom/grocy_AI/tests/run.php conversion-release-gate && npm --prefix custom/grocy_AI/tests/browser test -- --grep @conv04</automated></verify>
  <acceptance_criteria>The maintainer confirms readable, accessible state/provenance and native Save behavior on both branch builds without product-row cleanup.</acceptance_criteria>
  <done>Human visual/function verification approves the release-facing Phase 4 UI.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| configured production database → CLI | Read-only diagnostics can expose or mutate household data if misimplemented. |
| release evidence → activation claim | Stale or partial proof could ship a bad cache projection. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-16 | Information Disclosure | report/CLI | mitigate | Aggregate redacted DTO/JSON only; never emit names, barcodes, raw evidence, URLs, SQL, or exception text. |
| T-04-17 | Tampering | coverage/report endpoint | mitigate | Enforce permission; open CLI DB read-only with bootstrap disabled; fixture snapshots prove no writes. |
| T-04-18 | Tampering | release activation gate | mitigate | Require immutable dual-branch characterization, parity, and protected-output comparisons; high-severity mismatch blocks release. |
| T-04-SC | Tampering | package installs | accept | No package installation is planned. |
</threat_model>
<verification>Run focused coverage/CLI/release commands, full PHP suite, and focused browser tests before the checkpoint.</verification>
<success_criteria>The coverage report is read-only and redacted, and Phase 4 can be released only with current dual-branch equivalence evidence.</success_criteria>
<output>Create `.planning/phases/04-reusable-conversion-model/04-06-SUMMARY.md` when done</output>
