---
phase: 04-reusable-conversion-model
plan: "06"
type: execute
wave: 7
depends_on: [04-09]
files_modified:
  - custom/grocy_AI/src/GrocyAiApiController.php
  - custom/grocy_AI/routes.php
  - views/quantityunitconversionsresolved.blade.php
  - public/viewjs/quantityunitconversionsresolved.js
  - public/custom/grocy_AI/conversion-explanations.js
  - public/custom/grocy_AI/conversion-explanations.test.js
  - custom/grocy_AI/tests/browser/specs/conversions.spec.js
  - custom/grocy_AI/tests/conversions.php
  - custom/grocy_AI/tests/run.php
autonomous: true
requirements: [CONV-05, CONV-07]
must_haves:
  truths:
    - "D-09: A maintainer can inspect winning source/status for resolved conversions and expand full provenance."
    - "D-10: Blocked and unavailable rows identify the correction state and never expose an inferred usable factor."
  artifacts:
    - path: views/quantityunitconversionsresolved.blade.php
      provides: provenance/status columns alongside native resolved rows
  key_links:
    - from: quantityunitconversionsresolved.js
      to: /api/grocy-ai/conversions/resolved-provenance
      via: DataTable-safe read enrichment
      pattern: resolved-provenance
---

## Phase Goal

**As a** maintainer, **I want to** inspect the effective source for a resolved conversion, **so that** I can recognize exact defaults, approximate profiles, unavailable estimates, and blockers before changing rules.

<objective>
Complete the source-explanation slice in Grocy's resolved-conversions view without replacing its native filter/table behavior.

Purpose: make precedence and conflict outcomes inspectable where Grocy exposes resolved rows.
Output: closed provenance read and accessible table details.
</objective>
<execution_context>@/Users/ian/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/04-reusable-conversion-model/04-UI-SPEC.md
@.planning/phases/04-reusable-conversion-model/04-PATTERNS.md
@.planning/phases/04-reusable-conversion-model/04-09-SUMMARY.md
<interfaces>
From `quantityunitconversionsresolved.js`: preserve existing DataTables initialization, quantity-unit filter, and `colReorder.transpose` behavior.
From Plan 03 DTO: provenance can contain only closed source/category/version/status/reason/detail fields.
</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add a resolved-provenance read contract keyed to native rows</name>
  <files>custom/grocy_AI/src/GrocyAiApiController.php, custom/grocy_AI/routes.php</files>
  <read_first>custom/grocy_AI/src/GrocyAiApiController.php; custom/grocy_AI/routes.php; controllers/StockController.php; custom/grocy_AI/src/GrocyAiConversionService.php</read_first>
  <behavior>
    - Test 1: only valid product/from/to identifiers return a closed source/status/provenance DTO matching one resolver result.
    - Test 2: unavailable/blocked entries omit usable factors and retain bounded reasons.
    - Test 3: the endpoint is permission-protected and read-only.
  </behavior>
  <action>Add a narrow `MASTER_DATA_EDIT` endpoint that validates the resolved-row identity and invokes the existing conversion service only in inspection mode. Do not rebuild cache, activate profiles, bootstrap writable schema, or query by unbounded product data. Per D-09: response semantics must identify the winner; per D-10: identify the correction blocker instead of silently substituting a factor.</action>
  <verify><automated>php custom/grocy_AI/tests/run.php conversion-resolution && php -l custom/grocy_AI/src/GrocyAiApiController.php && php -l custom/grocy_AI/routes.php && php -l custom/grocy_AI/tests/conversions.php && php -l custom/grocy_AI/tests/run.php</automated></verify>
  <acceptance_criteria>A valid resolved pair returns concise provenance; invalid/blocked/unavailable pairs cannot leak a factor or trigger a write.</acceptance_criteria>
  <done>The native resolved table has a safe, deterministic provenance source.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add source/status/details columns without breaking the native resolved table</name>
  <files>views/quantityunitconversionsresolved.blade.php, public/viewjs/quantityunitconversionsresolved.js, public/custom/grocy_AI/conversion-explanations.js, public/custom/grocy_AI/conversion-explanations.test.js, custom/grocy_AI/tests/browser/specs/conversions.spec.js</files>
  <read_first>views/quantityunitconversionsresolved.blade.php; public/viewjs/quantityunitconversionsresolved.js; public/custom/grocy_AI/conversion-explanations.js; .planning/phases/04-reusable-conversion-model/04-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: each row shows source and text status, while a real `Show conversion details` button expands full precision/provenance using safe text rendering.
    - Test 2: Exact/Approximate/Unavailable/Blocked are not color-only, are keyboard accessible, and remain usable at 320px.
    - Test 3: original filtering/reordering and native row rendering continue to work; enrichment failures preserve the table and show bounded recovery copy.
    - Test 4: a fast DOM/fixture suite verifies status/detail rendering, text-only provenance insertion, disclosure state, and stale visible-row responses before browser E2E coverage.
  </behavior>
  <action>Extend the existing view and DataTable only after native initialization, preserving current filters and reorder semantics. Add visible source/status and a text-labeled disclosure; move secondary precision/prose into the expanded region at narrow widths. Use the UI-SPEC copy/states, `aria-live`/`role=alert` conventions, current-request guards, and no raw HTML from API values. Per D-09: do not add an editing/activation control or a second resolved-data source; per D-10: preserve a visible correction blocker. This is inspection only.</action>
  <verify><automated>node --test public/custom/grocy_AI/conversion-explanations.test.js && npm --prefix custom/grocy_AI/tests/browser test -- --grep @conv04 && php -l views/quantityunitconversionsresolved.blade.php && node --check public/viewjs/quantityunitconversionsresolved.js</automated></verify>
  <acceptance_criteria>Focused browser tests prove native table behaviors survive and the expanded provenance state cannot make blocked/unavailable values appear usable.</acceptance_criteria>
  <done>The resolved-conversions screen visibly explains its deterministic source/result.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| resolved row identifiers → API | Client-selected keys must map to a legitimate bounded resolver lookup. |
| provenance DTO → DataTable DOM | Server-provided text must not execute or alter native table semantics. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-13 | Tampering | resolved lookup | mitigate | Strict integer identity validation, permission check, closed resolver DTO, no activation side effects. |
| T-04-14 | Information Disclosure | details UI | mitigate | Use text nodes/fixed fields and bounded error copy; do not render raw SQL, evidence, or exceptions. |
| T-04-15 | Denial of Service | DataTable enrichment | mitigate | Request only visible/bounded pairs with current-request guards; preserve native table on failure. |
| T-04-SC | Tampering | package installs | accept | No package installation is planned. |
</threat_model>
<verification>Run focused `@conv04` browser tests and the conversion-resolution PHP suite.</verification>
<success_criteria>The native resolved conversion view shows a deterministic source/status and accessible details, with no accidental factor for unavailable or blocked results.</success_criteria>
<output>Create `.planning/phases/04-reusable-conversion-model/04-06-SUMMARY.md` when done</output>
