---
context: phase
phase: 04-reusable-conversion-model
task: 1
total_tasks: 20
status: paused
last_updated: 2026-08-28
---

# Current State

Phase 4 work continues on `codex/phase4-characterization`. Plans 04-01 through 04-06 and 04-09 are implemented and committed; the working tree is clean. The next unimplemented plan is 04-07, "read-only coverage diagnostics".

## Completed Work

- 04-01: disposable dual-branch characterization; no reusable projection selected.
- 04-02: inactive conversion catalog and native pre-save boundary.
- 04-03: inactive sourced profiles and deterministic resolver.
- 04-04: native conversion-form validation/status.
- 04-05: product conversion status API.
- 04-09: read-only product conversion provenance UI plus zero-write browser proof.
- 04-06: resolved-conversion provenance read plus native-table source/status columns.

## Remaining Work

- 04-07: strictly read-only coverage diagnostics.
- 04-08: evidence-gated activation and release parity (`autonomous: false` — requires the user).
- 04-10: maintainer-only evidence-gated promotion command.

## Plan ordering correction

`04-06-PLAN.md` declares `depends_on: [04-09]` and reads `04-09-SUMMARY.md`; the roadmap places 04-09 in Wave 6 and 04-06 in Wave 7. The earlier handoff listed 04-06 first, which was stale. Wave order is 04-05 → 04-09 → 04-06 → 04-07 → 04-08 → 04-10.

## Decisions Made

- Keep every reusable profile/rule inactive until current immutable dual-branch evidence supports activation.
- Preserve normal product-scoped Grocy conversion saves; no custom conversion persistence path.
- Only a `product_native` status may carry a factor; the browser redacts the factor a second time.
- Blocker codes never reach the DOM; they map to closed correction-category sentences.
- `GrocyAiConversionService::UnitKeyForName` is the single owner of the Grocy-unit-name to catalog-key mapping; inspection callers must not restate it.
- Plan 04-07 is inspection-only: it must reuse the existing bounded reads and must not add a cleanup, activation, or projection path.

## Blockers

- Five browser specs fail on both engines and already failed at end-of-Phase-3 commit `ccade631`: `@mob01` overflow, `@mob08` 320px overflow, `@enr05` 320px responsive review, `barcode-handoff @enr02 @enr03 @enr09`, and `contract-review @enr05 @enr06 @enr09`. The 320px overflow is exactly 16px and is caused by the fixture's `.row { margin: 0 -8px; }` on `#grocy-ai-product-enrichment` inside the 16px-padded body. This contradicts the Phase 1 and Phase 2 mobile completion records and needs its own quick task.
- GitHub CLI is not authenticated on this machine. `origin` already points to `https://github.com/ATECHPCS/grocy.git`; authenticate or configure Git credentials before pushing this branch.

## Required Reading (in order)

1. `.planning/HANDOFF.json` — current machine-readable status and GitHub handoff.
2. `.planning/phases/04-reusable-conversion-model/04-07-PLAN.md` — exact next-slice contract.
3. `.planning/phases/04-reusable-conversion-model/04-06-SUMMARY.md` and `04-09-SUMMARY.md` — the bounded inspection reads and copy 04-07 must reuse.
4. `.planning/phases/04-reusable-conversion-model/04-UI-SPEC.md` — required status and accessibility behavior.
5. `.planning/phases/04-reusable-conversion-model/04-CHARACTERIZATION.md` — immutable activation boundary.
6. `custom/grocy_AI/src/GrocyAiConversionService.php` and the native resolved-table files — interfaces to retain.

## Infrastructure State

- No servers or watchers are running.
- Branch: `codex/phase4-characterization`.
- Remote: `origin` → `https://github.com/ATECHPCS/grocy.git`.
- GitHub CLI: unauthenticated.
- Local toolchain (Linux): PHP 8.5.10 with `pdo_sqlite`/`mbstring`/`curl`/`xml`/`gd`/`intl`/`zip`/`bcmath`, Composer packages installed under `packages/` so the real-Blade gate runs, Node 22, Playwright 1.62.1 with Chromium and WebKit installed. The repository requires PHP 8.5; system PHP 8.3 cannot install the lock file.

## Pre-Execution Critique Required

- Design artifact: `.planning/phases/04-reusable-conversion-model/04-07-PLAN.md`
- Gate: confirm the bounded implementation design with the user before modifying code; then use tests first.

## Next Action

Resume Plan 04-07. Once GitHub credentials are available, push `codex/phase4-characterization` to the existing private ATECHPCS remote.
