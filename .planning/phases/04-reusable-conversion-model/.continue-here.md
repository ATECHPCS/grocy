---
context: phase
phase: 04-reusable-conversion-model
task: 1
total_tasks: 20
status: paused
last_updated: 2026-08-23T22:24:15.832Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These constraints were discovered by the Phase 4 plan verifier. They are structural safety requirements, not suggestions.

- [ ] CONSTRAINT: Native universal-cache bypass — a generic native `quantity_unit_conversions` universal POST/PUT can rebuild Grocy's resolved cache. It must always fail closed or produce only a named inactive revision, including after activation. Only `ActivateVerifiedRuleset`, invoked by the approved maintainer CLI after current immutable dual-branch proof, may write the universal/cache projection.

**Do not proceed until this is understood.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|----------------------|
| Native universal cache bypass | Planning review proved a normal universal POST/PUT could alter Grocy's resolved cache before the evidence gate. | blocking | Enforce scope-aware controller interception: product-scoped package/count and measured-density saves pass; reusable universal writes fail closed or stage inactive. `ActivateVerifiedRuleset` is the sole projection writer. |
| Premature projection choice | The safe cache projection mechanism was initially unresolved and cannot be assumed from main alone. | blocking | Execute 04-01's disposable main/stable characterization before selecting any projection; require current immutable evidence in 04-08/04-10. |

<current_state>

Phase 4 is fully planned and verified but not executed. Start with Plan 04-01, which characterizes `cache__quantity_unit_conversions_resolved` and its triggers in disposable fixtures on both branches. No production, Docker, or household database work is authorized by the plan's first wave.

</current_state>

<completed_work>

Completed planning work:

- Phase 3 marked complete; food taxonomy and product-group evidence are deployed.
- Phase 4 context, research, UI contract, validation strategy, and pattern map are committed.
- Ten Phase 4 plans (`04-01` through `04-10`) passed the plan checker.
- All `CONV-01` through `CONV-09` requirements and all 15 locked Phase 4 decisions have explicit plan coverage.

</completed_work>

<remaining_work>

- Execute Plans 04-01 through 04-10 in wave order.
- First action: implement and run only the disposable dual-branch characterization harness from `04-01-PLAN.md`.
- Do not activate/project reusable rules until the later immutable evidence gate is complete.

</remaining_work>

<decisions_made>

- Universal mass/volume defaults are editable through Grocy's native quantity-unit screens, but their submitted revisions are inactive until promotion.
- Food-type mass/volume results are sourced, explicitly approximate, and require an explicit Phase 3 taxonomy assignment; no profile means no estimate.
- Product-scoped package/count and measured-density conversions keep Grocy's native Save path; Phase 6 owns any cleanup of existing product-specific conversions.
- The approved CLI-only `activate-verified-conversion-ruleset.php` command is the sole promotion path and must require a named inactive revision plus current immutable main/stable proof identifiers.

</decisions_made>

<blockers>

None currently. The constraints above are planned safeguards, not external blockers.

</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable status and next action.
2. `.planning/phases/04-reusable-conversion-model/.continue-here.md` — the cache-bypass constraints before acting.
3. `.planning/phases/04-reusable-conversion-model/04-CONTEXT.md` — user-locked decisions.
4. `.planning/phases/04-reusable-conversion-model/04-RESEARCH.md` — cache/trigger findings and source choices.
5. `.planning/phases/04-reusable-conversion-model/04-UI-SPEC.md` — native UI and no-toggle contract.
6. `.planning/phases/04-reusable-conversion-model/04-VALIDATION.md` — test cadence and disposable-fixture rules.
7. `.planning/phases/04-reusable-conversion-model/04-01-PLAN.md` — immediate execution plan.

## Infrastructure State

- Main worktree: `/Users/ian/Documents/Repos/grocy`, branch `atech-main`, clean at pause time and ahead of `origin/atech-main` by local planning commits.
- Stable worktree: `/Users/ian/Documents/Repos/grocy-atech-release`; do not modify it until Plan 04's branch-specific work calls for it.
- No dev server, watcher, deployment, or database process is running from this Phase 4 planning session.

## Pre-Execution Critique Required

- Design artifacts: `04-RESEARCH.md`, `04-UI-SPEC.md`, and plans `04-01` through `04-10`.
- Critique focus: verify that 04-01's characterization stays fully disposable and that no plan permits a native universal cache write outside `ActivateVerifiedRuleset`.
- Gate: do not begin projection/activation implementation until the characterization outcome is committed and later plans consume it.

<context>

The key implementation risk is Grocy's materialized resolved-conversion cache. The Phase 4 plan intentionally uses a long, evidence-gated progression rather than adding universal rules directly. The user approved the maintainer-only promotion command after the plan checker showed generic native saves otherwise could not safely make universal defaults editable.

</context>

<next_action>

Start with: `$gsd-resume-work`, then `$gsd-execute-phase 4`. Execute `04-01` first and stop if its dual-branch characterization cannot establish a compatible projection contract.

</next_action>
