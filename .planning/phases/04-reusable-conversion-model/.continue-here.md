---
context: phase
phase: 04-reusable-conversion-model
task: 1
total_tasks: 20
status: ready
last_updated: 2026-08-29
---

# Current State

Phase 4 work continues on `codex/phase4-characterization`. **Plans 04-01 through 04-09 are complete.**
Plan 04-08's human-verify checkpoint was approved on 2026-08-29. Only **04-10** remains in Phase 4.

## Completed Work

- 04-01: disposable dual-branch characterization; no reusable projection selected.
- 04-02: inactive conversion catalog and native pre-save boundary.
- 04-03: inactive sourced profiles and deterministic resolver.
- 04-04: native conversion-form validation/status.
- 04-05: product conversion status API.
- 04-09: read-only product conversion provenance UI plus zero-write browser proof.
- 04-06: resolved-conversion provenance read plus native-table source/status columns.
- 04-07: read-only coverage report, maintainer page, refresh read, and bootstrap-disabled CLI.
- 04-08: sole `ActivateVerifiedRuleset` transaction, immutable evidence ledger,
  `release-gate.sh conversions`, portable manifest catch-up (20 -> 35 paths), README/CUSTOMIZATIONS.
  Commits `f8505f57`, `ca7c9295`. Human checkpoint approved.

## Remaining Work

- 04-10: maintainer-only evidence-gated promotion command. `autonomous: true`, but it carries a
  `user_setup` prerequisite that must be settled first.

## Blocker before 04-10

`04-10-PLAN.md` requires `GROCY_AI_MAINTAINER_AUTH_FILE`: an absolute path to a maintainer-readable
secret file **outside the repository and outside `GROCY_DATAPATH`**. It must never enter Git or a
command-line argument. Decide that path before starting.

## Decisions Made

- `GrocyAiConversionService::ActivateVerifiedRuleset()` is the only authority that may activate a
  reusable revision or create the universal native rows a projection needs. 04-10's CLI must call it
  rather than reimplementing any part of the gate.
- `04-CHARACTERIZATION.md` on disk is the only evidence authority. It still records **no selected
  projection**, so every real activation call fails closed with `selected_projection_absent`.
- The generic native pre-save hook returns the fixed inactive-revision rejection and writes no
  module row, keeping the shared read-only validation GET provably zero-write.
- Grocy's `quantity_unit_conversions_INS` trigger derives inverse rows;
  `cache__quantity_unit_conversions_resolved` is product-scoped.
- Coverage diagnostics report `ready`/`present`/`passed` only from a recorded evidence row with at
  least one active revision bound to it.

## Verification State (all green as of 2026-08-29)

```sh
GROCY_AI_PHP=php8.5 bash custom/grocy_AI/tests/release-gate.sh conversions   # PASS (conversions)
php8.5 custom/grocy_AI/tests/run.php                                          # All 122 checks passed
php8.5 custom/grocy_AI/tests/run.php conversion-release-gate                   # passed
php8.5 custom/grocy_AI/tests/run.php conversion-post-activation-bypass         # passed
npm --prefix custom/grocy_AI/tests/browser test -- --grep @conv04              # 36 passed
npm --prefix custom/grocy_AI/tests/browser test                                # 184/184 passed
```

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable status and the pending user_setup decision.
2. `.planning/phases/04-reusable-conversion-model/04-10-PLAN.md` — the next slice.
3. `.planning/phases/04-reusable-conversion-model/04-08-SUMMARY.md` — the gate 04-10 must call.
4. `custom/grocy_AI/README.md` section "Reusable conversion model" — the ownership contract.
5. `MISTAKES.md` — repo-specific code-level traps.

## Infrastructure State

- No servers or watchers are running.
- Branch: `codex/phase4-characterization`. Remote: `origin` -> `https://github.com/ATECHPCS/grocy.git`.
- **Run PHP as `php8.5`.** Default `php` on this Linux box is 8.4.25; `composer.json` requires
  `8.5.*`. `release-gate.sh conversions` honours `GROCY_AI_PHP` and `GROCY_AI_STABLE_REPO`.
- Both maintained branches live in this one checkout: main evidence commit
  `f53665ca19c0e2abcd095f79a93865cfbf396a1d` (on `codex/phase4-characterization`) and stable
  `6605ae6c2034c6679381de185cc567d80d38db79` (on `atech-release`).
- `php custom/grocy_AI/tests/run.php conversion-characterization` needs two separate checkouts at
  those commits and does not run here.
- Phase 4 module files are not yet mirrored to `atech-release`; stable byte parity for them needs the
  Phase 4 stable portable/adapter commits.

## Next Action

Settle the `GROCY_AI_MAINTAINER_AUTH_FILE` path with the user, then execute `04-10-PLAN.md`.
