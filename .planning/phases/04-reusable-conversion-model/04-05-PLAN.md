---
phase: 04-reusable-conversion-model
plan: "05"
type: execute
wave: 5
depends_on: [04-04]
files_modified:
  - custom/grocy_AI/src/GrocyAiApiController.php
  - custom/grocy_AI/routes.php
  - custom/grocy_AI/tests/conversions.php
  - custom/grocy_AI/tests/run.php
autonomous: true
requirements: [CONV-04, CONV-05]
must_haves:
  truths:
    - "A product editor can distinguish product override, approximate profile, universal default, unavailable, and blocked states beside native conversions."
    - "Package-derived facts can be reviewed but only the existing native Save action persists a product conversion."
  artifacts:
    - path: public/custom/grocy_AI/conversion-explanations.js
      provides: read-only product conversion provenance rendering
  key_links:
    - from: conversion-explanations.js
      to: /api/grocy-ai/products/{productId}/conversion-status
      via: authenticated inspection request
      pattern: conversion-status
---
<objective>
Surface product-level provenance in the native conversion area without changing any product-specific rows or persistence controls.

Purpose: make the inactive/active source distinction clear at the product decision point.
Output: bounded status read and a zero-write product-screen contract for the following UI plan.
</objective>
<execution_context>@/Users/ian/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/04-reusable-conversion-model/04-UI-SPEC.md
@.planning/phases/04-reusable-conversion-model/04-04-SUMMARY.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Expose a bounded permission-checked product conversion-status read</name>
  <files>custom/grocy_AI/src/GrocyAiApiController.php, custom/grocy_AI/routes.php, custom/grocy_AI/tests/conversions.php, custom/grocy_AI/tests/run.php</files>
  <behavior>
    - Test 1: valid product IDs return only closed status, source/version, usable precise factor, and bounded details.
    - Test 2: malformed/missing products and unauthorized sessions return bounded failures with no SQL, evidence, or household detail.
    - Test 3: the endpoint performs no bootstrap, activation, projection, taxonomy write, or product-conversion mutation.
  </behavior>
  <action>Add a MASTER_DATA_EDIT inspection route using the Plan 03 DTO. Validate IDs strictly and map exceptions to fixed responses. Per D-09/D-14, show provenance only; this endpoint cannot call Plan 08 activation or alter native/cache rows.</action>
  <verify><automated>php custom/grocy_AI/tests/run.php conversion-resolution && php -l custom/grocy_AI/src/GrocyAiApiController.php && php -l custom/grocy_AI/routes.php && php -l custom/grocy_AI/tests/conversions.php && php -l custom/grocy_AI/tests/run.php</automated></verify>
  <done>Product status is safely available as a read-only source for the native UI.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Prove product-status reads cannot become a reusable activation bypass</name>
  <files>custom/grocy_AI/tests/conversions.php, custom/grocy_AI/tests/run.php</files>
  <behavior>
    - Test 1: status requests made before and after a ruleset activation return the same closed read contract and never invoke `ActivateVerifiedRuleset`, a native universal write, selected projection adapter, or cache refresh.
    - Test 2: a blocked or inactive source exposes no usable factor; a valid product-scoped package/count POST and measured-density PUT remain governed by the native save hook rather than this endpoint.
  </behavior>
  <action>Add fixture snapshots and call-spy assertions around the product-status route. Per D-09/D-14, keep this route a pure inspection boundary: it cannot consume an inactive revision, update evidence, change an active source/version, or alter a native/cache row. Preserve the Plan 02/08 distinction that generic universal native POST/PUT are fail-closed, while product-scoped normal saves retain their independent authority.</action>
  <verify><automated>php custom/grocy_AI/tests/run.php conversion-resolution && php custom/grocy_AI/tests/run.php conversion-native-save-hook && php -l custom/grocy_AI/tests/conversions.php && php -l custom/grocy_AI/tests/run.php</automated></verify>
  <done>Fixture evidence proves product-status inspection remains non-mutating even after reusable activation.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| product page browser → status API | Product ID and browser state are untrusted. |
| package suggestion → native editor | Facts can be useful but must not create rows automatically. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-10 | Elevation of Privilege | status API | mitigate | Permission check, strict IDs, fixed DTO/errors. |
| T-04-11 | Tampering | package suggestion | mitigate | Review/prefill only; native Save is sole durable write. |
| T-04-SC | Tampering | package installs | accept | No package installation is planned. |
</threat_model>
<verification>Run the focused conversion-resolution contract suite and changed-PHP syntax checks.</verification>
<success_criteria>A bounded product-status DTO is available with no mutation or activation path; Plan 04-09 consumes it to render the product screen.</success_criteria>
<output>Create `.planning/phases/04-reusable-conversion-model/04-05-SUMMARY.md` when done</output>
