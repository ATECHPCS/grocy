---
phase: 04-reusable-conversion-model
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - custom/grocy_AI/tests/conversion-characterization.php
  - custom/grocy_AI/tests/conversions.php
  - custom/grocy_AI/tests/run.php
  - custom/grocy_AI/tests/fixtures/conversion-characterization-main.json
  - custom/grocy_AI/tests/fixtures/conversion-characterization-stable.json
  - .planning/phases/04-reusable-conversion-model/04-CHARACTERIZATION.md
autonomous: true
requirements: [CONV-08, CONV-09]
must_haves:
  truths:
    - "Maintainer has a deterministic, disposable report for both maintained branches before any reusable conversion projection is selected."
    - "The report compares cache and protected consumer outcomes without opening or writing household data."
  artifacts:
    - path: custom/grocy_AI/tests/conversion-characterization.php
      provides: dual-branch disposable SQLite characterization harness
    - path: .planning/phases/04-reusable-conversion-model/04-CHARACTERIZATION.md
      provides: evidence-backed selected projection contract or explicit blocking finding
  key_links:
    - from: custom/grocy_AI/tests/run.php
      to: custom/grocy_AI/tests/conversion-characterization.php
      via: conversion-characterization dispatch
      pattern: conversion-characterization
    - from: 04-CHARACTERIZATION.md
      to: later projection plans
      via: selected cache schema/trigger contract
      pattern: selected projection
---

## Phase Goal

**As a** household maintainer, **I want to** receive reusable, explainable conversions with one deterministic effective result, **so that** Grocy's established inventory behavior remains equivalent.

<objective>
Characterize the real resolved-cache contract in disposable fixtures on both maintained branches before choosing any reusable-rule projection.

Purpose: make the projection decision evidence-driven and preserve household data and protected Grocy consumers.
Output: a repeatable dual-branch harness and a checked-in characterization finding that later plans must follow.
</objective>

<execution_context>
@/Users/ian/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ian/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-reusable-conversion-model/04-CONTEXT.md
@.planning/phases/04-reusable-conversion-model/04-RESEARCH.md
@.planning/phases/04-reusable-conversion-model/04-VALIDATION.md
@.planning/phases/04-reusable-conversion-model/04-PATTERNS.md
@.planning/phases/03-food-taxonomy-categorization-pilot/03-03-SUMMARY.md

<interfaces>
From migrations/0225.sql: `cache__quantity_unit_conversions_resolved` is rebuilt by `quantity_unit_conversions` INSERT/UPDATE/DELETE triggers.
From services/StockService.php and services/RecipesService.php: protected paths query the cache by `product_id`, `from_qu_id`, and `to_qu_id`.
From custom/grocy_AI/tests/run.php: focused commands are added as conditional fixture dispatches and must not change existing Phase 1–3 behavior.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the failing disposable dual-branch characterization contract</name>
  <files>custom/grocy_AI/tests/conversion-characterization.php, custom/grocy_AI/tests/conversions.php, custom/grocy_AI/tests/run.php, custom/grocy_AI/tests/fixtures/conversion-characterization-main.json, custom/grocy_AI/tests/fixtures/conversion-characterization-stable.json</files>
  <read_first>migrations/0208.sql; migrations/0225.sql; services/StockService.php; services/RecipesService.php; custom/grocy_AI/tests/taxonomy.php; custom/grocy_AI/tests/run.php; .planning/phases/04-reusable-conversion-model/04-RESEARCH.md</read_first>
  <behavior>
    - Test 1: `conversion-characterization` creates only temporary SQLite databases outside `GROCY_DATAPATH`, snapshots `sqlite_master` cache/trigger definitions and baseline cache rows, then discards each fixture.
    - Test 2: it exercises one native default and one product override per branch and records row-key/factor/path deltas plus stock, recipe, purchase, consumption, price, transfer, meal-plan, and quantity-display fixture outputs.
    - Test 3: missing branch manifests, writes below a configured `GROCY_DATAPATH`, or unequal protected-output baselines fail closed with a redacted reason.
  </behavior>
  <action>Per D-12 and D-13, add a fixture-only dual-checkout contract before any projection code. Use explicit main/stable repository roots or immutable fixture manifests supplied by the harness; reject a production data path rather than falling back to it. Capture only schema, trigger, cache-key, factor/path, aggregate comparison, and query-plan metadata—never product names, barcodes, source URLs, secrets, or raw database dumps. Start with `EXPECTED_RED: conversion-characterization` until both branch contracts and comparison assertions are implemented; retain the raw structured manifest fixtures as deterministic, non-household test data.</action>
  <verify><automated>php custom/grocy_AI/tests/run.php conversion-characterization && php -l custom/grocy_AI/tests/conversion-characterization.php && php -l custom/grocy_AI/tests/conversions.php && php -l custom/grocy_AI/tests/run.php</automated></verify>
  <acceptance_criteria>The focused command exits nonzero before its contract exists, then exits zero only after it emits validated main/stable manifests from disposable databases and proves no configured data-path write occurred.</acceptance_criteria>
  <done>A deterministic characterization command covers both branches and all protected consumer categories without production-database access.</done>
</task>

<task type="auto">
  <name>Task 2: Record the evidence-bound projection decision and activation gate</name>
  <files>.planning/phases/04-reusable-conversion-model/04-CHARACTERIZATION.md</files>
  <read_first>custom/grocy_AI/tests/conversion-characterization.php; custom/grocy_AI/tests/fixtures/conversion-characterization-main.json; custom/grocy_AI/tests/fixtures/conversion-characterization-stable.json; .planning/phases/04-reusable-conversion-model/04-RESEARCH.md</read_first>
  <action>Run the disposable harness against both maintained branch checkouts and write the exact cache schema, trigger behavior, supported additive fields (if any), row ownership/key semantics, protected-consumer comparison, chosen smallest compatible projection, and immutable branch commit identifiers. If the reports disagree or any protected output changes, record the blocking evidence and state that reusable profiles/projection remain inactive; do not select an alternative by inference. This task implements D-12/D-13 and preserves the constraint that existing product-specific rows remain unchanged until Phase 6.</action>
  <verify><automated>php custom/grocy_AI/tests/run.php conversion-characterization && php -l custom/grocy_AI/tests/conversion-characterization.php && php -l custom/grocy_AI/tests/conversions.php && php -l custom/grocy_AI/tests/run.php && rg -n "main|stable|selected projection|inactive|stock|recipe|purchase|consumption|price|transfer|meal-plan|quantity" .planning/phases/04-reusable-conversion-model/04-CHARACTERIZATION.md</automated></verify>
  <acceptance_criteria>The finding names both branch revisions, gives the exact selected projection or a documented blocker, and demonstrates that no household database or existing product conversion row was written.</acceptance_criteria>
  <done>Later plans have a concrete, evidence-backed projection contract and a hard inactive gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| fixture harness → SQLite | Test code executes SQL that resembles Grocy cache maintenance. |
| main ↔ stable checkout | A projection is safe only if behavior matches both maintained branches. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-01 | Tampering | characterization harness | mitigate | Reject `GROCY_DATAPATH`, use temporary databases only, snapshot before/after, and delete fixtures after each run. |
| T-04-02 | Information disclosure | characterization manifests | mitigate | Persist only redacted schema/aggregate metadata; forbid household values and raw dumps. |
| T-04-03 | Tampering | branch projection choice | mitigate | Block later activation unless both immutable branch manifests and protected-output comparisons pass. |
| T-04-SC | Tampering | package installs | accept | No package installation is planned. |
</threat_model>

<verification>
Run `php custom/grocy_AI/tests/run.php conversion-characterization`; inspect the report for both branch revisions and a no-write assertion.
</verification>

<success_criteria>
The first Phase 4 deliverable is a repeatable, disposable dual-branch characterization gate—not a guessed cache projection.
</success_criteria>

<output>Create `.planning/phases/04-reusable-conversion-model/04-01-SUMMARY.md` when done</output>
