---
phase: 04-reusable-conversion-model
plan: "08"
type: execute
wave: 8
depends_on: [04-07]
files_modified:
  - custom/grocy_AI/src/GrocyAiConversionMigration.php
  - custom/grocy_AI/src/GrocyAiConversionService.php
  - custom/grocy_AI/tests/conversions.php
  - custom/grocy_AI/tests/run.php
  - custom/grocy_AI/tests/release-gate.sh
  - custom/grocy_AI/portable-files.txt
  - custom/grocy_AI/README.md
  - CUSTOMIZATIONS.md
autonomous: false
requirements: [CONV-02, CONV-03, CONV-05, CONV-08, CONV-09]
must_haves:
  truths:
    - "No reusable universal or profile rule becomes active unless current immutable evidence passes for both maintained branches and every protected consumer."
    - "After the gate passes, the selected native/cache projection has one source/version owner and preserves normal product-scoped save authority."
  artifacts:
    - path: custom/grocy_AI/src/GrocyAiConversionService.php
      provides: sole gate-protected ActivateVerifiedRuleset transaction
    - path: custom/grocy_AI/tests/release-gate.sh
      provides: portable/stable immutable-evidence and consumer-parity gate
  key_links:
    - from: ActivateVerifiedRuleset
      to: grocy_ai_conversion_activation_evidence
      via: immutable main/stable revisions and protected-output checks
      pattern: activation_evidence
    - from: ActivateVerifiedRuleset
      to: cache__quantity_unit_conversions_resolved
      via: exactly the Plan 01 selected branch adapter/trigger contract
      pattern: selected projection
---
<objective>
Make activation and release parity a dedicated final delivery after diagnostics, with one transaction as the only reusable native/cache write authority.

Purpose: prevent an ordinary universal native save or any report/UI path from changing the effective conversion cache before dual-branch evidence passes.
Output: immutable evidence ledger, sole activation/projection operation, portable/stable gate, and release-facing verification.
</objective>
<execution_context>@/Users/ian/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/04-reusable-conversion-model/04-CHARACTERIZATION.md
@.planning/phases/04-reusable-conversion-model/04-CONTEXT.md
@.planning/phases/04-reusable-conversion-model/04-VALIDATION.md
@.planning/phases/04-reusable-conversion-model/04-07-SUMMARY.md
<interfaces>
Ownership contract: `grocy_ai_conversion_rule_revisions` owns reusable universal/profile source, source version, precise factor, revision hash, and inactive/active lifecycle; native `quantity_unit_conversions` retains ownership only of normal product-scoped conversions and the gate-created universal projection rows.
Before this plan succeeds, `GenericEntityApiController` rejects reusable universal POST/PUT before native trigger/cache work. Product-scoped package/count and measured-density POST/PUT remain normal native saves.
`04-CHARACTERIZATION.md` supplies the immutable main/stable commits and the one selected branch adapter/cache contract; it is not recomputed or guessed by an HTTP request.
</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement the sole evidence-bound reusable activation and projection transaction</name>
  <files>custom/grocy_AI/src/GrocyAiConversionMigration.php, custom/grocy_AI/src/GrocyAiConversionService.php, custom/grocy_AI/tests/conversions.php, custom/grocy_AI/tests/run.php</files>
  <read_first>.planning/phases/04-reusable-conversion-model/04-CHARACTERIZATION.md; controllers/Api/GenericEntityApiController.php; custom/grocy_AI/src/GrocyAiConversionService.php</read_first>
  <behavior>
    - Test 1: missing, stale, altered, failed, or unequal immutable main/stable commit, cache/trigger contract, or stock/recipe/purchase/consumption/price/quantity-display/transfer/meal-plan proof returns inactive and leaves module records, native rows, and cache snapshots unchanged.
    - Test 2: only `ActivateVerifiedRuleset` may transition an eligible `grocy_ai_conversion_rule_revisions` record active; generic native universal POST/PUT remains rejected until this transaction commits.
    - Test 3: on complete proof, one transaction records immutable evidence, activates source/version-owned revisions, and applies exactly the Plan 01 selected projection: gate-created D-01 universal `quantity_unit_conversions` rows are written by the transaction and Grocy's characterized native triggers rebuild their cache rows; profile projection uses only the named selected branch adapter and its exact selected cache keys. No direct ad-hoc cache SQL, normal product-row modification, or Phase 6 cleanup is allowed.
    - Test 4: once active, a normal native universal edit is admitted only after current full graph validation and evidence-hash match; the pre-save hook records a new inactive revision and rejects the write if it would require a new projection without a new successful `ActivateVerifiedRuleset` transaction.
  </behavior>
  <action>Create idempotent module evidence/revision tables and `ActivateVerifiedRuleset()` as the sole operation allowed to write reusable native/cache projections. It must run in one transaction: reload the exact Plan 01 immutable main/stable revisions, characterization checksum, selected adapter identifier, cache key schema, and all protected-output proof; validate every candidate graph; persist the evidence hash/revision IDs; then activate source/version-owned module records and call only the selected adapter. Universal normal-screen edits are fail-closed/deferred as inactive revisions until this transaction has passed again. Do not let the controller, validation endpoints, coverage report, or UI create active reusable records. Preserve D-03 product-save authority and D-14 unchanged existing product rows; do not write/cache arbitrary product/package/count rows.
  </action>
  <verify><automated>php custom/grocy_AI/tests/run.php conversion-release-gate && php custom/grocy_AI/tests/run.php conversion-native-save-hook && php -l custom/grocy_AI/src/GrocyAiConversionMigration.php && php -l custom/grocy_AI/src/GrocyAiConversionService.php && php -l custom/grocy_AI/tests/conversions.php && php -l custom/grocy_AI/tests/run.php</automated></verify>
  <done>One evidence-bound transaction—and no earlier native save or inspection path—can activate reusable rules or produce their selected cache effects.</done>
</task>
<task type="auto">
  <name>Task 2: Enforce portable/stable release parity and document the activation authority</name>
  <files>custom/grocy_AI/tests/release-gate.sh, custom/grocy_AI/portable-files.txt, custom/grocy_AI/tests/conversions.php, custom/grocy_AI/tests/run.php, custom/grocy_AI/README.md, CUSTOMIZATIONS.md</files>
  <read_first>custom/grocy_AI/tests/check-portable-parity.sh; custom/grocy_AI/portable-files.txt; .planning/phases/04-reusable-conversion-model/04-CHARACTERIZATION.md</read_first>
  <action>Extend the SHA-pinned portable/stable release gate with every Phase 4 module artifact and the exact evidence ledger contract. Fail closed if either immutable branch revision, characterized trigger/cache adapter, activation evidence checksum, or protected-consumer comparison is absent/unequal; assert inactive rules create no cache rows. Document the one transaction ownership, native-trigger/cache behavior, stable-only adapter differences, and the Phase 6-only cleanup boundary in module docs/CUSTOMIZATIONS. Do not add a data migration, cleanup operation, or broad upstream churn.</action>
  <verify><automated>bash custom/grocy_AI/tests/release-gate.sh conversions && php custom/grocy_AI/tests/run.php conversion-release-gate && php custom/grocy_AI/tests/run.php && npm --prefix custom/grocy_AI/tests/browser test -- --grep @conv04 && php -l custom/grocy_AI/tests/conversions.php && php -l custom/grocy_AI/tests/run.php</automated></verify>
  <done>Release proof covers every portable artifact and makes the universal/profile activation authority and cache behavior auditable.</done>
</task>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm evidence-gated reusable activation and native product saves</name>
  <action>Pause only after every automated release-gate, protected-consumer, controller integration, and browser check passes; this verifies both maintained branch builds preserve the fail-closed reusable gate and normal product-scoped native save behavior.</action>
  <what-built>Evidence-gated reusable activation plus the native provenance/coverage surfaces.</what-built>
  <how-to-verify>1. On both maintained branches, sign in with MASTER_DATA_EDIT and open Conversion coverage. 2. Confirm incomplete or changed evidence shows inactive/blocker and cannot enable a universal save. 3. Confirm a valid product-scoped package/count and measured-density conversion still saves through the normal Grocy form. 4. After passing evidence, inspect universal, approximate, unavailable, and blocked states on product/resolved screens at desktop and 320px. 5. Confirm no existing product conversion row was replaced or removed.</how-to-verify>
  <resume-signal>Type "approved" or describe the issue.</resume-signal>
  <verify><automated>php custom/grocy_AI/tests/run.php conversion-release-gate && npm --prefix custom/grocy_AI/tests/browser test -- --grep @conv04</automated></verify>
  <done>Human verification confirms the fail-closed gate and normal product-scoped save behavior on both branches.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| immutable branch evidence → activation transaction | Stale or partial proof must never change effective cache state. |
| native universal edit → reusable projection | An ordinary native write must not bypass source/version or release evidence. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-18 | Tampering | activation gate | mitigate | Require immutable dual-branch evidence, checksum, exact selected adapter, and every protected-output proof in one transaction. |
| T-04-19 | Tampering | native universal Save | mitigate | Pre-save rejects/defer inactive revisions; only `ActivateVerifiedRuleset` creates/activates projection/cache effects. |
| T-04-SC | Tampering | package installs | accept | No package installation is planned. |
</threat_model>
<verification>Run release gate, full module suite, browser coverage, and all changed PHP syntax checks before the checkpoint.</verification>
<success_criteria>Reusable rules cannot change the resolved cache until current immutable dual-branch evidence passes; product-scoped native conversions preserve their normal Save behavior.</success_criteria>
<output>Create `.planning/phases/04-reusable-conversion-model/04-08-SUMMARY.md` when done</output>
