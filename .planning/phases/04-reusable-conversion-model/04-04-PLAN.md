---
phase: 04-reusable-conversion-model
plan: "04"
type: execute
wave: 4
depends_on: [04-03]
files_modified:
  - views/quantityunitconversionform.blade.php
  - public/viewjs/quantityunitconversionform.js
  - custom/grocy_AI/tests/browser/specs/conversions.spec.js
  - custom/grocy_AI/tests/browser/fixtures/quantityunitconversionform.html
  - custom/grocy_AI/tests/browser/support/server.mjs
autonomous: true
requirements: [CONV-02, CONV-04, CONV-06]
must_haves:
  truths:
    - "The normal conversion form explains that reusable universal saves remain unavailable until the immutable release gate passes."
    - "Product-scoped package/count and measured-density conversions continue through the unchanged native Save action."
  artifacts:
    - path: views/quantityunitconversionform.blade.php
      provides: native validation and inactive-gate status region
    - path: public/viewjs/quantityunitconversionform.js
      provides: revision-safe browser validation that retains native Save authority
  key_links:
    - from: public/viewjs/quantityunitconversionform.js
      to: /api/grocy-ai/conversions/validate
      via: current candidate validation before native POST/PUT
      pattern: conversions/validate
    - from: public/viewjs/quantityunitconversionform.js
      to: objects/quantity_unit_conversions
      via: unchanged native save call
      pattern: objects/quantity_unit_conversions
---

## Phase Goal

**As a** household maintainer, **I want to** see the current validation and activation state in Grocy's normal conversion editor, **so that** I never mistake an inactive reusable candidate for a saved effective rule.

<objective>
Add the browser/form slice after the server-side boundary exists, retaining the native persistence action and surfacing the fail-closed reusable activation rule.

Purpose: make the safe scope distinction comprehensible without introducing a parallel save path.
Output: accessible validation status, stale-response protection, and native-form browser coverage.
</objective>

<execution_context>@/Users/ian/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/04-reusable-conversion-model/04-CONTEXT.md
@.planning/phases/04-reusable-conversion-model/04-UI-SPEC.md
@.planning/phases/04-reusable-conversion-model/04-02-SUMMARY.md
<interfaces>
From Plan 02: `ValidateNativeConversionBeforeWrite` is the only pre-save authority; unactivated universal native writes are rejected before native/cache mutation, while valid product-scoped writes remain normal Grocy saves.
From `quantityunitconversionform.js`: preserve `#save-quconversion-button`, `Grocy.Components.UserfieldsForm.Save`, and the native `objects/quantity_unit_conversions` POST/PUT calls.
</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Render revision-safe reusable validation and activation status in the native form</name>
  <files>views/quantityunitconversionform.blade.php, public/viewjs/quantityunitconversionform.js, custom/grocy_AI/tests/browser/specs/conversions.spec.js, custom/grocy_AI/tests/browser/fixtures/quantityunitconversionform.html, custom/grocy_AI/tests/browser/support/server.mjs</files>
  <read_first>views/quantityunitconversionform.blade.php; public/viewjs/quantityunitconversionform.js; .planning/phases/04-reusable-conversion-model/04-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: changing a unit or factor invalidates earlier validation, keeps reusable Save unavailable until the current response is eligible and active, and cannot let a late response replace a newer state.
    - Test 2: inactive universal scope shows exact UI-SPEC inactive/gate copy with unit pair, precise factor, source/version, and conflict/cycle-free impact result when available; it never claims the rule is active.
    - Test 3: product-scoped package/count and measured-density candidates retain the native Save control and display the normal-scope explanation; reusable package/count or cross-dimension input shows bounded blocking copy.
  </behavior>
  <action>Per D-03/D-15, insert the UI-SPEC validation region after `#qu-conversion-inverse-info` and retain all existing form IDs, user-field save, modal/navigation, native error handling, and POST/PUT. Use the Plan 02 read-validation DTO and form revision token to provide incomplete, pending, active-ready, inactive-gate, blocked, and request-failure states with live-status/alert semantics, focus recovery, 44px controls, and non-color indicators. The universal state is disabled before Plan 08 gate proof; do not stage or send a custom write. Product-scoped native package/count/measured-density Save remains enabled after ordinary validation because it is not a reusable activation.
  </action>
  <verify><automated>npm --prefix custom/grocy_AI/tests/browser test -- --grep @conv04 && php -l views/quantityunitconversionform.blade.php && node --check public/viewjs/quantityunitconversionform.js</automated></verify>
  <done>Browser fixtures prove exact state copy, stale-response safety, and preserved native Save behavior for product scope.</done>
</task>
<task type="auto">
  <name>Task 2: Prove the browser invokes only the native authority after current validation</name>
  <files>public/viewjs/quantityunitconversionform.js, custom/grocy_AI/tests/browser/specs/conversions.spec.js, custom/grocy_AI/tests/browser/fixtures/quantityunitconversionform.html, custom/grocy_AI/tests/browser/support/server.mjs</files>
  <read_first>public/viewjs/quantityunitconversionform.js; custom/grocy_AI/tests/browser/specs/conversions.spec.js</read_first>
  <action>Add fixture and browser assertions that successful product-scope validation invokes exactly the existing native POST/PUT URL once, while inactive/blocked reusable states invoke no write URL. Assert the form preserves entered values after the API reports a server-side rejection and does not manufacture a source/version or cache status in the browser. Do not replace Grocy's existing native save handler or add a conversion persistence route.</action>
  <verify><automated>npm --prefix custom/grocy_AI/tests/browser test -- --grep @conv04 && node --check public/viewjs/quantityunitconversionform.js</automated></verify>
  <done>Form coverage proves client behavior cannot bypass or duplicate the server-side scope boundary.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser candidate → read validation | Unit IDs, factor, and stale responses are untrusted. |
| browser → native Save | Browser status must not create a new mutation authority. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-06B | Tampering | form state | mitigate | Current-request revision tokens, exact bounded DTO handling, and server revalidation at Plan 02's controller hook. |
| T-04-06C | Elevation of Privilege | native save UI | mitigate | Retain permission-checked native POST/PUT only; browser never calls activation/projection. |
| T-04-SC | Tampering | package installs | accept | No package installation is planned. |
</threat_model>
<verification>Run focused `@conv04` browser coverage plus the Blade PHP and JS syntax checks.</verification>
<success_criteria>The native editor clearly distinguishes inactive reusable rules from normal product-scoped saves, without any browser-side mutation bypass.</success_criteria>
<output>Create `.planning/phases/04-reusable-conversion-model/04-04-SUMMARY.md` when done</output>
