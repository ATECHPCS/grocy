---
phase: 04-reusable-conversion-model
plan: "04"
type: execute
wave: 4
depends_on: [04-03]
files_modified:
  - custom/grocy_AI/src/GrocyAiApiController.php
  - custom/grocy_AI/routes.php
  - views/productform.blade.php
  - public/custom/grocy_AI/conversion-explanations.js
  - public/custom/grocy_AI/grocy-ai.css
  - public/custom/grocy_AI/conversion-explanations.test.js
  - custom/grocy_AI/tests/browser/specs/conversions.spec.js
  - custom/grocy_AI/tests/browser/fixtures/productform.html
  - custom/grocy_AI/tests/browser/support/server.mjs
autonomous: true
requirements: [CONV-04, CONV-05]
must_haves:
  truths:
    - "A product editor visibly distinguishes product override, approximate profile, universal default, unavailable, and blocked outcomes."
    - "Package-derived data may be proposed but only normal native Save can persist a product conversion."
  artifacts:
    - path: public/custom/grocy_AI/conversion-explanations.js
      provides: safe read-only product conversion provenance rendering
  key_links:
    - from: conversion-explanations.js
      to: /api/grocy-ai/products/{productId}/conversion-status
      via: authenticated read request
      pattern: conversion-status
---

## Phase Goal

**As a** product editor, **I want to** see why a conversion applies or is unavailable beside native product conversions, **so that** I can consciously use an override or an approximate estimate.

<objective>
Surface product-level effective source/provenance in the native product conversion area without changing its existing rows or persistence controls.

Purpose: make deterministic resolution understandable at the decision point.
Output: permission-checked status read, responsive status line, and browser zero-write tests.
</objective>
<execution_context>@/Users/ian/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/04-reusable-conversion-model/04-UI-SPEC.md
@.planning/phases/04-reusable-conversion-model/04-PATTERNS.md
@.planning/phases/04-reusable-conversion-model/04-03-SUMMARY.md
<interfaces>
From `views/productform.blade.php`: retain the existing Product specific QU conversions table, Add action, and resolved-view link.
From Plan 03 resolver DTO: status is Exact, Approximate, Unavailable, or Blocked with source/version and bounded provenance detail.
</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Expose a bounded, permission-checked product conversion-status read</name>
  <files>custom/grocy_AI/src/GrocyAiApiController.php, custom/grocy_AI/routes.php</files>
  <read_first>custom/grocy_AI/src/GrocyAiApiController.php; custom/grocy_AI/routes.php; custom/grocy_AI/src/GrocyAiConversionService.php</read_first>
  <behavior>
    - Test 1: valid product IDs return only closed resolution status, source/version, precise factor when usable, and bounded details.
    - Test 2: malformed/missing products and unauthorized sessions receive bounded failures with no SQL, provider evidence, or household details.
    - Test 3: the status endpoint performs no write, bootstrap activation, profile projection, or modification of product conversion rows.
  </behavior>
  <action>Add an authenticated `MASTER_DATA_EDIT` read route matching the existing module group and delegate to the Plan 03 DTO. Validate path IDs strictly and map expected errors to fixed messages/statuses. This read endpoint must not call a write-capable bootstrap/activation path, create a conversion proposal, modify taxonomy, or alter product rows. Per D-09 and D-14, expose provenance for inspection only.</action>
  <verify><automated>php custom/grocy_AI/tests/run.php conversion-resolution && php -l custom/grocy_AI/src/GrocyAiApiController.php</automated></verify>
  <acceptance_criteria>The API returns deterministic status/provenance for an eligible product and explicit unavailable/blocked outcomes without exposing or changing household records.</acceptance_criteria>
  <done>Product-status data is safely available to the native UI.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Render accessible read-only source status beside native product conversions</name>
  <files>views/productform.blade.php, public/custom/grocy_AI/conversion-explanations.js, public/custom/grocy_AI/conversion-explanations.test.js, public/custom/grocy_AI/grocy-ai.css, custom/grocy_AI/tests/browser/specs/conversions.spec.js, custom/grocy_AI/tests/browser/fixtures/productform.html, custom/grocy_AI/tests/browser/support/server.mjs</files>
  <read_first>views/productform.blade.php; public/custom/grocy_AI/product-taxonomy.js; public/custom/grocy_AI/grocy-ai.css; custom/grocy_AI/tests/browser/specs/taxonomy.spec.js; .planning/phases/04-reusable-conversion-model/04-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: the product area displays concise source plus Exact/Approximate/Unavailable/Blocked text; only a disclosure shows full precision/provenance.
    - Test 2: no classification means exact no-estimate copy and no factor; approximate has non-color text/icon; blocked has no usable factor.
    - Test 3: product status requests and package-derived proposals issue zero writes; existing native Add/Save controls remain the only persistence path.
    - Test 4: a fast DOM fixture/unit suite verifies exact unavailable/inactive copy, safe text-node rendering, and stale status responses cannot replace the newer state; Playwright remains the browser E2E proof.
  </behavior>
  <action>Insert a read-only status line before the native product-conversion table and load it with a guarded IIFE patterned after product taxonomy. Implement the UI-SPEC exact unavailable/override copy, disclosure semantics, 44px targets, focus outline, night-mode/reduced-motion support, and stale-request protection. Per D-04, a verified package data proposal may prefill/review only within the native product conversion flow and requires explicit normal Save; never auto-create it. Per D-14, do not add cleanup, replacement, removal, confirmation, or bulk action for existing rows.</action>
  <verify><automated>node --test public/custom/grocy_AI/conversion-explanations.test.js && npm --prefix custom/grocy_AI/tests/browser test -- --grep @conv04 && node --check public/custom/grocy_AI/conversion-explanations.js</automated></verify>
  <acceptance_criteria>Browser fixtures prove all status states are understandable and accessible on mobile, while all non-native-save paths make zero writes and retain existing product conversion controls.</acceptance_criteria>
  <done>The product editor explains the current effective source without taking persistence authority from Grocy.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| product page browser → status API | Product ID and browser state are untrusted. |
| package suggestion → native editor | Package facts can be useful but must not become automatic rows. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-10 | Elevation of Privilege | product status API | mitigate | Enforce Grocy session and `MASTER_DATA_EDIT`, strict ID validation, bounded error DTOs. |
| T-04-11 | Tampering | package-derived proposal | mitigate | Permit only verified data as a review/prefill; retain normal native Save as the sole durable write. |
| T-04-12 | Information Disclosure | browser rendering | mitigate | Render fixed-key DTO fields as text nodes; no raw evidence, source URLs, or exception text. |
| T-04-SC | Tampering | package installs | accept | No package installation is planned. |
</threat_model>
<verification>Run focused `@conv04` browser tests and JS syntax validation.</verification>
<success_criteria>The product conversion screen makes source/uncertainty clear without automatic writes or Phase 6 cleanup behavior.</success_criteria>
<output>Create `.planning/phases/04-reusable-conversion-model/04-04-SUMMARY.md` when done</output>
