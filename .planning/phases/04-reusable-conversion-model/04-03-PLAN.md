---
phase: 04-reusable-conversion-model
plan: "03"
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - custom/grocy_AI/src/GrocyAiConversionMigration.php
  - custom/grocy_AI/src/GrocyAiConversionService.php
  - custom/grocy_AI/tests/conversions.php
  - custom/grocy_AI/tests/run.php
autonomous: true
requirements: [CONV-03, CONV-05, CONV-06]
must_haves:
  truths:
    - "D-05: An explicitly classified product can inspect a visibly approximate, small sourced profile set."
    - "D-06: An unprofiled food type states that no estimate is available rather than guessing."
    - "D-07: Every eligible profile exposes its named source and source version with its factor."
    - "D-08: Only explicit Phase 3 taxonomy assignment can make a food-type profile eligible; provider or product-group evidence cannot activate it."
    - "D-11: An approved sourced profile may offer a clearly labeled estimate, while unclassified or unprofiled products receive no estimate."
    - "D-09: Resolution identifies one winner by product override > food-type profile > universal and provides concise provenance."
    - "D-10: Conflicting effective paths block all usable factors and identify correction-safe blockers."
  artifacts:
    - path: custom/grocy_AI/src/GrocyAiConversionService.php
      provides: inactive profile eligibility, precedence, provenance, and conflict DTOs
  key_links:
    - from: GrocyAiConversionService
      to: grocy_ai_taxonomy_classifications
      via: current explicit non-null leaf lookup only
      pattern: grocy_ai_taxonomy_classifications
---
<objective>
Add narrow sourced profiles and deterministic inspection resolution while preserving the Plan 08-only activation boundary.

Purpose: make approximate density estimates explainable without inference or premature cache changes.
Output: inactive profile catalog, explicit-taxonomy guard, and conflict-free resolver tests.
</objective>
<execution_context>@/Users/ian/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/04-reusable-conversion-model/04-CHARACTERIZATION.md
@.planning/phases/04-reusable-conversion-model/04-CONTEXT.md
@.planning/phases/04-reusable-conversion-model/04-RESEARCH.md
@.planning/phases/03-food-taxonomy-categorization-pilot/03-03-SUMMARY.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Seed inactive sourced approximate profiles behind explicit taxonomy assignment</name>
  <files>custom/grocy_AI/src/GrocyAiConversionMigration.php, custom/grocy_AI/src/GrocyAiConversionService.php, custom/grocy_AI/tests/conversions.php, custom/grocy_AI/tests/run.php</files>
  <behavior>
    - Test 1: each selected starter profile has a closed taxonomy leaf, finite positive factor, approximate status, named USDA FDC source, FDC item ID, and release/version.
    - Test 2: provider evidence, product-group evidence, absent/Unclassified/stale assignment, excluded leaf, or absent profile returns `Unavailable` with no factor.
    - Test 3: profile rows are inactive and no profile touches taxonomy, products, existing conversion rows, native cache, or projection before Plan 08.
  </behavior>
  <action>Per D-05: seed a small source-controlled profile set and mark every result approximate. Per D-06: start with no more than three calibrated common-liquid/simple-ingredient profiles and return unavailable rather than guess for unprofiled food types. Per D-07: record each reviewed FDC record/portion calculation as the profile's named source and source version. Per D-08: read only `grocy_ai_taxonomy_classifications` current-leaf data; evidence helpers and product-group evidence are prohibited activation inputs. Per D-11: return a closed unavailable DTO without a factor whenever eligibility or the later activation gate is absent. Module records its own inactive lifecycle state, while no native Grocy row is created in this plan.</action>
  <verify><automated>php custom/grocy_AI/tests/run.php conversion-resolution && php -l custom/grocy_AI/src/GrocyAiConversionMigration.php && php -l custom/grocy_AI/src/GrocyAiConversionService.php && php -l custom/grocy_AI/tests/conversions.php && php -l custom/grocy_AI/tests/run.php</automated></verify>
  <done>Profiles are narrow, source/versioned, approximate, taxonomy-explicit, and inert pending Plan 08.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Resolve one inspection winner or block every ambiguous effective graph</name>
  <files>custom/grocy_AI/src/GrocyAiConversionService.php, custom/grocy_AI/tests/conversions.php, custom/grocy_AI/tests/run.php</files>
  <behavior>
    - Test 1: precedence is exactly product override > food-type profile > universal and inspection DTOs identify winning source/version/status.
    - Test 2: same-rank collisions, competing paths, cycles, reciprocal inconsistency, dimension mismatch, and tolerance drift return bounded blockers and no factor.
    - Test 3: inspection results remain inactive and cannot write a native row/cache or change product-specific rows.
  </behavior>
  <action>Implement complete candidate-graph validation and explicit ranking rather than relying on SQLite row order or Grocy recursive first-path behavior. Per D-09: return a single winner with its concise explanation only when eligible; per D-10: otherwise emit correction-safe identifiers and withhold the factor. Maintain D-14: existing product overrides are read as higher-precedence native rows but are neither modified nor cleaned up. Plan 08 alone may create/activate the characterization-selected native/cache projection.</action>
  <verify><automated>php custom/grocy_AI/tests/run.php conversion-resolution && php -l custom/grocy_AI/src/GrocyAiConversionService.php && php -l custom/grocy_AI/tests/conversions.php && php -l custom/grocy_AI/tests/run.php</automated></verify>
  <done>The backend exposes deterministic inactive provenance or a safe blocker, never a premature effective conversion.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| taxonomy assignment → profile eligibility | Only explicit classification can offer an estimate. |
| profile graph → inspection result | A wrong winner could later affect inventory consumers. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-07 | Tampering | profile eligibility | mitigate | Read only explicit current taxonomy assignment; evidence and groups cannot activate profiles. |
| T-04-08 | Tampering | effective resolver | mitigate | Rank all paths; validate cycles, reciprocal consistency, dimensions, and tolerance; block ambiguity. |
| T-04-SC | Tampering | package installs | accept | No package installation is planned. |
</threat_model>
<verification>Run conversion-resolution and lint every changed PHP file.</verification>
<success_criteria>Only explicit taxonomy assignments can inspect narrow sourced estimates, and no approximate or universal result activates before Plan 08.</success_criteria>
<output>Create `.planning/phases/04-reusable-conversion-model/04-03-SUMMARY.md` when done</output>
