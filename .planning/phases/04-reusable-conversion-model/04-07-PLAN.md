---
phase: 04-reusable-conversion-model
plan: "07"
type: execute
wave: 8
depends_on: [04-06]
files_modified:
  - custom/grocy_AI/src/GrocyAiConversionController.php
  - custom/grocy_AI/src/GrocyAiConversionService.php
  - custom/grocy_AI/routes.php
  - views/grocyai_conversioncoverage.blade.php
  - public/custom/grocy_AI/conversion-coverage.js
  - public/custom/grocy_AI/conversion-coverage.test.js
  - custom/grocy_AI/bin/validate-conversion-rules.php
  - custom/grocy_AI/tests/conversions.php
  - custom/grocy_AI/tests/run.php
autonomous: true
requirements: [CONV-07]
must_haves:
  truths:
    - "D-14: A maintainer can inspect a redacted, read-only report of coverage, missing paths, source, conflicts, cycles, and redundant product overrides without cleanup or removal."
    - "D-12: Diagnostics show inactive or blocked until dual-branch release evidence succeeds, and cannot activate or project rules."
  artifacts:
    - path: views/grocyai_conversioncoverage.blade.php
      provides: MASTER_DATA_EDIT read-only conversion coverage page
    - path: custom/grocy_AI/bin/validate-conversion-rules.php
      provides: bootstrap-disabled configured-database diagnostic report
  key_links:
    - from: GrocyAiConversionController
      to: GrocyAiConversionService
      via: read-only coverage DTO
      pattern: ConversionCoverage
---
<objective>
Deliver the maintainer-facing coverage diagnostics independently of release activation.

Purpose: expose rule health and missing-path evidence without creating a release or mutation path.
Output: permission-checked page/API, redacted CLI, and request-sequenced diagnostics.
</objective>
<execution_context>@/Users/ian/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/04-reusable-conversion-model/04-CHARACTERIZATION.md
@.planning/phases/04-reusable-conversion-model/04-UI-SPEC.md
@.planning/phases/04-reusable-conversion-model/04-VALIDATION.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build read-only conversion coverage and configured-database diagnostics</name>
  <files>custom/grocy_AI/src/GrocyAiConversionController.php, custom/grocy_AI/src/GrocyAiConversionService.php, custom/grocy_AI/routes.php, views/grocyai_conversioncoverage.blade.php, public/custom/grocy_AI/conversion-coverage.js, public/custom/grocy_AI/conversion-coverage.test.js, custom/grocy_AI/bin/validate-conversion-rules.php, custom/grocy_AI/tests/conversions.php, custom/grocy_AI/tests/run.php</files>
  <behavior>
    - Test 1: coverage reads expose aggregate rule/gate/coverage/unavailable/redundancy/blocker counts and closed categories only.
    - Test 2: missing, stale, failed, or incomplete branch evidence displays inactive/blocked and cannot invoke activation, projection, bootstrap writes, or product-row changes.
    - Test 3: CLI rejects missing/relative paths, opens the configured DB read-only with bootstrap disabled, and snapshots prove no writes or household-value output.
    - Test 4: fixture DOM tests prove exact inactive/ready/blocker copy and last-response-wins sequencing before Playwright coverage.
  </behavior>
  <action>Create a MASTER_DATA_EDIT controller/page and refresh API that call a bootstrap-disabled read-only coverage DTO. Render the UI-SPEC coverage title, source/gate summaries, blockers, missing paths, redundant product overrides, provenance, and compatibility-evidence status. Implement the CLI from Phase 3's read-only pattern. Per D-12: neither browser nor CLI diagnostics may bootstrap schema, project, or activate rules; per D-14: neither may mutate or remove existing product-specific conversions. Current evidence is reported only. Keep release and activation logic exclusively in Plan 08.</action>
  <verify><automated>php custom/grocy_AI/tests/run.php conversion-coverage && php custom/grocy_AI/tests/run.php conversion-readonly-cli && node --test public/custom/grocy_AI/conversion-coverage.test.js && php -l custom/grocy_AI/src/GrocyAiConversionController.php && php -l custom/grocy_AI/src/GrocyAiConversionService.php && php -l custom/grocy_AI/routes.php && php -l views/grocyai_conversioncoverage.blade.php && php -l custom/grocy_AI/bin/validate-conversion-rules.php && php -l custom/grocy_AI/tests/conversions.php && php -l custom/grocy_AI/tests/run.php && node --check public/custom/grocy_AI/conversion-coverage.js</automated></verify>
  <done>Maintainers can inspect all CONV-07 diagnostics through safe read-only surfaces, with no possible activation side effect.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| configured household database → CLI | Diagnostics must not expose or mutate household data. |
| coverage request → report DTO | Report state must not become an activation command. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-16 | Information Disclosure | report/CLI | mitigate | Aggregate redacted DTO/JSON only; no names, barcodes, URLs, SQL, or exception text. |
| T-04-17 | Tampering | coverage surfaces | mitigate | Permission checks, read-only DB connection/bootstrap disabled, and fixture before/after snapshots. |
| T-04-SC | Tampering | package installs | accept | No package installation is planned. |
</threat_model>
<verification>Run focused coverage/CLI checks, all listed PHP/Blade lint commands, and fast DOM coverage.</verification>
<success_criteria>Coverage diagnostics are observably read-only and never substitute for the immutable release/activation gate.</success_criteria>
<output>Create `.planning/phases/04-reusable-conversion-model/04-07-SUMMARY.md` when done</output>
