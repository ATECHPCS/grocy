---
phase: 05-bulk-maintenance-recovery-engine
plan: "02"
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - custom/grocy_AI/src/GrocyAiBulkMigration.php
  - custom/grocy_AI/routes.php
  - custom/grocy_AI/tests/bulk.php
  - custom/grocy_AI/tests/run.php
autonomous: true
requirements: [BULK-01, BULK-02, BULK-08]
must_haves:
  truths:
    - "D-02: The plan-item schema records object identity, immutable before-image, proposed value, reason, provenance, and ruleset version at generation time."
    - "D-10: The append-only audit ledger schema records actor, previewed/applied timestamps, module/version, per-item outcome, and exact before/after values."
    - "D-14/D-15: Creating the module tables changes no native Grocy table, adds no parallel write path, and stays namespaced and idempotent."
  artifacts:
    - path: custom/grocy_AI/src/GrocyAiBulkMigration.php
      provides: idempotent namespaced bootstrap for grocy_ai_bulk_plans, grocy_ai_bulk_plan_items, and the append-only grocy_ai_bulk_audit ledger
    - path: custom/grocy_AI/routes.php
      provides: require_once wiring for the new migration ahead of the bulk service
  key_links:
    - from: custom/grocy_AI/routes.php
      to: GrocyAI\Services\GrocyAiBulkMigration
      via: require_once of src/GrocyAiBulkMigration.php in module bootstrap order
      pattern: GrocyAiBulkMigration
    - from: custom/grocy_AI/tests/bulk.php
      to: GrocyAiBulkMigration::Bootstrap
      via: bulk-schema dispatch asserting idempotent creation and no native-table change
      pattern: bulk-schema
---

## Phase Goal

**As a** household maintainer, **I want to** have durable storage for bulk plans, items, and audit records that never touches native Grocy tables, **so that** the engine can record what it proposes and applies without becoming a parallel write authority.

<objective>
Add the inactive, namespaced bulk schema and its idempotent migration.

Purpose: create `grocy_ai_bulk_plans`, `grocy_ai_bulk_plan_items`, and the append-only `grocy_ai_bulk_audit` ledger with every field D-02 and D-10 require, without colliding with existing or fixture-spy tables and without modifying any native table.
Output: `GrocyAiBulkMigration` wired into the module bootstrap, proven idempotent and native-safe by a `bulk-schema` test that turns the relevant Plan 05-01 RED assertions green.
</objective>

<execution_context>@/Users/ian/.codex/get-shit-done/workflows/execute-plan.md</execution_context>

<context>
@.planning/phases/05-bulk-maintenance-recovery-engine/05-CONTEXT.md
@.planning/REQUIREMENTS.md
@custom/grocy_AI/src/GrocyAiConversionMigration.php
@custom/grocy_AI/src/GrocyAiTaxonomyMigration.php
@MISTAKES.md
<interfaces>
From `GrocyAiConversionMigration::Bootstrap(PDO)`: the module bootstrap idiom is a `!inTransaction()` guard, a `grocy_ai_<area>_migrations (version, applied_at)` table, `CREATE TABLE IF NOT EXISTS`/`CREATE INDEX IF NOT EXISTS`, and `INSERT OR IGNORE` of the `VERSION` const inside one transaction with rollback on throw.
From `custom/grocy_AI/routes.php`: services are wired by ordered `require_once __DIR__ . '/src/<Class>.php'` before the controller group; the new migration must be required before the bulk service (Plan 05-03).
From `MISTAKES.md`: before adding any `grocy_ai_bulk_*` table, grep `custom/grocy_AI/tests/conversions.php` and `custom/grocy_AI/tests/taxonomy.php` for a same-named placeholder/spy table (earlier plans plant `CREATE TABLE` spies of the same name); adding a real table of a name already spy-created breaks those fixtures with "table already exists".
Shared schema contract: `grocy_ai_bulk_plans(id, created_at, created_by, ruleset_version, operation_type, scope_json, counts_json, checksum, status, module_version)`; `grocy_ai_bulk_plan_items(id, plan_id, seq, object_type, object_id, operation, before_image_json, proposed_value_json, reason, provenance, selected, outcome, applied_at)`; `grocy_ai_bulk_audit(id, plan_id, plan_item_id, actor, event, event_at, module_version, before_json, after_json, outcome)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the idempotent namespaced bulk migration</name>
  <files>custom/grocy_AI/src/GrocyAiBulkMigration.php, custom/grocy_AI/tests/bulk.php, custom/grocy_AI/tests/run.php</files>
  <read_first>custom/grocy_AI/src/GrocyAiConversionMigration.php; custom/grocy_AI/src/GrocyAiTaxonomyMigration.php; custom/grocy_AI/tests/conversions.php; custom/grocy_AI/tests/taxonomy.php; MISTAKES.md</read_first>
  <behavior>
    - Test 1: `bulk-schema` runs `GrocyAiBulkMigration::Bootstrap($pdo)` twice on a fresh in-memory SQLite database and both runs succeed (idempotent), leaving exactly `grocy_ai_bulk_plans`, `grocy_ai_bulk_plan_items`, `grocy_ai_bulk_audit`, and a `grocy_ai_bulk_migrations` version row.
    - Test 2: each table exposes exactly the shared-contract columns (D-02 for plan items, D-10 for audit, plus the plan header columns), `grocy_ai_bulk_audit` is append-only (no UPDATE/DELETE path in the migration), and `grocy_ai_bulk_plan_items.plan_id` foreign-keys `grocy_ai_bulk_plans(id)`.
    - Test 3: bootstrapping alongside a fixture that also defines native tables (`products`, `quantity_unit_conversions`) creates no native table, drops no native table, and leaves `cache__quantity_unit_conversions_resolved` (when present) row-identical before and after.
  </behavior>
  <action>Create `GrocyAI\Services\GrocyAiBulkMigration` with `public const VERSION = 'v1'` and a `Bootstrap(PDO)` copying the `GrocyAiConversionMigration::Bootstrap` idiom exactly (transaction guard, `grocy_ai_bulk_migrations` table, `CREATE TABLE IF NOT EXISTS`, `INSERT OR IGNORE`). Define the three tables with the shared-contract columns; `grocy_ai_bulk_plans.status` and `grocy_ai_bulk_plan_items.outcome`/`selected` use SQLite `CHECK` constraints matching the closed vocabularies fixed in Plan 05-01; `module_version` is stored per row (source: `module-version.json`) so audit records are version-attributable per D-10. Add a `bulk-schema` dispatch to `run.php` and the assertions to `bulk.php`. Per MISTAKES.md, first grep `conversions.php`/`taxonomy.php` for any `grocy_ai_bulk_*` spy table; there is none today, but re-verify and record it. Create no native table and no trigger.</action>
  <verify><automated>grep -n "grocy_ai_bulk" custom/grocy_AI/tests/conversions.php custom/grocy_AI/tests/taxonomy.php; php8.5 custom/grocy_AI/tests/run.php bulk-schema && php8.5 -l custom/grocy_AI/src/GrocyAiBulkMigration.php && php8.5 -l custom/grocy_AI/tests/bulk.php</automated></verify>
  <acceptance_criteria>`bulk-schema` passes green, the grep finds no pre-existing `grocy_ai_bulk_*` spy table, double-bootstrap is idempotent, all three tables carry the exact shared-contract columns, and no native table or cache row changes.</acceptance_criteria>
  <done>The module owns an idempotent, native-safe, append-only bulk schema with every field D-02 and D-10 require.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the migration into the module bootstrap order</name>
  <files>custom/grocy_AI/routes.php, custom/grocy_AI/tests/bulk.php, custom/grocy_AI/tests/run.php</files>
  <read_first>custom/grocy_AI/routes.php; custom/grocy_AI/src/GrocyAiConversionMigration.php</read_first>
  <behavior>
    - Test 1: `routes.php` `require_once`s `src/GrocyAiBulkMigration.php` before the point where the bulk service (Plan 05-03) will be required, and the file remains PHP-parseable.
    - Test 2: requiring `routes.php` symbols does not eagerly create tables or issue writes — bootstrap remains explicit via `Bootstrap()`, matching the conversion/taxonomy wiring.
  </behavior>
  <action>Add `require_once __DIR__ . '/src/GrocyAiBulkMigration.php';` to `custom/grocy_AI/routes.php` in the existing ordered `require_once` block, immediately after the conversion service require and before the controller requires, so Plan 05-03's `GrocyAiBulkService` can depend on it. Do not add any route yet (endpoints are wired in Plan 05-11). Extend `bulk.php`/`run.php` only with the parse/ordering assertion needed to prove the wiring. Leave `portable-files.txt` and the release gate to Plan 05-11 per the breakdown.</action>
  <verify><automated>php8.5 -l custom/grocy_AI/routes.php && php8.5 custom/grocy_AI/tests/run.php bulk-schema</automated></verify>
  <acceptance_criteria>`routes.php` lints clean, requires the migration in the correct order, and the `bulk-schema` suite still passes.</acceptance_criteria>
  <done>The migration is wired into module bootstrap without adding a route or a parallel write path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| module migration → native database | New tables must never shadow, alter, or drop a native Grocy table or trigger. |
| test fixtures → module schema | Earlier fixture spy tables must not collide with the new namespaced tables. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-04 | Tampering | bulk migration | mitigate | Namespaced `CREATE TABLE IF NOT EXISTS` only, inside one guarded transaction; no native table or trigger touched. |
| T-05-05 | Repudiation | grocy_ai_bulk_audit | mitigate | Append-only ledger shape with actor, timestamps, module/version, and before/after; no UPDATE/DELETE path. |
| T-05-06 | Tampering | fixture-name collision | mitigate | Grep `conversions.php`/`taxonomy.php` for same-named spy tables before adding any `grocy_ai_bulk_*` table (MISTAKES.md). |
| T-05-SC | Tampering | package installs | accept | No package installation is planned. |
</threat_model>

<verification>
Run `php8.5 custom/grocy_AI/tests/run.php bulk-schema` (green), `php8.5 -l` on the migration and `routes.php`, and confirm the grep finds no pre-existing `grocy_ai_bulk_*` spy table. Re-run the Phase 1–4 suites (`grocy_AI` default, `conversion-*`, `taxonomy-*`) to prove no regression.
</verification>

<success_criteria>
The module gains an inactive, idempotent, append-only bulk schema carrying every plan/plan-item/audit field the engine needs, wired into bootstrap, with zero native-table or cache impact.
</success_criteria>

<output>Create .planning/phases/05-bulk-maintenance-recovery-engine/05-02-SUMMARY.md when done</output>
</content>
