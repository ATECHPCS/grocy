---
phase: 05-bulk-maintenance-recovery-engine
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - custom/grocy_AI/tests/bulk.php
  - custom/grocy_AI/tests/run.php
  - custom/grocy_AI/tests/fixtures/bulk-plan-cases.json
  - custom/grocy_AI/tests/fixtures/bulk-registry-cases.json
autonomous: true
requirements: [BULK-01, BULK-02, BULK-03, BULK-04, BULK-05, BULK-06, BULK-07, BULK-08, BULK-09, BULK-10]
must_haves:
  truths:
    - "D-01/D-02: A failing (RED) suite fixes the plan DTO, plan-item DTO, and plan-checksum contract before any production bulk code exists."
    - "D-05/D-06: The suite fixes the typed-operation-registry shape and the closed named-operation vocabulary (assign_taxonomy_leaf / set_unclassified)."
    - "D-07/D-08/D-09/D-10/D-11/D-12: The suite fixes the closed blocker/outcome vocabulary and asserts zero-write, immutable-before-image, named-operation-only, optimistic-concurrency, single-transaction, idempotency, audit, rollback-refusal, and non-authoritative-export claims."
    - "No production file changes; the plan stops at the intentional RED gate (mirrors 02-01/04-01)."
  artifacts:
    - path: custom/grocy_AI/tests/bulk.php
      provides: RED bulk-engine contract suite fixing every Phase 5 DTO, registry, checksum, and vocabulary shape
    - path: custom/grocy_AI/tests/fixtures/bulk-plan-cases.json
      provides: deterministic non-household plan/plan-item/audit fixture cases
  key_links:
    - from: custom/grocy_AI/tests/run.php
      to: custom/grocy_AI/tests/bulk.php
      via: bulk-contract dispatch case
      pattern: bulk-contract
    - from: custom/grocy_AI/tests/bulk.php
      to: GrocyAI\Services\GrocyAiBulkService (Plan 05-03/05-04) and GrocyAiBulkMigration (Plan 05-02)
      via: DTO / registry / checksum / vocabulary contract the later plans must satisfy
      pattern: GrocyAiBulk
---

## Phase Goal

**As a** household maintainer, **I want to** trust that a reviewed bulk plan is exactly what applies, **so that** bulk maintenance never becomes an unaudited or arbitrary write path.

<objective>
Lock the bulk-engine contract as a failing (RED) test suite before any production behavior is written.

Purpose: fix the plan/plan-item/audit DTO shapes, the closed typed-operation-registry shape, the SHA-256 plan-checksum contract, and the closed blocker/outcome vocabulary so every later Phase 5 plan is graded against one immovable specification.
Output: a deterministic `bulk` test module and fixtures that fail at an intentional RED gate and change no production file.
</objective>

<execution_context>@/Users/ian/.codex/get-shit-done/workflows/execute-plan.md</execution_context>

<context>
@.planning/phases/05-bulk-maintenance-recovery-engine/05-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-reusable-conversion-model/04-08-SUMMARY.md
@custom/grocy_AI/src/GrocyAiTaxonomyService.php
<interfaces>
From `custom/grocy_AI/tests/run.php`: focused commands are added as conditional `if (($argv[1] ?? null) === '<mode>')` dispatches that require the mode file and must not change existing Phase 1–4 behavior; a RED contract emits an `EXPECTED_RED: <marker>` line to STDERR and exits nonzero (see `expectedRed()` and `runConversionCharacterizationContract`).
From `GrocyAiTaxonomyService::AssignProductTaxonomy(int $productId, array $assignment)`: the proving write accepts exactly `['leaf_slug','ruleset_version']` or `['unclassified','ruleset_version']`, rejects any other key set, and requires `ruleset_version === GrocyAiTaxonomyMigration::VERSION`.
From `GrocyAiConversionService`: the checksum idiom is `hash('sha256', CanonicalJson([...]))` (see `CharacterizationFactsHash`, `EvidenceHash`, `RevisionHash`); the bulk plan checksum must reuse this SHA-256/canonical-JSON idiom.
Shared naming contract (all Phase 5 plans): service `GrocyAI\Services\GrocyAiBulkService` with methods `GeneratePlan`, `SetItemSelection`, `ApplyPlan`, `PreviewRollback`, `ExportPlan`; migration `GrocyAiBulkMigration`; tables `grocy_ai_bulk_plans`, `grocy_ai_bulk_plan_items`, `grocy_ai_bulk_audit`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix the plan/plan-item/audit DTO, registry, and checksum shapes as RED contract cases</name>
  <files>custom/grocy_AI/tests/bulk.php, custom/grocy_AI/tests/run.php, custom/grocy_AI/tests/fixtures/bulk-plan-cases.json, custom/grocy_AI/tests/fixtures/bulk-registry-cases.json</files>
  <read_first>custom/grocy_AI/tests/run.php; custom/grocy_AI/tests/conversions.php; custom/grocy_AI/tests/taxonomy.php; custom/grocy_AI/src/GrocyAiTaxonomyService.php; custom/grocy_AI/src/GrocyAiConversionService.php; .planning/phases/05-bulk-maintenance-recovery-engine/05-CONTEXT.md</read_first>
  <behavior>
    - Test 1: `bulk-contract` asserts the plan DTO exposes exactly `id, created_at, created_by, ruleset_version, operation_type, scope_json, counts_json, checksum, status, module_version` and the plan-item DTO exposes exactly `id, plan_id, seq, object_type, object_id, operation, before_image_json, proposed_value_json, reason, provenance, selected, outcome, applied_at` (D-02); an audit-record DTO exposes exactly `id, plan_id, plan_item_id, actor, event, event_at, module_version, before_json, after_json, outcome` (D-10). Extra or missing keys fail closed.
    - Test 2: the plan `counts_json` contract fixes the exact count keys `included, excluded, skipped, conflicted, changed, unchanged` (D-01), and the plan checksum is asserted to be a lowercase 64-hex SHA-256 computed over item identities, before/proposed values, operation types, and ruleset version via the canonical-JSON idiom (D-03) — reordering items or fields must not change the checksum, and mutating any covered value must change it.
    - Test 3: the typed-operation-registry shape is a closed map whose only members are `assign_taxonomy_leaf` and `set_unclassified` (D-05/D-06); each entry names the delegated shipped write `AssignProductTaxonomy` and the exact assignment key set it produces; any free-form entity, field, CRUD verb, or SQL string outside the closed map is a contract violation.
  </behavior>
  <action>Create `custom/grocy_AI/tests/bulk.php` exposing `runBulkContract()` and add a `bulk-contract` dispatch in `run.php` (mirroring the `conversion-characterization` conditional block) that requires `bulk.php` and calls it. Assert the exact DTO key sets, count keys, checksum idiom, and closed registry map named in the shared Phase 5 contract; drive the checksum reorder/mutation assertions from `custom/grocy_AI/tests/fixtures/bulk-plan-cases.json` and the registry cases from `custom/grocy_AI/tests/fixtures/bulk-registry-cases.json`. Because no `GrocyAiBulkService`/`GrocyAiBulkMigration` exists yet, guard on `class_exists`/`method_exists` and emit `EXPECTED_RED: bulk.contract_shapes` then exit nonzero until Plans 05-02..05-04 satisfy the shapes. Fixtures must be deterministic, non-household structured test data only (no product names, barcodes, secrets, or URLs).</action>
  <verify><automated>php8.5 custom/grocy_AI/tests/run.php bulk-contract; test $? -ne 0 && echo RED_OK; php8.5 -l custom/grocy_AI/tests/bulk.php && php8.5 -l custom/grocy_AI/tests/run.php</automated></verify>
  <acceptance_criteria>The `bulk-contract` command exits nonzero and prints `EXPECTED_RED: bulk.contract_shapes` before any production bulk class exists, and both fixture files parse as JSON with the closed count keys, DTO key sets, and closed registry map.</acceptance_criteria>
  <done>The plan/plan-item/audit DTO shapes, the closed registry shape, and the SHA-256 checksum contract are fixed as a failing, deterministic RED suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix the closed blocker/outcome vocabulary and the engine invariant claims as RED cases</name>
  <files>custom/grocy_AI/tests/bulk.php, custom/grocy_AI/tests/run.php, custom/grocy_AI/tests/fixtures/bulk-plan-cases.json</files>
  <read_first>custom/grocy_AI/src/GrocyAiConversionService.php; custom/grocy_AI/tests/conversions.php; .planning/phases/05-bulk-maintenance-recovery-engine/05-CONTEXT.md; .planning/phases/04-reusable-conversion-model/04-08-SUMMARY.md</read_first>
  <behavior>
    - Test 1: the closed per-item `outcome` vocabulary is fixed to exactly `{pending, applied, conflict, skipped, rejected, rolled_back}` and the closed `blocker` vocabulary to exactly the named reasons Phase 5 uses (for example `unknown_operation`, `before_image_stale`, `plan_checksum_mismatch`, `not_selected`, `already_applied`, `manual_edit_after_apply`) — any outcome or blocker outside the closed sets fails the contract (D-05/D-07/D-09/D-11).
    - Test 2: the suite states the invariant claims as assertions against the (not-yet-existing) service surface — zero-write generation (D-01), immutable before-image captured at generation time (D-02), named-operation-only apply (D-05), optimistic-concurrency refusal (D-07), single `BEGIN IMMEDIATE` transaction with no network/provider/companion call under lock (D-08), checksum-keyed idempotency (D-09), append-only audit with authenticated actor (D-10), rollback-refusal of later manual edits (D-11), and non-authoritative redacted export (D-12).
    - Test 3: every claim guards on the absent production surface and routes to a single `EXPECTED_RED: bulk.engine_invariants` gate; the suite performs no write and touches no native Grocy table.
  </behavior>
  <action>Extend `bulk.php` with `runBulkInvariants()` and add a `bulk-invariants` dispatch in `run.php`. Encode the closed outcome and blocker vocabularies as fixed arrays and assert membership; encode each D-01..D-12 invariant as a named claim that references the exact `GeneratePlan`/`SetItemSelection`/`ApplyPlan`/`PreviewRollback`/`ExportPlan` method it will later exercise. Guard on `class_exists('GrocyAI\\Services\\GrocyAiBulkService')` and emit `EXPECTED_RED: bulk.engine_invariants` until the later plans implement them. Do not create any `grocy_ai_bulk_*` table here (schema is Plan 05-02) and issue no SQL beyond in-memory fixture assertions; this task stops at the RED gate exactly like 02-01/04-01.</action>
  <verify><automated>php8.5 custom/grocy_AI/tests/run.php bulk-invariants; test $? -ne 0 && echo RED_OK; php8.5 -l custom/grocy_AI/tests/bulk.php && php8.5 -l custom/grocy_AI/tests/run.php</automated></verify>
  <acceptance_criteria>The `bulk-invariants` command exits nonzero with `EXPECTED_RED: bulk.engine_invariants`, the closed outcome/blocker sets are asserted, and no production file under `custom/grocy_AI/src` is modified.</acceptance_criteria>
  <done>The closed blocker/outcome vocabulary and all nine engine invariants are pinned as a failing contract that later plans must turn green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| browser/companion → bulk plan | Operation names, field targets, and payloads are untrusted and must map only to the closed registry. |
| reviewed plan → applied plan | The checksum must bind the reviewed artifact to the applied artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-01 | Tampering | plan checksum contract | mitigate | Fix a SHA-256/canonical-JSON checksum over immutable content so reorder is stable and any value change is detected. |
| T-05-02 | Elevation of Privilege | operation registry shape | mitigate | Fix a closed named-operation map; no free-form entity/field/CRUD/SQL member is permitted. |
| T-05-03 | Repudiation | audit-record DTO | mitigate | Fix an append-only audit DTO carrying actor, event, timestamps, module/version, and before/after. |
| T-05-SC | Tampering | package installs | accept | No package installation is planned. |
</threat_model>

<verification>
Run `php8.5 custom/grocy_AI/tests/run.php bulk-contract` and `php8.5 custom/grocy_AI/tests/run.php bulk-invariants`; both must exit nonzero at their `EXPECTED_RED` markers, and `php8.5 -l` must pass on the changed test files. Confirm `git status` shows no change under `custom/grocy_AI/src`, `public/`, or `routes.php`.
</verification>

<success_criteria>
The first Phase 5 deliverable is a deterministic, failing contract suite that fixes every plan/plan-item/audit DTO, the closed typed-operation registry, the SHA-256 plan checksum, and the closed blocker/outcome vocabulary — not any production behavior.
</success_criteria>

<output>Create .planning/phases/05-bulk-maintenance-recovery-engine/05-01-SUMMARY.md when done</output>
</content>
</invoke>
