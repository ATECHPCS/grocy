---
phase: 05-bulk-maintenance-recovery-engine
plan: "11"
type: execute
wave: 11
depends_on: [05-01, 05-02, 05-03, 05-04, 05-05, 05-06, 05-07, 05-08, 05-09, 05-10]
files_modified:
  - custom/grocy_AI/portable-files.txt
  - custom/grocy_AI/tests/release-gate.sh
  - custom/grocy_AI/routes.php
  - custom/grocy_AI/src/GrocyAiApiController.php
  - custom/grocy_AI/tests/browser/specs/bulk.spec.js
  - custom/grocy_AI/tests/browser/fixtures/bulk-review.html
  - custom/grocy_AI/README.md
  - CUSTOMIZATIONS.md
autonomous: false
requirements: [BULK-01, BULK-02, BULK-03, BULK-04, BULK-05, BULK-06, BULK-07, BULK-08, BULK-09, BULK-10]
must_haves:
  truths:
    - "D-15: Every new Phase 5 file is a declared portable file that exists, and the manifest stays sorted-unique with safe relative paths."
    - "D-13: Preview, selection, the selected diff, export, and rollback preview are wired as reads on the /api/grocy-ai group under PERMISSION_MASTER_DATA_EDIT; the durable apply is an authenticated, audited in-app action under the same permission, with no maintainer CLI apply added."
    - "D-05/D-08/D-09: A release-gate `bulk` mode proves manifest membership, zero-write generation, named-operation-only apply, single-transaction + idempotency, audit append-only, and rollback refusal — each fail-closed-verified."
    - "D-15: The full unit and browser suites pass on both maintained branches (single-checkout fallback), and every changed PHP file lints under php8.5."
    - "D-14: README and CUSTOMIZATIONS document the engine and its authority boundary — Grocy remains the sole durable authority, and the actual inventory sweep belongs to Phase 6."
  artifacts:
    - path: custom/grocy_AI/tests/release-gate.sh
      provides: a dual-branch `bulk` release-gate mode proving the Phase 5 invariants
    - path: custom/grocy_AI/routes.php
      provides: MASTER_DATA_EDIT-gated bulk read endpoints plus the authenticated audited apply action
  key_links:
    - from: custom/grocy_AI/tests/release-gate.sh (bulk mode)
      to: custom/grocy_AI/portable-files.txt
      via: required_portable_paths membership + sorted-unique/safe-path assertions
      pattern: portable_manifest_check
    - from: custom/grocy_AI/routes.php
      to: GrocyAiApiController bulk endpoints
      via: /api/grocy-ai group with CorsMiddleware + JsonMiddleware and per-method PERMISSION_MASTER_DATA_EDIT
      pattern: master_data_edit_group
---

## Phase Goal

**As a** release maintainer, **I want to** wire the bulk engine's endpoints, add every Phase 5 file to portable parity, and prove the engine's invariants in a dual-branch `bulk` release gate, **so that** the reviewed-apply-audit-export-rollback workflow ships identically on both maintained branches with its safety contract enforced, not just claimed.

<objective>
Deliver the Phase 5 integration and release proof: route wiring under the correct authority, portable-parity manifest membership, a fail-closed `bulk` release-gate mode, green dual-branch unit+browser suites, and maintainer documentation of the engine and its authority boundary.

Purpose: turn the eight capability slices into one shippable, provably safe, portable feature.
Output: manifest + gate + routes + browser spec + docs, verified on both branches.
</objective>

<execution_context>@/Users/ian/.codex/get-shit-done/workflows/execute-plan.md</execution_context>

<context>
@.planning/phases/05-bulk-maintenance-recovery-engine/05-CONTEXT.md
@.planning/phases/04-reusable-conversion-model/04-08-SUMMARY.md
@.planning/phases/04-reusable-conversion-model/04-10-SUMMARY.md
<interfaces>
From `custom/grocy_AI/tests/release-gate.sh`: existing single-argument modes `taxonomy` and `conversions` (dispatched by `[ "$#" -eq 1 ] && [ "$1" = <mode> ]`), the helpers `fail`/`pass`/`run_quiet`/`sha256`, the `stable_repo`/`stable_ref` single-checkout fallback, and the `$php_runner` (`GROCY_AI_PHP`) hoist. The `conversions` mode is the template for a new `bulk` mode: it asserts required portable paths, manifest sorted-unique/safe-paths, source-contains-contract greps, fail-closed static checks, and `run_quiet` unit runs.
From `custom/grocy_AI/portable-files.txt`: a flat, sorted, newline-delimited manifest (currently 36 paths); every module file must appear exactly once and exist on disk.
From `custom/grocy_AI/routes.php`: the `/api/grocy-ai` group wrapped in `CorsMiddleware` + `JsonMiddleware`, with `require_once` service wiring; new bulk service/migration are required here and endpoints registered on this group. Root `routes.php` already conditionally requires this file under `GROCY_FEATURE_FLAG_GROCY_AI` — no root change is needed.
From `GrocyAiApiController`: `User::CheckPermission($request, User::PERMISSION_MASTER_DATA_EDIT)`, `ApiResponse`/`DiagnosticResponse`/`GenericErrorResponse`. The taxonomy write precedent is `PUT /products/{productId}/taxonomy` -> `AssignProductTaxonomy`, an authenticated permission-checked mutation on this group.
From Plans 05-02..05-10: `GrocyAiBulkService` (`GeneratePlan`, `SetItemSelection`, `ApplyPlan`, `PreviewRollback`, rollback apply, `ExportPlan`), `GrocyAiBulkMigration` (`grocy_ai_bulk_plans`, `grocy_ai_bulk_plan_items`, append-only `grocy_ai_bulk_audit`), `custom/grocy_AI/tests/bulk.php` and its run.php modes (`bulk-generate`, `bulk-registry`, `bulk-selection`, `bulk-conflict`, `bulk-apply`, `bulk-audit`, `bulk-rollback`, `bulk-export` — the canonical set registered by 05-02..05-10; the controller-endpoint assertions ride under `bulk-selection`/`bulk-apply`, and 05-01 also registers `bulk-contract`/`bulk-invariants`/`bulk-schema`), and `public/custom/grocy_AI/bulk-review.js` + `bulk-review.test.js`.
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add Phase 5 files to portable parity and a fail-closed `bulk` release-gate mode + browser spec</name>
  <files>custom/grocy_AI/portable-files.txt, custom/grocy_AI/tests/release-gate.sh, custom/grocy_AI/tests/browser/specs/bulk.spec.js, custom/grocy_AI/tests/browser/fixtures/bulk-review.html</files>
  <read_first>custom/grocy_AI/tests/release-gate.sh (taxonomy_release_gate + conversions_release_gate); custom/grocy_AI/portable-files.txt; custom/grocy_AI/tests/browser/specs/conversions.spec.js; MISTAKES.md</read_first>
  <behavior>
    - Test 1 (manifest membership): every new Phase 5 file under a manifest-tracked tree (`custom/grocy_AI/**`, `public/custom/grocy_AI/**`) appears exactly once in `portable-files.txt` and exists on disk; the manifest remains LC_ALL=C sorted-unique with no absolute or `..` paths. `views/grocyai_bulkreview.blade.php` is deliberately excluded (Blade views are carried by the branch-adapter / changed-paths mechanism, matching the untracked `views/grocyai_conversioncoverage.blade.php`).
    - Test 2 (bulk gate, zero-write generation): the gate proves generation is read-only by running the `bulk-generate` unit mode and statically asserting the service issues no ad-hoc native/cache SQL and no direct write to native or taxonomy tables.
    - Test 3 (bulk gate, named-op-only apply, no network): the gate asserts `GrocyAiBulkService` resolves operations only through the closed registry and that its sole durable delegate is `->AssignProductTaxonomy(`, with no free-form entity/field/CRUD/SQL target; and that the apply/rollback path in `GrocyAiBulkService.php` contains NO network primitive (grep for `curl_`, `file_get_contents('http`, `fsockopen`, `stream_socket`, and provider/companion client calls asserts zero matches, fail-closed). Verified alongside the `bulk-registry` unit mode.
    - Test 4 (bulk gate, contract + transaction + idempotency + audit + rollback): the gate runs 05-01's `bulk-contract`, `bulk-invariants`, and `bulk-schema` modes so the RED contract is proven GREEN, and asserts a single guarded transaction (`exec('BEGIN IMMEDIATE')`/one commit path), a checksum-keyed idempotency existence check, an append-only `grocy_ai_bulk_audit` (no UPDATE/DELETE against it in the service), and runs the `bulk-apply`, `bulk-audit`, and `bulk-rollback` unit modes so single-transaction, idempotency, append-only, and `manual_edit_after_apply` rollback refusal are each fail-closed-verified.
    - Test 5 (authority surface): the gate asserts the apply action is permission-checked (`PERMISSION_MASTER_DATA_EDIT`) and that no `bin/` command applies a bulk plan (no maintainer CLI apply added in Phase 5).
    - Test 6 (browser): `bulk.spec.js` (tagged `@bulk05`, against `bulk-review.html`) proves the review/select, selected-diff, export-download, and rollback-preview surfaces render read-only and reuse the Bootstrap 4 controls.
  </behavior>
  <action>Append every new Phase 5 file that lives under a manifest-tracked tree to `portable-files.txt` in sorted position — the complete union across all eleven plans: `custom/grocy_AI/src/GrocyAiBulkMigration.php` (05-02), `custom/grocy_AI/src/GrocyAiBulkService.php` (05-03), `custom/grocy_AI/src/GrocyAiBulkController.php` (05-05), `custom/grocy_AI/tests/bulk.php` (05-01), `custom/grocy_AI/tests/fixtures/bulk-plan-cases.json` (05-01), `custom/grocy_AI/tests/fixtures/bulk-registry-cases.json` (05-01), `custom/grocy_AI/tests/browser/fixtures/bulk-review.html` (this plan), `custom/grocy_AI/tests/browser/specs/bulk.spec.js` (this plan), `public/custom/grocy_AI/bulk-review.js` (05-05), and `public/custom/grocy_AI/bulk-review.test.js` (05-05). `GrocyAiBulkController.php` stays in the manifest (it is under `custom/grocy_AI/src/`, follows the `GrocyAiConversionController` manifest precedent, and was one of the artifacts Batch C originally omitted). Do NOT add `views/grocyai_bulkreview.blade.php` to the manifest: `portable-files.txt` tracks only `custom/grocy_AI/**` and `public/custom/grocy_AI/**`, whereas Blade views live in the core `views/` tree and are carried by the branch-adapter / changed-paths mechanism (the existing `views/grocyai_conversioncoverage.blade.php` is correctly untracked). Instead, note `views/grocyai_bulkreview.blade.php` in the Phase 5 changed-paths / branch-adapter handling so it ships on both branches through that mechanism. (Do NOT fold in the pre-existing conversion-blade cleanup — that is out of scope for Phase 5. If any additional `custom/grocy_AI/tests/fixtures/bulk-*.json` fixtures were created by later plans, reconcile them into this list too.) Add a `bulk_release_gate()` function modeled exactly on `conversions_release_gate()` and dispatch it via `if [ "$#" -eq 1 ] && [ "$1" = bulk ]`. In it: (a) build `required_portable_paths` of the new files (NOT the Blade view — it is not a manifest-tracked path), assert each is grep-Fqx in the manifest and exists, and reuse the manifest sorted-unique/safe-path assertions; (b) statically assert the service contains a closed operation registry and `->AssignProductTaxonomy(`, contains no `INSERT|UPDATE|DELETE|REPLACE` against `products|grocy_ai_taxonomy_classifications|quantity_unit_conversions|cache__`, and no `UPDATE|DELETE` against `grocy_ai_bulk_audit`; (c) assert one `BEGIN IMMEDIATE`/single-commit apply path and a checksum idempotency check; (d) assert `GrocyAiApiController` permission-checks the apply with `PERMISSION_MASTER_DATA_EDIT` and that `find custom/grocy_AI/bin -name '*.php'` contains no `->ApplyPlan(` (no CLI apply); (e) statically assert `GrocyAiBulkService.php` contains NO network primitive anywhere in the apply/rollback path — grep for `curl_`, `file_get_contents('http`, `fsockopen`, `stream_socket`, and any provider/companion client call — asserting zero matches (fail-closed if any appear), proving apply/rollback never touch the network while the lock is held; (f) `run_quiet` the `bulk-contract`, `bulk-invariants`, `bulk-schema`, `bulk-generate`, `bulk-registry`, `bulk-selection`, `bulk-conflict`, `bulk-apply`, `bulk-audit`, `bulk-rollback`, and `bulk-export` modes (including 05-01's `bulk-contract`/`bulk-invariants`/`bulk-schema` so the RED contract is proven GREEN) and `php -l` the new PHP files through `$php_runner`; (g) end with `echo "RELEASE_GATE: PASS (bulk)"`. Use `$php_runner`/`GROCY_AI_PHP`, `$stable_repo`/`$stable_ref`, and the `sha256()` helper throughout per MISTAKES.md — no bare `php`, no hardcoded macOS path, no literal path count. Add `bulk.spec.js` + `bulk-review.html` mirroring the conversions spec conventions.
  <verify><automated>GROCY_AI_PHP=php8.5 bash custom/grocy_AI/tests/release-gate.sh bulk && npm --prefix custom/grocy_AI/tests/browser test -- --grep @bulk05 && sh -n custom/grocy_AI/tests/release-gate.sh</automated></verify>
  <acceptance_criteria>The `bulk` gate passes and fails closed if any Phase 5 file is unlisted, if the service gains an ad-hoc native write, if the apply loses its permission check, or if a CLI apply command is added; the browser spec proves read-only review surfaces.</acceptance_criteria>
  <done>Portable parity includes every Phase 5 file and a dual-branch `bulk` gate proves the engine's invariants fail-closed.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the bulk endpoints under the correct authority and document the engine</name>
  <files>custom/grocy_AI/routes.php, custom/grocy_AI/src/GrocyAiApiController.php, custom/grocy_AI/README.md, CUSTOMIZATIONS.md</files>
  <read_first>custom/grocy_AI/routes.php; routes.php; custom/grocy_AI/src/GrocyAiApiController.php; custom/grocy_AI/README.md; CUSTOMIZATIONS.md</read_first>
  <behavior>
    - Test 1: `require_once` wiring loads `GrocyAiBulkMigration` (already required by 05-02) and `GrocyAiBulkService` (already required by 05-05), and the `/api/grocy-ai` group exposes the full bulk endpoint set, each route defined exactly once — the plan read, selection, and selected-diff registered by 05-05; the durable apply action registered by 05-07; the audit read registered by 05-08; the rollback-preview and rollback-execute endpoints registered by 05-09; plus the generate/preview read and the export read this plan adds — all reachable through the existing `CorsMiddleware`/`JsonMiddleware` stack, with no duplicate or conflicting route definition.
    - Test 2: every bulk read endpoint and the apply action call `User::CheckPermission($request, User::PERMISSION_MASTER_DATA_EDIT)`; an under-permissioned request is rejected before any read or write.
    - Test 3: the apply action is authenticated and audited (it appends `grocy_ai_bulk_audit`) and there is no maintainer CLI apply path; reads never mutate.
    - Test 4: `routes.php` and `GrocyAiApiController.php` lint clean under php8.5, and the module suite still passes.
  </behavior>
  <action>In `custom/grocy_AI/routes.php`, ensure the `require_once` for `GrocyAiBulkMigration.php` (added by 05-02) is present and add the `require_once` for `GrocyAiBulkService.php` only if an earlier plan (05-05) has not already wired it — never duplicate an existing `require_once`. Register only the endpoints deferred to this plan on the `/api/grocy-ai` group: the generate/preview read and the export read (its `ExportBulkPlan` controller method landed in 05-10). Do NOT redefine the plan-read/selection/selected-diff routes (owned by 05-05), the durable apply route (owned by 05-07), the audit read route (owned by 05-08), or the rollback-preview and rollback-execute routes (owned by 05-09) — reference and verify those are present exactly once instead. Add the matching new controller methods (generate/preview) to `GrocyAiApiController`, permission-checking `PERMISSION_MASTER_DATA_EDIT` and mapping bounded service errors to `DiagnosticResponse`/`GenericErrorResponse`; the `ExportBulkPlan` method already landed in 05-10 and the rollback-preview/rollback-execute methods already landed in 05-09. Do not add a `bin/` apply command. Document in `custom/grocy_AI/README.md` a new "Bulk maintenance & recovery engine" section: the plan/review/select/apply/audit/export/rollback workflow, the closed named-operation registry proving against `AssignProductTaxonomy`, the optimistic-concurrency + single-transaction + idempotency contract, the append-only audit ledger, the non-authoritative export, and the authority boundary (Grocy is the sole durable authority; apply is a user-facing audited action under `MASTER_DATA_EDIT`, not a maintainer CLI; the full inventory sweep and conversion-cleanup are Phase 6). Add the ownership/no-parallel-write statement to `CUSTOMIZATIONS.md`.</action>
  <verify><automated>php8.5 custom/grocy_AI/tests/run.php && php8.5 -l custom/grocy_AI/routes.php && php8.5 -l custom/grocy_AI/src/GrocyAiApiController.php</automated></verify>
  <acceptance_criteria>Reads and the audited apply are wired under `MASTER_DATA_EDIT` on the existing group with no CLI apply, and the docs describe the engine and its authority boundary.</acceptance_criteria>
  <done>The engine is reachable at the correct authority and its boundary is documented for maintainers.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm the dual-branch bulk suite and the reviewed-apply-audit-recover workflow</name>
  <action>Pause only after every automated check passes on this checkout: the `bulk` release gate, the full module unit suite, the browser suite (including `@bulk05`), the frontend node tests, and php8.5 lint on all changed files. Confirm the single-checkout stable fallback (`GROCY_AI_STABLE_REF`/`atech-release`) resolves so the gate is branch-identical, and that the manifest membership holds for both maintained branches.</action>
  <what-built>The wired bulk engine (preview/select/apply/audit/export/rollback) with portable parity, the `bulk` release gate, and maintainer documentation.</what-built>
  <how-to-verify>1. Sign in with MASTER_DATA_EDIT and generate a bulk plan; confirm exact included/excluded/skipped/conflicted/changed/unchanged counts and that generation writes nothing. 2. Select/reject individual items and confirm the selected diff matches the apply set. 3. Apply; confirm only selected non-conflicted items changed, that a drifted item was refused as a conflict with no partial write, and that re-applying repeats no mutation. 4. Open the audit trail and confirm actor, previewed/applied times, module/version, per-item outcome, and before/after values. 5. Export JSON and CSV; confirm redaction (no secrets/tokens/unrelated household detail) and the non-authoritative marker. 6. Preview rollback; confirm a manually edited field is refused and a clean item is reversible, then roll back and confirm the audit records it. 7. Confirm the same behavior at 320px, and that no maintainer CLI apply exists.</how-to-verify>
  <resume-signal>Type "approved" or describe the issue.</resume-signal>
  <verify><automated>GROCY_AI_PHP=php8.5 bash custom/grocy_AI/tests/release-gate.sh bulk && php8.5 custom/grocy_AI/tests/run.php && npm --prefix custom/grocy_AI/tests/browser test</automated></verify>
  <done>Human verification confirms the reviewed-apply-audit-export-rollback workflow and the fail-closed authority boundary on both branches.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| module files → portable parity | An unlisted or drifted Phase 5 file would ship differently on the two branches. |
| browser/companion request → bulk endpoints | Preview/select/export/rollback and apply are attackable surfaces that must stay permission-checked and named-operation-only. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-11-01 | Tampering | portable parity | mitigate | The `bulk` gate asserts manifest membership, sorted-unique/safe paths, and existence for every Phase 5 file, fail-closed. |
| T-05-11-02 | Elevation of Privilege | bulk endpoints | mitigate | Every read and the apply require authenticated `PERMISSION_MASTER_DATA_EDIT`; no CLI apply is added. |
| T-05-11-03 | Tampering | apply integrity | mitigate | The gate statically proves named-operation-only apply, no ad-hoc native/cache SQL, single-transaction, checksum idempotency, and append-only audit. |
| T-05-11-04 | Repudiation | audit trail | mitigate | The append-only `grocy_ai_bulk_audit` records actor/times/versions/outcomes and the gate forbids service UPDATE/DELETE against it. |
| T-05-11-SC | Tampering | package installs | accept | No package installation is planned. |
</threat_model>
<verification>Run `GROCY_AI_PHP=php8.5 bash custom/grocy_AI/tests/release-gate.sh bulk`, `php8.5 custom/grocy_AI/tests/run.php`, `npm --prefix custom/grocy_AI/tests/browser test`, the frontend node tests, and php8.5 lint on all changed files before the checkpoint.</verification>
<success_criteria>Every Phase 5 file is in portable parity; the `bulk` release gate fail-closed-proves zero-write generation, named-operation-only apply, single-transaction + idempotency, append-only audit, and rollback refusal; endpoints are wired under `MASTER_DATA_EDIT` with an authenticated audited apply and no CLI apply; the full unit+browser suites pass on both branches; README/CUSTOMIZATIONS document the engine and its authority boundary.</success_criteria>
<output>Create .planning/phases/05-bulk-maintenance-recovery-engine/05-11-SUMMARY.md when done</output>
