---
phase: 05-bulk-maintenance-recovery-engine
plan: "03"
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - custom/grocy_AI/src/GrocyAiBulkService.php
  - custom/grocy_AI/tests/bulk.php
  - custom/grocy_AI/tests/run.php
autonomous: true
requirements: [BULK-01, BULK-02]
must_haves:
  truths:
    - "D-01: GeneratePlan produces a bounded, zero-mutation dry-run plan reporting exact included/excluded/skipped/conflicted/changed/unchanged counts."
    - "D-02: Every plan item records stable object identity, an immutable before-image captured at generation time, the proposed value, a named reason, provenance, and the ruleset version."
    - "D-03: The plan carries a SHA-256 checksum over its immutable content so the reviewed plan and the applied plan are provably the same artifact."
    - "D-14: Generation reads current object values through existing read paths and issues no native write and no ad-hoc cache SQL."
  artifacts:
    - path: custom/grocy_AI/src/GrocyAiBulkService.php
      provides: GeneratePlan producing bounded counts, immutable before-images, and a deterministic plan checksum with zero writes
  key_links:
    - from: custom/grocy_AI/src/GrocyAiBulkService.php
      to: GrocyAiBulkMigration tables (grocy_ai_bulk_plans, grocy_ai_bulk_plan_items)
      via: GeneratePlan persists the plan header and immutable items only
      pattern: grocy_ai_bulk_plan
    - from: custom/grocy_AI/tests/bulk.php
      to: GrocyAiBulkService::GeneratePlan
      via: bulk-generate dispatch asserting counts, checksum, and zero-write snapshot
      pattern: bulk-generate
---

## Phase Goal

**As a** household maintainer, **I want to** generate a bounded dry-run plan with exact counts and an immutable checksum that changes nothing, **so that** I can review precisely what a bulk operation would do before anything is written.

<objective>
Implement `GeneratePlan`: a bounded, zero-mutation dry-run that reports exact counts, captures immutable before-images, and seals the plan with a deterministic checksum.

Purpose: make review trustworthy — what is counted and imaged at generation time is exactly what a later apply is bound to by checksum.
Output: `GrocyAiBulkService::GeneratePlan` persisting a plan header and immutable items, proven zero-write by schema, state, `total_changes`, and a before/after snapshot.
</objective>

<execution_context>@/Users/ian/.codex/get-shit-done/workflows/execute-plan.md</execution_context>

<context>
@.planning/phases/05-bulk-maintenance-recovery-engine/05-CONTEXT.md
@.planning/REQUIREMENTS.md
@custom/grocy_AI/src/GrocyAiTaxonomyService.php
@custom/grocy_AI/src/GrocyAiConversionService.php
@custom/grocy_AI/src/GrocyAiBulkMigration.php
<interfaces>
From `GrocyAiTaxonomyService`: current object values are read through existing read paths — `ReadProductTaxonomy(int $productId)` returns `current_leaf`, `suggested_leaf`, `evidence_source`, `ruleset_version`, `provider_category`, `confidence_band`, `reason_code`; `ValidateInventoryTaxonomy()` returns the bounded per-outcome counts. Use these, never ad-hoc cache SQL. The before-image and proposed value are over the WRITTEN field only — the classification leaf slug from `current_leaf` (`null` for unclassified) via the public `ReadProductTaxonomy` — never the volatile evidence fields (`suggested_leaf`/`evidence_source`/`provider_category`/`confidence_band`/`reason_code`), and never the private `CurrentLeaf` helper.
From `GrocyAiConversionService`: the checksum idiom is `hash('sha256', CanonicalJson([...]))` where `CanonicalJson` sorts keys for determinism; the bulk plan checksum reuses this idiom over item identities, before/proposed values, operation types, and ruleset version (D-03).
From Plan 05-02: `grocy_ai_bulk_plans` and `grocy_ai_bulk_plan_items` already exist with the shared-contract columns; `GeneratePlan` writes only these module tables.
From Plan 05-01: the plan DTO, plan-item DTO, closed count keys (`included, excluded, skipped, conflicted, changed, unchanged`), and closed reason/outcome vocabulary are fixed by the RED contract this plan turns green.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement GeneratePlan with bounded exact counts, immutable items, and a deterministic checksum</name>
  <files>custom/grocy_AI/src/GrocyAiBulkService.php, custom/grocy_AI/tests/bulk.php, custom/grocy_AI/tests/run.php</files>
  <read_first>custom/grocy_AI/src/GrocyAiTaxonomyService.php; custom/grocy_AI/src/GrocyAiConversionService.php; custom/grocy_AI/src/GrocyAiBulkMigration.php; custom/grocy_AI/tests/taxonomy.php; .planning/phases/05-bulk-maintenance-recovery-engine/05-CONTEXT.md</read_first>
  <behavior>
    - Test 1: on a fixture of products with mixed taxonomy states, `GeneratePlan` returns a plan whose `counts_json` reports exact `included, excluded, skipped, conflicted, changed, unchanged` values matching the fixture, following the fixed bucket-to-count mapping (below), and the sum reconciles with the bounded scope size. Specifically the `conflicted` count is `0` at generation (conflict is an apply-time optimistic-concurrency judgement, not a generation concept), and `included == changed + unchanged`.
    - Test 2: each persisted `grocy_ai_bulk_plan_items` row carries a stable `object_type`/`object_id` identity, an immutable `before_image_json` capturing ONLY the written field — the current classification leaf slug (`current_leaf`'s slug, or `null` when unclassified) read via the public `ReadProductTaxonomy`, NOT the whole DTO with its volatile evidence fields (`suggested_leaf`, `evidence_source`, `provider_category`, `confidence_band`, `reason_code`) — a `proposed_value_json` in the same written-field shape, a named `reason` drawn from the closed vocabulary, a `provenance` string, and the `ruleset_version`; the plan header records `ruleset_version`, `operation_type`, `scope_json`, and the checksum.
    - Test 3: the plan `checksum` is a lowercase 64-hex SHA-256 over item identities, before/proposed values, operation types, and ruleset version via the canonical-JSON idiom; regenerating the same scope yields the same checksum, reordering items does not change it, and mutating any before/proposed value does.
  </behavior>
  <action>Create `GrocyAI\Services\GrocyAiBulkService` (constructor takes an optional PDO and bootstraps `GrocyAiBulkMigration`, mirroring `GrocyAiTaxonomyService`). Implement `GeneratePlan(array $scope): array` returning the Plan 05-01 plan DTO. Read current values only through existing read paths (`ReadProductTaxonomy`/`ValidateInventoryTaxonomy`); classify each in-scope object into exactly one closed count bucket; capture the before-image once at generation and never re-derive it (D-02); compute the checksum with a private canonical-JSON SHA-256 helper reusing the `EvidenceHash` idiom (D-03).

Fixed bucket-to-count mapping — project `ValidateInventoryTaxonomy`'s five per-outcome buckets (`mapped`, `unclassified`, `excluded`, `conflicting`, `low_confidence`) into the six closed bulk count keys as follows: (1) `excluded` bucket → `excluded` count (excluded-disposition items get no proposed operation); (2) `unclassified` + `low_confidence` + `conflicting` buckets → `skipped` count (no confident, actionable suggestion to propose — the evidence-level `conflicting` bucket is a stored-evidence reason conflict, NOT the apply-time optimistic-concurrency conflict, so it is skipped at generation, never counted as `conflicted`); (3) `mapped` bucket → `included` count (an actionable proposal — set the current leaf to the suggested leaf via `assign_taxonomy_leaf`/`set_unclassified`), further split so `changed` = included items whose proposed leaf differs from the captured before-image and `unchanged` = included items whose current leaf already equals the proposed value (`included == changed + unchanged`). (4) `conflicted` is reserved `0` at generation — it becomes meaningful only at apply, where 05-06's `DetectApplyConflicts` re-reads live reality and 05-07 records per-item `conflict`. Document this mapping inline so review is unambiguous. Bound the scope per BULK-01 (a deterministic, documented cap on objects examined per call — no TTL and no fixed item cap on the result set per the locked stale-plan decision). Persist the plan header and items in `grocy_ai_bulk_plans`/`grocy_ai_bulk_plan_items`; set every item `outcome` to `pending` and `selected` per the default selection rule. Add a `bulk-generate` dispatch to `run.php`.</action>
  <verify><automated>php8.5 custom/grocy_AI/tests/run.php bulk-generate && php8.5 -l custom/grocy_AI/src/GrocyAiBulkService.php && php8.5 -l custom/grocy_AI/tests/bulk.php</automated></verify>
  <acceptance_criteria>`bulk-generate` proves exact counts for a bounded scope, immutable before-images with identity/proposed/reason/provenance/ruleset fields, and a deterministic reorder-stable, mutation-sensitive checksum.</acceptance_criteria>
  <done>`GeneratePlan` produces a bounded, immutable, checksum-sealed dry-run plan from real read paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove generation is zero-write by schema, state, total_changes, and snapshot</name>
  <files>custom/grocy_AI/src/GrocyAiBulkService.php, custom/grocy_AI/tests/bulk.php, custom/grocy_AI/tests/run.php</files>
  <read_first>custom/grocy_AI/tests/conversions.php; custom/grocy_AI/src/GrocyAiConversionService.php; MISTAKES.md</read_first>
  <behavior>
    - Test 1: before and after `GeneratePlan`, a full snapshot of every native table (`products`, `quantity_unit_conversions`, `cache__quantity_unit_conversions_resolved`, taxonomy classifications) is byte-identical; only the module `grocy_ai_bulk_plans`/`grocy_ai_bulk_plan_items` tables gain rows.
    - Test 2: `sqlite_master` schema is unchanged across generation (no created/dropped native object), and a `total_changes`-style change counter attributes zero row changes to any native table.
    - Test 3: `GeneratePlan` issues no ad-hoc cache SQL and calls no provider/companion/network path — the only writes are the plan/plan-item inserts on the module tables.
  </behavior>
  <action>Add a zero-write proof to `bulk.php` modeled on the conversion read-only assertions: snapshot native tables and `sqlite_master` before generation, run `GeneratePlan`, and assert equality except for the expected module-table inserts, per MISTAKES.md never asserting cache/row counts from reasoning — snapshot and diff the actual rows. Ensure `GrocyAiBulkService` opens no transaction that writes native rows and performs no cache SQL. Route this proof through the `bulk-generate` command (or a `bulk-generate-zero-write` sub-case) so the release gate (Plan 05-11) can invoke it.</action>
  <verify><automated>php8.5 custom/grocy_AI/tests/run.php bulk-generate && php8.5 -l custom/grocy_AI/src/GrocyAiBulkService.php</automated></verify>
  <acceptance_criteria>The snapshot/`sqlite_master`/change-counter assertions prove generation changes no native table and issues no cache SQL, with only the expected module-table inserts.</acceptance_criteria>
  <done>Generation is demonstrably zero-mutation against native Grocy state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| current object values → before-image | Before-images must be captured once at generation and never re-derived at apply. |
| generation → native database | Dry-run generation must not write, project, or issue cache SQL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-07 | Tampering | before-image capture | mitigate | Capture the before-image once at generation from existing read paths and store it immutably in `before_image_json`. |
| T-05-08 | Tampering | plan checksum | mitigate | SHA-256/canonical-JSON checksum over identities, before/proposed values, operation types, and ruleset version; reorder-stable, mutation-sensitive. |
| T-05-09 | Tampering | generation write path | mitigate | Snapshot native tables and `sqlite_master`, assert zero native change and no cache SQL; writes limited to module tables. |
| T-05-SC | Tampering | package installs | accept | No package installation is planned. |
</threat_model>

<verification>
Run `php8.5 custom/grocy_AI/tests/run.php bulk-generate` (green), `php8.5 -l` on the service and test files, and confirm the before/after native snapshot and `sqlite_master` are identical across generation.
</verification>

<success_criteria>
`GeneratePlan` returns a bounded dry-run plan with exact counts, immutable before-images, and a deterministic checksum, provably without mutating any native Grocy state.
</success_criteria>

<output>Create .planning/phases/05-bulk-maintenance-recovery-engine/05-03-SUMMARY.md when done</output>
</content>
