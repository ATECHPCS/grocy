---
phase: 06-inventory-categorization
plan: "03"
type: execute
wave: 2
depends_on: ["06-01"]
requirements: [DATA-01]
must_haves:
  truths:
    - "A per-product exclusion override exists as a migration-seeded Grocy userfield defaulting to included, and the profilers honor it deterministically."
    - "The configured non-food group set (Supplements) is excluded via a taxonomy mapping-rule, not ad-hoc SQL, so exclusion reruns identically."
    - "Exclusion counts are reproducible: the same snapshot yields the same excluded set on every run (supports DATA-07)."
  artifacts:
    - path: "custom/grocy_AI/src/GrocyAiTaxonomyMigration.php"
      provides: "Seed a closed exclusion mapping-rule for the configured non-food group(s); create the per-product override userfield definition"
    - path: "custom/grocy_AI/src/GrocyAiInventoryScope.php"
      provides: "Single owner of the in-scope predicate: active product AND group not excluded AND override != excluded; used by both profilers"
    - path: "custom/grocy_AI/tests/inventory_scope.php"
      provides: "RED-first unit proof of the exclusion predicate + reproducibility (registered in tests/run.php)"
---

## Objective
Make the in-scope selection explicit, config-driven, and reproducible before either profiler runs. On current prod this guards only ~2 Supplements items, but it must be deterministic for DATA-07 zero-diff rerun and act as a tripwire if excluded categories ever grow.

## Facts (verified 2026-09-08)
Exclusion today runs through `grocy_ai_taxonomy_mapping_rules` (`disposition='excluded'`, keyed on provider_category); `ValidateInventoryTaxonomy` already reports an `excluded` count. Product group is the scorer's primary evidence. There are no baby/pet groups; only "Supplements" is borderline non-food; 0 inactive products.

<tasks>
<task type="execute">
  <name>Task 1: RED scope predicate contract</name>
  <action>Add custom/grocy_AI/tests/inventory_scope.php (registered in tests/run.php) asserting: a product is in-scope iff active=1 AND its product group is not in the configured excluded set AND its per-product override is not 'excluded'; a Supplements product is out; an override='excluded' product is out even if its group is fine; the predicate is pure/deterministic (same input → same result); an ungrouped product is in-scope (grouping is a later pass, not an exclusion). RED first.</action>
  <verify><automated>php8.5 custom/grocy_AI/tests/run.php 2>&1 | rg -i "inventory_scope" ; echo "expected: present and RED"</automated></verify>
  <acceptance_criteria>
    - Assertions cover active, group-excluded, override-excluded, ungrouped-still-in, and determinism.
    - Registered and failing before GrocyAiInventoryScope exists.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Exclusion userfield + Supplements mapping seed</name>
  <action>Extend GrocyAiTaxonomyMigration (new VERSION bump, IF NOT EXISTS, additive only): create the per-product override userfield definition (closed value set {included, excluded}, default included) via Grocy's userfield mechanism, and seed a mapping-rule marking the configured non-food group ("Supplements") disposition='excluded'. Store the excluded-group list as a module config constant (reviewable in git). Touch no native product/stock data.</action>
  <verify><automated>php8.5 -l custom/grocy_AI/src/GrocyAiTaxonomyMigration.php</automated></verify>
  <acceptance_criteria>
    - Migration is additive/idempotent and records its version; no ALTER/DROP of a native table.
    - Excluded-group list is a versioned config constant; Supplements seeded as excluded.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: GrocyAiInventoryScope predicate (green)</name>
  <action>Implement GrocyAiInventoryScope as the single owner of the in-scope query/predicate (active AND group-not-excluded AND override!=excluded), reading the config constant + the override userfield + the mapping-rules. Both the group and classification profilers must consult it — no restating the predicate elsewhere. Turn Task 1 GREEN.</action>
  <verify><automated>php8.5 custom/grocy_AI/tests/run.php 2>&1 | rg -i "inventory_scope"</automated></verify>
  <acceptance_criteria>
    - One predicate owner; deterministic; excluded set reproducible across runs on the same data.
  </acceptance_criteria>
</task>
</tasks>

<success_criteria>
- In-scope selection is explicit, config-driven, and reruns identically (DATA-01, supports DATA-07).
- Supplements + any override-excluded products are held out; ungrouped products remain in-scope for grouping.
</success_criteria>

<output>Create 06-03-SUMMARY.md with the predicate, the excluded-group config, and the userfield definition.</output>
