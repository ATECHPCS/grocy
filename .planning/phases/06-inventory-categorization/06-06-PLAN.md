---
phase: 06-inventory-categorization
plan: "06"
type: execute
wave: 2
depends_on: ["06-01"]
requirements: [DATA-03, DATA-04, DATA-05]
must_haves:
  truths:
    - "A read-only audit asserts there are zero product-specific quantity unit conversions and records the 62 global conversions, closing DATA-03/04/05 by verification."
    - "A regression tripwire fails the audit if any product-specific conversion (product_id NOT NULL) ever appears."
    - "No deletion, comparison, or cleanup code is built; the audit writes nothing to any Grocy table."
  artifacts:
    - path: "custom/grocy_AI/bin/audit-conversions.php"
      provides: "Read-only conversion audit: assert 0 product-specific rows, summarize the global set, non-zero exit if product-specific rows exist"
    - path: "custom/grocy_AI/tests/conversion_audit.php"
      provides: "RED-first proof: passes with 0 product-specific rows, trips (non-zero) when a product-scoped row is present (registered in tests/run.php)"
---

## Objective
Close the conversion-cleanup requirements by verification. The live fact-find found 0 product-specific conversions (all 62 rows global); the "~101" figure was stale. Rather than build deletion machinery for nothing, ship a read-only audit + tripwire.

## Facts (verified 2026-09-08)
`quantity_unit_conversions` = 62 rows, every one `product_id IS NULL`. `quantity_unit_conversions_resolved` is a computed view (not stored junk). No product-scoped conversions exist to profile, compare, or remove.

<tasks>
<task type="execute">
  <name>Task 1: RED audit contract + tripwire</name>
  <action>Add custom/grocy_AI/tests/conversion_audit.php (registered) asserting, RED first: against a DB with only global conversions the audit PASSES and reports the global count; against a DB seeded with one product_id NOT NULL conversion the audit FAILS (tripwire) and names the offending product; the audit issues only SELECTs (zero-write, assert via a diff/total_changes check).</action>
  <verify><automated>php8.5 custom/grocy_AI/tests/run.php 2>&1 | rg -i "conversion_audit" ; echo "expected: present and RED"</automated></verify>
  <acceptance_criteria>
    - Encodes pass-on-zero, trip-on-product-scoped, and zero-write.
    - Registered and failing before the audit exists.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: audit-conversions.php (green, read-only)</name>
  <action>Implement custom/grocy_AI/bin/audit-conversions.php: SELECT count of product-specific conversions; if > 0 print each offending row and exit non-zero; else summarize the global conversions and exit 0. Read-only only — no INSERT/UPDATE/DELETE anywhere. Turn Task 1 GREEN.</action>
  <verify><automated>php8.5 -l custom/grocy_AI/bin/audit-conversions.php && php8.5 custom/grocy_AI/tests/run.php 2>&1 | rg -i "conversion_audit"</automated></verify>
  <acceptance_criteria>
    - Passes on the current shape (0 product-specific), exits non-zero if any appear.
    - Provably zero-write; the 62 globals are left untouched.
  </acceptance_criteria>
</task>
</tasks>

<success_criteria>
- DATA-03/04/05 closed by verification: 0 product-specific conversions confirmed, globals untouched, no deletion code built.
- A durable tripwire catches any future reappearance of product-specific conversions.
</success_criteria>

<output>Create 06-06-SUMMARY.md recording the audit result (0 product-specific, 62 global) and the tripwire behavior.</output>
