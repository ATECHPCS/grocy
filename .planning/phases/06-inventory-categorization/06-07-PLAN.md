---
phase: 06-inventory-categorization
plan: "07"
type: execute
wave: 5
depends_on: ["06-02", "06-04", "06-05", "06-06"]
requirements: [DATA-01, DATA-02, DATA-06, DATA-07]
must_haves:
  truths:
    - "The bulk-review UI can generate, review (conflicts-first), apply, and roll back all three Phase 6 passes (product-group, classification, conversion audit) reusing the existing bulk-review surface."
    - "The full Phase 6 acceptance runs on the local snapshot: group -> classify -> audit, each proven DATA-06 zero-diff and DATA-07 rollback + zero-diff rerun."
    - "A browser test covers the group-suggestion and conflict-first classification review flows."
  artifacts:
    - path: "custom/grocy_AI/views"
      provides: "Bulk-review UI surfaces the suggest_product_group operation + conflict-first ordering; reuses the existing review/apply/rollback controls"
    - path: "custom/grocy_AI/tests/browser/specs/categorization.spec.js"
      provides: "Browser coverage: generate group plan, review conflicts-first classification, apply, rollback (deterministic fixtures)"
    - path: ".planning/phases/06-inventory-categorization/06-ACCEPTANCE.md"
      provides: "Recorded end-to-end snapshot run: per-pass DATA-06 checksum result + DATA-07 rollback/rerun evidence"
---

## Objective
Put the three passes in front of the user on the existing bulk-review page and record the full-phase acceptance on production-shaped data.

## Facts (verified)
Bulk API + UI already exist (`/bulk/plans` generate/apply/rollback/audit routes; bulk-review page). The `suggest_product_group` operation (06-04) and conflict-first ordering (06-05) need surfacing; no new engine routes required beyond what generation/operation selection needs.

<tasks>
<task type="execute">
  <name>Task 1: Surface the three passes in bulk-review</name>
  <action>Extend the bulk-review UI so the user can generate each pass (product-group suggestion, classification, conversion audit-report), see conflict-first ordering + confidence bands, select/deselect, apply, and roll back — reusing the existing review/apply/rollback controls and export. The conversion audit surfaces as a read-only report (no apply). Keep all writes behind the existing STOCK/MASTER_DATA gates.</action>
  <verify><automated>php8.5 custom/grocy_AI/tests/run.php 2>&1 | rg -i "bulk|taxonomy|group" | rg -iv fail</automated></verify>
  <acceptance_criteria>
    - All three passes are generatable + reviewable from the bulk-review page; conversion pass is report-only.
    - Conflict-first order and confidence bands are visible; apply/rollback reuse existing controls.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Browser coverage</name>
  <action>Add custom/grocy_AI/tests/browser/specs/categorization.spec.js (deterministic fixtures, deny-by-default server like the other specs): generate a group-suggestion plan and apply/rollback; open a classification plan and assert conflicts render before low-confidence before confident; assert Unclassified items are retained/deselected. Keep the full browser matrix green.</action>
  <verify><automated>cd custom/grocy_AI/tests/browser && (npx playwright test specs/categorization.spec.js 2>&1 | tail -5 || echo "run in browser harness")</automated></verify>
  <acceptance_criteria>
    - New spec passes; existing browser matrix stays green (no regressions).
    - Review ordering and Unclassified retention are asserted in-browser.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: Full-phase acceptance on the snapshot</name>
  <action>On the 06-01 snapshot, run the end-to-end sequence — group-suggestion apply, classification apply, conversion audit — capturing verify-inventory-diff before/after each pass (DATA-06: only approved tables/columns changed) and a rollback + zero-diff rerun for the two writing passes (DATA-07). Record commands, counts (ungrouped before/after; confident classifications before/after), and diff results in 06-ACCEPTANCE.md. If the snapshot is unavailable, record the blocker and mark acceptance pending rather than fabricating results.</action>
  <verify><automated>test -f .planning/phases/06-inventory-categorization/06-ACCEPTANCE.md && rg -i "DATA-06|DATA-07|rollback|diff" .planning/phases/06-inventory-categorization/06-ACCEPTANCE.md</automated></verify>
  <acceptance_criteria>
    - Each writing pass shows approved-only diffs and exact rollback + zero-diff rerun on real data.
    - Ungrouped count drops and confident-classification count rises, both recorded.
  </acceptance_criteria>
</task>
</tasks>

<success_criteria>
- DATA-01/02: all three passes reviewable + applyable from the UI; conflicts-first; Unclassified retained.
- DATA-06/07: full-phase run on production-shaped data proves approved-only changes and exact rollback + zero-diff rerun.
</success_criteria>

<output>Create 06-07-SUMMARY.md linking 06-ACCEPTANCE.md and summarizing the UI surface + browser coverage.</output>
