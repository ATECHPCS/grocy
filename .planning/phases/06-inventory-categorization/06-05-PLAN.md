---
phase: 06-inventory-categorization
plan: "05"
type: execute
wave: 4
depends_on: ["06-04"]
requirements: [DATA-01, DATA-02]
must_haves:
  truths:
    - "The classification plan orders review conflicts first, then low-confidence, then confident, and leaves below-threshold products Unclassified."
    - "A conflict is defined as: the suggested classification contradicts the existing product-group signal, OR two candidate leaf types score within the tie margin."
    - "Re-running classification after the group-suggestion pass reclassifies newly-grouped products with stronger evidence, and the whole pass stays reviewable and reversible on the snapshot."
  artifacts:
    - path: "custom/grocy_AI/src/GrocyAiTaxonomyService.php"
      provides: "Conflict detection (suggestion vs existing group signal; candidate tie margin) + confidence banding surfaced on ReadProductTaxonomy/ValidateInventoryTaxonomy"
    - path: "custom/grocy_AI/src/GrocyAiBulkService.php"
      provides: "Review ordering for the taxonomy plan: conflicts -> low-confidence -> confident; below-threshold items emitted as set_unclassified/deselected"
    - path: "custom/grocy_AI/tests/taxonomy.php"
      provides: "Extend with conflict-first ordering, tie-margin, threshold->Unclassified, and post-grouping re-run assertions"
---

## Objective
Harden the *existing* classification bulk pass to satisfy DATA-02's "review conflicting and low-confidence first," and prove it improves after grouping. The apply/rollback plumbing already exists (`assign_taxonomy_leaf`/`set_unclassified`); this plan is scoring/ordering + real-data confirmation, not new write code.

## Facts (verified)
`GeneratePlan` already emits taxonomy items with reason/provenance and pre-selects only changed items; `ValidateInventoryTaxonomy` already buckets excluded/unclassified/low_confidence/conflicting. This plan makes the `conflicting` and `low_confidence` bands drive review ORDER and makes conflict detection explicit.

<tasks>
<task type="execute">
  <name>Task 1: RED conflict + ordering contract</name>
  <action>Extend custom/grocy_AI/tests/taxonomy.php (RED first) asserting: conflict when suggested leaf's implied group differs from the product's existing Grocy group signal (e.g. group Seafood vs suggested Dairy); conflict when top-2 candidate leaf types are within the tie margin; below-threshold confidence yields set_unclassified (Unclassified retained), never a forced leaf; the generated plan's review order is conflicts -> low-confidence -> confident; and a product newly grouped by 06-04 moves from unclassified to a confident suggestion on re-run.</action>
  <verify><automated>php8.5 custom/grocy_AI/tests/run.php 2>&1 | rg -i "taxonomy" ; echo "expected: new conflict/ordering assertions RED"</automated></verify>
  <acceptance_criteria>
    - Encodes both conflict forms, threshold->Unclassified, review ordering, and post-grouping improvement.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Conflict detection + confidence banding (green)</name>
  <action>Implement conflict detection in GrocyAiTaxonomyService (suggestion-vs-existing-group contradiction; candidate tie margin as a named constant) and surface a stable confidence band + conflict flag on the read path. Do not change AssignProductTaxonomy's write shape.</action>
  <verify><automated>php8.5 -l custom/grocy_AI/src/GrocyAiTaxonomyService.php && php8.5 custom/grocy_AI/tests/run.php 2>&1 | rg -i "taxonomy"</automated></verify>
  <acceptance_criteria>
    - Conflict + band are deterministic and derived only from stored evidence + group signal.
    - Write path unchanged; classification still lands in grocy_ai_taxonomy_classifications only.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: Review ordering + Unclassified emission (green) + snapshot re-run</name>
  <action>Make the taxonomy plan generator order items conflicts -> low-confidence -> confident and emit below-threshold products as deselected set_unclassified (Unclassified retained). On the 06-01 snapshot (skip if absent), run: group-suggestion apply -> classification generate -> assert more products now score confident than before grouping, and the plan applies + rolls back clean (verify-inventory-diff: only taxonomy + bulk tables change).</action>
  <verify><automated>php8.5 custom/grocy_AI/tests/run.php 2>&1 | rg -i "taxonomy|ordering|unclassified"</automated></verify>
  <acceptance_criteria>
    - Review order is conflicts-first; below-threshold stays Unclassified and deselected.
    - Post-grouping re-run measurably increases confident classifications; apply/rollback clean on the snapshot.
  </acceptance_criteria>
</task>
</tasks>

<success_criteria>
- DATA-02: conflicts and low-confidence surface first; Unclassified is retained where appropriate; only approved assignments apply.
- DATA-01: classification runs over the in-scope set and improves after grouping, reviewable and reversible.
</success_criteria>

<output>Create 06-05-SUMMARY.md with the conflict definitions, the tie margin/threshold constants, and the before/after grouping classification counts.</output>
