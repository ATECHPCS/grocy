---
phase: 02-enrichment-contract-barcode-handoff-secure-media
plan: "06"
type: execute
wave: 6
depends_on: ["02-05"]
files_modified:
  - .planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-ENVIRONMENT-PREFLIGHT.md
  - custom/grocy_AI/tests/run.php
  - custom/grocy_AI/tests/browser/fixtures/productform.html
  - custom/grocy_AI/tests/browser/support/server.mjs
  - custom/grocy_AI/tests/browser/specs/secure-media.spec.js
  - /Users/ian/Documents/Repos/grocy-mcp/tests/test_enrichment_contract.py
  - /Users/ian/Documents/Repos/grocy-mcp/tests/test_secure_media.py
autonomous: true
requirements: [ENR-07, ENR-08, ENR-09]
must_haves:
  truths:
    - "The first user-visible media happy path is an intentional named RED: structured front candidate, explicit thumbnail/full actions, transient selection, zero persistence."
    - "Every SSRF, redirect, timeout, byte, content, decoder, handle, authorization, and redaction boundary has a direct deterministic case."
  artifacts:
    - path: "/Users/ian/Documents/Repos/grocy-mcp/tests/test_secure_media.py"
      provides: "Injected resolver/peer/clock/stream adversarial matrix"
    - path: "custom/grocy_AI/tests/browser/specs/secure-media.spec.js"
      provides: "Same-origin demand-load and transient image behavior"
  key_links:
    - from: "resolved approved address set"
      to: "actual connected peer"
      via: "test transport exposes peer identity and rejects mismatch"
      pattern: "peer"
---

## Phase Goal

**As a** Grocy user, **I want to** review trustworthy structured suggestions, hand off barcodes without duplicates, and select real package images, **so that** enrichment cannot persist hidden changes or access media unsafely.

<objective>
Specify secure media completely before changing network or proxy implementation.

Purpose: Make every HIGH media threat directly executable and distinguish expected RED assertions from infrastructure failure.
Output: Companion, Grocy, and browser adversarial tests plus confirmed locked bounds.
</objective>

<execution_context>
@/Users/ian/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ian/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-05-SUMMARY.md
@.planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-RESEARCH.md
@.planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-VALIDATION.md
@.planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-UI-SPEC.md
@custom/grocy_AI/tests/assert-expected-red.sh
@/Users/ian/Documents/Repos/grocy-mcp/constraints-phase2.txt

<interfaces>
Locked media values per R-02/R-03: two redirects, no downgrade, 2-second connect, 12-second total, 2,000–3,000,000 bytes, JPEG/PNG/WebP, dimensions 32–4096, <=16MP, 900-second TTL, 512 handles, distinct thumbnail/full handles.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create exact companion and Grocy media boundary tests</name>
  <files>.planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-ENVIRONMENT-PREFLIGHT.md, custom/grocy_AI/tests/run.php, /Users/ian/Documents/Repos/grocy-mcp/tests/test_enrichment_contract.py, /Users/ian/Documents/Repos/grocy-mcp/tests/test_secure_media.py</files>
  <behavior>
    - Reject non-HTTP(S), userinfo/fragments/nonstandard ports, every blocked IPv4/IPv6 class, mixed answers, rebind/peer mismatch, redirect loop/third hop/downgrade.
    - Reject connect/total timeout, oversized Content-Length before body, streamed overflow at first excess byte, below-minimum, MIME/signature mismatch, malformed/zero/out-of-range dimensions, and >16MP.
    - Unknown/expired/wrong-variant handles and unauthorized requests fail before resolver/fetch work.
  </behavior>
  <action>Create deterministic injected resolver, actual-peer, clock, and streamed-transport tests with no live DNS/network. Assert exact boundary inclusivity and response headers. Recheck the redacted fixture corpus against locked R-02/R-03 and record only counts/maxima; an incompatible fixture blocks and must not widen limits. Add exact RED markers for missing peer binding, stream abort, decoded pixel validation, and auth-before-work. Define the exact companion class/method names `SecureMediaTests.test_peer_binding` and `SecureMediaTests.test_stream_overflow`. Syntax/import/discovery must succeed from `/Users/ian/Documents/Repos/grocy-mcp` before `assert-expected-red.sh` runs each named case through its `--cwd` interface; the wrapper must verify that cwd before collection so an import or checkout error cannot be mistaken for intentional RED.</action>
  <verify>
    <automated>(cd /Users/ian/Documents/Repos/grocy-mcp && .venv/bin/python -m py_compile tests/test_secure_media.py && .venv/bin/python -c 'import importlib; module = importlib.import_module("tests.test_secure_media"); case = getattr(module, "SecureMediaTests"); assert callable(getattr(case, "test_peer_binding")); assert callable(getattr(case, "test_stream_overflow"))') && php -l custom/grocy_AI/tests/run.php && custom/grocy_AI/tests/assert-expected-red.sh 'EXPECTED_RED: media.peer_binding' --cwd /Users/ian/Documents/Repos/grocy-mcp -- .venv/bin/python -m unittest tests.test_secure_media.SecureMediaTests.test_peer_binding && custom/grocy_AI/tests/assert-expected-red.sh 'EXPECTED_RED: media.stream_overflow' --cwd /Users/ian/Documents/Repos/grocy-mcp -- .venv/bin/python -m unittest tests.test_secure_media.SecureMediaTests.test_stream_overflow && custom/grocy_AI/tests/assert-expected-red.sh 'EXPECTED_RED: media.pixel_limit' -- php custom/grocy_AI/tests/run.php --case media.pixel_limit</automated>
  </verify>
  <done>Every network/content/handle/auth boundary is syntax-valid and fails only at its named missing assertion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the explicit same-origin media happy-path RED</name>
  <files>custom/grocy_AI/tests/browser/fixtures/productform.html, custom/grocy_AI/tests/browser/support/server.mjs, custom/grocy_AI/tests/browser/specs/secure-media.spec.js</files>
  <behavior>
    - Structured front image appears before a separate `Unverified search alternatives` group.
    - Zero image requests occur before `Load thumbnail`; thumbnail and full variants each require their own user action.
    - Full bytes remain a transient File until final staging; candidate failure preserves fields, barcode, native picture, Save controls, and zero durable writes.
    - DOM/JSON/console/diagnostics contain no external URL, raw handle, secret, or payload canary.
  </behavior>
  <action>Extend the existing real production asset fixture and loopback server, not a disconnected mock UI. Add exact request counters, object-URL lifecycle, auth denial, expiry/wrong-variant, cancellation/stale cases, and one named `EXPECTED_RED: media.same_origin_happy_path`. Run `node --check` and Playwright `--list` first; then require only that named assertion failure.</action>
  <verify>
    <automated>node --check custom/grocy_AI/tests/browser/support/server.mjs && node --check custom/grocy_AI/tests/browser/specs/secure-media.spec.js && npm --prefix custom/grocy_AI/tests/browser test -- --list --grep '@enr07|@enr08|@enr09' && custom/grocy_AI/tests/assert-expected-red.sh 'EXPECTED_RED: media.same_origin_happy_path' -- npm --prefix custom/grocy_AI/tests/browser test -- --project=chromium-mobile --grep '@enr08 same-origin media happy path'</automated>
  </verify>
  <done>The user-visible media slice and every failure-preservation rule are intentional RED with infrastructure green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| URL/DNS answer → actual peer | Rebinding can cross from public resolution to private connection. |
| Stream → memory/decoder | Bytes and decoded dimensions are attacker-controlled. |
| Handle → candidate bytes | Guessing, expiry, replay, and variant confusion must fail generically. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-06-01 | Spoofing/Information Disclosure | SSRF peer | mitigate | Per-hop resolution plus actual-peer membership tests. |
| T-02-06-02 | Denial of Service | Stream/decoder | mitigate | Exact deadline/byte/dimension/pixel boundary fixtures. |
| T-02-06-03 | Elevation of Privilege | Media routes | mitigate | Auth-before-handle/resolver counters. |
| T-02-06-04 | Tampering | Hidden file staging | mitigate | Browser native-file and mutation counters before final stage/Save. |
</threat_model>

<verification>
All named RED commands must fail for the named assertion only; unrelated suite/import/auth/fixture failures block.
</verification>

<success_criteria>
- R-02/R-03 boundaries are exact and directly tested.
- No live network is used.
- Secure-media implementation has a complete three-tier acceptance target.
</success_criteria>

<output>
Create `.planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-06-SUMMARY.md` when done.
</output>
