---
phase: 02-enrichment-contract-barcode-handoff-secure-media
plan: "12"
type: execute
wave: 12
depends_on: ["02-11"]
files_modified:
  - .planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-RELEASE-MANIFEST.md
  - .planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-PHASE1-BASELINE.sha256
  - custom/grocy_AI/tests/release-gate.sh
  - custom/grocy_AI/tests/deployment-gate.sh
autonomous: true
requirements: [ENR-01, ENR-02, ENR-03, ENR-04, ENR-05, ENR-06, ENR-07, ENR-08, ENR-09]
must_haves:
  truths:
    - "Release approval derives from direct clean-branch, ancestry, exact commit-scope, blob-parity, and test assertions."
    - "The immutable main candidate remains an ancestor of current main, and every later committed or dirty path is confined to the gate mode's closed allowlist."
    - "Deployment approval derives from live revision/digest/marker, mount, protected fingerprint, auth, and rollback comparisons—not evidence-table PASS text."
    - "Exactly the Phase 1 phone acceptance and evidence/phone-timings artifacts have a recorded, verified byte baseline before Phase 2 deployment or acceptance."
  artifacts:
    - path: "custom/grocy_AI/tests/release-gate.sh"
      provides: "Fail-closed three-repository Git and suite gate"
    - path: "custom/grocy_AI/tests/deployment-gate.sh"
      provides: "Fail-closed live identity/mount/fingerprint/auth/rollback gate"
    - path: ".planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-PHASE1-BASELINE.sha256"
      provides: "Exactly two recorded hashes for 01-PHONE-ACCEPTANCE.md and evidence/phone-timings.jsonl"
  key_links:
    - from: "02-RELEASE-MANIFEST.md immutable fields"
      to: "Git and live container state"
      via: "scripts parse values then compare system state directly"
      pattern: "release-gate|deployment-gate"
---

## Phase Goal

**As a** Grocy user, **I want to** review trustworthy structured suggestions, hand off barcodes without duplicates, and select real package images, **so that** enrichment cannot persist hidden changes or access media unsafely.

<objective>
Create executable release/deployment evidence gates before any deployment.

Purpose: Eliminate self-attested PASS rows and preserve independent HIGH-threat verification.
Output: Release gate, deployment gate, immutable Phase 1 baseline hash, and finalized candidate manifest.
</objective>

<execution_context>
@/Users/ian/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ian/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-11-SUMMARY.md
@.planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-RELEASE-MANIFEST.md
@.planning/phases/01-safety-baseline-mobile-diagnostics/01-PHONE-ACCEPTANCE.md
@.planning/phases/01-safety-baseline-mobile-diagnostics/evidence/phone-timings.jsonl
@custom/grocy_AI/portable-files.txt
@custom/grocy_AI/phase2-changed-paths.txt
@custom/grocy_AI/tests/check-portable-parity.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement direct immutable release assertions</name>
  <files>custom/grocy_AI/tests/release-gate.sh, .planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-RELEASE-MANIFEST.md</files>
  <action>Create a POSIX/Bash fail-closed gate with explicit modes `candidate|predeploy|evidence`. Parse only full 40-hex immutable `main_candidate_sha`, companion candidate, stable portable candidate, and stable adapter candidate SHAs plus closed path allowlists from the manifest. In every mode, require documented branch names, candidate object existence/type commit, `git merge-base --is-ancestor <main_candidate_sha> HEAD`, exact companion HEAD equality, exact stable adapter HEAD equality, adapter parent exactly stable portable SHA, the stable portable commit diff exactly `phase2-changed-paths.txt`, the adapter commit diff exactly its explicit adapter allowlist, and zero byte mismatch across the full `portable-files.txt` parity manifest—including unchanged `GrocyAiDiagnostic.php`. Replace mutable main HEAD equality with two fail-closed checks: every committed main path after the immutable candidate must belong to the manifest's exact post-candidate gate/evidence allowlist, and current dirty paths must be a subset of the selected mode's exact allowlist with no untracked-directory collapsing. `candidate` permits only the currently created manifest/release-gate paths; `predeploy` requires no dirty paths; `evidence` permits only the current Phase 2 deployment/acceptance evidence paths. All modes retain synchronized module/cache markers, no lockfile/dependency scope outside recorded companion constraints, and PHP/Python/Chromium/WebKit suites. Do not read or grep manifest PASS/FAIL rows as authority. Print only closed assertion names and final status; no secrets or household values.</action>
  <verify>
    <automated>bash -n custom/grocy_AI/tests/release-gate.sh && custom/grocy_AI/tests/release-gate.sh candidate .planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-RELEASE-MANIFEST.md</automated>
  </verify>
  <done>One executable gate independently proves clean refs, exact ancestry/scope/parity, and test status.</done>
</task>

<task type="auto">
  <name>Task 2: Implement direct deployment comparisons and Phase 1 byte baseline</name>
  <files>custom/grocy_AI/tests/deployment-gate.sh, .planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-PHASE1-BASELINE.sha256</files>
  <action>Record SHA-256 using repository-root-relative paths for exactly these two Phase 1 evidence artifacts and no others: `.planning/phases/01-safety-baseline-mobile-diagnostics/01-PHONE-ACCEPTANCE.md` and `.planning/phases/01-safety-baseline-mobile-diagnostics/evidence/phone-timings.jsonl`; do not modify either source file. Require the baseline manifest to contain exactly two entries with those paths, verify both bytes with `shasum -a 256 -c` before deployment and in every later deployment-gate mode, and preserve the acceptance artifact's `SKIPPED — NOT ACCEPTED` semantics—the hash is immutability proof, never retroactive acceptance. Create a fail-closed deployment gate accepting exactly `candidate|predeploy|postdeploy-companion|postdeploy-stable|postsmoke|final` modes plus manifest/evidence paths. Reuse the release gate's immutable ancestry and exact path-set rules. `candidate` permits only the currently created deployment-gate and Phase 1 baseline paths; `predeploy` requires a clean worktree and validates rollback/mount/protected baselines before replacement; each postdeploy/postsmoke/final mode permits only the explicit current deployment/acceptance evidence paths and rejects every unrelated committed or uncommitted path. It must use the established SSH identity without printing credentials and directly compare: recorded candidate SHAs to OCI revision labels; recorded image digests to running container image IDs/digests; stable cache/module marker to the running file and served asset query; exactly one container per port; `/etc/komodo/grocy` source mounted at `/config` with RW=true; protected table/file aggregate hashes before versus after; migration/index presence with no row mutation; authenticated contract/owner/thumbnail/full closed outcomes; unauthenticated 401/403 outcomes with zero-work fixture counters; prior image/config rollback identifiers resolvable; and the exact two-entry Phase 1 baseline. Credentials/GTIN/handles remain process-local. Evidence prose may be written after comparisons, but is never the tested authority.</action>
  <verify>
    <automated>baseline=.planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-PHASE1-BASELINE.sha256 && expected=$(printf '%s\n' '.planning/phases/01-safety-baseline-mobile-diagnostics/01-PHONE-ACCEPTANCE.md' '.planning/phases/01-safety-baseline-mobile-diagnostics/evidence/phone-timings.jsonl') && actual=$(awk '{print $2}' "$baseline" | LC_ALL=C sort) && test "$(awk 'END { print NR }' "$baseline")" -eq 2 && test "$actual" = "$expected" && bash -n custom/grocy_AI/tests/deployment-gate.sh && shasum -a 256 -c "$baseline" && custom/grocy_AI/tests/deployment-gate.sh candidate .planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-RELEASE-MANIFEST.md</automated>
  </verify>
  <done>Deployment and Phase 1 immutability are directly testable before the first container replacement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Evidence prose → release decision | Self-authored PASS text is not trustworthy evidence. |
| Recorded Git SHA → running image | Mutable tags/labels can diverge from deployed bytes. |
| Container restart → persistent data | Mount or schema drift can mutate household state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-12-01 | Spoofing/Repudiation | Release identity | mitigate | Full SHA, parent, exact path-set, and clean-worktree comparisons. |
| T-02-12-02 | Spoofing | Running containers | mitigate | Live OCI revision/image digest/marker comparison. |
| T-02-12-03 | Tampering | Persistent data/Phase 1 evidence | mitigate | Before/after protected hashes and baseline checksum verification. |
| T-02-12-04 | Elevation of Privilege | Routes | mitigate | Authenticated and unauthenticated outcomes with zero-work assertions. |
</threat_model>

<verification>
Run both scripts in `candidate` mode; introduce a temporary wrong expected SHA, post-candidate committed path, and unrelated dirty path through isolated arguments/fixtures and confirm each gate exits nonzero before restoring the test input. Then prove `predeploy` accepts only clean state and `evidence`/postdeploy modes accept only their exact evidence allowlists. Do not mutate live state for negative checks.
</verification>

<success_criteria>
- All release/deployment HIGH claims are directly executable.
- No evidence-table PASS grep remains authoritative.
- Exactly `01-PHONE-ACCEPTANCE.md` and `evidence/phone-timings.jsonl` are byte-baselined and reverified without changing `SKIPPED — NOT ACCEPTED` semantics.
</success_criteria>

<output>
Create `.planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-12-SUMMARY.md` when done.
</output>
