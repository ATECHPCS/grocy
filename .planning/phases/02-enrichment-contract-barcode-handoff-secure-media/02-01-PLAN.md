---
phase: 02-enrichment-contract-barcode-handoff-secure-media
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-ENVIRONMENT-PREFLIGHT.md
  - custom/grocy_AI/tests/assert-expected-red.sh
  - custom/grocy_AI/tests/fixtures/enrichment-v2-cases.json
  - custom/grocy_AI/tests/run.php
  - custom/grocy_AI/tests/browser/fixtures/productform.html
  - custom/grocy_AI/tests/browser/specs/contract-review.spec.js
  - /Users/ian/Documents/Repos/grocy-mcp/tests/test_enrichment_contract.py
  - /Users/ian/Documents/Repos/grocy-mcp/constraints-phase2.txt
autonomous: true
requirements: [ENR-01, ENR-05, ENR-09]
must_haves:
  truths:
    - "The first end-to-end contract-v2 name-review happy path exists as a named intentional RED test before implementation."
    - "Repeated top-level and nested raw JSON keys are rejected before ordinary decoding can collapse them."
    - "Brand, package-size, food-type, barcode-collision, media-bound, and dependency decisions are rechecked from closed read-only facts without exposing household values."
  artifacts:
    - path: "custom/grocy_AI/tests/assert-expected-red.sh"
      provides: "Fail-closed named RED assertion gate"
    - path: "custom/grocy_AI/tests/fixtures/enrichment-v2-cases.json"
      provides: "Raw valid/adversarial v2 documents including duplicate members"
    - path: ".planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-ENVIRONMENT-PREFLIGHT.md"
      provides: "Redacted execution-time confirmation of R-01 through R-07"
  key_links:
    - from: "raw JSON fixture bytes"
      to: "future GrocyAiContract::DecodeAndValidateRaw"
      via: "fixtures retain repeated keys as raw strings, never decoded fixture objects"
      pattern: "duplicate_(top_level|nested)"
---

## Phase Goal

**As a** Grocy user, **I want to** review trustworthy structured suggestions, hand off barcodes without duplicates, and select real package images, **so that** enrichment cannot persist hidden changes or access media unsafely.

<objective>
Lock Phase 2's resolved runtime facts and create trustworthy RED contract tests before production changes.

Purpose: Prevent JSON parser ambiguity, destination drift, floating companion dependencies, or accidental scope expansion from entering implementation.
Output: Named RED harness, raw duplicate-key fixtures, first browser contract slice, dependency constraints, and redacted preflight evidence.
</objective>

<execution_context>
@/Users/ian/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ian/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-CONTEXT.md
@.planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-RESEARCH.md
@.planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-VALIDATION.md
@.planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-UI-SPEC.md
@custom/grocy_AI/tests/run.php
@custom/grocy_AI/tests/browser/specs/happy-path.spec.js
@/Users/ian/Documents/Repos/grocy-mcp/pyproject.toml

<interfaces>
Resolved facts are R-01 through R-07 in 02-RESEARCH.md. `assert-expected-red.sh EXPECTED_MARKER [--cwd ABSOLUTE_DIRECTORY] -- command...` must verify and enter the requested directory before execution, accept only the named assertion failure, reject exit codes other than the test runner's assertion-failure code, and reject parse/import/fixture/auth/browser-launch/dependency failures.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the failing contract-v2 name-review slice and raw duplicate-key matrix</name>
  <files>custom/grocy_AI/tests/assert-expected-red.sh, custom/grocy_AI/tests/fixtures/enrichment-v2-cases.json, custom/grocy_AI/tests/run.php, custom/grocy_AI/tests/browser/fixtures/productform.html, custom/grocy_AI/tests/browser/specs/contract-review.spec.js, /Users/ian/Documents/Repos/grocy-mcp/tests/test_enrichment_contract.py</files>
  <behavior>
    - Named happy path: a valid integer version-2 direct structured name suggestion renders current/suggested values and complete provenance, selected only for blank current content per D-01 through D-04.
    - Raw JSON with repeated `contract_version`, repeated nested source members, or escaped-equivalent repeated names is rejected wholesale; the fixture loader must preserve raw bytes until Grocy validation.
    - Wrong version, unknown/missing/type-invalid fields, raw URLs, and nutrition/allergen/dietary/medical members produce only `contract_invalid` and zero mutation calls.
  </behavior>
  <action>Write syntax-valid PHP, Python, and Playwright tests before production code. Keep adversarial documents as literal raw JSON strings so no test helper collapses duplicate members. In the companion test module, use the exact discoverable name `EnrichmentContractTests.test_v2_name_review` and make its assertion emit the exact standalone marker `EXPECTED_RED: contract.python_v2_name_review`. Add the browser/PHP markers `EXPECTED_RED: contract.v2_name_review`, `EXPECTED_RED: contract.duplicate_top_level`, and `EXPECTED_RED: contract.duplicate_nested`. Build `assert-expected-red.sh` with `mktemp`, independent command execution, exact standalone marker matching, and optional `--cwd ABSOLUTE_DIRECTORY`; the wrapper must reject a missing/non-directory cwd before execution, enter that cwd itself, and explicitly reject syntax/import/discovery/fixture-server/auth/browser-launch/dependency errors rather than treating arbitrary nonzero status as success. Run PHP lint, a companion import plus named-test discovery smoke from the grocy-mcp checkout, `node --check`, and Playwright `--list` separately before expected-RED assertions. Nutrition Facts remains rejection-only per the deferred fence.</action>
  <verify>
    <automated>php -l custom/grocy_AI/tests/run.php && (cd /Users/ian/Documents/Repos/grocy-mcp && .venv/bin/python -m py_compile tests/test_enrichment_contract.py && .venv/bin/python -c 'import importlib; module = importlib.import_module("tests.test_enrichment_contract"); case = getattr(module, "EnrichmentContractTests"); test = getattr(case, "test_v2_name_review"); assert callable(test)') && custom/grocy_AI/tests/assert-expected-red.sh 'EXPECTED_RED: contract.python_v2_name_review' --cwd /Users/ian/Documents/Repos/grocy-mcp -- .venv/bin/python -m unittest tests.test_enrichment_contract.EnrichmentContractTests.test_v2_name_review && node --check custom/grocy_AI/tests/browser/specs/contract-review.spec.js && npm --prefix custom/grocy_AI/tests/browser test -- --list --grep '@enr01|@enr05|@enr09' && custom/grocy_AI/tests/assert-expected-red.sh 'EXPECTED_RED: contract.v2_name_review' -- npm --prefix custom/grocy_AI/tests/browser test -- --project=chromium-mobile --grep '@enr01 contract-v2 name review' && custom/grocy_AI/tests/assert-expected-red.sh 'EXPECTED_RED: contract.duplicate_top_level' -- php custom/grocy_AI/tests/run.php --case contract.duplicate_top_level && custom/grocy_AI/tests/assert-expected-red.sh 'EXPECTED_RED: contract.duplicate_nested' -- php custom/grocy_AI/tests/run.php --case contract.duplicate_nested</automated>
  </verify>
  <done>The happy path and duplicate-key attacks fail only at named missing behavior, while infrastructure errors remain blocking.</done>
</task>

<task type="auto">
  <name>Task 2: Reconfirm resolved deployment facts without mutation</name>
  <files>.planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-ENVIRONMENT-PREFLIGHT.md, /Users/ian/Documents/Repos/grocy-mcp/constraints-phase2.txt</files>
  <action>Use the established read-only SSH/container path and write only allowlisted facts. Per R-04/R-05, require active `products.brand` type `text-single-line`, no package-size destination, and no food-type destination; any drift blocks target wiring and must not create a field. Per R-06, run the exact future checksum-valid canonical expression against production and record only counts/hash; nonzero collision groups block migration with no row output or cleanup. Per R-02/R-03, record the fixed two-redirect, 32–4096, 16MP, 2KB–3MB, JPEG/PNG/WebP decision and confirm deterministic fixtures fit; incompatibility blocks rather than widening. Per R-07, export sorted installed distribution names/versions from the running companion, require Python 3.12.13, HTTPX 0.28.1, Starlette 1.6.0, and Uvicorn 0.52.1, write the complete non-secret constraints file, and record its SHA-256. Do not record product/userfield values, barcodes, URLs, handles, credentials, headers, or payloads.</action>
  <verify>
    <automated>rg -n '^## Resolved preflight|brand_target: products.brand:text-single-line|package_size_target: none|food_type_target: none|canonical_collision_groups: 0|redirect_limit: 2|dimension_range: 32-4096|pixel_limit: 16000000|dependency_constraints_sha256: [0-9a-f]{64}$' .planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-ENVIRONMENT-PREFLIGHT.md && rg -n '^httpx==0\.28\.1$|^starlette==1\.6\.0$|^uvicorn==0\.52\.1$' /Users/ian/Documents/Repos/grocy-mcp/constraints-phase2.txt && test "$(LC_ALL=C sort /Users/ian/Documents/Repos/grocy-mcp/constraints-phase2.txt | shasum -a 256 | awk '{print $1}')" = "$(sed -n 's/^dependency_constraints_sha256: //p' .planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-ENVIRONMENT-PREFLIGHT.md)"</automated>
  </verify>
  <done>All formerly open decisions are confirmed or execution stops fail-closed before implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Raw companion bytes → JSON decoder | Duplicate object names must be detected before lossy decoding. |
| Production metadata → planning artifact | Read-only facts must be allowlisted and household values excluded. |
| Running dependency set → reproducible build | Floating lower bounds cannot silently change HTTP behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-01-01 | Tampering | Raw JSON | mitigate | Preserve literal fixtures and require nested duplicate-aware rejection before decode. |
| T-02-01-02 | Information Disclosure | Preflight | mitigate | Record only destination names/types, aggregate counts/hashes, bounds, and package versions. |
| T-02-01-03 | Supply-chain Tampering | Companion constraints | mitigate | Capture and hash the deployed package set; no install occurs in this plan. |
</threat_model>

<verification>
Run both task commands, confirm every suite was syntax/discovery checked independently, and confirm no product/GTIN/provider value appears in the preflight artifact.
</verification>

<success_criteria>
- Exact named RED failures replace arbitrary nonzero acceptance.
- Top-level and nested duplicate-key attacks survive fixtures and are specified for wholesale rejection.
- R-01 through R-07 are execution-ready and no deferred field is introduced.
</success_criteria>

<output>
Create `.planning/phases/02-enrichment-contract-barcode-handoff-secure-media/02-01-SUMMARY.md` when done.
</output>
