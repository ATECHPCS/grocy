---
phase: 08-purchase-capture-stock-intake
plan: "03"
type: execute
wave: 3
depends_on: ["08-02"]
requirements: [CAP-01, CAP-02, CAP-08]
must_haves:
  truths:
    - "A user can open a mobile capture page, start a trip, and rapid-scan barcodes with immediate per-scan feedback (known/unknown + running quantity), reusing the enrichment card's scan component."
    - "The capture workflow is reachable from a real sidebar menu entry, not only a typed URL."
  artifacts:
    - path: "views/grocyai_capture.blade.php"
      provides: "Mobile scan-loop page bound to the capture endpoints"
    - path: "public/custom/grocy_AI/capture.js"
      provides: "Scan loop: submit barcode, render running trip lines, reuse scanner component"
    - path: "custom/grocy_AI/src/GrocyAiCaptureController.php"
      provides: "Capture page controller (STOCK_PURCHASE) + sidebar menu injection"
---

## Objective
Give the workflow a fast mobile face: a scan-loop page that appends to the live trip with instant known/unknown feedback, discoverable from the sidebar.

<tasks>
<task type="execute">
  <name>Task 1: Capture page + scan loop</name>
  <action>Add views/grocyai_capture.blade.php and public/custom/grocy_AI/capture.js: a big Scan control (reuse the enrichment card's existing camera/scanner path), a manual GTIN input, and a live-updating trip line list showing product name (or "Unknown — needs product") + running quantity. Each scan POSTs to .../scan and renders the returned line. Mobile-first; follow 01-UI-SPEC spacing/typography/touch-target conventions.</action>
  <verify><automated>php8.5 -l custom/grocy_AI/src/GrocyAiCaptureController.php ; node --check public/custom/grocy_AI/capture.js</automated></verify>
  <acceptance_criteria>
    - Scanning appends/increments lines live with correct known/unknown labels.
    - Reuses the existing scanner component rather than a new decoder.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Route + sidebar tile</name>
  <action>Add GET /grocyai/capture → GrocyAiCaptureController::Capture (STOCK_PURCHASE). Inject a sidebar menu entry for "Purchase capture" via the module's core-hook mechanism so it appears in the responsive nav.</action>
  <verify><automated>php8.5 -l routes.php ; rg -n "grocyai/capture|Purchase capture" routes.php views</automated></verify>
  <acceptance_criteria>
    - The page renders for a STOCK_PURCHASE user and is linked from the sidebar.
  </acceptance_criteria>
</task>
</tasks>

<success_criteria>
- Rapid mobile capture works end-to-end against the live queue and is menu-discoverable.
</success_criteria>

<output>Create 08-03-SUMMARY.md with the page/asset/menu references.</output>
