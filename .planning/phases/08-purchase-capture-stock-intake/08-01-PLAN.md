---
phase: 08-purchase-capture-stock-intake
plan: "01"
type: execute
wave: 1
depends_on: []
requirements: [CAP-01, CAP-02, CAP-06, CAP-07, CAP-08]
must_haves:
  truths:
    - "RED contract tests describe trip lifecycle, coalescing, ownership resolution, partial commit, idempotency, and the stock-write boundary before any production code exists."
    - "A namespaced grocy_ai_capture_* schema is created via IF NOT EXISTS with a dedicated migration ledger and never alters/drops a native Grocy table."
  artifacts:
    - path: "custom/grocy_AI/src/GrocyAiCaptureMigration.php"
      provides: "Bootstrap for capture_migrations/trips/lines/audit tables, mirroring GrocyAiBulkMigration"
    - path: "custom/grocy_AI/tests/capture.php"
      provides: "RED unit contract for the capture engine (registered in tests/run.php)"
---

## Objective
Lock the Phase 8 contract with failing tests and stand up the inactive namespaced schema, so every later wave has a red bar to turn green and a durable place to persist trips/lines/audit without touching native tables.

<tasks>
<task type="execute">
  <name>Task 1: RED capture contract tests</name>
  <action>Add custom/grocy_AI/tests/capture.php asserting (all initially failing): trip status vocabulary open/reviewing/committed; same-barcode coalescing increments one line; scan resolves known/unknown via ResolveOwner; commit checksum + per-item applied_at idempotency; partial commit leaves unresolved/deselected lines in the trip; and that no capture/scan/review path writes to stock/stock_log (only CommitTrip does). Register it in tests/run.php.</action>
  <verify><automated>php8.5 custom/grocy_AI/tests/run.php 2>&1 | rg -i "capture" ; echo "expected: capture assertions present and RED"</automated></verify>
  <acceptance_criteria>
    - capture.php is registered and its assertions fail for the not-yet-built engine.
    - Assertions encode coalescing, known/unknown, partial commit, idempotency, and the stock-write boundary.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Inactive namespaced schema + migration</name>
  <action>Add GrocyAiCaptureMigration (VERSION 'v1', Bootstrap with its own grocy_ai_capture_migrations ledger) creating grocy_ai_capture_trips, grocy_ai_capture_lines (with the COALESCE(canonical_gtin, scanned_barcode) unique index), and grocy_ai_capture_audit via CREATE TABLE IF NOT EXISTS with CHECK-constrained closed vocabularies. Mirror GrocyAiBulkMigration exactly; touch no native table.</action>
  <verify><automated>php8.5 -l custom/grocy_AI/src/GrocyAiCaptureMigration.php</automated></verify>
  <acceptance_criteria>
    - Bootstrap is idempotent (IF NOT EXISTS), guarded by a transaction check, and records its version.
    - Schema matches 08-DESIGN.md; no ALTER/DROP of any native table.
  </acceptance_criteria>
</task>
</tasks>

<success_criteria>
- Capture contract tests exist and are RED.
- The namespaced schema/migration lints and follows the bulk pattern.
</success_criteria>

<output>Create 08-01-SUMMARY.md with the RED test list and the schema/migration reference.</output>
