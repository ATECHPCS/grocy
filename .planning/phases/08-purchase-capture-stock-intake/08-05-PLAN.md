---
phase: 08-purchase-capture-stock-intake
plan: "05"
type: execute
wave: 5
depends_on: ["08-04"]
requirements: [CAP-05, CAP-06, CAP-07, CAP-08]
must_haves:
  truths:
    - "Commit writes only selected known lines as one native Grocy purchase transaction sharing a transaction_id, applying the product's purchase→stock factor and any barcode amount override, with best-before auto-computed unless overridden."
    - "Unresolved and deselected lines remain in the trip after a partial commit."
    - "Commit is idempotent and conflict-safe: checksum + per-item applied_at ledger + in-lock ownership re-resolve; a re-tap or interruption cannot double-add; committed trips archive read-only with the transaction_id."
  artifacts:
    - path: "custom/grocy_AI/src/GrocyAiCaptureService.php"
      provides: "ChecksumForTrip + CommitTrip (BEGIN IMMEDIATE native purchase batch)"
    - path: "custom/grocy_AI/src/GrocyAiCaptureController.php"
      provides: "commit endpoint (STOCK_PURCHASE) with confirmed-checksum"
---

## Objective
Commit a reviewed trip to real stock — the first module code that writes stock — through one auditable, idempotent, conflict-safe native purchase batch, leaving anything unresolved behind.

<tasks>
<task type="execute">
  <name>Task 1: Checksum + CommitTrip transaction</name>
  <action>Implement ChecksumForTrip (SHA-256 over selected known lines, mirroring GrocyAiBulkService::ChecksumForPlan) and CommitTrip(tripId, actor, confirmedChecksum): recompute + hash_equals the checksum and refuse before any write; open one raw BEGIN IMMEDIATE; for each selected+known line with null applied_at, re-resolve owner in-lock (changed owner → mark conflict + skip), compute stock amount = quantity × purchase→stock factor (or barcode amount override), call StockService::AddProduct(productId, amount, bestBefore=override|auto, 'purchase', today, price, location, store, &txnId, …, note) sharing one transaction_id, and stamp applied_at; single COMMIT (ROLLBACK on any Throwable). No network calls under lock.</action>
  <verify><automated>php8.5 custom/grocy_AI/tests/run.php 2>&1 | rg -i "commit|idempot|conflict|partial|factor"</automated></verify>
  <acceptance_criteria>
    - Re-running commit is idempotent (already-applied lines skipped); interruption leaves the DB consistent.
    - Deselected/unknown/conflict lines are not written and remain in the trip (partial commit).
    - purchase→stock factor and barcode overrides are applied; a shared transaction_id groups the batch.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Commit endpoint + audit + archive</name>
  <action>Add POST /api/grocy-ai/capture/trips/{id}/commit (STOCK_PURCHASE, confirmed_checksum body). Write append-only audit rows (actor, per-line before/after, transaction_id). When every selected line is applied, set trip status committed + committed_at + transaction_id and make it read-only; otherwise keep it reviewing. Confirm no stock write occurs on any path other than CommitTrip.</action>
  <verify><automated>php8.5 -l custom/grocy_AI/src/GrocyAiCaptureController.php && php8.5 custom/grocy_AI/tests/run.php 2>&1 | rg -i "audit|archive|boundary|stock-write"</automated></verify>
  <acceptance_criteria>
    - Committed trips are immutable and carry the Grocy transaction_id.
    - The stock-write boundary test confirms only CommitTrip touches stock/stock_log.
  </acceptance_criteria>
</task>
</tasks>

<success_criteria>
- Selected known lines land in Grocy stock as one native purchase batch; partial/idempotent/conflict-safe; trip archived.
</success_criteria>

<output>Create 08-05-SUMMARY.md with the commit/idempotency/partial evidence and the boundary attestation.</output>
