---
phase: 08-purchase-capture-stock-intake
plan: "06"
type: execute
wave: 6
depends_on: ["08-05"]
requirements: [CAP-01, CAP-02, CAP-03, CAP-04, CAP-05, CAP-06, CAP-07, CAP-08]
must_haves:
  truths:
    - "The native module suite and a new mobile-browser capture suite pass on the dev host, covering rapid capture/coalescing, review, unknown lifecycle, partial commit, idempotency, and the stock-write boundary."
    - "The feature is deployed and reachable, with no regression to existing enrichment/taxonomy/conversion behavior."
  artifacts:
    - path: "custom/grocy_AI/tests/browser/specs/purchase-capture.spec.js"
      provides: "Mobile-browser acceptance for the capture→review→commit flow"
    - path: ".planning/phases/08-purchase-capture-stock-intake/08-ACCEPTANCE.md"
      provides: "Suite counts + deploy evidence for Phase 8"
---

## Objective
Prove the whole flow on reproducible automated evidence and ship it — lean rigor, no portable/stable mirror (that folds into Phase 7).

<tasks>
<task type="execute">
  <name>Task 1: Automated acceptance suite</name>
  <action>Add purchase-capture.spec.js to the mobile-browser projects covering: rapid scan + coalescing, known/unknown resolution, review edits + trip defaults, unknown handoff + auto re-resolve, partial commit, double-commit idempotency, and the assertion that only commit writes stock. Keep native capture.php green. Run php8.5 custom/grocy_AI/tests/run.php and the browser release suite.</action>
  <verify><automated>php8.5 custom/grocy_AI/tests/run.php && (cd custom/grocy_AI/tests/browser && npm run test:release)</automated></verify>
  <acceptance_criteria>
    - Both suites pass on the dev host; counts recorded in 08-ACCEPTANCE.md.
    - Capture browser specs cover coalescing, unknown lifecycle, partial commit, and idempotency.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Deploy + no-regression check</name>
  <action>Deploy the module (per the project's standard deploy path). Smoke the capture + review pages authenticated (STOCK_PURCHASE), confirm existing enrichment/taxonomy/conversion pages still render, and record redacted deployment evidence (routes/statuses/versions only). Do NOT perform portable-file parity or stable-branch mirroring — deferred to Phase 7.</action>
  <verify><automated>rg -n "capture|PASS|deployed" .planning/phases/08-purchase-capture-stock-intake/08-ACCEPTANCE.md</automated></verify>
  <acceptance_criteria>
    - Deployed capture/review pages return authenticated 200 / unauth rejected; no regression to prior pages.
    - Evidence contains only redacted routes/statuses/versions.
  </acceptance_criteria>
</task>
</tasks>

<success_criteria>
- Phase 8 passes native + mobile-browser acceptance and is deployed; CAP-01…08 satisfied; portable/stable mirror explicitly deferred to Phase 7.
</success_criteria>

<output>Create 08-06-SUMMARY.md and 08-ACCEPTANCE.md with suite counts and deploy evidence.</output>
