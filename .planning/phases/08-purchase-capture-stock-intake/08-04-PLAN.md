---
phase: 08-purchase-capture-stock-intake
plan: "04"
type: execute
wave: 4
depends_on: ["08-03"]
requirements: [CAP-03, CAP-04]
must_haves:
  truths:
    - "A user can review a trip and adjust per-line quantity, optional price, and selection, delete lines, and set trip-level location/store defaults; best-before stays auto-computed."
    - "An unknown line links to the existing /product/new enrichment flow and auto re-resolves to known by barcode when the trip is reopened."
  artifacts:
    - path: "views/grocyai_capture_review.blade.php"
      provides: "Trip list + per-trip review screen"
    - path: "public/custom/grocy_AI/capture-review.js"
      provides: "Lean per-line edits, trip defaults, unknown handoff, re-resolve on load"
---

## Objective
Turn a captured trip into a reviewed, commit-ready set: lean per-line edits + trip defaults, with unknowns handed to enrichment and auto-reconnected on return.

<tasks>
<task type="execute">
  <name>Task 1: Review screen — lean edits + trip defaults</name>
  <action>Add views/grocyai_capture_review.blade.php and capture-review.js: list trips (status), and per selected trip render lines with quantity stepper, optional price, select/deselect checkbox, and delete, plus trip-level default location/store selectors. Best-before is not edited here (auto). Wire to PUT line/trip endpoints. Add GET /grocyai/capture/review route (STOCK_PURCHASE).</action>
  <verify><automated>php8.5 -l routes.php ; node --check public/custom/grocy_AI/capture-review.js</automated></verify>
  <acceptance_criteria>
    - Per-line quantity/price/selected/delete and trip defaults persist via the API.
    - Lean per-line set only (no per-line location/store/best-before), per Q11.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Unknown handoff + auto re-resolve</name>
  <action>For unknown lines, show a "Create product" link to the existing /product/new enrichment flow (prefilled with the scanned barcode). On LoadTrip, re-resolve every unknown/conflict line by barcode via ResolveOwner so a line flips to known once the product+barcode exists (Q10). Assert re-resolution in capture.php.</action>
  <verify><automated>php8.5 custom/grocy_AI/tests/run.php 2>&1 | rg -i "capture|re-resolve|unknown"</automated></verify>
  <acceptance_criteria>
    - Reopening a trip after product creation shows the previously-unknown line as known and commit-eligible.
    - Enrichment handoff requires MASTER_DATA_EDIT; capture stays STOCK_PURCHASE.
  </acceptance_criteria>
</task>
</tasks>

<success_criteria>
- Review + unknown lifecycle work; a trip can be brought to fully-known, selected state.
</success_criteria>

<output>Create 08-04-SUMMARY.md with review actions and the re-resolve evidence.</output>
