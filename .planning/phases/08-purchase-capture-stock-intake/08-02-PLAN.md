---
phase: 08-purchase-capture-stock-intake
plan: "02"
type: execute
wave: 2
depends_on: ["08-01"]
requirements: [CAP-01, CAP-02, CAP-08]
must_haves:
  truths:
    - "A user with STOCK_PURCHASE can start and close a trip and scan barcodes into it over a live connection; each scan resolves ownership immediately and writes only capture rows (zero stock writes)."
    - "Repeated scans of the same barcode coalesce into one line whose quantity increments; distinct barcodes create distinct lines."
  artifacts:
    - path: "custom/grocy_AI/src/GrocyAiCaptureService.php"
      provides: "StartTrip, ScanIntoTrip (resolve+coalesce), LoadTrip, SetStatus, SetTripDefaults, UpdateLine"
    - path: "custom/grocy_AI/routes.php"
      provides: "capture trip + scan + line endpoints under Cors/Json + STOCK_PURCHASE"
---

## Objective
Deliver the server-side capture queue: start/close a trip and rapid-scan into it, resolving known/unknown per scan and coalescing duplicates — the live-connection core of the workflow, with no stock writes yet.

<tasks>
<task type="execute">
  <name>Task 1: Capture service — trip + scan + coalesce</name>
  <action>Implement GrocyAiCaptureService with StartTrip(actor), ScanIntoTrip(tripId, barcode) using GrocyAiBarcodeService::ResolveOwner to set status/resolved_product_id/canonical_gtin and coalescing on COALESCE(canonical_gtin, scanned_barcode) (increment quantity on repeat), LoadTrip(tripId) returning trip + ordered lines, SetStatus (open→reviewing), SetTripDefaults(location/store), and UpdateLine (quantity/price/selected/delete). Lazy Bootstrap in the constructor. Write an audit row per mutating action. No stock/stock_log writes anywhere in this service yet.</action>
  <verify><automated>php8.5 custom/grocy_AI/tests/run.php 2>&1 | rg -i "capture"</automated></verify>
  <acceptance_criteria>
    - Trip lifecycle + coalescing + known/unknown resolution assertions from 08-01 pass.
    - No INSERT/UPDATE against stock or stock_log occurs.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Capture API routes</name>
  <action>Add POST /api/grocy-ai/capture/trips, POST .../trips/{id}/scan, GET .../trips/{id}, PUT .../trips/{id}, PUT .../trips/{id}/lines/{seq} to the module route group (Cors+Json middleware), each gated on User::PERMISSION_STOCK_PURCHASE via the controller. Return the closed trip/line DTOs.</action>
  <verify><automated>php8.5 -l custom/grocy_AI/src/GrocyAiCaptureService.php && php8.5 -l custom/grocy_AI/routes.php</automated></verify>
  <acceptance_criteria>
    - Endpoints resolve and enforce STOCK_PURCHASE.
    - Unauthenticated calls are rejected consistent with the existing module routes.
  </acceptance_criteria>
</task>
</tasks>

<success_criteria>
- Start/scan/coalesce/known-unknown behavior is green; zero stock writes.
</success_criteria>

<output>Create 08-02-SUMMARY.md with the service/endpoint map and coalescing evidence.</output>
