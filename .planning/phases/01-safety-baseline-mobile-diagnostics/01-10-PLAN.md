---
phase: 01-safety-baseline-mobile-diagnostics
plan: "10"
type: execute
wave: 10
depends_on: ["01-09"]
files_modified:
  - .planning/phases/01-safety-baseline-mobile-diagnostics/01-10-PLAN.md
  - .planning/phases/01-safety-baseline-mobile-diagnostics/01-PHONE-ACCEPTANCE.md
  - .planning/phases/01-safety-baseline-mobile-diagnostics/01-10-SUMMARY.md
autonomous: true
requirements: [MOB-01, MOB-02, MOB-03, MOB-04, MOB-05, MOB-06, MOB-07, MOB-08]
rebaseline:
  decided: "2026-09-01"
  by: operator
  from: "checkpoint:human-verify — physical-phone matrix, 60 redacted latency samples, and manual normal-Save spine"
  to: "automated execute — deterministic native + mobile-browser acceptance runnable on the dev host"
  reason: "A physical household phone on the deployed LAN plus ~60 manual scans and deployed-host digest access is not feasible for a solo maintainer. The enrichment feature is already deployed and every non-human gate is green, so acceptance is relaxed to the automatable evidence."
  residual_risk: "Real-device latency signature (p50/p95) and physical UI interaction on actual hardware/LAN topology are NOT measured. Deterministic fixtures assert the same states and the exact 15,000ms deadline, but observed on-device timings remain unverified and are deferred to any future real-device session."
must_haves:
  truths:
    - "The deterministic native module suite and the mobile-browser release suite both pass on the dev host, covering the UI-SPEC state matrix (valid/invalid, timeout, offline, not-found, companion-unavailable, provider-error, partial-image, cancelled, success, recovery) without a physical device."
    - "Automated coverage proves no automatic retry after reconnect/pageshow, current-intent stale suppression, the exact 15,000ms browser deadline, and preserved form/Save controls during degraded providers."
    - "Automated coverage proves enrichment is zero-write: no product, barcode, category, stock, conversion, or file mutation occurs during search/preview/cancel/timeout/failed-media."
    - "Portable parity holds for the Phase 1 file set, so the accepted evidence corresponds to the committed portable bytes."
    - "The physical-latency and manual-interaction acceptance is explicitly recorded as WAIVED with its residual risk, not as PASS."
  artifacts:
    - path: ".planning/phases/01-safety-baseline-mobile-diagnostics/01-PHONE-ACCEPTANCE.md"
      provides: "Automated acceptance results, the recorded physical-latency waiver, and its residual-risk statement"
    - path: ".planning/phases/01-safety-baseline-mobile-diagnostics/01-10-SUMMARY.md"
      provides: "Relaxed-gate acceptance summary with suite counts and the re-baseline decision"
---

## Phase Goal

**As a** phone user and maintainer, **I want to** operate enrichment dependably and localize failures, **so that** slow or unavailable providers never turn into unsafe or unexplained behavior.

<objective>
Close Phase 1 on the automatable evidence available to a solo maintainer: the deterministic native and mobile-browser suites that exercise the UI-SPEC state matrix, no-auto-retry, the exact 15,000ms deadline, and zero-write enrichment, plus portable parity — while explicitly waiving the infeasible physical-phone latency/interaction acceptance and recording its residual risk.

Purpose: Turn deterministic, reproducible evidence into a defensible Phase 1 acceptance without fabricating physical-device samples.
Output: Passing native + browser + parity evidence, a recorded physical-latency waiver, and the acceptance summary.
</objective>

<context>
@.planning/phases/01-safety-baseline-mobile-diagnostics/SKELETON.md
@.planning/phases/01-safety-baseline-mobile-diagnostics/01-UI-SPEC.md
@.planning/phases/01-safety-baseline-mobile-diagnostics/01-PHONE-ACCEPTANCE.md
@.planning/phases/01-safety-baseline-mobile-diagnostics/01-09-SUMMARY.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Run the automatable acceptance suite and record the physical-latency waiver</name>
  <files>.planning/phases/01-safety-baseline-mobile-diagnostics/01-PHONE-ACCEPTANCE.md</files>
  <action>Run the deterministic native module suite and the mobile-browser release suite on the dev host and confirm both pass. The browser suite must include the UI-SPEC state coverage: invalid length/checksum (no request), valid-ready, searching, cancelled, offline, exact-timeout, not-found, companion/provider error, partial-image, success, recovery, no-auto-retry after reconnect/pageshow, current-intent stale suppression, and preserved form/Save controls during degraded providers, plus zero-write assertions. Confirm portable parity for the Phase 1 file set. Record the suite names and pass counts in 01-PHONE-ACCEPTANCE.md. In the same file, replace the prior SKIP note with an explicit WAIVED entry for the physical-phone latency signature and manual on-device interaction, citing the re-baseline decision and residual risk from this plan's frontmatter. Do not fabricate device metadata or timing samples; the evidence checker and phone-timings.jsonl remain available for any future real-device session but are not required for this relaxed acceptance.</action>
  <verify>
    <automated>php8.5 custom/grocy_AI/tests/run.php && (cd custom/grocy_AI/tests/browser && npm run test:release) && rg -n "WAIVED|automated_acceptance|zero_enrichment_writes|portable_parity" .planning/phases/01-safety-baseline-mobile-diagnostics/01-PHONE-ACCEPTANCE.md</automated>
  </verify>
  <acceptance_criteria>
    - Native module suite passes on the dev host with its check count recorded.
    - Mobile-browser release suite passes on the dev host with its test count recorded, covering the UI-SPEC state matrix, no-auto-retry, exact 15,000ms deadline, and zero-write behavior.
    - Portable parity for the Phase 1 file set holds (or the exact deviation is recorded).
    - 01-PHONE-ACCEPTANCE.md records the physical-latency/interaction acceptance as WAIVED with its residual risk, never as PASS, and contains no fabricated device metadata or timing samples.
  </acceptance_criteria>
  <done>Phase 1 is accepted on reproducible automated evidence, with the infeasible physical-device acceptance explicitly waived and its residual risk recorded.</done>
</task>

</tasks>

<verification>
<automated>php8.5 custom/grocy_AI/tests/run.php && (cd custom/grocy_AI/tests/browser && npm run test:release)</automated>
The relaxed gate requires the deterministic native and mobile-browser suites to pass on the dev host and the physical-latency waiver to be recorded. It no longer requires physical-phone timing samples, the manual scenario walk, or the manual normal-Save spine.
</verification>

<success_criteria>
- The native and mobile-browser suites pass on the dev host and their counts are recorded.
- Automated coverage proves the UI-SPEC states, no-auto-retry, exact deadline, and zero-write enrichment.
- The physical-phone latency signature and manual interaction are recorded as WAIVED with residual risk, not fabricated.
- Portable parity for the Phase 1 file set holds or its deviation is recorded.
</success_criteria>

<output>
Create `.planning/phases/01-safety-baseline-mobile-diagnostics/01-10-SUMMARY.md` with the suite counts, the recorded physical-latency waiver, and the re-baseline decision reference.
</output>
