---
phase: 01-safety-baseline-mobile-diagnostics
plan: "10"
type: execute
wave: 10
depends_on: ["01-09"]
files_modified:
  - .planning/phases/01-safety-baseline-mobile-diagnostics/01-PHONE-ACCEPTANCE.md
  - .planning/phases/01-safety-baseline-mobile-diagnostics/evidence/phone-timings.jsonl
autonomous: false
requirements: [MOB-01, MOB-02, MOB-03, MOB-04, MOB-05, MOB-06, MOB-07, MOB-08]
must_haves:
  truths:
    - "Per D-01/D-03/D-08, an available household phone on the actual LAN completes the approved matrix and passes locked p50/p95/deadline gates without re-baselining."
    - "Per D-04, committed phone evidence contains only the closed redacted fields and no product/GTIN/credential/header/payload/URL/token content."
    - "Per D-05/D-06, reconnect, pageshow, retry, races, and degraded providers never trigger automatic work or block normal editing/Save."
    - "A user-selected product proves the existing authenticated read→normal Save→reload→restore→reload spine using only redacted booleans/timings; enrichment remains zero-write."
  artifacts:
    - path: ".planning/phases/01-safety-baseline-mobile-diagnostics/01-PHONE-ACCEPTANCE.md"
      provides: "Signed physical matrix, deployment prerequisite linkage, and normal-Save restoration proof"
    - path: ".planning/phases/01-safety-baseline-mobile-diagnostics/evidence/phone-timings.jsonl"
      provides: "Redacted physical samples passing locked nearest-rank thresholds"
  key_links:
    - from: "01-09-SUMMARY.md stable_adapter_sha/image digest"
      to: "physical-phone acceptance"
      via: "operator verifies the exact deployed artifact before sampling"
      pattern: "[0-9a-f]{40}"
    - from: "phone-timings.jsonl"
      to: "evidence/check-phone-timings.py"
      via: "closed-schema final release decision"
      pattern: "cached|metadata|image_attachment"
    - from: "normal product form Save"
      to: "existing SQLite product row"
      via: "authenticated Save/reload/restore only"
      pattern: "normal_save_restore"
---

## Phase Goal

**As a** phone user and maintainer, **I want to** operate enrichment dependably and localize failures, **so that** slow or unavailable providers never turn into unsafe or unexplained behavior.

<objective>
Complete the physical-phone acceptance and prove the existing normal-Save read/write/restoration spine against the exact stable artifact deployed in Plan 01-09.

Purpose: Turn deterministic and deployment confidence into signed household-device evidence without exposing household data.
Output: Passing redacted timing samples, the full physical interaction/degraded matrix, and normal-Save restoration proof.
</objective>

<execution_context>
@/Users/ian/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ian/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-safety-baseline-mobile-diagnostics/SKELETON.md
@.planning/phases/01-safety-baseline-mobile-diagnostics/01-UI-SPEC.md
@.planning/phases/01-safety-baseline-mobile-diagnostics/01-PHONE-ACCEPTANCE.md
@.planning/phases/01-safety-baseline-mobile-diagnostics/evidence/check-phone-timings.py
@.planning/phases/01-safety-baseline-mobile-diagnostics/evidence/phone-timings.jsonl
@.planning/phases/01-safety-baseline-mobile-diagnostics/01-09-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Accept the physical-phone matrix and normal-Save restoration spine</name>
  <read_first>
    .planning/phases/01-safety-baseline-mobile-diagnostics/SKELETON.md
    .planning/phases/01-safety-baseline-mobile-diagnostics/01-UI-SPEC.md
    .planning/phases/01-safety-baseline-mobile-diagnostics/01-PHONE-ACCEPTANCE.md
    .planning/phases/01-safety-baseline-mobile-diagnostics/evidence/check-phone-timings.py
    .planning/phases/01-safety-baseline-mobile-diagnostics/evidence/phone-timings.jsonl
    .planning/phases/01-safety-baseline-mobile-diagnostics/01-09-SUMMARY.md
  </read_first>
  <files>.planning/phases/01-safety-baseline-mobile-diagnostics/01-PHONE-ACCEPTANCE.md, .planning/phases/01-safety-baseline-mobile-diagnostics/evidence/phone-timings.jsonl</files>
  <action>First confirm the running source SHA, immutable image digest, cache marker, and stable prerequisites exactly match Plan 01-09. Using any available household phone/browser and the actual LAN topology per D-08, record device/OS/browser/version/viewport/orientation/network route and run the D-01 UI-SPEC matrix: manual valid/invalid, camera success, camera permission denied, normal LAN, shaped slow LAN, disconnected/reconnected LAN, exact timeout, Cancel, explicit Retry, repeat tap/scan, background/foreground, Back, not-found, companion unavailable, metadata provider unavailable, image host unavailable/partial, search/review/name and image staging, diagnostic copy/fallback, and reload. Confirm local feedback ≤250ms, no automatic retry after reconnect/pageshow per D-05, current-intent stale suppression, exact UI copy/states, preserved fields/image/Save controls per D-06, and zero enrichment writes. Capture 20 redacted successful samples each for `cached`, `metadata`, and `image_attachment` plus one exact 15,000ms timeout. Then authenticate normally, have the operator select a test product without recording its ID, GTIN, name, field value, or derived identifier; read it, change one pre-approved reversible field through normal Save, reload to confirm, restore through normal Save, and reload to confirm. Record only redacted booleans/timings for `operator_selected_product_read`, `normal_save_write`, `reload_confirmed`, `restoration_save`, and `final_reload_confirmed`. Do not call SQL, an enrichment write endpoint, or any automatic retry. Approval is blocked unless every scenario and locked threshold passes without re-baselining.</action>
  <verify>
    <automated>python3 .planning/phases/01-safety-baseline-mobile-diagnostics/evidence/check-phone-timings.py .planning/phases/01-safety-baseline-mobile-diagnostics/evidence/phone-timings.jsonl && rg -n "PASS|normal_save_restore|stable_adapter_sha|stable_image_digest|portable_parity|zero_enrichment_writes" .planning/phases/01-safety-baseline-mobile-diagnostics/01-PHONE-ACCEPTANCE.md</automated>
  </verify>
  <acceptance_criteria>
    - Running stable SHA/image digest/cache marker match Plan 01-09 exactly before sampling.
    - Checker exits 0 with 20 successful samples per path, cached p95≤1000ms, metadata p95≤5000ms, image p95≤5000ms, and timeout exactly 15000ms; p50/p95/counts are recorded.
    - Every UI-SPEC scenario is PASS with device/browser/network/version metadata, exact named UI state, current-intent behavior, explicit retry only, preserved form/image/Save state, and no forbidden evidence field/value.
    - Diagnostic copy contains only versions, trace, stages, statuses, and timings; no GTIN, product/inventory data, credential, header, cookie, payload, URL/query, token, or raw exception appears in evidence.
    - Operator-selected read, normal Save write, reload, restoration Save, and final reload are all PASS using only redacted booleans/timings; enrichment write evidence remains zero.
    - User response is exactly `approved`; otherwise record the failing scenario and keep Phase 1 incomplete without changing thresholds.
  </acceptance_criteria>
  <done>The exact stable artifact passes physical-phone budgets, degraded interaction, privacy, zero-write enrichment, and normal-Save restoration acceptance.</done>
</task>

</tasks>

<threat_model>
## Assets

- Household inventory data, authenticated normal Save authority, live diagnostic/timing evidence, and release decision integrity.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Physical phone/LAN → evidence files | Live device and network observations enter committed artifacts. |
| Browser diagnostic DTO → copied report | Request-local data is reduced to the approved allowlist. |
| Normal Save → SQLite | One explicit reversible user write crosses the durable boundary. |
| Human observations → release gate | Operator-reported scenarios affect phase acceptance. |

## STRIDE Threat Register

Likelihood, impact, and severity use Low/Medium/High qualitative ratings. ASVS mappings are official version-pinned 5.0.0 Level 1 controls unless the row names a verified category with no precise L1 control.

| Threat ID | Category | Component | Likelihood | Impact | Severity | ASVS 5.0.0 L1 / Verified Category | Disposition | Mitigation Task | Verification Evidence |
|---|---|---|---|---|---|---|---|---|---|
| T-01-10-01 | Information Disclosure | Live evidence/diagnostic copy | Medium | High | HIGH | `v5.0.0-15.3.1` | mitigate | Task 1 uses the closed evidence checker and records only redacted device/network metadata, statuses, booleans, and timings. | Checker pass, forbidden-field scan, diagnostic canaries, and acceptance review. |
| T-01-10-02 | Tampering | Stale/duplicate response | Medium | High | HIGH | `v5.0.0-2.3.1` | mitigate | Task 1 physically verifies current-intent ordering, coalescing, cancellation, Back/background behavior, and explicit retry. | Repeat tap/scan, late response, lifecycle, cancel/retry entries PASS. |
| T-01-10-03 | Denial of Service | Retry/provider path | High | Medium | HIGH | ASVS V2 Anti-automation and V16 availability verified; no precise L1 control (`V2.4.1`, `V16.5.2` are Level 2) | mitigate | Task 1 verifies no automatic retry and enforces locked feedback/deadline/p95 gates on real LAN paths. | Request counts, exact timeout sample, and nearest-rank checker pass. |
| T-01-10-04 | Tampering/Repudiation | Normal Save/restoration | Medium | High | HIGH | `v5.0.0-2.3.1`, `v5.0.0-8.3.1` | mitigate | Task 1 allows the write only through the authenticated expected Save sequence, verifies reload, and immediately restores the original value. | Redacted five-step boolean/timing spine evidence and final reload PASS. |
| T-01-10-05 | Elevation of Privilege | Deployed routes/Save | Medium | High | HIGH | `v5.0.0-8.2.1`, `v5.0.0-8.3.1` | mitigate | Task 1 uses normal authentication/authorization and the deployed route evidence inherited from Plan 01-09. | Exact artifact linkage, authenticated session, unauthorized smoke reference, and normal Save UI path. |

## HIGH-Threat Release Gate

Every row is HIGH and must pass its physical, automated, privacy, and restoration evidence. No threat may be waived or re-baselined; any failure leaves Phase 1 incomplete.

## Residual Risks

- Physical evidence represents the recorded available household phone and actual LAN topology, as explicitly selected by D-08, rather than every possible client/network.
</threat_model>

<verification>
<automated>php custom/grocy_AI/tests/run.php && (cd /Users/ian/Documents/Repos/grocy-mcp && .venv/bin/python -m unittest tests.test_enrichment tests.test_http_api tests.test_diagnostics) && npm --prefix custom/grocy_AI/tests/browser run test:release && STABLE_SHA=$(git -C /Users/ian/Documents/Repos/grocy-atech-release rev-parse HEAD) && bash custom/grocy_AI/tests/check-portable-parity.sh --stable-sha "$STABLE_SHA" && python3 .planning/phases/01-safety-baseline-mobile-diagnostics/evidence/check-phone-timings.py .planning/phases/01-safety-baseline-mobile-diagnostics/evidence/phone-timings.jsonl</automated>
At the final release gate, require the full deterministic suites, exact deployed SHA/digest linkage, SHA-pinned parity, signed physical matrix, passing redacted timing checker, and normal Save read/write/restoration proof.
</verification>

<success_criteria>
- The exact stable artifact passes the full physical-phone scenario matrix.
- Locked p50/p95/deadline gates pass without re-baselining.
- Live evidence and diagnostic copy contain no forbidden household or credential data.
- Normal Save proves read/write/reload/restoration; enrichment remains zero-write.
- Human sign-off is `approved`.
</success_criteria>

<output>
Create `.planning/phases/01-safety-baseline-mobile-diagnostics/01-10-SUMMARY.md` with redacted threshold results, scenario sign-off, and normal-Save restoration evidence references.
</output>
