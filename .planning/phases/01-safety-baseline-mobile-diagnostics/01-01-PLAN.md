---
phase: 01-safety-baseline-mobile-diagnostics
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [MOB-08]
must_haves:
  truths:
    - "Per D-07, implementation can proceed with the smallest approved two-engine browser matrix only after the test dependency is authenticated by a human."
    - "The maintainer explicitly verifies that @playwright/test@1.62.1 is the official Microsoft package before any install occurs."
  artifacts:
    - path: ".planning/phases/01-safety-baseline-mobile-diagnostics/01-01-SUMMARY.md"
      provides: "Explicit package-identity approval record and unchanged-worktree evidence"
  key_links:
    - from: "official npm package page"
      to: "github.com/microsoft/playwright"
      via: "repository metadata and Microsoft ownership"
      pattern: "@playwright/test.*1.62.1"
---

## Phase Goal

**As a** phone user and maintainer, **I want to** operate enrichment dependably and localize failures, **so that** slow or unavailable providers never turn into unsafe or unexplained behavior.

<objective>
Authorize the only new package in Phase 1 after verifying its legitimacy, as required by the package gate and D-07.

Purpose: Prevent a supply-chain assumption from becoming an unattended install.
Output: A recorded blocking approval for official `@playwright/test@1.62.1`; no repository file or dependency is changed.
</objective>

<execution_context>
@/Users/ian/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ian/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-safety-baseline-mobile-diagnostics/01-RESEARCH.md
@.planning/phases/01-safety-baseline-mobile-diagnostics/01-VALIDATION.md
@.planning/phases/01-safety-baseline-mobile-diagnostics/01-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify and authorize the official Playwright package</name>
  <read_first>
    .planning/phases/01-safety-baseline-mobile-diagnostics/01-RESEARCH.md
    .planning/phases/01-safety-baseline-mobile-diagnostics/01-VALIDATION.md
  </read_first>
  <files>None — approval gate only; do not create or modify package files in this task.</files>
  <action>Open `https://www.npmjs.com/package/@playwright/test/v/1.62.1`, the linked `https://github.com/microsoft/playwright` repository, and the official `https://playwright.dev/docs/intro` installation page. Confirm exact package name `@playwright/test`, exact version `1.62.1`, Microsoft ownership, repository link, and that the registry manifest has no `postinstall` script. Do not install, run `npx`, or approve a similarly named package. Record approval only when all five checks match.</action>
  <verify>
    <automated>npm view @playwright/test@1.62.1 name version repository.url scripts.postinstall --json</automated>
  </verify>
  <acceptance_criteria>
    - Automated output reports name `@playwright/test`, version `1.62.1`, repository `https://github.com/microsoft/playwright.git`, and no `scripts.postinstall` value.
    - Human response is exactly `approved` after checking the official npm, GitHub, and Playwright pages; any mismatch blocks Plan 01-02.
    - `git status --short` is unchanged after the checkpoint.
  </acceptance_criteria>
  <done>Official package identity and version are human-authorized; no install has occurred.</done>
</task>

</tasks>

<threat_model>
## Assets

- Developer workstation, repository integrity, and test-runner execution authority.

## Trust Boundaries

| Boundary | Description |
|---|---|
| npm registry/GitHub → local workstation | Untrusted package metadata and executable dependency cross into the development environment. |

## STRIDE Threat Register

Likelihood, impact, and severity use Low/Medium/High qualitative ratings. OWASP IDs use the official version-pinned ASVS 5.0.0 form; both component-management controls below are Level 1.

| Threat ID | Category | Component | Likelihood | Impact | Severity | ASVS 5.0.0 L1 | Disposition | Mitigation Task | Verification Evidence |
|---|---|---|---|---|---|---|---|---|---|
| T-01-01-SC | Tampering | `@playwright/test` install | Medium | High | HIGH | `v5.0.0-15.1.1`, `v5.0.0-15.2.1` | mitigate | Task 1 blocks Plan 01-02 until the exact component/version is verified against the documented update and provenance policy. | `npm view @playwright/test@1.62.1 ... --json`, official npm/GitHub/Playwright human check, unchanged `git status`. |
| T-01-01-01 | Spoofing | Package identity | Medium | High | HIGH | `v5.0.0-15.1.1`, `v5.0.0-15.2.1` | mitigate | Task 1 cross-checks package, version, Microsoft repository, and install scripts and rejects lookalikes. | Blocking `approved` response plus exact registry metadata. |

## HIGH-Threat Release Gate

Both HIGH rows require Task 1 evidence and explicit approval. Any mismatch or missing evidence blocks Plan 01-02 and this plan cannot complete.

## Residual Risks

- Human verification establishes provenance, not a formal code audit; dependency remains isolated to the test-only workspace and locked by `package-lock.json` in Plan 01-02.
</threat_model>

<verification>
No package files change and the checkpoint records explicit approval of the exact official package/version.
</verification>

<success_criteria>
- The supply-chain gate is approved or execution stops.
- No dependency is installed by this plan.
</success_criteria>

<output>
Create `.planning/phases/01-safety-baseline-mobile-diagnostics/01-01-SUMMARY.md` recording the approval evidence and no repository mutation.
</output>
