---
phase: 01-safety-baseline-mobile-diagnostics
plan: "08"
type: execute
wave: 8
depends_on: ["01-07"]
files_modified:
  - .planning/phases/01-safety-baseline-mobile-diagnostics/01-PHONE-ACCEPTANCE.md
  - .planning/phases/01-safety-baseline-mobile-diagnostics/evidence/phone-timings.jsonl
autonomous: false
requirements: [MOB-07, MOB-08]
must_haves:
  truths:
    - "Per D-01/D-03/D-08, an available household phone on the actual LAN completes the full approved scenario matrix and passes locked p50/p95/deadline gates."
    - "Per D-04, recorded live evidence contains only the closed redacted fields and no product/GTIN/credential/payload/token content."
    - "Per D-06, stable deployed enrichment can fail without blocking normal work, and a designated product proves a real SQLite read/write/restoration only through normal Save."
    - "Stable `atech-release` adaptation is explicit, preserves branch-specific controller/routes/cache-marker behavior and `/etc/komodo/grocy:/config`, and never switches the planning checkout from `atech-main`."
  artifacts:
    - path: ".planning/phases/01-safety-baseline-mobile-diagnostics/01-PHONE-ACCEPTANCE.md"
      provides: "Signed stable deployment, brownfield skeleton, and physical-phone acceptance record"
    - path: ".planning/phases/01-safety-baseline-mobile-diagnostics/evidence/phone-timings.jsonl"
      provides: "Redacted live samples passing locked thresholds"
  key_links:
    - from: "stable deployed product form"
      to: "existing SQLite product row"
      via: "authenticated normal Save/reload/restore only"
      pattern: "normal_save_restore"
    - from: "phone-timings.jsonl"
      to: "check-phone-timings.py"
      via: "final nonzero/zero release decision"
      pattern: "PASS"
---

## Phase Goal

**As a** phone user and maintainer, **I want to** operate enrichment dependably and localize failures, **so that** slow or unavailable providers never turn into unsafe or unexplained behavior.

<objective>
Complete the explicit stable-deployment and physical-phone gates under D-01/D-03/D-04/D-06/D-08, including the brownfield skeleton's real authenticated route and normal Save read/write proof.

Purpose: Turn deterministic confidence into evidence from the maintained production topology without mutating the planning branch or hiding deployment differences.
Output: Stable adaptation/deployment sign-off, full-stack Save/reload restoration proof, redacted phone samples, and passing release thresholds.
</objective>

<execution_context>
@/Users/ian/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ian/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-safety-baseline-mobile-diagnostics/SKELETON.md
@.planning/phases/01-safety-baseline-mobile-diagnostics/01-UI-SPEC.md
@.planning/phases/01-safety-baseline-mobile-diagnostics/01-PHONE-ACCEPTANCE.md
@.planning/phases/01-safety-baseline-mobile-diagnostics/01-PATTERNS.md
@custom/grocy_AI/portable-files.txt
@custom/grocy_AI/tests/check-portable-parity.sh
@CUSTOMIZATIONS.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Adapt and deploy the tested portable slice to stable without switching the planning checkout</name>
  <read_first>
    .planning/phases/01-safety-baseline-mobile-diagnostics/01-PATTERNS.md
    .planning/phases/01-safety-baseline-mobile-diagnostics/01-PHONE-ACCEPTANCE.md
    custom/grocy_AI/portable-files.txt
    custom/grocy_AI/tests/check-portable-parity.sh
    CUSTOMIZATIONS.md
  </read_first>
  <files>.planning/phases/01-safety-baseline-mobile-diagnostics/01-PHONE-ACCEPTANCE.md</files>
  <action>On the established release/deployment host or an explicitly separate `atech-release` worktree/clone, port the manifest-listed portable content semantically and commit it on `atech-release`; do not checkout/switch the `/Users/ian/Documents/Repos/grocy` planning worktree away from `atech-main`. Preserve stable's `Grocy\Controllers\BaseApiController` adapter, class-based JSON middleware, root route hook, pinned `Dockerfile.atech` base/COPY layout, and independent `custom/grocy_AI/version.json` cache marker; increment `Customization` because route/view integration changed and update stable `CUSTOMIZATIONS.md`. Build/deploy the immutable stable image using the existing DockerKomodo path, preserve `/etc/komodo/grocy:/config`, restart, and record source commit/image digest/cache marker/route+asset HTTP status/persistent-volume continuity in the acceptance file. Run status, enrichment, selected-image, timeout/degraded, and normal form render smoke; never write via enrichment.</action>
  <verify>
    <automated>bash custom/grocy_AI/tests/check-portable-parity.sh && php custom/grocy_AI/tests/run.php && npm --prefix custom/grocy_AI/tests/browser run test:release</automated>
  </verify>
  <acceptance_criteria>
    - Planning checkout remains branch `atech-main` with no branch switch/rename and companion remains `main`.
    - Stable adaptation has its own `atech-release` commit; portable parity passes and only documented controller/routes/view/cache-marker adapters differ.
    - Deployed image digest/source/cache marker are recorded; product form, enrichment route, selected-image route, JS, and CSS return expected 200/authenticated behavior after cache invalidation.
    - Persistent `/etc/komodo/grocy:/config` data and existing products/images remain present after restart.
    - Enrichment smoke produces zero product/barcode/stock/file writes.
  </acceptance_criteria>
  <done>Stable deployment runs the portable slice with documented adapters, fresh caches, preserved data, and separate branch history.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Record physical-phone budgets, degraded paths, and real normal-Save spine proof</name>
  <read_first>
    .planning/phases/01-safety-baseline-mobile-diagnostics/SKELETON.md
    .planning/phases/01-safety-baseline-mobile-diagnostics/01-UI-SPEC.md
    .planning/phases/01-safety-baseline-mobile-diagnostics/01-PHONE-ACCEPTANCE.md
    .planning/phases/01-safety-baseline-mobile-diagnostics/evidence/check-phone-timings.py
    .planning/phases/01-safety-baseline-mobile-diagnostics/evidence/phone-timings.jsonl
  </read_first>
  <files>.planning/phases/01-safety-baseline-mobile-diagnostics/01-PHONE-ACCEPTANCE.md, .planning/phases/01-safety-baseline-mobile-diagnostics/evidence/phone-timings.jsonl</files>
  <action>Using any available household phone/browser and the actual LAN topology per D-08, record device/OS/browser/version/viewport/orientation/network route and run the acceptance matrix: manual valid/invalid, camera success, permission denied, normal LAN, shaped slow LAN, disconnected/reconnected LAN, timeout/Cancel/explicit Retry, repeat tap/scan, background/foreground, Back, not-found, companion unavailable, metadata provider unavailable, image host unavailable/partial, search/review/name and image staging, diagnostic copy/fallback, and reload. Capture 20 redacted successful samples each for cached, metadata, and image attachment plus one 15,000ms timeout. For the walking skeleton, authenticate normally, open a designated test product (real read), record only its ID, change one pre-approved reversible field through the normal Save button (real write), reload to confirm, restore its original value through normal Save, and reload to confirm restoration; record outcomes/durations but never values. Do not call SQL, an enrichment write endpoint, or an automatic retry.</action>
  <verify>
    <automated>python3 .planning/phases/01-safety-baseline-mobile-diagnostics/evidence/check-phone-timings.py .planning/phases/01-safety-baseline-mobile-diagnostics/evidence/phone-timings.jsonl && rg -n "PASS|normal_save_restore|stable_image_digest|portable_parity" .planning/phases/01-safety-baseline-mobile-diagnostics/01-PHONE-ACCEPTANCE.md</automated>
  </verify>
  <acceptance_criteria>
    - Checker exits 0 with cached p95≤1000ms, metadata p95≤5000ms, image p95≤5000ms, and exact timeout=15000ms; p50/p95/counts are recorded.
    - Every UI-SPEC scenario has PASS with device/browser/network/version metadata and no forbidden evidence key/value.
    - Diagnostic copy contains versions, trace, stages, statuses, timings and none of the forbidden data; no automatic request occurs on reconnect/pageshow.
    - Designated product read, normal Save write, reload, restoration Save, and final reload all PASS; enrichment route counters/log evidence show zero persistence.
    - User response is `approved`; otherwise describe the exact failing scenario and keep the phase incomplete without re-baselining.
  </acceptance_criteria>
  <done>Physical phone, LAN budgets, degraded behavior, stable deployment, and existing DB read/write spine are all proven and signed off.</done>
</task>

</tasks>

<threat_model>
## Assets

- Production household data, stable branch/image, persistent volume, credentials, live diagnostic/timing evidence.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Portable main code → stable adapter branch | Tested code crosses branch/framework differences. |
| Stable image → persistent volume | Rebuilt application mounts irreplaceable household state. |
| Physical phone/LAN → evidence files | Live environment details enter committed evidence. |
| Normal Save → SQLite | Explicit user-authorized write crosses the durable boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-08-01 | Tampering | Stable adaptation/cache | mitigate | Separate stable commit, manifest parity, documented adapters, cache-marker bump, route/asset smoke. |
| T-01-08-02 | Information Disclosure | Live evidence/diagnostics | mitigate | Closed schema/checker, forbidden-key rejection, product ID only for Save proof, no values/GTIN/secrets. |
| T-01-08-03 | Tampering | Stale/duplicate live response | mitigate | Physical race matrix verifies token/coalescing behavior and explicit retry. |
| T-01-08-04 | Denial of Service | Retry/provider path | mitigate | No automatic retry, locked layered deadlines, slow/disconnected tests. |
| T-01-08-05 | Tampering/Repudiation | Hidden writes | mitigate | Enrichment zero-write evidence; real write only through designated normal Save and immediate restoration/reload proof. |
| T-01-08-06 | Elevation of Privilege | Deployed routes | mitigate | Existing auth and `MASTER_DATA_EDIT` verified on stable image. |

## Residual Risks

- Physical evidence reflects the recorded household device/topology, not every possible client; D-08 explicitly selects available devices and actual LAN as the baseline.
</threat_model>

<verification>
Require separate stable commit/deploy evidence, green portable parity/full suites, passing redacted timing checker, signed phone matrix, and normal Save read/write/restoration proof.
</verification>

<success_criteria>
- Stable deployment is adapted without switching the planning checkout.
- Persistent household data survives image restart.
- Physical latency and degraded-path gates pass without re-baselining.
- Existing authenticated route/UI/SQLite read-write spine is proven; enrichment remains zero-write.
</success_criteria>

<output>
Create `.planning/phases/01-safety-baseline-mobile-diagnostics/01-08-SUMMARY.md` with stable and physical evidence references; do not include forbidden live values.
</output>
