---
phase: 01-safety-baseline-mobile-diagnostics
plan: "07"
type: execute
wave: 7
depends_on: ["01-06"]
files_modified:
  - custom/grocy_AI/tests/browser/specs/responsive-a11y.spec.js
  - custom/grocy_AI/tests/browser/package.json
  - custom/grocy_AI/tests/check-portable-parity.sh
  - custom/grocy_AI/portable-files.txt
  - custom/grocy_AI/README.md
  - .planning/phases/01-safety-baseline-mobile-diagnostics/01-PHONE-ACCEPTANCE.md
  - .planning/phases/01-safety-baseline-mobile-diagnostics/evidence/phone-timings.jsonl
  - .planning/phases/01-safety-baseline-mobile-diagnostics/evidence/check-phone-timings.py
  - .planning/phases/01-safety-baseline-mobile-diagnostics/01-VALIDATION.md
autonomous: true
requirements: [MOB-08]
must_haves:
  truths:
    - "Per D-01/D-07, automated Chromium/WebKit coverage enforces 320/375/390/768px layout, 44px targets, keyboard/live-region semantics, and reduced motion."
    - "Per D-03/D-08, a deterministic nearest-rank checker enforces the locked p95 ≤1s/≤5s/≤5s and exact 15s baseline using any available household phone metadata."
    - "Per D-04, performance/release evidence schema rejects GTIN, product, credential, header, cookie, URL, payload, inventory, and image-token fields."
    - "Per D-06, the release gate includes normal Save availability and a zero-write enrichment assertion."
  artifacts:
    - path: "custom/grocy_AI/tests/browser/specs/responsive-a11y.spec.js"
      provides: "Two-engine responsive/accessibility/reduced-motion gate"
    - path: ".planning/phases/01-safety-baseline-mobile-diagnostics/evidence/check-phone-timings.py"
      provides: "Deterministic schema/sample/nearest-rank threshold checker"
    - path: "custom/grocy_AI/tests/check-portable-parity.sh"
      provides: "Read-only atech-main versus atech-release portable-file parity report without checkout"
    - path: ".planning/phases/01-safety-baseline-mobile-diagnostics/01-PHONE-ACCEPTANCE.md"
      provides: "Exact stable/physical execution and evidence protocol"
  key_links:
    - from: "evidence/phone-timings.jsonl"
      to: "evidence/check-phone-timings.py"
      via: "closed JSONL schema and nearest-rank p50/p95"
      pattern: "cached|metadata|image_attachment"
    - from: "custom/grocy_AI/portable-files.txt"
      to: "an explicit recorded stable commit SHA"
      via: "read-only `git show $stable_sha:$path` parity script"
      pattern: "--stable-sha"
---

## Phase Goal

**As a** phone user and maintainer, **I want to** operate enrichment dependably and localize failures, **so that** slow or unavailable providers never turn into unsafe or unexplained behavior.

<objective>
Turn D-01/D-03/D-04/D-06/D-07/D-08 into repeatable automated mobile, latency-evidence, portable-parity, and stable-smoke gates.

Purpose: Make release claims mechanically checkable before the external device/deployment checkpoints.
Output: Responsive/a11y tests, redacted evidence schema/checker, portable manifest/parity command, acceptance procedure, and finalized Nyquist map.
</objective>

<execution_context>
@/Users/ian/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ian/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-safety-baseline-mobile-diagnostics/01-UI-SPEC.md
@.planning/phases/01-safety-baseline-mobile-diagnostics/01-VALIDATION.md
@.planning/phases/01-safety-baseline-mobile-diagnostics/01-PATTERNS.md
@CUSTOMIZATIONS.md
@custom/grocy_AI/README.md
@custom/grocy_AI/tests/browser/playwright.config.js
@custom/grocy_AI/tests/browser/package.json
@public/custom/grocy_AI/product-enrichment.js
@public/custom/grocy_AI/grocy-ai.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce responsive, accessibility, lifecycle, and deterministic budget behavior</name>
  <read_first>
    .planning/phases/01-safety-baseline-mobile-diagnostics/01-UI-SPEC.md
    custom/grocy_AI/tests/browser/playwright.config.js
    custom/grocy_AI/tests/browser/package.json
    public/custom/grocy_AI/product-enrichment.js
    public/custom/grocy_AI/grocy-ai.css
    views/productform.blade.php
  </read_first>
  <files>custom/grocy_AI/tests/browser/specs/responsive-a11y.spec.js, custom/grocy_AI/tests/browser/package.json</files>
  <behavior>
    - Both engines at 320, 375, 390, and 768px have no horizontal overflow, visible labels/recovery text, and ≥44x44 actionable targets.
    - Invalid feedback is assertive, progress/terminal feedback polite, `aria-busy` exact, focus rules correct, Enter/Escape correct, and spinner respects reduced motion.
    - Orientation/background/foreground/Back leaves no modal/spinner/disabled Search/obsolete result and starts no automatic request.
    - Virtual-clock assertions enforce ≤250ms validation/busy feedback and exact 15,000ms browser timeout.
  </behavior>
  <action>Add `@mob08` assertions for the exact viewport matrix in both configured projects, using computed layout/target measurements, ARIA/focus/keyboard inspection, reduced-motion emulation, orientation resize, and lifecycle events. Add a `test:release` script that runs all browser specs without retries. Keep actual CSS/JS fixes in Plan 01-06; if a deterministic contract regression is found here, repair only the exact source file and record the deviation rather than weakening tests.</action>
  <verify>
    <automated>npm --prefix custom/grocy_AI/tests/browser test -- --grep @mob08</automated>
  </verify>
  <acceptance_criteria>
    - Focused `@mob08` checks exit 0 for `chromium-mobile` and `webkit-mobile`; the full `test:release` command remains the plan/wave gate.
    - All four widths have `scrollWidth <= clientWidth` and every required action reports both dimensions ≥44px.
    - Busy/validation timing assertions pass at ≤250ms and timeout transitions only at 15000ms virtual time.
    - No lifecycle test records an automatic request or stale result.
  </acceptance_criteria>
  <done>Automated mobile behavior, layout, accessibility, and browser budgets are release-gated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create redacted physical timing evidence and threshold checker</name>
  <read_first>
    .planning/phases/01-safety-baseline-mobile-diagnostics/01-UI-SPEC.md
    .planning/phases/01-safety-baseline-mobile-diagnostics/01-VALIDATION.md
    .planning/phases/01-safety-baseline-mobile-diagnostics/01-RESEARCH.md
  </read_first>
  <files>.planning/phases/01-safety-baseline-mobile-diagnostics/01-PHONE-ACCEPTANCE.md, .planning/phases/01-safety-baseline-mobile-diagnostics/evidence/phone-timings.jsonl, .planning/phases/01-safety-baseline-mobile-diagnostics/evidence/check-phone-timings.py</files>
  <behavior>
    - Checker requires 20 successful samples each for `cached`, `metadata`, and `image_attachment`, plus one exact browser-timeout sample.
    - It computes deterministic nearest-rank p50/p95 and fails unless cached p95≤1000ms, metadata p95≤5000ms, image p95≤5000ms, timeout=15000ms.
    - It exits nonzero on malformed/unknown fields, missing metadata, insufficient samples, threshold breach, or any forbidden field/key pattern.
  </behavior>
  <action>Create the acceptance procedure with required device/OS/browser/version, viewport/orientation, Wi-Fi/LAN/VPN route, server/version/contract, scenario, attempt, outcome, and overall/stage milliseconds. Create an empty/comment-free JSONL evidence file or schema-valid non-production example excluded from sample counts. Implement a dependency-free Python nearest-rank checker with a closed field allowlist and forbidden-key rejection for GTIN/UPC/product/value/credential/key/auth/header/cookie/CSRF/payload/body/inventory/URL/query/token/history. It must print per-path count/p50/p95/threshold/PASS|FAIL and return exit 1 on any violation; no re-baseline flag exists. Include the full physical scenario checklist from UI-SPEC and the normal Save/read/write/restoration evidence fields without recording product values.</action>
  <verify>
    <automated>python3 .planning/phases/01-safety-baseline-mobile-diagnostics/evidence/check-phone-timings.py --self-test && python3 -m py_compile .planning/phases/01-safety-baseline-mobile-diagnostics/evidence/check-phone-timings.py</automated>
  </verify>
  <acceptance_criteria>
    - Self-test covers threshold boundary, nearest-rank ordering, insufficient samples, malformed JSON, unknown key, forbidden key, and returns 0 only when expected pass/fail cases match.
    - Production evidence with fewer than required samples returns exit 1 and lists exact missing counts.
    - No evidence schema field can contain GTIN/product/inventory/credential/header/cookie/payload/URL/image-token data.
    - Threshold constants are exactly 1000/5000/5000/15000ms and cannot be overridden by evidence input.
  </acceptance_criteria>
  <done>Physical evidence can be captured and checked deterministically without leaking household data or silently changing policy.</done>
</task>

<task type="auto">
  <name>Task 3: Add read-only dual-branch parity and finalize the validation map</name>
  <read_first>
    CUSTOMIZATIONS.md
    custom/grocy_AI/README.md
    .planning/phases/01-safety-baseline-mobile-diagnostics/01-PATTERNS.md
    .planning/phases/01-safety-baseline-mobile-diagnostics/01-VALIDATION.md
  </read_first>
  <files>custom/grocy_AI/portable-files.txt, custom/grocy_AI/tests/check-portable-parity.sh, custom/grocy_AI/README.md, .planning/phases/01-safety-baseline-mobile-diagnostics/01-VALIDATION.md</files>
  <action>Define the exact portable Phase 1 file manifest as module version, diagnostic/service core, native tests, module README, and public JS/CSS; classify controller, routes, product-form hook, stable cache marker, and stable `CUSTOMIZATIONS.md` as Plan 01-09 adapters rather than byte-portable files. Add a POSIX shell checker that asserts current branch is `atech-main`, requires `--stable-sha &lt;40-hex-commit&gt;`, validates the object as a commit, and compares portable paths with `git show "$stable_sha:$path"`/temporary files only. The checker must never resolve an implicit moving branch ref and never checkout/switch/merge; it reports identical portable files separately from documented controller/routes/view/cache-marker adapter differences and exits nonzero until stable adaptation is complete. Add commands for PHP, companion, focused Playwright checks, the plan/wave `test:release` gate, timing evidence, SHA-pinned parity, and stable smoke to README. Update `01-VALIDATION.md` task IDs to `01-02`…`01-10`, waves 2…10, plan-local threat IDs, actual file paths, `nyquist_compliant: true`, and `wave_0_complete: true` only because every declared harness/spec now exists and passes.</action>
  <verify>
    <automated>bash -n custom/grocy_AI/tests/check-portable-parity.sh && php custom/grocy_AI/tests/run.php && (cd /Users/ian/Documents/Repos/grocy-mcp && .venv/bin/python -m unittest tests.test_diagnostics) && npm --prefix custom/grocy_AI/tests/browser run test:smoke</automated>
  </verify>
  <acceptance_criteria>
    - Shell syntax, PHP, companion, and browser commands exit 0.
    - Parity script contains no `checkout`, `switch`, `merge`, `rebase`, `reset`, or branch-writing command and fails clearly on current stable mismatches.
    - Parity script rejects a missing, moving-ref, abbreviated, or non-commit `--stable-sha` value and compares only the supplied full commit SHA.
    - Validation table has no `TBD`, placeholder `T-01` style ID, or missing file marker for deterministic tests.
    - README states separate repositories/working directories/commits and the stable cache marker requirement.
  </acceptance_criteria>
  <done>Deterministic suites and read-only branch comparison are one documented release gate, ready for stable adaptation and live evidence.</done>
</task>

</tasks>

<threat_model>
## Assets

- Release evidence integrity, household privacy, maintained branches, stable cache/version correctness.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Human-measured JSONL → release checker | Manually recorded data enters automated gate. |
| Git refs → parity report | Stable/main branch contents are compared without mutation. |
| Browser emulation → release claim | Deterministic emulation supplies coverage but not physical-network proof. |

## STRIDE Threat Register

Likelihood, impact, and severity use Low/Medium/High qualitative ratings. IDs below are official ASVS 5.0.0 Level 1 controls.

| Threat ID | Category | Component | Likelihood | Impact | Severity | ASVS 5.0.0 L1 | Disposition | Mitigation Task | Verification Evidence |
|---|---|---|---|---|---|---|---|---|---|
| T-01-07-01 | Tampering | Timing evidence | Medium | High | HIGH | `v5.0.0-2.2.1`, `v5.0.0-2.2.2` | mitigate | Task 2 validates closed evidence at the trusted checker with fixed thresholds/sample counts. | Self-tests for malformed, unknown, insufficient, boundary, and nearest-rank cases. |
| T-01-07-02 | Information Disclosure | Evidence JSONL | Medium | High | HIGH | `v5.0.0-15.3.1` | mitigate | Task 2 permits only the minimum evidence subset and rejects forbidden categories. | Forbidden/unknown-key self-tests and schema validation. |
| T-01-07-03 | Tampering | Branch parity | Medium | High | HIGH | `v5.0.0-15.1.1`, `v5.0.0-15.2.1` | mitigate | Task 3 documents the maintained component file set and compares it read-only against one explicit stable SHA. | Shell syntax, forbidden-command scan, invalid/missing SHA failure, SHA-pinned parity report. |
| T-01-07-04 | Information Disclosure | Test diagnostics | Low | High | MEDIUM | `v5.0.0-15.3.1` | mitigate | Tasks 1 and 3 keep the full privacy-canary suite in the plan/wave release command. | `test:release` at plan/wave gate. |
| T-01-07-SC | Tampering | Locked Playwright install | Low | High | MEDIUM | `v5.0.0-15.1.1`, `v5.0.0-15.2.1` | mitigate | Task 1 uses the locked dependency previously approved in Plan 01-01. | Lockfile/version check inherited plus focused MOB-08 run. |

## HIGH-Threat Release Gate

T-01-07-01 through T-01-07-03 must have passing Task 2/3 self-tests and SHA-pinned parity evidence. The plan/wave full suites are mandatory; any HIGH failure blocks stable adaptation.

## Residual Risks

- Empty evidence cannot pass; Plan 01-09 requires the actual deployment context and Plan 01-10 requires physical-phone evidence.
</threat_model>

<verification>
<automated>php custom/grocy_AI/tests/run.php && (cd /Users/ian/Documents/Repos/grocy-mcp && .venv/bin/python -m unittest tests.test_enrichment tests.test_http_api tests.test_diagnostics) && npm --prefix custom/grocy_AI/tests/browser run test:release && python3 .planning/phases/01-safety-baseline-mobile-diagnostics/evidence/check-phone-timings.py --self-test && bash -n custom/grocy_AI/tests/check-portable-parity.sh</automated>
At the plan/wave gate, run all deterministic suites, checker self-tests, shell syntax, and the SHA-pinned read-only parity report; confirm validation metadata points to real passing files.
</verification>

<success_criteria>
- Two-engine full suite is green at every required width.
- Evidence checker is deterministic, privacy-safe, and threshold-locked.
- Branch parity is inspectable without changing branches.
- Nyquist validation map contains no missing deterministic gap.
</success_criteria>

<output>
Create `.planning/phases/01-safety-baseline-mobile-diagnostics/01-07-SUMMARY.md`.
</output>
