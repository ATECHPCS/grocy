---
phase: 01-safety-baseline-mobile-diagnostics
plan: "02"
type: tdd
wave: 2
depends_on: ["01-01"]
files_modified:
  - custom/grocy_AI/tests/browser/package.json
  - custom/grocy_AI/tests/browser/package-lock.json
  - custom/grocy_AI/tests/browser/playwright.config.js
  - custom/grocy_AI/tests/browser/support/server.mjs
  - custom/grocy_AI/tests/browser/fixtures/productform.html
  - custom/grocy_AI/tests/browser/specs/happy-path.spec.js
autonomous: true
requirements: [MOB-01, MOB-02, MOB-04, MOB-07, MOB-08]
must_haves:
  truths:
    - "Per D-01/D-02, a real mobile engine loads the actual phase-owned JS/CSS in the existing Bootstrap product-form shape."
    - "Per D-03/D-06, the first failing end-to-end test specifies immediate valid-GTIN feedback, a bounded search, review-only results, and normal Save controls that remain usable."
    - "Per D-07, deterministic network fakes—not live providers—drive the ordinary browser suite."
  artifacts:
    - path: "custom/grocy_AI/tests/browser/playwright.config.js"
      provides: "Chromium and WebKit mobile projects plus deterministic loopback web server"
    - path: "custom/grocy_AI/tests/browser/fixtures/productform.html"
      provides: "Synthetic product form loading actual product-enrichment.js and grocy-ai.css"
    - path: "custom/grocy_AI/tests/browser/specs/happy-path.spec.js"
      provides: "Failing end-to-end contract for the phone enrichment happy path"
  key_links:
    - from: "custom/grocy_AI/tests/browser/fixtures/productform.html"
      to: "public/custom/grocy_AI/product-enrichment.js"
      via: "allowlisted loopback static server"
      pattern: "product-enrichment\\.js"
    - from: "custom/grocy_AI/tests/browser/specs/happy-path.spec.js"
      to: "/api/grocy-ai/products/enrich/upc/*"
      via: "Playwright page.route deterministic envelope"
      pattern: "products/enrich/upc"
---

## Phase Goal

**As a** phone user and maintainer, **I want to** operate enrichment dependably and localize failures, **so that** slow or unavailable providers never turn into unsafe or unexplained behavior.

<objective>
Create the isolated mobile-browser harness and the first failing end-to-end happy-path test, locking D-01/D-02/D-03/D-06/D-07 into executable behavior before production changes.

Purpose: Establish the MVP RED gate and deterministic feedback loop.
Output: Locked Playwright workspace, actual-asset fixture, two mobile-engine config, and a test that fails because Phase 1 phone behavior is not implemented yet.
</objective>

<execution_context>
@/Users/ian/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ian/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-safety-baseline-mobile-diagnostics/01-UI-SPEC.md
@.planning/phases/01-safety-baseline-mobile-diagnostics/01-VALIDATION.md
@.planning/phases/01-safety-baseline-mobile-diagnostics/01-PATTERNS.md
@public/custom/grocy_AI/product-enrichment.js
@public/custom/grocy_AI/grocy-ai.css
@views/productform.blade.php
@public/viewjs/productform.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install the approved isolated harness and write the failing phone happy path</name>
  <read_first>
    .planning/phases/01-safety-baseline-mobile-diagnostics/01-UI-SPEC.md
    .planning/phases/01-safety-baseline-mobile-diagnostics/01-VALIDATION.md
    .planning/phases/01-safety-baseline-mobile-diagnostics/01-PATTERNS.md
    public/custom/grocy_AI/product-enrichment.js
    public/custom/grocy_AI/grocy-ai.css
    views/productform.blade.php
    public/viewjs/productform.js
  </read_first>
  <files>custom/grocy_AI/tests/browser/package.json, custom/grocy_AI/tests/browser/package-lock.json, custom/grocy_AI/tests/browser/playwright.config.js, custom/grocy_AI/tests/browser/support/server.mjs, custom/grocy_AI/tests/browser/fixtures/productform.html, custom/grocy_AI/tests/browser/specs/happy-path.spec.js</files>
  <behavior>
    - A 390px Chromium/WebKit page accepts valid leading-zero GTIN `012345678905`, shows ready feedback within 250ms, and starts exactly one request.
    - A deterministic success envelope renders `Product details found`, preserves the original form name/file state until explicit preview actions, and never calls product/barcode/stock/file/save endpoints.
    - Both normal Save buttons remain enabled and clickable before, during, and after enrichment.
  </behavior>
  <action>From `/Users/ian/Documents/Repos/grocy`, create the nested private npm workspace and install only the human-approved `@playwright/test@1.62.1`, generating (not hand-editing) `package-lock.json`; install only Chromium and WebKit browser binaries. Define scripts `test`, `test:smoke`, and `test:install`. Configure projects named exactly `chromium-mobile` and `webkit-mobile`, functional viewport 390x844, one worker, no retries, trace on first retry, and a built-in Node server bound to `127.0.0.1`. The server must serve only the fixture and the two real phase-owned assets from explicit allowlisted roots and reject traversal. The fixture mirrors the existing enrichment card, representative `#name`, `#product-picture`, `.save-product-button` controls for both Save actions, and the camera event target; it must contain no fake persistence implementation. Load a pinned local jQuery-compatible runtime adapter before the production script and define deterministic minimal adapters for `window.Grocy`, `Grocy.Api`, `Grocy.FrontendHelpers`, `Grocy.BarcodeScanned`, Bootstrap collapse behavior, URL/base-path helpers, clipboard, and `URL.createObjectURL`/`revokeObjectURL` that match the identifiers and callback signatures actually consumed by `product-enrichment.js`; adapters may observe calls and dispatch events but must not implement persistence or enrichment behavior. Keep every runtime dependency as an allowlisted fixture asset or inline deterministic adapter—no CDN/network fetches. Write `@smoke @mob01 @mob02 @mob04 @mob07 @mob08` happy-path assertions above. Run RED and confirm failure is an unmet Phase 1 UI assertion, not a module/config/server/global error. Do not edit production files in this task.</action>
  <verify>
    <automated>npm --prefix custom/grocy_AI/tests/browser run test:smoke</automated>
  </verify>
  <acceptance_criteria>
    - `npm ls --prefix custom/grocy_AI/tests/browser @playwright/test --depth=0` reports exactly `@playwright/test@1.62.1` and the root Grocy `package.json`/`yarn.lock` remain unchanged.
    - Both configured projects can open the fixture and load the real JS/CSS with HTTP 200.
    - The smoke test exits nonzero solely on a documented missing Phase 1 behavior assertion; no import, browser-launch, static-server, or fixture error is accepted as RED.
    - Request counters report zero calls to product/barcode/stock/file/save routes during the failing enrichment scenario.
  </acceptance_criteria>
  <done>The isolated harness is operational and the first end-to-end phone contract is RED for the intended missing behavior.</done>
</task>

</tasks>

<threat_model>
## Assets

- Production dependency tree, local filesystem, browser-test process, and zero-write invariant.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Test package → workstation | Approved executable dev dependency runs locally. |
| Loopback browser → fixture server | Browser-controlled paths request local files. |
| Deterministic response → DOM | Synthetic untrusted provider payload enters actual feature code. |

## STRIDE Threat Register

Likelihood, impact, and severity use Low/Medium/High qualitative ratings. OWASP IDs are official ASVS 5.0.0 Level 1 controls; where a control is architectural rather than feature-specific, the mapping names the exact property verified here.

| Threat ID | Category | Component | Likelihood | Impact | Severity | ASVS 5.0.0 L1 | Disposition | Mitigation Task | Verification Evidence |
|---|---|---|---|---|---|---|---|---|---|
| T-01-02-SC | Tampering | npm install | Medium | High | HIGH | `v5.0.0-15.1.1`, `v5.0.0-15.2.1` | mitigate | Task 1 installs only the previously approved exact version in the isolated test workspace and generates a lockfile. | `npm ls ... @playwright/test --depth=0`, root dependency diff check, lockfile review. |
| T-01-02-01 | Information Disclosure | Fixture payload/console | Low | Medium | MEDIUM | `v5.0.0-15.3.1` | mitigate | Task 1 exposes only the minimum synthetic fixture fields and never serves config/data paths. | Allowlist server tests and synthetic-only fixture inspection. |
| T-01-02-02 | Elevation of Privilege | Static server path resolution | Medium | High | HIGH | `v5.0.0-5.3.2` | mitigate | Task 1 binds to loopback, uses trusted fixed path mappings, and rejects traversal/arbitrary paths. | Both-engine 200 checks for allowed assets plus traversal/arbitrary-file rejection tests. |
| T-01-02-03 | Tampering | Hidden enrichment writes | Low | High | MEDIUM | `v5.0.0-2.3.1` | mitigate | Task 1 keeps the harness flow review-only and fails on any skipped/extra persistence step. | Product/barcode/stock/file/save route counters remain zero. |

## HIGH-Threat Release Gate

T-01-02-SC and T-01-02-02 must have their Task 1 checks passing before RED is accepted; dependency or path-isolation failures are harness failures, not valid feature RED.

## Residual Risks

- Emulation does not prove household radio/network behavior; Plan 01-10 supplies the physical gate after stable deployment in Plan 01-09.
</threat_model>

<verification>
Run `node --check` on config/server/spec, verify the fixture loads actual assets in both engines, then preserve the intentional RED result for the exact missing happy-path behavior.
</verification>

<success_criteria>
- Approved dependency is isolated and locked.
- Browser harness boots deterministically in Chromium and WebKit.
- First E2E test fails for the intended production behavior gap.
- No production or persistence code changes.
</success_criteria>

<output>
Create `.planning/phases/01-safety-baseline-mobile-diagnostics/01-02-SUMMARY.md` with RED evidence and the exact failing assertion.
</output>
