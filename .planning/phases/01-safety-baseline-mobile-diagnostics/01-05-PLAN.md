---
phase: 01-safety-baseline-mobile-diagnostics
plan: "05"
type: execute
wave: 5
depends_on: ["01-04"]
files_modified:
  - custom/grocy_AI/module-version.json
  - custom/grocy_AI/src/GrocyAiDiagnostic.php
  - custom/grocy_AI/src/GrocyAiService.php
  - custom/grocy_AI/src/GrocyAiApiController.php
  - custom/grocy_AI/routes.php
  - custom/grocy_AI/tests/run.php
  - custom/grocy_AI/README.md
autonomous: true
requirements: [MOB-01, MOB-02, MOB-05, MOB-06, MOB-07]
must_haves:
  truths:
    - "Per D-03, Grocy enforces 12s total and 2s connect regardless of a larger legacy setting and reports bounded `Server-Timing`."
    - "Per D-04, Grocy validates/replaces trace context and returns only a closed allowlist diagnostic envelope without raw exceptions or sensitive values."
    - "Per D-06, all enrichment and diagnostic operations remain authenticated GET/read operations with no database or file write."
  artifacts:
    - path: "custom/grocy_AI/src/GrocyAiDiagnostic.php"
      provides: "Strict trace and closed DTO normalization boundary"
    - path: "custom/grocy_AI/module-version.json"
      provides: "Portable module and diagnostic contract versions"
    - path: "custom/grocy_AI/tests/run.php"
      provides: "Checksum/trace/timeout/outcome/privacy contract assertions"
  key_links:
    - from: "custom/grocy_AI/src/GrocyAiApiController.php"
      to: "custom/grocy_AI/src/GrocyAiService.php"
      via: "permission-first request and finite safe envelope"
      pattern: "PERMISSION_MASTER_DATA_EDIT"
    - from: "custom/grocy_AI/src/GrocyAiService.php"
      to: "grocy-mcp /v1/products/enrich/upc"
      via: "validated traceparent plus Guzzle 12s/2s options"
      pattern: "connect_timeout.*2"
---

## Phase Goal

**As a** phone user and maintainer, **I want to** operate enrichment dependably and localize failures, **so that** slow or unavailable providers never turn into unsafe or unexplained behavior.

<objective>
Expose the companion's bounded stage information through Grocy's authenticated API as a strict, redacted trace/timing envelope, implementing D-03/D-04/D-06.

Purpose: Give the browser safe localization data while keeping Grocy as the permission boundary and sole normal persistence authority.
Output: Portable module version, diagnostic helper, bounded Guzzle transport, finite controller mapping, and green PHP contract checks.
</objective>

<execution_context>
@/Users/ian/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ian/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-safety-baseline-mobile-diagnostics/01-RESEARCH.md
@.planning/phases/01-safety-baseline-mobile-diagnostics/01-PATTERNS.md
@custom/grocy_AI/src/GrocyAiService.php
@custom/grocy_AI/src/GrocyAiApiController.php
@custom/grocy_AI/routes.php
@custom/grocy_AI/tests/run.php
@custom/grocy_AI/README.md
@/Users/ian/Documents/Repos/grocy-mcp/grocy_mcp/diagnostics.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED PHP contracts for GTIN, trace, budgets, envelopes, and privacy</name>
  <read_first>
    .planning/phases/01-safety-baseline-mobile-diagnostics/01-RESEARCH.md
    .planning/phases/01-safety-baseline-mobile-diagnostics/01-PATTERNS.md
    custom/grocy_AI/tests/run.php
    custom/grocy_AI/src/GrocyAiService.php
    custom/grocy_AI/src/GrocyAiApiController.php
    custom/grocy_AI/routes.php
    /Users/ian/Documents/Repos/grocy-mcp/grocy_mcp/diagnostics.py
  </read_first>
  <files>custom/grocy_AI/tests/run.php</files>
  <behavior>
    - PHP accepts the same valid GTIN vectors as browser/companion and rejects invalid checksum before transport.
    - Transport seam receives exact `timeout=12`, `connect_timeout=2`, validated `traceparent`, redirects disabled, and timing capture options.
    - Invalid/zero/malformed trace is replaced; valid trace ID is preserved with a new parent ID.
    - Response/diagnostic JSON contains only finite approved fields/enums and excludes every forbidden canary, raw exception, URI, header, API key, and GTIN.
  </behavior>
  <action>Extend the existing counter-based native harness before production edits. Evolve test transport expectations to an explicit options structure while retaining backwards-safe deterministic injection. Mirror the shared GTIN/checksum vectors; seed forbidden canaries in URL-like fields, response extras, headers, tokens, exception text, and payload; assert they are absent from serialized normalized output. Add cases for companion success, `partial_image`, `not_found`, timeout, provider error, malformed JSON, invalid trace, and bounded nullable timing. Run RED and commit as `test(01-05): define Grocy diagnostic boundary`.</action>
  <verify>
    <automated>php custom/grocy_AI/tests/run.php</automated>
  </verify>
  <acceptance_criteria>
    - RED failures identify missing checksum/trace/options/DTO behavior only.
    - Harness still ends with one aggregate exit status and runs without Composer bootstrap.
    - No test contains or invokes a product/stock/file persistence operation.
  </acceptance_criteria>
  <done>Grocy boundary behavior is specified in the established native test seam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the closed Grocy diagnostic and transport boundary</name>
  <read_first>
    custom/grocy_AI/tests/run.php
    custom/grocy_AI/src/GrocyAiService.php
    custom/grocy_AI/src/GrocyAiApiController.php
    custom/grocy_AI/routes.php
    custom/grocy_AI/README.md
    /Users/ian/Documents/Repos/grocy-mcp/grocy_mcp/diagnostics.py
  </read_first>
  <files>custom/grocy_AI/module-version.json, custom/grocy_AI/src/GrocyAiDiagnostic.php, custom/grocy_AI/src/GrocyAiService.php, custom/grocy_AI/src/GrocyAiApiController.php, custom/grocy_AI/routes.php, custom/grocy_AI/README.md</files>
  <behavior>
    - Permission is checked before enrichment; expected failures map to finite safe outcomes and never return exception messages.
    - `traceparent` propagates browser→Grocy→companion only; `tracestate` is ignored.
    - `Server-Timing` contains only allowlisted metric names/durations and diagnostic DTO contains Grocy 4.6/module/companion/contract versions.
  </behavior>
  <action>Create `module-version.json` with explicit module version and diagnostic contract `1`. Create `GrocyAiDiagnostic` in namespace `GrocyAI\Services` with constants for schema/outcome/stage/status/error/cache enums, strict W3C v00 parsing/generation using `random_bytes`, bounded duration coercion, manifest fallback, and field-by-field DTO normalization. Require it explicitly before service/controller in `custom/grocy_AI/routes.php`; keep existing GET routes and middleware. Change `GrocyAiService::EnrichByUpc` to accept validated trace context/options, verify checksum, pass only `traceparent` plus existing safe headers to companion, hard-cap Guzzle `timeout` at 12.0 and `connect_timeout` at 2.0, keep redirects false, use `on_stats`, and normalize only the companion's closed outcome/diagnostic fields while retaining the existing preview payload. Change controller to read inbound `traceparent`, validate/replace it, preserve `MASTER_DATA_EDIT`, map expected failures to safe finite envelopes/HTTP status, and emit allowlisted `Server-Timing`; never expose `$ex->getMessage()`. Update README contract/budgets/privacy/zero-write/version documentation. Do not add route verbs, persistence, schema files, or schema push. Run GREEN and commit as `feat(01-05): add Grocy trace and diagnostic boundary`.</action>
  <verify>
    <automated>php custom/grocy_AI/tests/run.php && php -l custom/grocy_AI/src/GrocyAiDiagnostic.php && php -l custom/grocy_AI/src/GrocyAiService.php && php -l custom/grocy_AI/src/GrocyAiApiController.php && php -l custom/grocy_AI/routes.php</automated>
  </verify>
  <acceptance_criteria>
    - All PHP checks and lints exit 0.
    - Test seam observes exact total/connect values 12/2, redirects false, one validated trace header, and no `tracestate`.
    - Controller source has no expected-failure `GenericErrorResponse(...$ex->getMessage...)` path.
    - Serialized diagnostics and status exclude API key, GTIN, URLs, headers, payloads, cookies, image tokens, and raw exceptions.
    - `! rg -n -i '\b(INSERT|UPDATE|DELETE|REPLACE)\b|DatabaseService|DatabaseMigrationService|migrations?/|->(insert|update|delete|store)\s*\(' custom/grocy_AI/src/GrocyAiDiagnostic.php custom/grocy_AI/src/GrocyAiService.php custom/grocy_AI/src/GrocyAiApiController.php` proves no SQL mutation, migration registration, database gateway, or write method was added; diagnostic `schema_version` constants/fields are explicitly permitted.
  </acceptance_criteria>
  <done>Grocy exposes the safe companion contract with fixed budgets, correlation, and no hidden writes.</done>
</task>

</tasks>

<threat_model>
## Assets

- Grocy session/permission boundary, companion API key, diagnostic integrity, user preview/form state.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser → Grocy controller | Untrusted route parameter/header enters authenticated service. |
| Grocy → companion | API key and validated owned trace leave Grocy. |
| Companion response → browser | Untrusted JSON becomes safe preview/diagnostic envelope. |

## STRIDE Threat Register

Likelihood, impact, and severity use Low/Medium/High qualitative ratings. IDs below are official ASVS 5.0.0 Level 1 controls; explicit category-only rows identify an applicable ASVS area without a precise L1 requirement.

| Threat ID | Category | Component | Likelihood | Impact | Severity | ASVS 5.0.0 L1 / Verified Category | Disposition | Mitigation Task | Verification Evidence |
|---|---|---|---|---|---|---|---|---|---|
| T-01-05-01 | Information Disclosure | Error/diagnostic response | Medium | High | HIGH | `v5.0.0-15.3.1` | mitigate | Tasks 1-2 define and implement a minimum-field DTO with fixed errors and no raw sensitive values. | Forbidden-canary serialization tests and no exception-message source path. |
| T-01-05-02 | Spoofing/Tampering | GTIN and inbound trace | High | High | HIGH | `v5.0.0-2.2.1`, `v5.0.0-2.2.2` | mitigate | Tasks 1-2 validate checksum/trace at the trusted Grocy service/controller boundary and securely replace invalid traces. | Shared checksum vectors, zero transport on invalid GTIN, valid/invalid/all-zero trace cases. |
| T-01-05-03 | Denial of Service | Guzzle request | High | Medium | HIGH | ASVS V16 Error Handling/availability verified; no precise L1 control (`V16.5.2` is Level 2). | mitigate | Tasks 1-2 enforce 12s/2s, no redirects, and no automatic retry. | Injected transport observes exact options; timeout cases remain finite. |
| T-01-05-04 | Elevation of Privilege | Enrichment routes | Medium | High | HIGH | `v5.0.0-8.2.1`, `v5.0.0-8.3.1` | mitigate | Task 2 preserves authenticated middleware and checks `MASTER_DATA_EDIT` at the trusted controller before work. | Permission-first controller tests and route/middleware inspection. |
| T-01-05-05 | Tampering/Repudiation | Hidden persistence | Medium | High | HIGH | `v5.0.0-2.3.1`, `v5.0.0-8.3.1` | mitigate | Tasks 1-2 retain the review-only sequence, GET routes, and normal Save as the sole authorized writer. | No-persistence `rg` gate, route verb inspection, and zero write operations in tests. |

## HIGH-Threat Release Gate

Every row is HIGH and has a Task 1/2 mitigation. PHP contract/lint, privacy, permission, timeout, and no-persistence evidence must all pass before Plan 01-06.

## Residual Risks

- `Server-Timing` is browser-visible to the authenticated caller; metric names and durations are intentionally coarse and contain no identifiers.
</threat_model>

<verification>
Run the native PHP suite/lints and inspect serialized canary fixtures, route verbs, permissions, headers, and exact timeout options.
</verification>

<success_criteria>
- PHP contract suite is green.
- Safe finite Grocy envelope correlates to companion without leaking raw data.
- 12s/2s budgets and `Server-Timing` are exact.
- No persistence/schema behavior is introduced.
</success_criteria>

<output>
Create `.planning/phases/01-safety-baseline-mobile-diagnostics/01-05-SUMMARY.md`.
</output>
