# Quick task 260924-r2 summary

**Deployed:** `atech-release` commit `87677993f514edbabcd1579752268769ece567a1` fast-forwarded on GitHub from `6605ae6c`. Running image `sha256:d22abaeff119680fcbb62f3b5a72b70fc3302b5bdf3ebfc9941fb10f4abb207c`; previous image `sha256:eff902e0871185b64b9b36436d8fa20828f8deebb97c74015c8d370d2780d13b` retained as `atechpcs/grocy-ai:4.6-rollback-pre-phases1-8`. Compose recreated only `grocy`; `/etc/komodo/grocy:/config` remained unchanged. Cache marker is `ATECHPCS-grocy_AI-14`.

**Build provenance:** The pinned `lscr.io/linuxserver/grocy:v4.6.0-ls334` registry returned HTTP 429 while Docker resolved its manifest. The candidate image was built from an exact `git archive` of the stable commit with a temporary build-only `FROM atechpcs/grocy-ai:4.6` substitution. That local previous image is itself LinuxServer Grocy `v4.6.0-ls334`; the committed Dockerfile retains the pinned upstream `FROM`. The resulting image has all 30 COPY steps, PHP syntax clean, and no `.snapshots` directory. A future normal Komodo rebuild can use the fast-forwarded `atech-release` source when the registry is available.

**Data safety:** Before replacement, `VACUUM INTO` created `/etc/komodo/grocy/backup-predeploy-phases1-8-20260924.db` (20,254,720 bytes; SHA-256 `1abf2cf55a329ce18b346b97f841bff2a712efe5577a3684e080bdceef5cbc55`, integrity `ok`). Four namespaced module migrations ran successfully in the new container; postdeploy integrity was `ok` with 22 module tables. Of 37 native tables, only `userfields` (one expected taxonomy-scope definition added, none removed) and `api_keys` (`last_used` timestamps from authenticated smoke) differ from the backup. Every other native table, including stock, stock history, conversions, recipes, prices, products, and files metadata, has identical row content. API keys are otherwise identical when `last_used` is excluded.

**Live smoke:** One existing API key was used in memory and never logged. Authenticated GETs returned 200 for `/api/grocy-ai/status`, capture trips, conversion coverage, bulk conversion audit, conversion validation, `/grocyai/capture`, `/grocyai/capture/review`, `/grocyai/bulkreview`, `/grocyai/conversioncoverage`, `/product/new`, and `/quantityunitconversion/new`. Unauthenticated status returned 401. No live purchase commit or bulk apply was performed. A temporary authenticated smoke script initially held its SQLite connection during HTTP requests, exhausting five PHP workers; stopping that script and closing its cursor/connection restored the service, and the repeat smoke passed. The running image and mount were reconfirmed after the remote branch push.

**Remaining observation:** Physical-phone camera, network degradation, normal Save, and real household review/commit behavior must be confirmed by the maintainer. Existing Phase 1 Plan 01-10 and Phase 5 Plan 05-11 Task 3 remain explicitly unaccepted. No live stock write was used as a deployment test.
