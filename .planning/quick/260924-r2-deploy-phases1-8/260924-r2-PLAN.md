---
phase: quick
plan: "260924-r2"
type: execute
depends_on: ["260924-r1"]
requirements: [REL-01, REL-02, REL-03, REL-04, REL-05, REL-06, REL-07, CAP-01, CAP-02, CAP-03, CAP-04, CAP-05, CAP-06, CAP-07, CAP-08]
---

# Deploy Phases 1–8 to the stable production service

1. Pin a stable commit with 92-path portable parity, 272 PHP checks, 206 Chromium/WebKit checks, and passing taxonomy/conversion/bulk gates. Build its image; verify route/controller syntax, required files, and absence of local snapshots.
2. Rehearse module migrations and HTTP page/API routes on a disposable scrubbed production-shaped database. Fix any stable Grocy 4.6 integration errors before touching the live container.
3. Back up the live SQLite database, retain the old image ID, replace only the Grocy Compose service while preserving `/etc/komodo/grocy:/config`, and bootstrap namespaced module schema.
4. Smoke authenticated read-only module APIs and pages, unauthenticated denial, image/mount identity, and database integrity. Compare protected native tables with the backup; rollback if required checks fail.
5. Fast-forward the remote `atech-release` branch to the deployed commit so future Komodo builds use the same source. Record physical-phone and maintainer verification that still requires human observation.

**Output:** Summary and Phase 8 acceptance with only versions, hashes, counts, paths, and HTTP statuses; no product, barcode, credential, or household record values.
