---
phase: quick
plan: "260813-1bt"
type: execute
wave: 1
depends_on: []
files_modified:
  - public/custom/grocy_AI/product-enrichment.js
  - public/custom/grocy_AI/grocy-ai.css
  - custom/grocy_AI/tests/browser/fixtures/productform.html
  - custom/grocy_AI/tests/browser/specs/mobile-enrichment.spec.js
  - /Users/ian/Documents/Repos/grocy-atech-release/public/custom/grocy_AI/product-enrichment.js
  - /Users/ian/Documents/Repos/grocy-atech-release/public/custom/grocy_AI/grocy-ai.css
  - /Users/ian/Documents/Repos/grocy-atech-release/custom/grocy_AI/version.json
  - /Users/ian/Documents/Repos/grocy-atech-release/CUSTOMIZATIONS.md
autonomous: false
requirements: [MOB-01, MOB-08]
must_haves:
  truths:
    - "In Chrome at the supported touch viewports, the visible GTIN input has a computed height of at least 44px."
    - "Invalid GTIN length or checksum applies Bootstrap's visible invalid treatment as well as the existing accessible error contract, and returning to a valid GTIN clears both."
    - "When Chrome reports camera permission denied, the enrichment card shows the approved camera-unavailable recovery once, returns focus to usable manual GTIN entry, and starts no enrichment request."
    - "The stable checkout contains byte-identical versions of all seven portable paths at an immutable commit, uses a separate cache-marker commit, and the deployed image is pinned to the latter commit and digest."
    - "A signed-in live Chrome retest passes all three gaps without being represented as physical-phone evidence or completing Phase 01 Plan 10."
  artifacts:
    - path: "public/custom/grocy_AI/product-enrichment.js"
      provides: "Accessible invalid-class lifecycle and Chrome camera-permission recovery within the portable module boundary"
    - path: "public/custom/grocy_AI/grocy-ai.css"
      provides: "Module-owned 44px GTIN touch target"
      contains: "#grocy-ai-upc"
    - path: "custom/grocy_AI/tests/browser/specs/mobile-enrichment.spec.js"
      provides: "Deterministic Chromium/WebKit regressions for all three confirmed Chrome gaps"
    - path: "/Users/ian/Documents/Repos/grocy-atech-release/custom/grocy_AI/version.json"
      provides: "Stable deployed asset/view cache invalidation marker"
      contains: "ATECHPCS-grocy_AI-4"
  key_links:
    - from: "public/custom/grocy_AI/product-enrichment.js"
      to: "#grocy-ai-upc"
      via: "is-invalid/aria-invalid synchronization and focus restoration"
      pattern: "is-invalid|aria-invalid|focus"
    - from: "#grocy-ai-scan-button"
      to: "navigator.permissions camera state"
      via: "deny-aware delegation to the existing Grocy scanner without modifying the upstream camera component"
      pattern: "permissions.*camera|cameraUnavailable"
    - from: "custom/grocy_AI/portable-files.txt"
      to: "stable adapter commit"
      via: "check-portable-parity.sh with one supplied full 40-hex SHA"
      pattern: "--stable-sha"
    - from: "/Users/ian/Documents/Repos/grocy-atech-release/custom/grocy_AI/version.json"
      to: "deployed JavaScript and CSS"
      via: "Dockerfile.atech root version overlay and product-form asset query string"
      pattern: "Customization"
---

<objective>
Close the three Chrome-confirmed Phase 1 UI gaps, propagate the portable fix to stable without widening the seven-file boundary, deploy with an explicit cache bump, and retest the running application in signed-in Chrome.

Purpose: Make the locked touch, invalid-state, and camera-denied recovery contracts true in the real Bootstrap/Chrome surface while retaining deterministic cross-browser coverage and immutable stable provenance.
Output: Tested portable JavaScript/CSS, a focused browser regression spec, separate stable portable and cache commits, an immutable deployment, and live Chrome approval that is not physical-phone evidence.
</objective>

<execution_context>
@/Users/ian/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ian/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/phases/01-safety-baseline-mobile-diagnostics/01-UI-SPEC.md
@.planning/phases/01-safety-baseline-mobile-diagnostics/01-09-SUMMARY.md
@public/custom/grocy_AI/product-enrichment.js
@public/custom/grocy_AI/grocy-ai.css
@custom/grocy_AI/tests/browser/fixtures/productform.html
@custom/grocy_AI/tests/browser/specs/gtin-validation.spec.js
@custom/grocy_AI/tests/browser/specs/responsive-a11y.spec.js
@custom/grocy_AI/tests/browser/playwright.config.js
@custom/grocy_AI/tests/check-portable-parity.sh
@custom/grocy_AI/portable-files.txt
@custom/grocy_AI/README.md
@/Users/ian/Documents/Repos/grocy-atech-release/CUSTOMIZATIONS.md
@/Users/ian/Documents/Repos/grocy-atech-release/Dockerfile.atech

<interfaces>
Existing portable browser contracts:
- `clearError()` and `showError(message)` own `#grocy-ai-upc` validity presentation.
- `validateInput()` is the single manual-input and camera-event validation path; invalid input makes zero requests.
- `#grocy-ai-scan-button` delegates to `#camerabarcodescanner-start-button[data-target="grocy-ai-upc"]`; the shared scanner remains outside this change.
- `data-camera-unavailable` supplies the approved text: `Camera scanning is unavailable. Enter the GTIN manually.`
- Playwright discovers specs only under `custom/grocy_AI/tests/browser/specs`; the requested `tests/mobile-enrichment.spec.js` path is absent and must not create a second, undiscovered test tree.
- The immutable starting commits observed during planning are main `1ac357047cce3e76a1cd48c8176316d05e7a20c2` and stable adapter `770ba4f11b362061fbd7bf5c66549840235f1152`; execution must stop and reconcile rather than overwrite unexpected work if either checkout has moved or is dirty.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reproduce and fix the three Chrome gaps inside the portable module</name>
  <read_first>
    AGENTS.md
    .planning/phases/01-safety-baseline-mobile-diagnostics/01-UI-SPEC.md
    public/custom/grocy_AI/product-enrichment.js
    public/custom/grocy_AI/grocy-ai.css
    custom/grocy_AI/tests/browser/fixtures/productform.html
    custom/grocy_AI/tests/browser/specs/gtin-validation.spec.js
    custom/grocy_AI/tests/browser/specs/responsive-a11y.spec.js
    custom/grocy_AI/tests/browser/playwright.config.js
  </read_first>
  <files>public/custom/grocy_AI/product-enrichment.js, public/custom/grocy_AI/grocy-ai.css, custom/grocy_AI/tests/browser/fixtures/productform.html, custom/grocy_AI/tests/browser/specs/mobile-enrichment.spec.js</files>
  <behavior>
    - Touch target: with the fixture's generic `.form-control` no longer supplying a false-positive minimum height, `#grocy-ai-upc` is at least 44px high at 320, 375, 390, and 768px.
    - Invalid styling: invalid length and invalid checksum each add `is-invalid`, set `aria-invalid=true`, expose the exact assertive message, keep Search disabled, and make zero requests; a valid edit removes `is-invalid`, sets `aria-invalid=false`, and hides the error.
    - Camera denied: an already-denied Chrome camera permission and a prompt-to-denied transition each show the exact neutral camera-unavailable message once, leave no scanner modal or busy state, focus the unchanged manual input, and make zero enrichment requests.
    - Camera delegation: granted, prompt-without-denial, unsupported, or rejected Permissions API checks still delegate exactly once to the existing Grocy scanner control rather than replacing the scanner implementation.
  </behavior>
  <action>First create `specs/mobile-enrichment.spec.js` and adjust the fixture so the three behaviors fail against current production code; prove RED with the Chromium-mobile project. Then make the narrow portable fix. Give `#grocy-ai-upc` a module-owned `min-height: 44px` so the real Bootstrap form-control height cannot violate the D-01 touch contract. Synchronize Bootstrap `is-invalid` with the existing `aria-invalid` and error visibility in `showError`/`clearError`, using those two functions as the only validity-style owners. Add a small camera-unavailable recovery helper and make the custom scan action query Chrome's camera permission when that API exists: handle a pre-existing `denied` state without opening the scanner; observe a prompt-to-denied change so the shared scanner's rejection is reflected in the enrichment card; focus the unchanged GTIN input; and delegate exactly once to the existing hidden Grocy scanner for granted/prompt, unsupported, or Permissions-query-error paths. Do not edit `public/viewjs/components/camerabarcodescanner.js`, change the approved text, create an alternate scanner, clear user input, start a search, add automatic retry, or touch normal Save behavior. Run the focused spec in Chromium, then the complete no-retry Chromium/WebKit release suite, and commit only the four task files on `atech-main`; capture the resulting full 40-hex main fix SHA directly from the commit result for Task 2.</action>
  <verify>
    <automated>npm --prefix custom/grocy_AI/tests/browser run test -- mobile-enrichment.spec.js --project=chromium-mobile && npm --prefix custom/grocy_AI/tests/browser run test:release</automated>
  </verify>
  <acceptance_criteria>
    - The focused test is demonstrably RED before production changes and GREEN afterward.
    - The spec covers all three gaps and scanner delegation; no generic fixture CSS supplies the GTIN's 44px minimum.
    - The complete Chromium/WebKit deterministic suite exits 0 with retries disabled.
    - Production changes remain confined to the two existing portable browser assets; the shared Grocy scanner, product-form template, root dependencies, and Save handlers are unchanged.
    - The main fix commit is atomic and its full immutable SHA is recorded for stable propagation.
  </acceptance_criteria>
  <done>The three Chrome gaps are regression-covered and fixed within the existing portable UI boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror the seven-file boundary, bump stable caches, and deploy the immutable image</name>
  <read_first>
    .planning/phases/01-safety-baseline-mobile-diagnostics/01-09-SUMMARY.md
    custom/grocy_AI/portable-files.txt
    custom/grocy_AI/tests/check-portable-parity.sh
    custom/grocy_AI/README.md
    /Users/ian/Documents/Repos/grocy-atech-release/CUSTOMIZATIONS.md
    /Users/ian/Documents/Repos/grocy-atech-release/custom/grocy_AI/version.json
    /Users/ian/Documents/Repos/grocy-atech-release/Dockerfile.atech
  </read_first>
  <files>/Users/ian/Documents/Repos/grocy-atech-release/public/custom/grocy_AI/product-enrichment.js, /Users/ian/Documents/Repos/grocy-atech-release/public/custom/grocy_AI/grocy-ai.css, /Users/ian/Documents/Repos/grocy-atech-release/custom/grocy_AI/version.json, /Users/ian/Documents/Repos/grocy-atech-release/CUSTOMIZATIONS.md</files>
  <action>Require clean `atech-main` and `atech-release` checkouts and use the full main fix SHA captured from Task 1, never a moving branch name. Materialize every path in the existing seven-line `portable-files.txt` into the stable checkout from that immutable main commit, confirm only portable paths differ, run stable native tests, and commit the resulting portable changes as one stable portable commit; capture its full 40-hex SHA. Do not add the Playwright fixture/spec to the portable manifest and do not change the five documented stable framework seams. In a second stable commit, increment `custom/grocy_AI/version.json` from `ATECHPCS-grocy_AI-3` to `ATECHPCS-grocy_AI-4` and update stable `CUSTOMIZATIONS.md` with the main fix SHA, stable portable SHA, unchanged seven-file boundary, and reason for the cache bump; capture the full stable adapter/cache SHA. Run the main native suite, companion deterministic unit suites, complete browser release suite, stable native suite, shell syntax check, and parity using that supplied adapter/cache SHA. Build and deploy only the exact adapter/cache commit through the established `Dockerfile.atech`/DockerKomodo path, retain the pinned base/COPY layout and `/etc/komodo/grocy:/config` mount, record the immutable image digest, and prove the running OCI revision, marker `ATECHPCS-grocy_AI-4`, JavaScript/CSS hashes, product-form asset query, authentication boundary, persistence counts/hashes, and zero-write enrichment smoke match the new artifact. Keep all credentials and household identifiers out of output and do not modify `01-PHONE-ACCEPTANCE.md` or `phone-timings.jsonl`.</action>
  <verify>
    <automated>bash -n custom/grocy_AI/tests/check-portable-parity.sh && php custom/grocy_AI/tests/run.php && (cd /Users/ian/Documents/Repos/grocy-mcp && .venv/bin/python -m unittest tests.test_enrichment tests.test_http_api tests.test_diagnostics) && npm --prefix custom/grocy_AI/tests/browser run test:release && php /Users/ian/Documents/Repos/grocy-atech-release/custom/grocy_AI/tests/run.php && bash custom/grocy_AI/tests/check-portable-parity.sh --stable-sha "$STABLE_ADAPTER_SHA"</automated>
  </verify>
  <acceptance_criteria>
    - `STABLE_ADAPTER_SHA` is populated from the exact second stable commit result, validated as 40 hexadecimal characters, and supplied unchanged to parity; parity reports identical=7, mismatched=0, missing=0.
    - Stable has a portable-only commit followed by a cache/customization-only commit; neither commit widens the seven portable paths or the five documented adapter paths.
    - Main native, companion, full Chromium/WebKit, stable native, shell syntax, and SHA-pinned parity suites all exit 0.
    - The deployed image is content-addressed, labels the exact stable adapter/cache SHA, exposes marker `ATECHPCS-grocy_AI-4`, serves the new JS/CSS bytes, and preserves the existing persistent mount and aggregate data fingerprints.
    - Live smoke remains authenticated, GET/read-only, redacted, and zero-write; no phone acceptance artifact or timing sample is created or changed.
  </acceptance_criteria>
  <done>The portable fix is reproducibly identical on stable, cache-invalidated, fully verified, and deployed from an immutable commit/image.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Retest the running stable artifact in signed-in Chrome</name>
  <read_first>
    .planning/phases/01-safety-baseline-mobile-diagnostics/01-UI-SPEC.md
    .planning/phases/01-safety-baseline-mobile-diagnostics/01-09-SUMMARY.md
    /Users/ian/Documents/Repos/grocy-atech-release/custom/grocy_AI/version.json
  </read_first>
  <files>None</files>
  <action>Open the deployed product form in the user's existing signed-in Chrome session and verify the exact image revision/cache marker from Task 2 before interaction. Hard reload, use Chrome responsive emulation at 390x844 and 320px width, and inspect `#grocy-ai-upc.getBoundingClientRect().height` as at least 44px without horizontal overflow. Enter `1234567` and an invalid supported-length checksum to confirm the exact error copy, visible red Bootstrap invalid border, `aria-invalid=true`, disabled Search, and zero enrichment request; replace with `012345678905` without submitting and confirm the red treatment/error clear and `aria-invalid=false`. Reset the site's camera permission, choose Block/Deny when prompted by Scan barcode, and confirm the neutral camera-unavailable copy appears once, the scanner modal closes or never opens, focus returns to the unchanged usable GTIN field, the card is not busy, normal Save remains available, and no enrichment request occurs. Restore the prior camera site permission after the check. Record only PASS/FAIL, Chrome version, viewport, stable adapter/cache SHA, and image digest in the quick summary. This desktop Chrome/devtools check is not physical-phone evidence: do not write Phase 1 phone artifacts, claim actual phone/LAN timing, or complete Plan 01-10.</action>
  <verify>
    <automated>npm --prefix custom/grocy_AI/tests/browser run test -- mobile-enrichment.spec.js --project=chromium-mobile && bash custom/grocy_AI/tests/check-portable-parity.sh --stable-sha "$STABLE_ADAPTER_SHA"</automated>
    <human-check>In signed-in Chrome against the running stable image, confirm PASS for the 44px GTIN height, visible invalid styling and clearing, permission-denied recovery/focus/no-request behavior, exact deployed provenance, and unchanged normal Save availability.</human-check>
  </verify>
  <acceptance_criteria>
    - All three live Chrome checks pass against the exact deployed stable SHA/image after a hard reload.
    - The camera-denied path produces one approved recovery state and leaves manual entry and normal Save usable with no enrichment network request.
    - Evidence contains no GTIN/product value, URL, credential, cookie, header, payload, token, screenshot with household data, or physical-phone claim.
    - User response is `approved`; otherwise record the failing gap and leave the quick task incomplete.
  </acceptance_criteria>
  <resume-signal>Type `approved` if all live Chrome checks pass, or describe the failing gap.</resume-signal>
  <done>The immutable deployed fix passes a live signed-in Chrome retest without being promoted to physical-phone acceptance evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser permission state → enrichment UI | Chrome's camera permission result influences local recovery and scanner delegation. |
| Main immutable commit → stable portable files | Portable browser bytes cross between separate worktrees. |
| Stable commit → deployed image/cache | Exact source and marker become a running artifact with persistent household data mounted. |
| Signed-in Chrome → live evidence | Authenticated observations are reduced to redacted PASS/FAIL and provenance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-Q-1BT-01 | Tampering | Invalid-field UI state | mitigate | Task 1 makes one pair of helpers own `is-invalid`, `aria-invalid`, and error visibility and proves invalid→valid clearing with zero requests. |
| T-Q-1BT-02 | Denial of Service | Camera permission recovery | mitigate | Task 1 handles denied state locally, delegates at most once, restores focus, and never retries or starts enrichment; Task 3 verifies the running behavior. |
| T-Q-1BT-03 | Tampering | Main→stable propagation | mitigate | Task 2 materializes the fixed seven-path manifest from a full main SHA, separates portable/cache commits, and runs parity against one supplied full stable SHA. |
| T-Q-1BT-04 | Tampering/Denial of Service | Deployment/cache/persistent mount | mitigate | Task 2 pins source and image provenance, bumps the stable marker, verifies served hashes, and preserves/fingerprints the existing data mount. |
| T-Q-1BT-05 | Information Disclosure | Live Chrome and deployment evidence | mitigate | Tasks 2-3 record only versions, immutable provenance, aggregate hashes/counts, and PASS/FAIL; all household values and auth material remain excluded. |
| T-Q-1BT-SC | Tampering | Package supply chain | accept | No package install or dependency change is permitted; the existing exact Playwright lock remains unchanged. |
</threat_model>

<source_coverage>
SOURCE | ID | Feature/Requirement | Task | Status | Notes
GOAL | — | Fix GTIN touch target, invalid styling, and camera-denied recovery; mirror stable and verify Chrome | 1-3 | COVERED | Deterministic fix, immutable stable propagation/deploy, then live Chrome.
REQ | MOB-01 | String-preserving local validation with exact invalid behavior and zero request | 1,3 | COVERED | Adds visible Bootstrap state without changing validator or request behavior.
REQ | MOB-08 | 44px touch targets, accessible recovery, responsive Chrome behavior | 1,3 | COVERED | Module-owned target and permission-denied manual recovery.
RESEARCH | — | No research artifact applies in quick mode | — | EXCLUDED | The task explicitly prohibits a research phase and uses established Phase 1 patterns.
CONTEXT | — | Preserve exactly seven portable paths and full immutable commit parity | 2 | COVERED | Browser tests stay outside the manifest; parity checks all seven.
CONTEXT | — | Run deterministic suites, cache-aware stable deployment, and live Chrome retest | 1-3 | COVERED | Includes complete suites, marker `-4`, immutable image, live approval.
CONTEXT | — | Do not claim physical-phone evidence | 2,3 | COVERED | Phone acceptance and timings remain untouched; Chrome emulation is labeled non-physical.
</source_coverage>

<verification>
<automated>php custom/grocy_AI/tests/run.php && (cd /Users/ian/Documents/Repos/grocy-mcp && .venv/bin/python -m unittest tests.test_enrichment tests.test_http_api tests.test_diagnostics) && npm --prefix custom/grocy_AI/tests/browser run test:release && php /Users/ian/Documents/Repos/grocy-atech-release/custom/grocy_AI/tests/run.php && bash -n custom/grocy_AI/tests/check-portable-parity.sh && bash custom/grocy_AI/tests/check-portable-parity.sh --stable-sha "$STABLE_ADAPTER_SHA"</automated>
Require the two immutable stable commits, seven-of-seven parity, cache marker/image/source/hash agreement, persistent-data continuity, zero-write live smoke, and explicit live Chrome approval. A physical phone, timing thresholds, and Phase 01 Plan 10 remain outside this quick task.
</verification>

<success_criteria>
- All three confirmed Chrome gaps have deterministic regressions and pass the complete Chromium/WebKit release suite.
- The implementation stays inside the portable JavaScript/CSS boundary and leaves the shared scanner and normal Save path unchanged.
- Stable parity passes for exactly seven files at a full immutable adapter/cache SHA, and stable cache invalidation uses `ATECHPCS-grocy_AI-4`.
- The running image/digest serves the fixed bytes while preserving household data and zero-write enrichment behavior.
- Signed-in live Chrome passes the touch, invalid-state, and camera-denied checks; no physical-phone claim or evidence is produced.
</success_criteria>

<output>
Create `.planning/quick/260813-1bt-fix-phase-1-gtin-touch-target-invalid-st/260813-1bt-SUMMARY.md` with main fix SHA, stable portable SHA, stable adapter/cache SHA, image digest, deterministic suite counts, seven-file parity result, cache/deployment checks, and redacted live Chrome PASS/FAIL. Do not modify Phase 1 phone-acceptance artifacts.
</output>
