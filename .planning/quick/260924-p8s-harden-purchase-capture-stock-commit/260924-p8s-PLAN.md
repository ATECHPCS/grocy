---
phase: quick
plan: "260924-p8s"
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [CAP-05, CAP-06, CAP-08]
files_modified:
  - custom/grocy_AI/src/GrocyAiCaptureService.php
  - custom/grocy_AI/tests/capture.php
  - custom/grocy_AI/tests/capture_native_transaction.php
  - services/StockService.php
  - CUSTOMIZATIONS.md
---

# Quick task 260924-p8s: Harden the Phase 8 purchase commit

## Objective

Prove that purchase capture retains one atomic native stock transaction when Grocy compacts stock, and refuses a changed reviewed line after acquiring the write lock. Keep the current checkout untouched by working from the Phase 6 tip in `codex/production-phases1-8`.

## Tasks

1. Reproduce native `CompactStockEntries()` failing when `AddProduct()` is called inside capture's `BEGIN IMMEDIATE`. Add a test that also checks caller rollback and normal standalone compaction. Make compaction manage a transaction only when it owns one; document the upstream hook in `CUSTOMIZATIONS.md`.
2. Reproduce a line edit between the first checksum comparison and `BEGIN IMMEDIATE`. Re-read trip status and checksum under the acquired write lock; return a conflict outcome and write no stock when either changed.
3. Run the focused native and capture invariants, the default PHP suite, Phase 8 mobile browser coverage, the full browser matrix, and the bulk release gate. Record actual results and any skipped production-snapshot checks.

## Acceptance

- A split requiring native compaction succeeds inside capture's caller transaction, which remains open until the caller commits or rolls back.
- Standalone native compaction still commits its own transaction.
- A changed reviewed line between the two checksum checks produces `checksum_mismatch` with zero stock calls and no open transaction.
- No production database or deployed image is changed by this quick task.

## Output

Write `260924-p8s-SUMMARY.md` with the regression evidence, verification results, and remaining release gates. Phase 8 Plan 08-06 and stable production promotion remain separate follow-on work.
