---
phase: quick
plan: "260924-p8n"
type: execute
wave: 1
depends_on: ["260924-p8s"]
autonomous: true
requirements: [CAP-05, CAP-06, CAP-08]
files_modified:
  - custom/grocy_AI/src/GrocyAiCaptureService.php
  - custom/grocy_AI/tests/capture.php
  - custom/grocy_AI/tests/capture_native_snapshot.php
  - CUSTOMIZATIONS.md
---

# Quick task 260924-p8n: Prove native purchase on a scrubbed snapshot

## Objective

Exercise Phase 8 through the actual Grocy stock writer on a disposable copy of a freshly scrubbed production-shaped SQLite database. Compare the resulting stock and stock-log deltas, including native compaction, and prove repeat-commit idempotency without touching production data.

## Tasks

1. Refresh the gitignored snapshot through the documented read-only `snapshot-refresh.sh`; verify SQLite integrity and that API keys and sessions were scrubbed. Run the Phase 6 snapshot-backed contract suite and conversion audit.
2. Make the capture contract fixture model the current Grocy 4.6 schema. Read the purchase-to-stock factor from `uihelper_product_details`, the same resolved view used by native `StockService`; do not query the column dropped from `products` by migration 0207.
3. Add a test that copies only the scrubbed snapshot into a disposable directory, resolves an owned GTIN, seeds a matching native purchase, commits one capture trip with the real `StockService::AddProduct()`, checks the stock delta against stock_log and compaction, and verifies repeat commit writes nothing. Delete the disposable copy on exit.
4. Run the focused and snapshot-backed suites and record actual evidence. Keep 08-06 open until deployment and signed-in acceptance are complete.

## Acceptance

- The red fixture fails against the removed products column, then passes using the current derived view.
- A real native purchase from capture succeeds on a disposable snapshot copy; `stock` delta equals its `stock_log` purchase amount and matching entries compact under the outer transaction.
- Repeating the same commit leaves stock unchanged.
- The original scrubbed snapshot and household production database are not modified by tests.
