---
phase: quick
plan: "260924-r1"
type: execute
wave: 1
depends_on: ["260924-p8s", "260924-p8n"]
autonomous: true
requirements: [REL-01, REL-02, REL-03, REL-04, CAP-01, CAP-02, CAP-03, CAP-04, CAP-05, CAP-06, CAP-07, CAP-08]
---

# Quick task 260924-r1: Mirror Phases 4–8 to stable

## Objective

Prepare a Grocy 4.6 stable release candidate from the current `atech-release` lineage. Preserve the stable framework and its existing Phase 1–3 adapters while carrying the reviewed Phase 4–8 module, Phase 8 native purchase fix, and required core hooks. Finish with an immutable local stable commit and executable parity and migration evidence; do not deploy in this task.

## Pinned inputs

- Development candidate: `317690db3f8f2a938c04f1b6c1dac08469350b5e` on `codex/production-phases1-8`.
- Stable base: `6605ae6c2034c6679381de185cc567d80d38db79` on `atech-release`.
- Worktrees: `grocy-production-phases1-8` and `grocy-stable-phases1-8`; both must be clean before replay.
- Only the scrubbed, gitignored `custom/grocy_AI/.snapshots/grocy-prod.sqlite` may be used for migration rehearsal. Never ship the snapshot or its raw intermediate.

## Tasks

1. Reconcile `portable-files.txt` with every Phase 4–8 module runtime asset and deterministic test. Keep `custom/grocy_AI/routes.php`, `custom/grocy_AI/src/GrocyAiApiController.php`, core Blade views, and `Dockerfile.atech` as named adapters. Update the parity and release gates to use the complete manifest without the obsolete twelve-path assumption, then prove the source checkout passes its taxonomy, conversion, bulk, capture, native, and browser suites.
2. Materialize only listed portable files from the pinned development commit into the stable worktree and commit that set independently. Preserve the stable base controller namespace, class-based JSON middleware, and existing normal Save lifecycle. Do not merge the development branch or copy unrelated `controllers/`, `services/`, `migrations/`, or `views/` files.
3. Adapt the stable route/controller and page hooks for conversion status, bulk review, categorization, and purchase capture. Add only the verified native conversion pre-save hook and `StockService::CompactStockEntries()` transaction fix. Extend `Dockerfile.atech` COPY entries for every stable core hook and view, update the cache marker and `CUSTOMIZATIONS.md`, and commit these adapters separately.
4. Run PHP syntax, stable native and capture suites, Chromium/WebKit browser suite, branch-parity and release gates, and module migration/upgrade rehearsal on a disposable scrubbed snapshot copy. Compare protected stock, history, recipe, price, conversion, image, and auth fingerprints before and after the rehearsal. Refuse the candidate if a required route, module table, hook, or served asset is absent.

## Acceptance

- Portable files are byte-identical between the pinned development candidate and one full 40-hex stable commit; stable adapter paths are separately enumerated and reviewed.
- Stable keeps the Grocy 4.6 upstream lineage and every existing Phase 1–3 behavior.
- The Phase 8 native purchase transaction and stale-checksum regressions pass on stable.
- No production database, deployed image, or live household stock is changed.

## Output

Write `260924-r1-SUMMARY.md` with exact commit IDs, changed-path lists, parity counts, suite output, migration fingerprints, and remaining deployment gates. Deployment and signed-in/physical-phone acceptance are subsequent quick tasks.
