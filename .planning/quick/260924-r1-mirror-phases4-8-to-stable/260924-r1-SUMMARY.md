# Quick task 260924-r1 summary

**Result:** Stable Grocy 4.6 candidate built and verified. Development code commit `8803951e45a032ebf30a320115ca942daee43d25`; stable adapter commit `87677993f514edbabcd1579752268769ece567a1` on the `atech-release` lineage. The 92 manifest paths match byte for byte at that stable commit.

The stable branch retains its controller namespace and class-based JSON middleware. Adapter paths are `custom/grocy_AI/routes.php`, `custom/grocy_AI/src/GrocyAiApiController.php`, `controllers/BaseApiController.php`, `controllers/GenericEntityApiController.php`, `services/StockService.php`, `public/viewjs/quantityunitconversionform.js`, `public/viewjs/quantityunitconversionsresolved.js`, `views/layout/default.blade.php`, `views/productform.blade.php`, `views/quantityunitconversionform.blade.php`, `views/quantityunitconversionsresolved.blade.php`, four `views/grocyai_*.blade.php` pages, `Dockerfile.atech`, `custom/grocy_AI/version.json`, `.gitignore`, `.dockerignore`, and `CUSTOMIZATIONS.md`. Stable commits `a1ad1bfb`, `0a8aebea`, and `b7adb6d6` carry the portable mirror and parity-helper updates separately from the adapters.

**Verification:** Development and stable PHP suites each passed 272 checks; the stable suite uses `php8.5 -d auto_prepend_file=packages/autoload.php`. The stable Chromium/WebKit mobile release suite passed 206 tests. Taxonomy, conversion, and bulk release gates passed. Focused native transaction and disposable-snapshot purchase tests passed. A disposable scrubbed snapshot migration kept 36 of 37 native tables byte-identical and added exactly one `userfields` definition; SQLite integrity was `ok` and 22 module tables were present. The isolated Grocy 4.6 HTTP smoke returned 200 for capture, review, bulk, conversion, product, and required assets after the one-time conversion bootstrap. It found and fixed translation-template render failures before live deployment.

**Boundary:** The source repository's `atech-main` branch diverges from this development candidate; it was not force-updated. The stable `atech-release` branch fast-forwards from `6605ae6c` to `87677993`. Deployment and user device acceptance are recorded separately in quick task 260924-r2 and Phase 8 acceptance.
